{
  "name": "@myelin/design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Solid components, design tokens, and self-hosted icons for Myelin.",
  "type": "module",
  "sideEffects": ["*.css"],
  "exports": {
    ".": "./src/index.ts",
    "./block-editor.css": "./src/block-editor.css",
    "./tokens.css": "./generated/tokens.css",
    "./tokens": "./generated/tokens.ts",
    "./sprite.svg": "./generated/assets/sprite.svg"
  },
  "files": [
    "src",
    "generated"
  ],
  "scripts": {
    "build:tokens": "node build-tokens.mjs",
    "build:assets": "node scripts/copy-assets.mjs",
    "build:verify": "node scripts/verify-tokens.mjs",
    "build": "pnpm build:tokens && pnpm build:assets && pnpm build:verify && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src styleguide build-tokens.mjs scripts",
    "pretest": "pnpm build:tokens && pnpm build:assets && pnpm build:verify",
    "test": "vitest run"
  },
  "dependencies": {
    "solid-js": "^1.9.4"
  },
  "devDependencies": {
    "@solidjs/testing-library": "^0.8.10",
    "@testing-library/jest-dom": "^6.6.3",
    "jsdom": "^25.0.1",
    "style-dictionary": "^5.5.0",
    "vite": "^6.4.3",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^3.2.6",
    "vitest-axe": "^0.1.0"
  }
}

{
  "name": "@myelin/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Myelin desktop+mobile shell (E0.7 / MR-018): one Solid app wrapped by Tauri 2. The Tauri Rust side (src-tauri) exposes commands that REUSE the Myelin crates (myelin-content render path, myelin-client config) — the 'one Rust core, three shells' proof. The full app shell slots in at MR-019.",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "build": "vite build && tsc --noEmit",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@myelin/design-system": "workspace:*",
    "@tauri-apps/api": "^2",
    "solid-js": "^1.9.4"
  },
  "devDependencies": {
    "@solidjs/testing-library": "^0.8.10",
    "@tauri-apps/cli": "^2",
    "@testing-library/jest-dom": "^6.6.3",
    "jsdom": "^25.0.1",
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^2.1.8"
  }
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the MR-018 shell: core IPC only (the two shared-core commands). No filesystem/shell/http plugins — minimal surface.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}

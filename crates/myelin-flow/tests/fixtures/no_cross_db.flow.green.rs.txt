// P-FLOW-01 GREEN fixture for `no-cross-db` — the lint MUST admit this. Depending on a sibling
// service's CONTRACT surface (its public types/traits/ids — here the ArtifactRef the journal stores
// references-not-payloads) is allowed; it is the frozen glue-crate coupling, not a reach into the
// data layer.
use myelin_refs::ArtifactRef;
use myelin_tenancy::TenantId;

pub fn run_subject(tenant: TenantId, input: ArtifactRef) {
    let _ = (tenant, input);
}

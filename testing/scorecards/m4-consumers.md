# M4 (consumer subsystems) exit-gate scorecard (CI-D9/D1/D5/D8/D11/D6/D4/D7 + AG-D4/CI-T1 (prod-image re-confirm, real microVM) + STOR-D1/D2 (restore-verify on CI stores) + ISS-P06/D2/D3/D4/D5/D6/D7/D8/D9/D11/D13 + CHAT-D5/D6/D7/D18/D8/D9/D10/D11/D12/D15/D16/D17 + contract-coverage)

> Generated: 2026-06-24. The build-layer realisation of the master band gate invariant (master-sequencing §2/§4, EI-01 §2): no later-band prompt runs over a red earlier gate. Each row is a dated green artifact read off the per-feature drill (this scorecard WIRES the drills, it does not re-implement them). A single RED row blocks M5 and is recorded honestly as claimed-not-proven, never edited green (EI-01 §3 / roadmap §5).

**Gate verdict: GREEN — M5 may start**

| Gate | Title | Verdict | Date | Permanent | Proof / reason |
|---|---|---|---|---|---|
| CI-D9 | ci-pipeline determinism → a re-driven CI pipeline lands on the same deterministic plan/result (no nondeterministic step) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p15_ci_pipeline` |
| CI-D1 | effectively-once → a CI trigger fires its effect exactly once across replay/redelivery (no double-run, no lost run) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p16_effectively_once` |
| CI-D5 | reserve/settle parity → a reserved CI budget settles exactly once; a crash leaves no double-charge / no orphaned reservation | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p17_reserve_settle_parity` |
| CI-D8/GIT-D10 | seam gate → the check_status producer/consumer seam dead-letters foreign/malformed, idempotent on dup, drops stale supersession | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p19_seam_gate` |
| CI-D11 | live-tail → the CI log live-tail is a references-not-payloads firehose pointer; resume replays exactly, no lost/dup byte-range | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p21_live_tail` |
| CI-D6 | fork cache-poison → a fork PR cannot poison the trusted build cache (cache key scoped, untrusted writes quarantined) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p22_fork_cache_poison` |
| CI-D4 | supply-chain fail-closed → an unverifiable/unsigned supply-chain artifact fails CLOSED (the build is denied, never admitted-by-default) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p23_supply_chain_fail_closed` |
| CI-D7 | fork-no-secrets → a fork-originated CI run reads 0 tenant secrets (the untrusted-fork secret boundary holds) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --test drills_ci_p24_fork_no_secrets` |
| AG-D4/CI-T1 | prod-image escape re-confirm → the COMMITTED prod CI runner image boots a real Firecracker microVM, runs the adversarial corpus, 0 escapes (proven-on-real-hardware) | PASS | 2026-06-24 | re-run-forever | [2026-06-24] PASS  `cargo test -p myelin-ci-sandbox --features integration --test escape_drill_ci_committed_gate_reconfirm_test` |
| STOR-D1/D2 | restore-verify on the CI stores → cross-seam OLTP↔blob↔index↔offset restore to a consistent point; RPO/RTO within bound, 0 loss (real PG + RustFS) | PASS | 2026-06-24 | re-run-forever | [2026-06-24] PASS  `cargo test -p myelin-ci-controlplane --features integration --test integration_ci_p27_restore_verify_ci_stores` |
| ISS-P06 | emit-iff-committed → an Issues write co-commits its outbox event in the same transaction; a rollback emits 0 | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_p06_emit_iff_committed` |
| ISS-D2 | cost-bounding → an adversarial Issues query/automation is cost-bounded and cannot run unbounded work (the resource ceiling holds) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d2_cost_bounding` |
| ISS-D3 | setexpr zero-leak → an Issues list SetExpr push-down lowers the authz predicate into the query; 0 cross-tenant / unauthorized rows leak | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d3_setexpr_zero_leak` |
| ISS-D4 | create-storm → a burst of concurrent issue creates serializes without a lost write / duplicate key collision | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d4_create_storm` |
| ISS-D5 | reorder zero-clobber → concurrent rank/reorder ops converge without clobbering a sibling's position (no lost reorder) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d5_reorder_zero_clobber` |
| ISS-D6-calendar | SLA business-calendar → an SLA timer honours the business calendar (working hours/holidays) — fires at the correct deadline | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d6_sla_business_calendar` |
| ISS-D6-escalation | SLA escalation → a breached SLA escalates up the ladder exactly once, without double-paging | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d6_sla_escalation` |
| ISS-D7 | stateful trigger → a stateful Issues automation trigger fires deterministically on the right state transition (no spurious / missed fire) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d7_stateful_trigger` |
| ISS-D8-rollup | rollup → a read-time Issues rollup is permission-filtered (conjoin); 0 leak across the aggregate | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d8_rollup` |
| ISS-D8-olap | OLAP feed → the Issues OLAP feed is consistent with the OLTP projection; no resurrected/erased rows in the analytics feed | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d8b_olap_feed` |
| ISS-D9 | import → an Issues import round-trips deterministically; no dropped / duplicated / mis-mapped issue on re-import | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d9_import` |
| ISS-D11 | erase-reaches-every-holder → an Issues erase reaches every holder (issue + comments + attachments + projections); 0 recoverable residual | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d11_erase_reaches_every_holder` |
| ISS-D13 | board sync → the Issues board view stays consistent with the underlying issue state under concurrent moves (no lost/ghost card) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-issues --test drill_iss_d13_board_sync` |
| CHAT-D5-unfurl | unfurl no-leak → a chat link unfurl never leaks content the viewer cannot see (the unfurl is authz-filtered, 0 cross-tenant leak) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d5_unfurl_no_leak` |
| CHAT-D5-humanise | humanise no-leak → message humanisation (key/name resolution) never leaks an unauthorized display name / identity | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d5_humanise_leak` |
| CHAT-D6/D7/D18 | invalidation → an unfurl/cache invalidation propagates correctly (a stale unfurl is invalidated on source change; no stale read) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d6_d7_d18_invalidation` |
| CHAT-D8 | erasure cascade → a chat erase cascades to every holder (message body + DEK + mentions + index); 0 recoverable residual PII | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d8_erasure_cascade` |
| CHAT-D9 | HITL exactly-once → a chat agent's human-approved effect applies exactly once across replay (no double-apply) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d9_hitl_exactly_once` |
| CHAT-D10 | HITL per-effect → each chat agent effect is gated by its own approval (per-effect HITL; an approval never blanket-approves a sibling) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d10_hitl_per_effect` |
| CHAT-D11 | search ACL → a chat search reads 0 messages the viewer cannot see (the search ACL pre-filter holds; revoke reflected) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d11_search_acl` |
| CHAT-D12 | read-state → chat read-state fan-out/dedup is consistent across surfaces (no lost/double read marker) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d12_read_state` |
| CHAT-D15 | reindex parity → a cold chat re-index pass equals the live projection; no stale/dup/resurrected message rows | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d15_reindex_parity` |
| CHAT-D16 | streaming → the chat live-streaming path delivers ordered frames; resume after a drop replays exactly, no lost/dup frame | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d16_streaming` |
| CHAT-D17 | explicit-first → a chat agent's explicit (human-issued) instruction takes precedence over an inferred one (the explicit-first ordering holds) | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo test -p myelin-chat --test drill_chat_d17_explicit_first` |
| contract-coverage | the contract-coverage scanner re-affirms the M4 CDC rows — no falsely-claimed/dropped row | PASS | 2026-06-24 | — | [2026-06-24] PASS  `cargo run -p myelin-lints --bin contract-coverage` |

**Two PERMANENT integration rows are RED-until-proven against the LIVE substrate.** The AG-D4/CI-T1 prod-image re-confirm and the STOR-D1/D2 restore-verify on the CI stores run `--features integration` against the live docker-compose stack (Postgres / RustFS / Valkey / NATS JetStream); the AG-D4 row additionally needs a real KVM-capable host. A DB-free (or non-KVM) run cannot flip them green — the proof command FAILS. Every other CI / Issues / Chat row is a deterministic per-feature drill / CDC pair that runs with no external backend. The two permanent rows are the re-run-forever markers: AG-D4 (the real-kernel escape gate, EI-01 §2 — RCE/sandbox escape outranks every feature) and STOR-D1/D2 (the shared Storage-owned restore gate — a backup never restored is not a backup, EI-01 §3).

**AG-D4/CI-T1 is PROVEN-ON-REAL-HARDWARE, NOT vacuous.** The prod-image re-confirm runs `--features integration` with `MYELIN_REQUIRE_KVM=1` set by the scorecard runner: on a host without /dev/kvm or firecracker the drill HARD-FAILS (it does not skip), so this row only goes green when the COMMITTED prod CI runner image actually boots a real Firecracker microVM, runs the adversarial corpus, and attests 0 escapes.

**AG-D4/CI-T1 — three NAMED residuals (proven-on-real-hardware is not absence-of-all-escapes):**
- (a) **one green run proves THIS config against THIS battery on THIS kernel** — continuous fuzzing + full CVE-corpus tracking + a pre-GA third-party pentest remain ongoing; a single green is necessary, not sufficient-forever.
- (b) **production must run on KVM-capable Scaleway hardware** (Elastic Metal / nested-virt) — an explicit infra requirement; on a non-KVM box this gate cannot be greened (the row hard-fails, never fakes green).
- (c) **single-box ≠ fleet** — multi-tenant density / blast-radius at 30× load still overlaps the unproven world-scale LOAD floor below.

**The ONE true remaining floor (named, dated deferral — NOT a row that reds this gate):** the **world-scale 30× LOAD / surge drills** (FLOW-D8 / AG-D6 / the CHAT + Issues surge under fleet-scale fan-out) need real fleet hardware (a multi-node cluster), so they are deferred to **M5** — not run on a single dev box. Everything else in M4 is proven with a dated artifact above. The deferral is visible, never invisible (EI-01 §1).

**Named residual (not a floor, run-when-available):** gVisor (`runsc`) as a SECOND escape-drill backend (CI-P28) — runsc is on PATH but running the corpus under it needs an OCI bundle + root/userns privileges this host lacks; the AG-D4 attestation records it as a NAMED parametrized residual, never faked green. Firecracker (the production default) IS the exercised gate backend.

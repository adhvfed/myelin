# Infra (integration) exit-gate scorecard (STOR-D-OUTBOX/RESTORE/RLS + ID-D-REBAC (--features integration) + 2 floor smokes)

> Generated: 2026-06-19. The build-layer realisation of the master band gate invariant (master-sequencing §2/§4, EI-01 §2): no later-band prompt runs over a red earlier gate. Each row is a dated green artifact read off the per-feature drill (this scorecard WIRES the drills, it does not re-implement them). A single RED row blocks the next band and is recorded honestly as claimed-not-proven, never edited green (EI-01 §3 / roadmap §5).

**Gate verdict: GREEN — the next band may start**

| Gate | Title | Verdict | Date | Permanent | Proof / reason |
|---|---|---|---|---|---|
| STOR-D-OUTBOX | outbox no-loss under crash → 0 lost / 0 ghost (real PG + real NATS JetStream) | PASS | 2026-06-19 | re-run-forever | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage3_drills drill1_outbox_no_loss_under_crash` |
| STOR-D-RESTORE | restore-verify cross-seam → rows⟷blobs⟷bus-offset consistent (real PG + real RustFS) | PASS | 2026-06-19 | re-run-forever | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage3_drills drill2_restore_verify_cross_seam` |
| STOR-D-RLS | (tenant, region) RLS isolation → cross-tenant leak = 0 (DB-enforced, NOBYPASSRLS role) | PASS | 2026-06-19 | — | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage3_drills drill3_tenant_region_rls_isolation` |
| ID-D-REBAC | ReBAC check/list_objects no-leak / no-N+1 → visible set exact, 1 reverse-index query (real PG tuples) | PASS | 2026-06-19 | — | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage3_drills drill4_rebac_check_list_objects_no_leak_no_n_plus_1` |
| EB-D-PARTITION | (EB-12) `(tenant, region)`-partitioned stream subject → structured §2.2 subject reaches the live broker; per-(tenant, subsystem) filter isolates one tenant's events from another's (the bulkhead) | PASS | 2026-06-20 | re-run-forever | [2026-06-20] PASS  `cargo test -p myelin-events --features integration --test integration_eb12_partition` (real NATS JetStream) |
| EB-D-RESIDENCY | (EB-13) residency-pin the Bus streams → a stream provisioned in region A has NO cross-region read path: a read scoped to region B captures 0 messages from the pinned stream (CP-D3 / STOR-D5 Bus slice; 0 cross-region reads) | PASS | 2026-06-20 | re-run-forever | [2026-06-20] PASS  `cargo test -p myelin-events --features integration --test integration_eb13_residency` (real NATS JetStream) |
| CP-D3/STOR-D5 | (P-CP-12 / P-096) four-layer region-pinning, store-layer leg → an out-of-region write (`row.region ≠ cell.region`) REJECTED by the DB's `(tenant, region)` RLS `WITH CHECK` (residency-pin write boundary); a cross-region read returns 0 rows (cross-region PII egress impossible). 0 out-of-region writes admitted, 0 cross-region read rows. NOBYPASSRLS app role. | PASS | 2026-06-19 | re-run-forever | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stor_d5_cross_region_egress_drill` (real PG, FORCE RLS WITH CHECK) |
| SRCH-D-LAYOUT | (SRCH-P03 / P-166) the Search service-shell forward-only migration (contract 1.5) → the `search_index_directory` CREATE applies forward-only against live Postgres; the `(tenant, region)` PRIMARY KEY keys per-tenant, per-region index directories (two tenants → two distinct rows; a duplicate `(tenant, region)` rejected by the PK); each directory carries its per-tenant index DEK ref (the encrypted-from-birth anchor, §3.1). Red-until-proven; DB-free run cannot flip it. | PASS | 2026-06-20 | re-run-forever | [2026-06-20] PASS  `cargo test -p myelin-search --features integration --test integration_srch_p03_index_directory` (real PG) |
| SANDBOX-SMOKE | hardened-container sandbox smoke → egress-deny + read-only-root + dropped caps asserted | PASS | 2026-06-19 | smoke (floor open) | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage4_floor_smokes sandbox_escape_containerized_smoke` |
| LOAD-10X-SMOKE | 10× containerized load smoke → myelin-harness LoadGenerator at 10× against the live PG+NATS stack survives | PASS | 2026-06-19 | smoke (floor open) | [2026-06-19] PASS  `cargo test -p myelin-storage --features integration --test stage4_floor_smokes load_10x_containerized_smoke` |

**Red-until-proven (the testing-policy ratchet).** Every integration row above is proven ONLY by its `cargo test --features integration` against the LIVE docker-compose stack (Postgres / RustFS / Valkey / NATS JetStream). A DB-free run cannot flip any row green — the proof command FAILS without the stack. Run via `scripts/integration-test.sh` (brings the stack up `--wait`, runs the suite). The four full drills (STOR-D-OUTBOX/RESTORE/RLS, ID-D-REBAC) ARE the whole gate.

**The two genuine floors (NAMED, still open — their smokes are not the full gate):**
- **SANDBOX-SMOKE** — the real-kernel SANDBOX-ESCAPE gate needs a real isolation kernel (gVisor / Firecracker microVM), not a Docker container — RED until run on one
- **LOAD-10X-SMOKE** — the WORLD-SCALE 30× LOAD drill needs real hardware (a multi-node cluster), not a single dev box — RED until run on real hardware

The smokes give each floor not-zero-coverage NOW (a hardened-container smoke: egress-deny + read-only-root + dropped caps; a 10× containerized load smoke via myelin-harness against the live stack). The full real-kernel SANDBOX-ESCAPE gate (gVisor / microVM) and the WORLD-SCALE 30× LOAD drill (real hardware) stay RED until run on the real substrate — the deferral is visible, never invisible (EI-01 §1).

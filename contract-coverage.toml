# contract-coverage.toml — the coverage manifest the contract-coverage scanner (P-S21 → P-037)
# reconciles against the frozen contract surface.
#
# SOURCE OF TRUTH FOR THE ROW SET: every `N.M` row of
#   planning/05-refined-shared-systems-architecture/contract-index.md  (§1–§13).
# The scanner parses that file directly for the authoritative row set, then checks THIS file:
#   - every contract-index row MUST have exactly one [[contract]] entry here (a missing one is a
#     silently-dropped contract — a build failure);
#   - status = "covered"  ⇒ `cdc` lists the test file(s) carrying the provider+consumer CDC pair;
#                            each named file MUST exist on disk and carry BOTH a provider-side and a
#                            consumer-side marker (a falsely-claimed pair is a build failure);
#   - status = "deferred" ⇒ `landing` names the prompt that will ship the CDC pair (an un-named
#                            floor is a build failure — EI-01 §1, name-your-floors).
#
# DOCTRINE: external-insights/01-process-and-quality-doctrine.md §5 — convert the "every contract
# has a CDC pair" discipline into a committed, LOUD, mechanical gate. A contract violation dropped
# silently is a multi-day misdiagnosis. NEVER weaken this gate to pass; ship the pair instead.
#
# WHEN A LATER PROMPT SHIPS A CONTRACT: flip its row here from `deferred` to `covered` and name its
# CDC file(s). The scanner is the meta-gate that prompt's DEFINITION OF DONE leans on.
#
# `landing` ids below are the GLOBAL ledger ids (P-NNN) from
#   planning/07-prompts/README.md  (the run table). The scanner does not verify the landing id is a
# real prompt (that is the ledger's job); it requires only that one is NAMED.

# ───────────────────────── §1 Bootstrap & service shell (substrate 00) ─────────────────────────

[[contract]]
row = "1.1"
title = "serve(AppSpec) lifecycle"
status = "covered"
# The substrate ships the lifecycle (PROVIDER); each service that boots over it is a CONSUMER. The
# Knowledge service shell (KN-P04 / P-294) is one such consumer — it hands its eight-field AppSpec
# to serve and re-implements no lifecycle. Each CDC file carries its own provider+consumer pair.
cdc = [
    "crates/myelin-substrate/tests/cdc_1_1_serve.rs",
    "crates/myelin-knowledge/tests/cdc_1_1_1_5_knowledge_boot_and_hot_tables.rs",
]

[[contract]]
row = "1.2"
title = "three-surface topology + tenant-from-token"
status = "covered"
cdc = ["crates/myelin-substrate/tests/cdc_1_2_topology.rs"]

[[contract]]
row = "1.3"
title = "liveness != readiness"
status = "covered"
cdc = ["crates/myelin-substrate/tests/cdc_1_3_liveness_readiness.rs"]

[[contract]]
row = "1.4"
title = "PersonalDataHolder auto-registration"
status = "covered"
# The substrate ships the MECHANISM + enforcement; AG-P3 / P-132 adds the Agent-Fabric side (its H11
# OLTP + H17 trace stores opened through the one door; an unregistered Fabric store fails the
# holder-registered architecture test). Each CDC carries its own provider+consumer pair.
cdc = [
    "crates/myelin-substrate/tests/cdc_1_4_1_5_holders_and_migrations.rs",
    "crates/myelin-agent-service/tests/cdc_10_1_agent_holder.rs",
]

[[contract]]
row = "1.5"
title = "forward-only online migrations"
status = "covered"
# The substrate ships the runner + the hot-table declaration MECHANISM; KN-P04 / P-294 adds the
# Knowledge OWNED half — the `block`/`db_row`/`doc_op` hot-table-flag declaration (the high-write
# tables KN-P05 creates) carried on the Knowledge AppSpec + proven refused-on-blocking-ALTER. Each
# CDC file carries its own provider+consumer pair.
# ISS-P11 (P-377, M4): the Issues governance schemes + the precedence algebra + the flexible-field
# model CONSUME 1.5 — the forward-only-migration lint HOLDS over the flexible-field add (the
# flexible-field model `myelin_issues::schemes` is ZERO-DDL: a custom field is a JSONB `props` write +
# a default-GIN-indexable facet over `issue_props_gin`, NEVER an ALTER on the hot `issue` table —
# proven in tests/lint_issue_spine.rs::forward_only_migration_holds_under_the_flexible_field_add). The
# scheme-precedence resolution (`myelin_issues::schemes::resolve`/`SchemeResolver`) is the
# deterministic, cached, off-the-hot-path most-specific-wins algebra; a scheme reassignment is a CONFIG
# write that touches 0 issue rows (the no-config = Linear-simple gate, proven in
# tests/e2e_iss_p11_scheme_precedence.rs). MANDATORY-CORE mutation floor MET on the precedence-
# resolution module: cargo-mutants on `src/schemes.rs` = 31 caught / 0 missed / 5 unviable (100% of
# viable mutants on the precedence + org_default + flexible-field path; precedence determinism is
# governance-correctness-bearing). FLOORS named: the hierarchy is a tree parent (constrained-DAG
# portfolios M5+); the projection-feeder generated-index promotion is ISS-P15 (cold facets ride GIN
# until a measured OQ-C threshold). The FSM interpreter + the QueryAst guards are ISS-P12.
# ISS-P15 (P-381, M4): the projection feeder CONSUMES 1.5 — when a custom facet crosses the measured
# OQ-C threshold (> 5% of a collection's view executions) the feeder PROVISIONS its generated/expression
# index via a forward-only ONLINE migration: a `CREATE INDEX CONCURRENTLY` over the `props` JSONB tail,
# asserted NON-BLOCKING on the declared-hot `issue` table (myelin_storage::migration::is_blocking_alter)
# + forward-only (no down). Proven 0-downtime against the LIVE dev-stack Postgres: a concurrent INSERT
# succeeds WHILE the CONCURRENTLY index builds, and EXPLAIN goes Seq Scan -> Index Scan on the generated
# index (integration_iss_p15_projection_feeder.rs, --features integration). A below-threshold facet
# provisions NO index (measured, never predicted).
cdc = [
    "crates/myelin-substrate/tests/cdc_1_4_1_5_holders_and_migrations.rs",
    "crates/myelin-knowledge/tests/cdc_1_1_1_5_knowledge_boot_and_hot_tables.rs",
    "crates/myelin-issues/tests/e2e_iss_p11_scheme_precedence.rs",
]

# 1.6 (architecture lints) is the committed-lint RATCHET itself — a gate, not a producer⇄consumer
# data seam, so it has no CDC pair (the lint crate's fixture matrix + workspace scan ARE its proof).
# It is recorded here so the row is not silently dropped; its landing is the M0 exit gate that
# greens all twelve lints (P-039).
[[contract]]
row = "1.6"
title = "the twelve architecture lints (the ratchet)"
status = "deferred"
landing = "P-039"

[[contract]]
row = "1.7"
title = "cross-language harness shim (frozen)"
# COVERED at CHAT-P3 / P-245: chat PINS the TE-21 connection-tier language (Rust default; the
# BEAM/Phoenix hatch written-but-CLOSED, opened only if CHAT-D3/D4 prove Rust intractable, CHAT-P26)
# and RECORDS the no-op against the frozen 1.7 cross-language harness shim. The CDC carries the
# PROVIDER (chat pins Rust + records the obligation) + CONSUMER (the shim is a NO-OP in the all-Rust
# default — no cross-language boundary; the obligation binds only on the BEAM divergence) sides. The
# divergence is written-but-closed — the shim's obligations are stated, satisfied as a no-op today.
# ENFORCED at P-S30 / P-319 (M4): myelin-harness ships the cross-language harness shim CONFORMANCE
# SUITE (the SEVEN §3.7 non-negotiables as an exhaustive enum + a DivergentTierProbe trait + the
# loud Enforced/RecordedNa discharge record). The specified→enforced floor is now real: a divergent
# (BEAM) tier CANNOT ship unless all seven clauses are green; today (chat = Rust) the gate is a
# LOUDLY-recorded, dated N/A (NOT a silent skip, EI-01 §4/§5) — discharged against chat's real pin.
status = "covered"
cdc = [
    "crates/myelin-chat/tests/cdc_1_7_chat_te21_pin.rs",
    "crates/myelin-harness/tests/shim_conformance_p_s30.rs",
]

# 1.8 (telemetry signal set) — the survival-signal contract every drill reads; greened transitively
# by every drill's signal assertions. Its dedicated CDC pair lands with the harness self-test prompt.
[[contract]]
row = "1.8"
title = "telemetry signal set (contract 1.8)"
status = "deferred"
landing = "P-039"

[[contract]]
row = "1.9"
title = "ResilientClient::call (timeout/breaker/bulkhead/retry)"
status = "deferred"
landing = "P-033"

[[contract]]
row = "1.10"
title = "FailStatic<T> bounded-staleness cache"
status = "covered"
cdc = ["crates/myelin-substrate/tests/cdc_1_10_fail_static.rs"]

[[contract]]
row = "1.11"
title = "protected-human-lane shed order"
status = "covered"
cdc = [
  "crates/myelin-substrate/tests/cdc_1_11_shed_order.rs",
  "crates/myelin-substrate/tests/cdc_1_11_agent_load.rs",
  "crates/myelin-git/tests/cdc_1_11_git_shed_order.rs",
]
# P-276 (GIT-P15): the GIT-SIDE consumer of the protected-human-lane shed order — the Git front-door
# shed gate (myelin-git/src/shed_clone.rs GitFrontDoorShed) WIRES the substrate ShedLane over the NEW
# Surface::GitFrontDoor budget (the OQ-K per-surface floor) read FROM THE THRESHOLDS FILE (REUSED, never
# re-defined, EI-01 §7): a clone storm's agent/CI lane sheds (429 + Retry-After) while the human's
# interactive fetch is served; a machine principal can never up-class to the protected human lane.
# The consumer-half CDC is cdc_1_11_git_shed_order.rs. FLOOR: the per-surface budget NUMBERS are tuned
# by the 30× clone-storm GIT-D6 → GIT-P34 (M5); here the order is asserted at 1× with mixed principals.
#
# P-356 (CI-P13): the CI-SIDE consumer of 1.11 — the scheduler fairness slice
# (myelin-ci-controlplane/src/fairness.rs) COMPOSES the CI lane shed order (`shed_order()` →
# [Deploy, Batch, Interactive], interactive held last = the protected-human-lane analogue inside CI)
# with the substrate ShedLane over Surface::CiDispatch (REUSED, never re-defined): the CI per-tenant
# in-flight cap (Backpressure::DEFAULT_TENANT_IN_FLIGHT_CAP) is asserted EQUAL to the substrate
# CiDispatch shed budget (one v1 floor, not two — default_cap_matches_substrate_ci_dispatch_budget).
# FLOOR: the cap + the DRR plan-tier weights are tuned by the 30× CI-D2 surge → CI-P30 (M5). The 1.8
# consumer half is the per-fair_key deficit-DESC ordering + the backpressured/queue-depth signals the
# DRR advance/replenish + the in-flight count produce (proven live in integration_ci_p13_fairness.rs).

# ──────────────── §2 Event envelope, outbox & consumer template (Bus myelin-events) ─────────────

[[contract]]
row = "2.1"
title = "EventEnvelope (the names/units anchor)"
status = "deferred"
landing = "P-011"

[[contract]]
row = "2.2"
title = "OutboxTx::emit(draft, cause) — the only emit path"
status = "covered"
# KN-P06/P-296 (M3) adds the Knowledge OWNER emit seam (myelin_knowledge::emit) — every state change
# emits via OutboxTx::emit ONLY (no-raw-publish), emit-iff-committed, PROVEN against the live dev-stack
# Postgres outbox co-commit (tests/integration_kn_d7_outbox.rs).
# CI-P23 (P-366, M4) adds the CI supply-chain verifier's CONSUMER side: every fail-closed refusal
# builds a `ci.supply_chain.verification_failed` EventDraft (the frozen envelope shape, a registered
# durable ci.* token, the `ci/run/<run_id>` aggregate, PII-free) to be emitted via the outbox ONLY.
# ISS-P06 (P-372, M4) adds the Issues silent-data-loss-safe write path (myelin_issues::write_path):
# validate → Id.check (+CaveatContext) → mutate typed core → OutboxTx::emit IN ONE TX; the issue is the
# aggregate; emit-iff-committed (0 ghost / 0 lost — kill-between-commit-and-publish drill green); the
# no-raw-publish lint GREEN over the source (emit is the only path).
# ISS-P09 (P-375, M4): the order_key CAS reorder (myelin_issues::reorder) CO-COMMITS its issue.reordered
# event via OutboxTx::emit on the SAME tx — emit-iff-committed: a LOST CAS commits neither the rank nor
# the event (0 silent clobber), a WIN co-commits exactly one issue.reordered per accepted move. Proven in
# drill_iss_d5_reorder_zero_clobber.rs (the reorder provider/consumer CDC pair + the ISS-D5 0-clobber drill).
cdc = [
    "crates/myelin-storage/tests/cdc_st_02_coloc.rs",
    "crates/myelin-knowledge/tests/cdc_2_2_2_3_2_4_2_9_kn_d7_outbox.rs",
    "crates/myelin-ci-controlplane/tests/cdc_supply_chain_2_2_10_6_4_7.rs",
    "crates/myelin-issues/tests/drill_iss_p06_emit_iff_committed.rs",
    "crates/myelin-issues/tests/integration_iss_p06_outbox.rs",
    "crates/myelin-issues/tests/drill_iss_d5_reorder_zero_clobber.rs",
]

[[contract]]
row = "2.3"
title = "outbox table + FOR UPDATE SKIP LOCKED relay"
status = "covered"
# KN-P06/P-296 (M3): the Knowledge emit seam stamps the aggregate = the doc/row/db (§4 ordering key);
# per-doc events stay per-aggregate ordered (monotonic seq), proven in the same CDC + live integration.
# ISS-P06 (P-372, M4): the Issues write path stamps the aggregate = the issue (the issue is the
# aggregate, UNIQUE(aggregate, seq)); chained create→update→transition events stay per-issue ordered
# (monotonic, gap-free), proven in the write-path provider CDC.
cdc = [
    "crates/myelin-storage/tests/cdc_st_02_coloc.rs",
    "crates/myelin-knowledge/tests/cdc_2_2_2_3_2_4_2_9_kn_d7_outbox.rs",
    "crates/myelin-issues/tests/drill_iss_p06_emit_iff_committed.rs",
    "crates/myelin-issues/tests/integration_iss_p06_outbox.rs",
]

[[contract]]
row = "2.4"
title = "EventHandler consumer template"
status = "covered"
# The one consumer template (the seven encoded rules) ships in myelin-events (P-S08/P-009) + its
# SUB-D2/BUS-D2 drills. KN-P06/P-296 (M3) adds the Knowledge OWNER consumer-template instance: the
# living-doc EventHandler with a `*`-free subjects() whitelist (rule 3) + idempotent-through-the-runtime
# (rules 1/4), wired into the Knowledge AppSpec consumer seam (the relay + consumer in the boot hook).
# ISS-P15 (P-381, M4) adds the Issues projection-feeder CONSUMER instance: the measured generated-index
# promotion EventHandler with a `*`-free `issue.issue.updated` whitelist + idempotent-on-event_id handle,
# admitted + driven through the Bus consumer runtime (cdc_2_4_issues_projection_feeder.rs).
# ISS-P18 (P-384, M4) adds the Issues rollup CONSUMER instance: the event-driven, debounced, incremental
# rollup EventHandler (`myelin_issues::rollup::RollupConsumer`) — a `*`-free whitelist over the
# rollup-driving issue.* deltas (issue.updated/transitioned/parent_changed) + idempotent-on-event_id
# handle (off the bus, NEVER in the write path); a malformed (no-subject) event is non-retryable poison.
# The ISS-D8(a) bounded-recompute (debounce-coalesce) + the input_hash no-op suppression (AG-6) are the
# loop-storm guard (drill_iss_d8_rollup.rs).
cdc = [
    "crates/myelin-events/tests/drills_sub_d2_consumer.rs",
    "crates/myelin-knowledge/tests/cdc_2_2_2_3_2_4_2_9_kn_d7_outbox.rs",
    "crates/myelin-issues/tests/cdc_2_4_issues_projection_feeder.rs",
    "crates/myelin-issues/tests/drill_iss_d8_rollup.rs",
]

[[contract]]
row = "2.5"
title = "consumer_dedup ledger"
status = "covered"
# CI-P10 (P-353, M4) adds the CI Trigger & Dispatch CONSUMER half: the dispatch `DedupLedger` models
# the `(consumer, event_id)` first-write-wins guard so one triggering push = exactly one run under
# at-least-once redelivery (the exactly-once-effect drill). The provider half is the events ledger.
# ISS-P06 (P-372, M4) adds the Issues consumer-dedup CDC half: a replay of the SAME stable issue.*
# event_id is dedup-suppressed (0 double-handle) — the write path's stable ids are dedup-safe on replay.
cdc = [
    "crates/myelin-events/tests/cdc_2_5_consumer_dedup.rs",
    "crates/myelin-ci-dispatch/tests/cdc_ci_dispatch_trigger.rs",
    "crates/myelin-issues/tests/drill_iss_p06_emit_iff_committed.rs",
]

[[contract]]
row = "2.6"
title = "reindex-from-source"
status = "covered"
# EB-22/P-142 ships the Bus's OWNED reindex-from-source seam (events::reindex(scope)) + the
# *.snapshot schema (deterministic event_id from (aggregate, version)) + a reference derived-store
# consumer proving cold == live (BUS-D5). EB-26/P-246 (M3) FILLED the two M3-owner `replay` bodies:
# Git's per-repo/per-blob/per-PR replay (`myelin_git::replay::GitReindexSource`) + Knowledge's
# page-subtree-at-BLOCK-granularity replay (`myelin_content::replay::KnowledgeReindexSource`) — both
# proven cold == live + idempotent against THIS seam. EB-27/P-327 (M4) FILLED the M4-owner `replay`
# bodies: CI's one-run replay (`myelin_ci_sandbox::replay::CiReindexSource` — run:/deployment:/pipeline:
# granular) + Issues' replay (`myelin_issues::replay::IssueReindexSource` — issue/relation/comment/rollup)
# + Chat's durable replay (`myelin_chat::replay::ChatReindexSource` — channel/message/thread; the
# firehose-only frames recover via resume, not replay) — all proven cold == live + idempotent + erased-skip
# (X-7). FLOOR (still owed): Refs per-blob, Search full reindex land with those subsystems' own prompts.
# GIT-P30/P-291 (M3-G7) greens GIT-D3 (reindex-from-source PARITY): the end-to-end drill that wipes +
# rebuilds ALL THREE Git-fed derived stores (Search code index, the check_status projection, the Refs
# lifecycle edges) and proves the cold rebuild BYTE-MATCHES live (one code path, no drift) + the
# check_status projection rebuilds from CI's ci.check.updated re-emit + an erased body does NOT
# resurrect (0 resurrected PII; the ONE posture residual) + the no-cross-db reach is structural
# (the only doc-entry path is the owner's project(ref)/replay — never a Git DB read).
cdc = [
    "crates/myelin-events/tests/cdc_2_6_reindex.rs",
    "crates/myelin-content/tests/cdc_2_9_2_6_knowledge_tokens_and_replay.rs",
    "crates/myelin-git/tests/drills_git_d3_reindex_parity.rs",
    # KN-P20/P-310 (M3 · KN-D6): Knowledge's FULL SERVICE-crate replay body
    # (myelin_knowledge::replay::KnowledgeReindexSource) — the superset of the content-crate
    # page/block floor: it adds the knowledge.row.snapshot row leg (off the flexible-DB rows) + the
    # refs.edge.snapshot TE-7 drift-correction (off the page_parent/db_relation typed tables, §3.1 —
    # the typed table WINS). KN-D6 (catalogue F4, was SCHED) GREENED: wipe -> replay -> cold == live
    # (the reindex-parity hash byte-matches), live consumer path only, a crash-mid-rebuild resume
    # converges idempotently (deterministic ids, 0 double-apply), an erased page/row is never
    # resurrected (X-7). The replay-idempotence gate (a re-run emits 0 new) is CI-green.
    "crates/myelin-knowledge/tests/cdc_2_6_knowledge_replay.rs",
    "crates/myelin-knowledge/tests/drill_kn_d6_reindex_parity.rs",
    # CI-P26 / P-369 (M4): the CI surfacing-side 2.6 CDC — CI's replay (the EB-27/P-327
    # `myelin_ci_sandbox::CiReindexSource`, RE-EXPORTED through `myelin_ci_controlplane::surfacing_index`,
    # one source of truth) rebuilds the derived view from the `ci.run.snapshot` drafts ALONE (cold == live,
    # no CI-DB read) + idempotent re-run. The surfacing read+ref half (5.1/5.6/5.7) is CI-P25/P-368.
    "crates/myelin-ci-controlplane/tests/cdc_6_3_2_6_7_3_ci_surfacing_index.rs",
    # ISS-P18 / P-384 (M4): the rollup reindex-from-source half — the DERIVED rollup aggregate
    # (`myelin_issues::rollup::RollupConsumer`) is rebuilt DRIFT-FREE off the source of truth (the
    # issue_relation edges + the leaf facts) via `reindex_from`, the SAME re-sum the live consumer runs
    # (steady-state + recovery share ONE code path). ISS-D8(b) reindex-parity (0 drift, byte-match) is
    # the green artifact; the rollup *.snapshot replay (IssueReindexSource, rollup: granular) is
    # deterministic (cold == live, idempotent re-run). drill_iss_d8_rollup.rs.
    "crates/myelin-issues/tests/drill_iss_d8_rollup.rs",
    # P-387 (ISS-P20, M4): the Issues OLAP FEED is rebuilt DRIFT-FREE off the source of truth
    # (IssueReindexSource → *.snapshot → the SAME IssueOlapConsumer handle body — steady-state +
    # recovery share one code path). ISS-D8(b) OLAP-feed reindex-parity (0 drift, projection byte-match
    # cold == live) is the green artifact; the restriction flag still excludes a restricted subject
    # after the rebuild (it reads the holder flag live). The provider (IssueReindexSource) + consumer
    # (IssueOlapConsumer) pair is in cdc_11_6_issues_olap_feed.rs; the reindex-parity drill is
    # drill_iss_d8b_olap_feed.rs + the chained-mutation e2e is e2e_iss_p20_olap.rs (consumer drills,
    # not pair-evidence — registered under 11.6's landing).
    "crates/myelin-issues/tests/cdc_11_6_issues_olap_feed.rs",
]

[[contract]]
row = "2.7"
title = "crypto-shred / tombstone on the log"
status = "covered"
cdc = ["crates/myelin-events/tests/cdc_2_7_bus_holder.rs"]

[[contract]]
row = "2.8"
title = "schema evolution / upcasters"
status = "covered"
cdc = ["crates/myelin-events/tests/cdc_2_8_upcaster.rs"]

[[contract]]
row = "2.9"
title = "event taxonomy + token table"
status = "covered"
# EB-02/P-042 ships the Bus grammar + seed CDC pair; GIT-P2/P-124 adds Git COMPLETING its `git.*`
# token list; CHAT-P1/P-128 adds Chat COMPLETING its `chat.*` list, split DURABLE-via-outbox vs
# FIREHOSE-only; ISS-P03/P-242 adds Issues COMPLETING its `issue.*` list incl. the registered
# `initiative` type token + the 2.1 EventEnvelope unit-anchor half (seconds-vs-millis rejected).
# EB-26/P-246 (M3) adds the Bus-owned per-subsystem token-list VALIDATION HARNESS
# (`myelin_events::TokenListHarness`) — the cross-subsystem registry each completed list registers
# INTO (admits the full list iff §6.1-conformant + own-prefixed + unique; rejects a malformed addition
# LOUDLY) — and registers the M3 owners' lists through it (Git's complete GIT_EVENT_TOKENS + Knowledge
# COMPLETING its new `knowledge.*` list, durable-via-outbox vs firehose-only). (contract 2.9: "each
# subsystem completes its list" — each subsystem registers, the Bus owns the grammar + the harness.)
# Each file carries both a provider and a consumer marker.
# KN-P06/P-296 (M3) EXTENDS the knowledge.* list IN PLACE (myelin_content::events) to the COMPLETE
# architecture-03 §1 OWNER taxonomy (the page/block/database/view/row/comment/mention lifecycle +
# access/DSR + the cross-cutting *.erased/*.snapshot) and proves every KnowledgeChange the emit seam
# can produce maps to a registered, grammatical token (cdc_2_2_2_3_2_4_2_9_kn_d7_outbox.rs).
# EB-27/P-327 (M4) validates the M4 owners' lists (CI/Issues/Chat) through the SAME harness
# (cdc_2_9_m4_token_lists.rs — admits each completed list in full + rejects a malformed addition
# loudly); CI's complete `ci.*` list lands in `myelin_ci_sandbox::events` (the M4 names freeze, split
# durable-via-outbox vs firehose-only ci.log.appended; the Δ1-superseded ci.status.updated /
# ci.run.passed deliberately absent). Issues/Chat already completed their lists (ISS-P03 / CHAT-P1).
# CI-P7/P-350 (M4) REGISTERS CI's complete `ci.*` taxonomy FROM ITS PROPER HOME — the CI Control
# Plane (`myelin_ci_controlplane::events`, the outbox producer / check emitter): it RE-EXPORTS the one
# canonical token source (`myelin_ci_sandbox::events`, EB-27 — no second token language) and adds (a)
# the control-plane registration entry `register_ci_taxonomy()` and (b) the §6.2 singular subsystem/
# type token-table validation the early grammar-only freeze lacked (`ci` is canonical + the CI type
# tokens run/job/check/deployment/runner/pipeline/log/artifact/cost/supply_chain). The CDC
# (cdc_2_9_ci_tokens.rs) pins the control-plane provider + the Bus §6.1/§6.2 consumer and that the
# registration equals the source byte-for-byte (coherence: ONE list, no drift).
cdc = [
    "crates/myelin-ci-controlplane/tests/cdc_2_9_ci_tokens.rs",
    "crates/myelin-events/tests/cdc_2_9_taxonomy.rs",
    "crates/myelin-events/tests/cdc_2_9_token_list_harness.rs",
    "crates/myelin-events/tests/cdc_2_9_m4_token_lists.rs",
    "crates/myelin-git/tests/cdc_2_9_git_tokens.rs",
    "crates/myelin-git/tests/cdc_2_9_git_token_list_harness.rs",
    "crates/myelin-chat/tests/cdc_2_9_chat_tokens.rs",
    "crates/myelin-issues/tests/cdc_2_9_issues_tokens.rs",
    "crates/myelin-content/tests/cdc_2_9_2_6_knowledge_tokens_and_replay.rs",
    "crates/myelin-knowledge/tests/cdc_2_2_2_3_2_4_2_9_kn_d7_outbox.rs",
]

# ──────────────── §3 Signals, Automations, Triggers & the firehose (Bus) ────────────────────────

[[contract]]
row = "3.1"
title = "define_signal_rule"
status = "deferred"
landing = "P-138"

[[contract]]
row = "3.2"
title = "register_automation"
status = "covered"
cdc = ["crates/myelin-query/tests/cdc_3_2_automations.rs"]

[[contract]]
row = "3.3"
title = "arm_trigger / disarm_trigger"
status = "covered"
cdc = ["crates/myelin-query/tests/cdc_3_3_triggers.rs"]

[[contract]]
row = "3.4"
title = "EventMatcher = the frozen QueryAst"
# P-137 (EB-17) LANDED the `EventMatcher` (= the frozen `QueryAst`) in `myelin-query` (matcher.rs:
# the one bounded predicate engine, the envelope projection, the JetStream subject-filter compile,
# the leak-free permission compose). CI-P10 (P-353, M4) is its FIRST cross-subsystem CONSUMER and
# ships the pair file: an `on: pull_request` CI trigger compiles to that one matcher (NOT a CI DSL,
# NOT CEL) — the PROVIDER half (myelin-query's matcher) + the CONSUMER half (CI dispatch) both in
# `cdc_ci_dispatch_trigger.rs`. The prior `deferred → P-137` deferral was stale (P-137 had shipped);
# this flips it covered now that a real consumer + pair exist.
status = "covered"
cdc = ["crates/myelin-ci-dispatch/tests/cdc_ci_dispatch_trigger.rs"]

# 3.5 firehose: SPLIT into the Bus-owned protocol half (subscribe/resume + zero-loss-replay) and the
# SUBSTRATE-owned bounded-and-sheds half (§7.7). The substrate half — per-connection in-flight frame
# caps + slow-consumer drop to resync_required (P-S28 / P-135) — shipped with its CDC pair
# (myelin-substrate/tests/cdc_3_5_firehose_backpressure.rs) + the SUB-D11 hot-stream drill. The
# Bus-owned protocol half NOW SHIPS in P-141 (EB-21, built FIRST per EI-04 §2.2): the resume-cursor
# subscription protocol (subscribe/resume + per-(stream,scope) monotonic seq + (last_seq,now] backfill
# losing zero ops + out-of-window -> resync_required + a bounded scope that rejects `*`), in
# myelin-events/src/firehose.rs. The D-10 reconnect-loses-zero-ops drill
# (myelin-events/tests/drills_eb21_firehose_d10.rs) proves 0 ops lost + the resync path + the
# over-broad-scope rejection. The scope-bounded selector + per-surface frame shed budgets are P-136
# (P-S29). The *.snapshot rebuild a resync_required falls back to (cold==live, BUS-D5) is P-142 (EB-22).
[[contract]]
row = "3.5"
title = "firehose transport + resume-cursor protocol"
status = "covered"
# CONSUMER leg — Notif's `inbox watch` (NOTIF-P15 / P-193, §7 / C4) rides this SAME frozen protocol
# (no bespoke Notif transport): scope = inbox:<principal> (a fourth bounded scope kind, EXTENDED in
# place on the one ScopeKind grammar, never `*`); a reconnect backfills (last_seq,now] then live (0
# items lost); an over-old cursor -> resync_required -> a full list_inbox cold rebuild (NAMED). The
# D-N11 resume-leg drill (drill_notif_d11.rs) proves 0 items lost.
cdc = [
    "crates/myelin-events/tests/cdc_3_5_firehose_resume_cursor.rs",
    "crates/myelin-notif/tests/cdc_3_5_notif_inbox_watch.rs",
    # CONSUMER leg — chat (CHAT-P3 / P-245) VALIDATES its per-view firehose scope shape
    # `channel:<id>` against this SAME frozen protocol's `*`-rejecting chokepoint (the bounded
    # `Channel` selector, never `*`; the `resync_required -> *.snapshot` fallback names chat's
    # durable channel/message/thread snapshots). No transport here (the live subscribe is CHAT-P9).
    "crates/myelin-chat/tests/cdc_3_5_chat_firehose_scope.rs",
    # OWNED-SEAM leg — Knowledge (KN-P07 / P-297) owns the resume-cursor + idempotent-apply
    # DISCIPLINE over the Bus transport seam: SEND_OP assigns the per-doc monotone op_seq (== the
    # firehose seq), PERSISTs idempotent on UNIQUE(op_id) (a re-delivered op is a no-op), and
    # CONNECT/RECONNECT backfills (last_seq, now] then live (0 lost, 0 dup) -> resync_required ->
    # the *.snapshot cold path (NAMED). The KN-D1 drill (drill_kn_d1_resume_cursor.rs) proves
    # 0-lost/0-dup across a real kill+sever, written to re-run green across the KN-P29 CRDT boundary.
    "crates/myelin-knowledge/tests/cdc_3_5_knowledge_resume_cursor.rs",
    # CONSUMER leg — CI's live-tail VIEWER (CI-P21 / P-364) rides this SAME frozen protocol: the
    # `LiveTail` viewer subscribes/resumes on the bounded `run:<id>` scope (never `*`); a reconnect
    # backfills (last_seq, now] then live (0 log lines lost — CI-D11); an over-window cursor ->
    # resync_required -> a range-read of the sealed segments (the durable archive, never lost). The
    # CI-D11 drill (drills_ci_p21_live_tail.rs) proves 0 lost lines + the bounded-scope rejection.
    "crates/myelin-ci-controlplane/tests/cdc_live_tail_3_5_11_8.rs",
]

# 3.6 reactive/dispatch tier (EB-23 / P-143). OWNED by the Bus; built in myelin-query (the
# DOCUMENTED DEVIATION — the tier consumes curated Signals + the EventMatcher, both of which live in
# myelin-query, which depends on myelin-events; siting it in myelin-events would invert the §2.9 DAG,
# the same deviation the matcher/signals/automations/triggers already follow). The CDC pair pins the
# 3.6 PROVIDER (the dispatch tier derives the nested-causality action envelope + reserves) against the
# 8.6 CONSUMER (the agent EventInbox reads the exact envelope) + the 11.7 CONSUMER (reserve/settle
# enforces no-balance-no-execution). BUS-D1/BUS-D3/BUS-D6 drills prove 0-lost/0-dup, replay==original,
# and depth-ceiling+tripwire-trips-the-breaker. FLOORS named: the 8.6 real EventInbox is AG-P4/P-216;
# the 11.7 real CostLedger is P-ST-16/P-103 + P-ST-19/P-146; the OQ-K shed-budget numbers are EB-29.
[[contract]]
row = "3.6"
title = "reactive/dispatch tier"
status = "covered"
cdc = ["crates/myelin-query/tests/cdc_3_6_dispatch_tier.rs"]

# ──────────────── §4 Identity & access (myelin-identity) ────────────────────────────────────────

# 4.1 authenticate -> Principal: BOTH halves now ship their CDC pair — the human/SSO half
# (OIDC/SAML/SCIM/passkey/SSH, P-065) and the capability-token + machine-identity half
# (PAT/CI/agent/deploy-key/per-job, P-066). The two pairs together cover the 4.1 seam.
[[contract]]
row = "4.1"
title = "authenticate -> Principal"
status = "covered"
cdc = [
    "crates/myelin-identity-service/tests/cdc_4_1_authenticate.rs",
    "crates/myelin-identity-service/tests/cdc_4_1_machine_identity.rs",
]

[[contract]]
row = "4.2"
title = "check (per-action gate, fail-closed)"
# COVERED by P-ID-09 / P-067: the depth-bounded Zanzibar userset-rewrite `check` engine
# (crates/myelin-identity-service/src/check_engine.rs) — memoised-per-request, fail-closed on
# genuine uncertainty, evaluated at the zookie snapshot, over the raw S3 tuples (direct grants +
# tuple-to-userset inheritance). The `CaveatContext` rider is the literal-only floor (a
# missing-context caveat → Conditional, never a silent Allow). The provider (the check engine via
# StoreBackedCheck) + consumer (a write-path caller gating an action) CDC pair ships here. Floors:
# the compiled-permission namespace engine → P-068 (P-ID-10); the full QueryAst caveat core → P-133
# (P-ID-22); the fail-static cache + zookie-bypass → P-073 (P-ID-15).
status = "covered"
# KN-P14 / P-304 (M3) adds the KNOWLEDGE consumer half: the Layer-2 per-op authority
# (myelin_knowledge::OpAuthorizer) gates EVERY collab op on `check(actor, edit|comment, page_ref,
# Strong@page.acl_zookie)` before merge (architecture 02 §3.1) — applies ONLY on `Allow`, fail-closes
# on Deny/Conditional/error, and reads at-or-after `page.acl_zookie` so a just-revoked editor's op is
# rejected (the 4.10 new-enemy guard). The consumer CDC + the just-revoked drill (0 stale-grant
# writes) + the schema-validation gate (0 invalid rows) ride here. The read-side list_objects SetExpr
# push-down is the named KN-P16 follow-on.
# ISS-P06 (P-372, M4) adds the ISSUES consumer half: the Issues write path
# (myelin_issues::apply_mutation) gates EVERY mutation on `check(actor, <perm>, issue_ref, Strong, +
# CaveatContext)` BEFORE the same-tx co-commit — applies ONLY on Allow, fail-closes on Deny/Conditional/
# error (the transaction is dropped without commit → nothing written, emit-iff-committed). A transition
# carries the transition-level CaveatContext (off the hot list_objects path, OQ-E). The drill proves a
# denied write co-commits nothing (0 ghost).
cdc = [
  "crates/myelin-identity-service/tests/cdc_4_2_check.rs",
  "crates/myelin-knowledge/tests/cdc_4_2_4_10_knowledge_per_op_authority.rs",
  "crates/myelin-issues/tests/drill_iss_p06_emit_iff_committed.rs",
]

[[contract]]
row = "4.3"
title = "list_objects (SetExpr push-down)"
# COVERED by P-ID-11 / P-069: the return-shape dispatch + the S4 `Ids` materialise path over the S8
# reverse index + the `Filter` push-down shape above the cardinality cap. The provider
# (StoreBackedCheck::list_objects → Ids|Filter, leak-free) + consumer (a list/board query rendering
# exactly the pre-filter, conjoining the Filter push-down, never a post-filter) CDC pair ships here.
# S8 (the per-tenant authz reverse index) is fed off the bus by the iam.tuple_written EventHandler
# and carries the revision_watermark. Floors: the full Filter SetExpr→SQL no-N+1 lowering + the
# watermark read-consistency path → P-070 (P-ID-12); list_subjects → P-071 (P-ID-13); the
# Ids↔Filter cardinality cap finalised at scale → P-074 (P-ID-31).
status = "covered"
# GIT-P26 / P-288 adds the GIT-side consumer half: `myelin_git::list_filter` lowers the returned
# `Filter{set_expr}` over Git's OWN id column (`repo.id`/`pr.id`, §5.3/§7.3) into ONE leak-free list
# query (the `authz_visible` JOIN, no N+1, no post-filter) + the code-search pre-filter (6.1). The
# git consumer CDC + the GIT-D11 chained drill ride here; the live one-query/0-leak/revoke-reflected
# proof is the `--features integration` test against the dev-stack Postgres.
# KN-P16 / P-306 adds the KNOWLEDGE-side consumer half: `myelin_knowledge::list_filter` lowers the
# returned `Filter{set_expr}` over Knowledge's OWN `db_row.id`/`page.id` (§4.1/§7.3) into ONE leak-free
# db VIEW query AND the **permission-correct `COUNT(*)`** (the KN-D5 count-leak-closed shape — the ACL
# conjunct INSIDE the aggregate so a COUNT over a row-restricted db reveals neither the existence nor
# the number of forbidden rows). The knowledge consumer CDC + the KN-D5 chained drill (0 leak, 0
# count-leak, revoke reflected) ride here; the live one-query / 0-leak / 0-count-leak / revoke-
# reflected proof is the `--features integration` test against the dev-stack Postgres
# (`integration_kn_d5_list_pushdown.rs`).
#
# KN-P18 / P-308 adds the ROLLUP consumer leg: a read-time rollup over a `rollup_source` relation
# CONJOINS the SAME `list_objects` SetExpr (the InRelation{read} → authz_visible JOIN over db_row.id)
# the view executor uses, so a restricted related row is never counted/summed for an unauthorized
# viewer (0 rollup leak, composing with KN-D5). The rollup-permission gate rides in
# cdc_13_3_4_3_knowledge_rollup.rs; the live permission-filtered rollup conjoin against the dev-stack
# Postgres is integration_kn_d10_rollup.rs.
#
# CI-P25 / P-368 (M4) adds the CI-side consumer half: `myelin_ci_controlplane::surfacing` lowers the
# returned `Filter{set_expr}` over CI's OWN `ci_run.run_id` column (§5.1 / §7.3 / OQ-E) into ONE
# leak-free run-list query (the `authz_visible` JOIN, no N+1, no post-filter) + the CI search
# pre-filter (`run_search_pre_filter`, the `search-requires-acl-filter` lint binder). The CI consumer
# CDC (`cdc_4_3_ci_list_pushdown.rs`) proves CI lowers the FROZEN SetExpr variants exactly + the
# leak-free / revoke-reflected row set; the `myelin ci list` CLI rides this push-down. The REAL
# one-query / 0-leak / tenant-scoped / revoke-reflected proof against the live dev-stack Postgres is
# `crates/myelin-ci-controlplane/tests/integration_ci_p25_list_pushdown.rs` (the `integration` feature).
cdc = [
  "crates/myelin-identity-service/tests/cdc_4_3_list_objects.rs",
  "crates/myelin-git/tests/cdc_4_3_git_list_pushdown.rs",
  "crates/myelin-knowledge/tests/cdc_4_3_knowledge_list_pushdown.rs",
  "crates/myelin-knowledge/tests/cdc_13_3_4_3_knowledge_rollup.rs",
  "crates/myelin-ci-controlplane/tests/cdc_4_3_ci_list_pushdown.rs",
]

[[contract]]
row = "4.4"
title = "list_subjects + explain"
# COVERED by P-ID-13 / P-071: the Zanzibar Expand served by S8 at 50k-member density.
# `list_subjects(object, permission, zookie?) → SubjectTree` flattens the concrete subjects holding a
# permission (a direct relation served by the S8 reverse index `subjects_for` — an indexed lookup at
# density, not a per-member scan; a compiled permission composes the four operators over the
# per-relation S8 expansions) + `explain → RewriteTrace` returns the userset-rewrite trace
# (non-empty, ending ALLOW/DENY, never a silent allow). The provider
# (StoreBackedCheck::list_subjects_in / explain_in over S3 + S8) + consumer (an admin-inspector /
# HITL-approver-set rendering the membership) CDC pair ships here. The 50k-density synthetic GATE is
# the lib unit `list_subjects_50k_member_density_within_budget`; the REAL-density proof (the watcher
# relation + Chat channels) is the named floor → P-134 (P-ID-23) + M4.
status = "covered"
cdc = ["crates/myelin-identity-service/tests/cdc_4_4_list_subjects.rs"]

[[contract]]
row = "4.5"
title = "delegation -> EffectivePolicy"
# COVERED by P-ID-17 / P-075: the monotone-intersection delegation algebra. `delegation(agent,
# trigger_actor) → EffectivePolicy` composes `agent.policy ∩ delegation ∩ tenant.policy` as a
# monotone set intersection over the macaroon/biscuit caveat sets (the SAME Authority::attenuate
# primitive `authenticate` uses — one intersection primitive), with the "you cannot delegate
# authority you do not have" re-check at mint (the delegation is first intersected with the trigger
# actor's own held authority, so a delegator who LOST the right shrinks the agent's effective set —
# the ID-D5 case). The provider (DelegationAlgebra via StoreBackedCheck::delegation_in, returning the
# frozen EffectivePolicy + the recorded IntersectionProof) + consumer (an EffectApi-side caller that
# applies a proposed effect iff its capability is inside the effective set) CDC pair ships here. The
# fourth conjunct (the ordinary object check run AS the agent) is wired via delegation_with_check_in
# over the live CheckEngine. ID-D5 (drill_id_d5_delegation) proves 0 effects escape the intersection
# + emits the intersection proof. The M2 re-run against the live EffectApi is the named floor → P-134
# (P-ID-23).
status = "covered"
cdc = ["crates/myelin-identity-service/tests/cdc_4_5_delegation.rs"]

[[contract]]
row = "4.6"
title = "write_tuples -> zookie"
# COVERED by P-ID-08 / P-057: the S3 ReBAC tuple store + write_tuples atomic write → advanced
# zookie → iam.tuple_written via the OUTBOX (the only emit path). The provider (write_tuples
# emits + returns the zookie) + consumer (a role-compile caller stamps the returned zookie on the
# object) CDC pair ships here. The 4.10 write-half (monotone-advancing zookie) rides this pair;
# the 4.10 read-half (S8 watermark) is the named floor → P-070 (P-ID-12).
status = "covered"
cdc = ["crates/myelin-identity-service/tests/cdc_4_6_write_tuples.rs"]

[[contract]]
row = "4.7"
title = "mint_run_token + revoke"
status = "deferred"
landing = "P-076"
# The self-hosted-runner SCOPE side of 4.7 (§4 — a self-hosted runner token is scoped to ONE tenant's
# `SelfHosted` jobs, cannot mint or act cross-tenant; the no-global-pool property at the identity
# layer) is EXERCISED against the live CI fragment (4.9) by P-ID-28 / P-321. The mint-half ceiling
# (the [`mint`] one-tenant `SelfHostedScopeViolation` on a `MachineKind::PerJob` mint) shipped at
# P-076/P-ID-18; P-ID-28 proves it end-to-end against the CI namespace: 0 cross-tenant tokens minted
# AND 0 cross-tenant job/secret reads by a self-hosted runner against the CI `run`/`secret` objects.
# CDC: crates/myelin-identity-service/tests/cdc_4_7_self_hosted_ci_scope.rs (the CI-dispatch consumer ↔
# Identity mint+check provider pair). Drill (CI-D10 scope side, dated green artifact):
# crates/myelin-identity-service/tests/drill_ci_d10_self_hosted_scope.rs.

[[contract]]
row = "4.8"
title = "resolve_pseudonym + erase (pseudonym map)"
status = "deferred"
landing = "P-078"
# P-273 (GIT-P12): the GIT half of 4.8 — pseudonymous-by-default commit identities, ENFORCED at
# receive-pack (the erasure-vs-immutability data-model gate, GIT-1). The data-model + codec half
# (pseudonymous-by-construction `myelin_git::commit::Commit`) shipped at P-257/GIT-P25; the schema
# `author_pseudonym`/`reviewer_pseudonym`/`pusher_pseudonym` columns (tagged `#[personal_data]`) at
# GIT-P3. P-273 adds the ADMISSION half: `myelin_git::commit::enforce_pseudonymous_commit` +
# `receive_pack::PushPolicy`'s pseudonymity rule REJECT a pushed commit whose author/committer
# identity is not `<pseudonym>@<tenant>.noreply` for the principal's tenant, BEFORE the ref moves —
# so the immutable object DB admits 0 cleartext PII in a commit identity field (the GIT-D2 GIT-1
# half). Enforcement default (OQ-10/R-8): REJECT-AT-PUSH (client-cooperative, sha-stable; the
# server-side rewrite-at-push mode is the GIT-P29 follow-on — see myelin_git::commit). 10.9
# instantiated BY REFERENCE (never restated). GF-7 floor: structural mechanism across
# GIT-P9/GIT-P12/GIT-P29; the lawful-basis residual is the ONE posture's [OPEN — LEGAL] (R-7,
# parallel/Legal). The receive-pack scan green artifact:
# crates/myelin-git/tests/drills_git_d2_receive_pack_pseudonymity.rs (0 cleartext-PII commit
# identities admitted, dated); the GIT half CDC:
# crates/myelin-git/tests/cdc_4_8_git_pseudonymous_commits.rs
# (cdc_4_8_git_enforces_pseudonymous_commit_identity_at_the_door).
# P-373 (ISS-P07, M4): the ISSUES CONSUMER half of 4.8 — pseudonymous-by-default Issues identity
# columns. `myelin_issues::pseudonym::{IssuePseudonym, pseudonymise}` admits ONLY a
# `<pseudonym>@<tenant>.noreply` handle into an `assignee`/`reporter`/`created_by`/actor column
# (resolved through the ONE Identity person↔pseudonym map, `resolve_pseudonym`) — a raw id / name /
# email is REFUSED, the write fails closed (0 raw id at rest, recon §X-7). The row stays `deferred`
# because the `resolve_pseudonym`/`erase` BODY is Identity's P-078; Issues is a CONSUMER of the frozen
# grammar (the `myelin_identity::PseudonymHandle` carrier). CDC + at-rest artifacts:
# crates/myelin-issues/tests/cdc_4_8_11_4_issues_pseudonym_dek.rs (provider: the write path resolves a
# pseudonym; consumer: the column parses back through the frozen grammar — 0 raw id) and the
# live-Postgres at-rest proof crates/myelin-issues/tests/integration_iss_p07_subject_dek.rs (the stored
# reporter column is a pseudonym, never the raw principal id).

[[contract]]
row = "4.9"
title = "per-subsystem ReBAC namespace fragment (engine + admit-contract + core hierarchy)"
# The engine + admit-contract + core-hierarchy half (Identity, P-068). The per-subsystem fragment
# SLICES of the same row land with each subsystem's freeze: the GIT fragment (ref-glob + CODEOWNERS-
# as-relations + approve_untrusted_ci + watcher) is GIT-P1 / P-123 — its CDC declares the Git
# fragment (CONSUMER) + admits it through the real engine (PROVIDER), proving it COMPILES into the
# cell schema (the GIT-P1 gate) and its rewrites are admissible (wired LIVE in GIT-P13 / P-ID-24).
# The ISSUES fragment SLICE (the `issue` namespace + the `- confidential` set-difference userset +
# `watcher` + the field/transition ABAC sub-objects) is ISS-P01 / P-125 — its CDC declares the
# Issues fragment (CONSUMER) + admits it through the real engine (PROVIDER), proving it COMPILES into
# the cell schema (the ISS-P01 gate) AND that the `- confidential` set-difference resolves LEAK-FREE
# (a confidential issue is absent from view for a non-grantee). The Id-side COMPILED Issues fragment
# (the rich FragmentDefs Id OWNS — the `issue`/`field`/`transition` types + the rewrites:
# `view = (parent_project->view − confidential) ∪ confidential_grant`, `comment = view`,
# `transition = assignee ∪ parent_project->view`, `manage = parent_project->view`,
# `field.view_field = parent_issue->view`, `transition.perform_transition = parent_issue->transition`)
# is P-ID-29 / P-322 (M4) — `issue_fragment.rs` (PROVIDER); its CDC
# (cdc_4_9_issue_fragment.rs) admits the three rich FragmentDefs into the cell schema, proves the
# confidential issue disappears for a normal reader BY CONSTRUCTION (the − confidential Exclusion, not
# a post-filter), the board conjoins in ONE query (the issue.id Filter push-down, §7.3), and the
# field/transition caveats hide a field / gate a transition OFF the hot path through the ONE QueryAst
# core (a denied one Deny, a missing-context one Conditional, never a silent allow). The ISS-D3 authz
# side is the dated green drill (drill_iss_d3_confidential_cross_tenant.rs): 0 confidential-issue
# leaks to a fleet of project readers, 0 cross-tenant leaks, AND 0 leak under zookie staleness (the
# watermark guard falls back to per-row check over authoritative S3 — the new-enemy guard).
# RECONCILIATION (EI-01 §1): the §5 vocabulary names the sub-objects `field`/`transition` (the
# canonical authz spelling Id owns); the Issues carrier (ISS-P01) names them `issue_field`/
# `issue_transition` (a prefixed spelling) — the permission NAMES (`view_field`/`perform_transition`)
# + the rewrite STRUCTURE match, which is what the CDC pins. The §5 `parent_project->read`/`->write`
# inheritance edges resolve through the core hierarchy's single compiled `project.view` (the SAME
# reconciliation the Git fragment makes for `repo.pull`/`repo.push`).
# The CHAT fragment SLICE (the `channel` + `message` namespace; the frozen `channel.read = member +
# parent_project->read` clause, recon §1; the `watcher` Notif read-fanout relation) is CHAT-P2 / P-244
# — its CDC declares the Chat fragment (CONSUMER) + admits it through the real engine (PROVIDER),
# proving it COMPILES into the cell schema (the CHAT-P2 gate) AND that `channel.read` resolves (a
# member reads; a public-channel project-reader inherits read; `message.view` inherits the channel's
# read; `watcher` is read-fanout, not a read grant). Rewrites wired LIVE on the Chat M4 spine; the
# runtime membership tuple writes are the CHAT-P8 floor.
# The Id-side COMPILED Chat fragment (the rich FragmentDefs Id OWNS — the `channel`/`message`/`unfurl`
# types + the rewrites: `channel.read = member ∪ parent_project->view` (watchable C8),
# `message.view = parent_channel->read`, `unfurl.view = parent_message->view`) is P-ID-30 / P-323 (M4)
# — `chat_fragment.rs` (PROVIDER); its CDC (cdc_4_9_chat_fragment.rs) admits the three rich FragmentDefs
# into the cell schema, proves `channel.read` resolves via the `member` arm AND the `parent_project->view`
# arm (a non-member denies), `message.view` inherits the channel, and the ambient channel list conjoins
# in ONE query (the channel.id Filter push-down, §7.3) leak-free (a non-member channel is ABSENT, no
# count leak). The CHAT authz side is the dated green drill (drill_chat_unfurl_tombstone_density.rs):
# (F1) a confidential unfurl degrades to a TOMBSTONE for a 64-member fleet — 0 chat title leak (the Refs
# check(viewer, view, target) over the target issue's own − confidential exclusion; Id never recomputes
# the target ACL); (F1) a non-member's channel search returns 0 results; (F1) list_subjects(channel,
# watcher) at 50k density returns ALL 50k watchers within budget, 0 cross-channel leak (§7.5, the
# ordinary S8 Expand; the tighter SLO is the M5 measured tunable P-ID-32).
# **With the Chat fragment admitted, the M1 engine-only floor (P-068) is CLOSED — all five subsystem
# fragments (Git/Knowledge/CI/Issues/Chat) now exist in the engine.**
# RECONCILIATION (EI-01 §1): §5 names the inheritance edge `parent_project->read`; it resolves through
# the core hierarchy's single compiled `project.view` (the SAME reconciliation Git/Issues make). The
# P-ID-23 / P-134 m2-consumers drill built an ad-hoc placeholder `channel` (member + watcher, no
# permissions) as a minimal watchable test fixture; the CANONICAL compiled Chat fragment now lives in
# `chat_fragment.rs` — the placeholder remains a valid self-contained fixture in its own test binary,
# not a second definition of the namespace.
# The KNOWLEDGE fragment SLICE (the `space`/`page`/`block`/`database_row` namespace; the
# page-tree-with-overrides rewrite `page.read = (parent_page->read ∪ parent_space->read ∪
# direct_reader) − direct_block`; the `database_row.read` row-level ACL the list_objects push-down
# keys on via `db_row.id` §7.3; the field-level column hiding as the off-hot-path `view_field`
# CaveatContext caveat over the ONE QueryAst core §8.6) is P-ID-26 / P-249 — Id's compiled
# Knowledge fragment (PROVIDER) admits the four rich FragmentDefs into the cell schema and its
# rewrites resolve the page-tree override LEAK-FREE (a `- direct_block` sub-page is absent from
# list_objects INCLUDING the COUNT). The Id-side compiled-fragment CDCs co-own the PROVIDER half.
# The KNOWLEDGE-side CONSUMER half (KN-P15 / P-305, M3) ships in `myelin-knowledge`: the read-side
# names-only fragment carrier (`myelin_knowledge::rebac_fragment` — the `space`/`page`/`block`/
# `database_row` types + the §5 read relations/permissions, the SAME canonical names the engine's
# rich fragment resolves over, asserted against the write-side carrier + the §5 vocabulary) + the
# page.read OVERRIDE-FORMULA gate (`page_read_override`: `(inherited ∪ direct_reader) − direct_block`
# — `direct_block` removes a narrowed sub-page, `direct_reader` adds it, the exclusion wins last) +
# the row-level `SetExpr` push-down SHAPE (`InRelation { row_reader, db_row.id }`, the KN-P16 SQL
# lowering target) + the no-cross-db lint green. The leak-critical SQL lowering itself is KN-P16/P-306.
# The CI fragment SLICE (the `ci_project`/`environment`/`secret`/`run` namespace; `run.view =
# parent_repo->pull`; `run.trigger = parent_repo->push`; the secret-non-inheritance invariant
# `secret.read = direct_reader` — DIRECT NARROW, NOT a parent_ci_project inheritance, CI-1 §1; the
# `read & !is_untrusted_fork` ABAC edge `run.read = run.view - is_untrusted_fork`, C7 §X-1) is
# P-ID-27 / P-320 — Id's compiled CI fragment (PROVIDER) admits the four rich FragmentDefs into the
# cell schema on top of the Git `repo` fragment (the `parent_repo->pull/push` inheritance terminates
# there), and its rewrites resolve the two security-critical invariants: a `ci_project` reader/admin
# gets 0 secret reads via inheritance (secret-non-inheritance), and a stamped untrusted-fork run's
# output is gated by construction by the Exclusion. The Id-side compiled-fragment CDC co-owns the
# PROVIDER half; the self-hosted-runner token SCOPE is exercised against this live fragment by P-ID-28
# / P-321 (CI-D10 scope side — 0 cross-tenant job/secret reads by a self-hosted runner; see the 4.7
# row note + drill_ci_d10_self_hosted_scope.rs).
# The CI-side CONSUMER half (CI-P8 / P-351, M4) ships in `myelin-ci-controlplane`: the read-side
# names-only fragment carrier (`myelin_ci_controlplane::rebac_fragment` — the
# `ci_project`/`environment`/`secret`/`run` types + the §5.2 relations/permissions, the SAME canonical
# names the engine's rich fragment resolves over) + the FROZEN `read & !is_untrusted_fork` edge's CI-side
# names anchor (the `is_untrusted_fork` SUBTRACTED relation + the `read` permission it gates, so the
# engine's `run.read = run.view − is_untrusted_fork` Exclusion compiles — a fork subject classified a
# non-reader BY CONSTRUCTION) + the secret-non-inheritance (`secret.read = direct_reader`, DIRECT NARROW,
# CI-1) + the protected-env `approve` `list_subjects` target + the `watcher` read-fanout relation. The
# CDC (cdc_4_9_ci_rebac_fragment.rs) asserts the frozen §5.2 vocabulary literals (it does NOT import the
# engine crate — the §2.9 acyclic DAG forbids a leaf consumer depending on `myelin-identity-service`); the
# engine-side cdc_4_9_ci_fragment.rs asserts the SAME literals + admits them through the real engine. The
# fragment CONSUMERS are named floors: the trust-tier classifier that stamps `is_untrusted_fork` (CI-P12),
# the secret-broker fork-no-secrets edge (CI-P24), the `list_objects` push-down (CI-P25).
status = "covered"
cdc = [
    "crates/myelin-identity-service/tests/cdc_4_9_namespace_engine.rs",
    "crates/myelin-identity-service/tests/cdc_4_9_git_fragment.rs",
    "crates/myelin-identity-service/tests/cdc_4_9_knowledge_fragment.rs",
    "crates/myelin-identity-service/tests/cdc_4_9_ci_fragment.rs",
    "crates/myelin-identity-service/tests/cdc_4_9_issue_fragment.rs",
    "crates/myelin-identity-service/tests/cdc_4_9_chat_fragment.rs",
    "crates/myelin-knowledge/tests/cdc_4_9_knowledge_rebac_fragment.rs",
    "crates/myelin-git/tests/cdc_4_9_git_fragment.rs",
    # The CODEOWNERS-as-relations RESOLVER slice (GIT-P16 / P-277, M3-G3): Git owns the CODEOWNERS
    # file PARSE + the path MATCHER (00-overview §1.1: "Git decides which glob a change matches"); the
    # resolver compiles each path pattern to a `ref:<repo>::<glob>#code_owner@<owner>` reviewer-
    # requirement tuple (the CONSUMER), byte-identical to the engine's `compile_codeowners` (the
    # PROVIDER half), and the CDC PROVES the written tuples resolve through the real engine's
    # `list_subjects(ref, code_owner)` Expand with 0 mis-resolved owners over a fixture CODEOWNERS.
    "crates/myelin-git/tests/cdc_4_9_git_codeowners.rs",
    "crates/myelin-issues/tests/cdc_4_9_issues_fragment.rs",
    "crates/myelin-chat/tests/cdc_4_9_chat_fragment.rs",
    "crates/myelin-ci-controlplane/tests/cdc_4_9_ci_rebac_fragment.rs",
    # CI-P10 (P-353, M4): the CI Trigger & Dispatch CONSUMER of the `read & !is_untrusted_fork` edge.
    # The trust-tier classifier reads the edge's verdict (`read_excludes_fork`) and stamps the SAME
    # evaluated tier onto BOTH `JobSpec.trust_tier` AND `CheckStatus.trust_tier` (X-1, 0 divergence) —
    # the security-critical single stamp. (The `is_untrusted_fork` STAMP-WRITE half stays CI-P12.)
    "crates/myelin-ci-dispatch/tests/cdc_ci_dispatch_trigger.rs",
]

[[contract]]
row = "4.10"
title = "Consistency / zookie semantics"
# COVERED by P-ID-15 / P-073: the zookie-bypass-S6 half (a `Strong`/zookie-stamped read BYPASSES
# the fail-static cache and is fail-closed-or-wait; a default-consistency `BoundedStale` read is
# served static during a hiccup). The read-your-writes / S8-watermark half rides the 4.6 write-half
# pair + P-070 (P-ID-12); this row's fail-static-bypass consumer+provider pair ships in the 4.11 CDC.
status = "covered"
# KN-P14 / P-304 (M3) adds the KNOWLEDGE read-your-writes consumer half: the Layer-2 per-op gate reads
# `check` at a STRONG Consistency at-or-after the page's stamped `page.acl_zookie` (architecture 03
# §3.3 the new-enemy guard), so a grant revoked at-or-after the stamp cannot be read stale — the
# just-revoked editor's op is rejected (0 stale-grant writes). A stale client zookie cannot downgrade
# the watermark (the read is at-or-after the page's CURRENT acl_zookie). The consumer CDC + drill ride
# the 4.2 pair; this row cites the 4.10 read-your-writes half Knowledge consumes.
cdc = [
  "crates/myelin-identity-service/tests/cdc_4_11_fail_static.rs",
  "crates/myelin-knowledge/tests/cdc_4_2_4_10_knowledge_per_op_authority.rs",
]

[[contract]]
row = "4.11"
title = "FailStatic bound (Id usage)"
# COVERED by P-ID-15 / P-073: S6 (the fail-static availability cache) + the structural bound
# `static_max ≤ revocation-SLA ≥ agent-token-TTL` (W = 5 min default-to-beat, `[OPEN — LEGAL]` L-1).
# PROVIDER = Identity's S6 (`failstatic_cache::FailStaticCache` over the M0 `FailStatic<T>`);
# CONSUMER = a critical-dep caller wrapping `check` in the `ResilientClient` (1.9) + S6 (1.10),
# surviving a transient Id hiccup on the coarse grant. The number W stays the L-1 legal follow-on.
status = "covered"
cdc = ["crates/myelin-identity-service/tests/cdc_4_11_fail_static.rs"]

# ──────────────── §5 ArtifactRef, refs & projection (myelin-refs) + Git<->CI check seam ─────────

[[contract]]
row = "5.1"
title = "ArtifactRef"
# COVERED by REF-P1 / P-052: the value-type half of 5.1 (parse/format + the frozen #sub grammar).
# The resolve half (5.2/5.7 tombstone ladder) is a separate row; this row's parse/format pair ships
# its provider+consumer CDC here.
status = "covered"
# CI-P25 / P-368 (M4) adds CI's owned canonical mints (`myelin://<t>/ci/{run|deployment|pipeline|
# runner|artifact}/<id>`) through the ONE refs codec (0 ungrammatical by construction); the
# provider+consumer pair (cdc_5_1_5_6_5_7_ci_surfacing.rs) round-trips the mint + strips the `#sub` to
# the canonical root. The deployment/pipeline/runner canonical-root type tokens are added to the
# frozen Bus §6.2 ARTIFACT_TYPE_TOKENS table (the EB-24 git/chat root-type extension, same posture).
# ISS-P08 / P-374 (M4) adds the ISSUES slice: the Hi/Lo allocator (myelin_issues::HiLoKeyAllocator)
# mints the STORED CANONICAL `<PROJECTKEY>-<seqno>` id as the `<id>` segment of the issue's
# `myelin://<t>/issue/issue/<key>` URN (recon REF-3 — the human key IS the ArtifactRef id). The
# provider+consumer pair (cdc_5_1_iss_key.rs) proves the allocator mints the canonical key, the ONE
# refs codec round-trips it byte-identical, and the render-time `#<seqno>` short form is REJECTED as a
# scope (display-only, never stored). The allocator's correctness drill is ISS-D4 (create-storm on one
# hot prefix): the DEFAULT-build leg is drill_iss_d4_create_storm.rs (24 workers x 1000 -> 24000
# distinct keys, 0 dup, monotonic 1..=total, per-prefix isolation) and the LIVE-Postgres
# `prefix_counter` `UPDATE … RETURNING` leg is integration_iss_p08_key_storm.rs (12 workers x 400 ->
# 4800 distinct keys, 0 dup, monotonic <= the durable high-water, OPS isolated — dated green
# 2026-06-23 against the dev stack). The allocation slots into the ISS-P06 write path via
# write_path::create_issue (the minted key co-commits with issue.created — contract 2.2).
cdc = [
    "crates/myelin-refs/tests/cdc_5_1_artifactref.rs",
    "crates/myelin-ci-controlplane/tests/cdc_5_1_5_6_5_7_ci_surfacing.rs",
    "crates/myelin-issues/tests/cdc_5_1_iss_key.rs",
]

[[contract]]
row = "5.2"
title = "resolve(ref, viewer, mode)"
# GIT-P18 / P-279 ships the GIT-OWNED half of 5.2/5.6: `project(ref, viewer)` for git artifacts
# (`myelin_git::project::Projector::project`) — the ONLY way Refs/Search/Notif read a git artifact
# (no cross-DB), per-viewer permission-checked: a denied viewer gets a content-free Tombstone, NEVER
# the title (the 0-leak invariant that feeds the M3-G5/M5 leak drills GIT-D11 / SRCH-D1/D3). The CDC
# carries the provider (git mints canonical keys + permission-first project) + consumer (a downstream
# reader sees the {title,state,icon} projection IFF authorized, else "(not available)") pair. FLOOR
# (named): the GENERAL Refs `resolve(ref, viewer, mode)` 4-step tombstone LADDER over ALL subsystems
# (the per-kind resolution spine) is the P-159 Refs-resolver follow-on; the `blob`/`#L<a>-L<b>`
# content-anchored sub-resolver is GIT-P24; cross-cell projection is single-home (the multi-cell floor).
# GIT-P31 / P-292 (M3-G8) wires that GIT-OWNED `project` (5.6) into the 5.2 resolve seam Notif's
# humanise consumes: `myelin_git::git_resolve::GitRefResolver` implements the frozen `RefResolvePort`
# (resolve `Display` → Projection|Tombstone) over the projector — promoting the GIT-P19 test-local
# resolve stand-in (the named floor) to a real producer-crate seam (the leak gate now runs over Git's
# real projection). cell-local (OQ-I); the cross-cell single-home resolve stays the named multi-cell floor.
# KN-P12 / P-302 (M3) adds the KNOWLEDGE CONSUMER half of 5.2: the `sync_block` read-projection FLOOR
# (`myelin_knowledge::render_sync_block`) renders the `sync_block` taxonomy node by resolving its
# `source` per viewer through the SAME 4-step ladder (permission → root → sub-resolve → erased),
# returning the LIVE source subtree IFF the viewer can read it, else a content-free tombstone (the
# `sync_block_leak == 0` gate + the reflect gate). The central Refs `resolve` engine is the R-M2
# follow-on; the per-viewer read decision threads the `SourceReadCheck` seam (the full ABAC body is
# KN-P16). FLOOR (named): read-projection only; editable-in-place multi-home is KQ-6, post-M5 on the
# CRDT (KN-P29).
status = "covered"
cdc = [
    "crates/myelin-git/tests/cdc_5_1_5_2_5_6_git_project.rs",
    "crates/myelin-git/tests/cdc_5_2_7_3_7_1_git_humanise_resolve.rs",
    "crates/myelin-knowledge/tests/cdc_5_2_knowledge_sync_block.rs",
]

[[contract]]
row = "5.3"
title = "backlinks / edges / traverse"
status = "covered"
# ISS-P17 / P-383 (M4) adds the ISSUES-OWNED bounded cycle-safe traverse over the `issue_relation`
# TE-7 source-of-truth (`myelin_issues::refs_glue::IssueRelationGraph::traverse`) — a depth-16
# (contract 5.3) BFS that is cycle-safe (a `blocked_by` cycle terminates with ONE finite result, never
# a hang) and depth-bounded (a node past depth 16 is not expanded). The impact/hierarchy surface + the
# unblock-trigger walk ride this. The Refs-service traverse (cdc_5_3_traverse.rs) is the cross-subsystem
# projection-graph half; this is the producer-owned typed-table walk.
# ISS-P18 / P-384 (M4) adds the ROLLUP consumer of the same depth-16 cycle-safe walk: the rollup's
# `walk_parent_edges` (`myelin_issues::rollup`) is the ancestor walk over the issue_relation `parent`
# edges (a `parent` row is `child --parent--> parent`, so the forward walk from a child IS its ancestor
# walk) — it REUSES the ONE IssueRelationGraph::traverse (no second traversal), depth-bounded at 16 +
# cycle-safe (a `parent` cycle terminates with a finite ancestor set, never a hang). drill_iss_d8_rollup.rs.
cdc = [
    "crates/myelin-refs-service/tests/cdc_5_3_backlinks.rs",
    "crates/myelin-refs-service/tests/cdc_5_3_traverse.rs",
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
    "crates/myelin-issues/tests/drill_iss_d8_rollup.rs",
]

[[contract]]
row = "5.4"
title = "refs.edge.created / refs.edge.removed"
# The 5.4 producer/consumer seam is REAL: producers emit `refs.edge.created` from the three structured
# content nodes via the outbox (no standalone edge-write API), and the Refs edge-builder consumes them.
# REF-P6/P-155 (the edge-builder consumer, cdc_5_4_edge_builder.rs) + REF-P8/P-157 (the content-node
# extraction + same-tx emit seam, cdc_5_4_emit_seam.rs) ship the Refs half. GIT-P17/P-278 adds the
# GIT-OWNED producer half — Git PR/review/comment bodies are `myelin-content` documents whose three
# structured nodes emit the byte-identical `refs.edge.created` wire shape the edge-builder ingests
# (`myelin_git::body::{extract_body_edges, emit_body_edges}`; the CDC models the consumer's field reads
# + the deterministic `edge_id` locally because `myelin-git` cannot depend on the Refs SERVICE crate —
# the §2.9 one-directional edge: `myelin-refs-service` depends on `myelin-git`). FLOOR (named): the
# typed-edge LIFECYCLE mirror (`Closes`/PR-link `refs.edge.*` with `rel_class='lifecycle'`) is the
# DISTINCT GIT-P19 producer + row 5.5; this row's content-node `reference`-class edges ship here.
status = "covered"
# The Git content-edge CDC carries BOTH sides in one file (provider = the Git body producer; consumer =
# the Refs edge-builder's field reads + the deterministic `edge_id`, modelled locally because
# `myelin-git` cannot depend on the Refs SERVICE crate). The Refs-side emit/edge-builder unit + CDC
# (cdc_5_4_emit_seam.rs / cdc_5_4_edge_builder.rs) prove the same seam from the Refs crate; the
# self-complete pair listed here satisfies the gate's per-file provider+consumer evidence.
# KN-P19 / P-309 (M3) adds the KNOWLEDGE-OWNED producer half: the three structured content nodes
# (`mention`/`artifact_ref`/`embed`) in a Knowledge page/block emit the byte-identical
# `refs.edge.created` (reference-class) wire shape via the outbox, NOT coalesced (a discrete edge
# fact); no standalone edge-write API (`myelin_knowledge::refs_glue::emit_content_edges`). The CDC
# carries the provider (the emit shape + the shared edge aggregate) + the consumer field reads
# (reference-class never aliases lifecycle), modelled locally (myelin-knowledge is a producer LEAF).
# ISS-P17 / P-383 (M4) adds the ISSUES-OWNED producer half: the three structured content nodes
# (`mention`/`artifact_ref`/`embed`) in an issue body/comment emit the byte-identical
# `refs.edge.created` (reference-class) wire shape via the outbox, NOT coalesced
# (`myelin_issues::refs_glue::emit_content_edges`). The CDC carries the provider (the emit shape + the
# shared edge aggregate) + the consumer field reads (reference-class never aliases lifecycle), modelled
# locally (myelin-issues is a producer LEAF).
cdc = [
    "crates/myelin-git/tests/cdc_5_4_13_1_git_content_edges.rs",
    "crates/myelin-knowledge/tests/cdc_5_4_5_5_5_6_5_7_knowledge_refs_glue.rs",
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
]

[[contract]]
row = "5.5"
title = "TE-7 typed-edge mirror"
# The Refs-OWNED mirror DISCIPLINE (the lifecycle vocabulary, the inverse pairing, the both-directions
# projection, drift reconvergence) shipped in REF-P14 / P-163 (`myelin_refs_service::mirror`). GIT-P19 /
# P-280 adds the GIT-OWNED PRODUCER half — as the PR lifecycle advances, a Git PR emits the forward
# lifecycle edges (`Closes <ISSUEKEY>` trailer on a merged PR → exactly one `closes` edge; a PR-link →
# exactly one `relates` edge) as `refs.edge.created` with `rel_class='lifecycle'`, in the SAME outbox
# transaction as the `git.pr.merged`/`git.pr.updated` event (emit-iff-committed; 0 dup/missed). This is
# the byte-identical wire shape the Refs mirror consumer ingests; the CDC models the consumer's field
# reads + the deterministic `edge_id` + the forward/inverse projection locally because `myelin-git`
# cannot depend on the Refs SERVICE crate (the §2.9 one-directional edge: `myelin-refs-service` depends
# on `myelin-git`). Git emits the FORWARD edge only; the inverse projection is the Refs mirror's
# (`relates` symmetric swap; `closes` has the `Inverse::None` floor — REF-P18/REF-P20 mint the token).
# KN-P19 / P-309 (M3) adds the KNOWLEDGE-OWNED PRODUCER half — the SAME transaction that writes a
# `page_parent` (`PageTree::set_parent`) / `db_relation` (`RelationStore::relate`) typed row emits
# `knowledge.page.parent_set` / `knowledge.relation.created`/`.removed` (`rel_class='lifecycle'`) via
# the outbox (`myelin_knowledge::refs_glue::{emit_page_parent_set, emit_relation_edge}`), so 0
# typed-row-without-edge. The typed table is the SOURCE OF TRUTH; Knowledge emits the FORWARD edge
# only — the inverse `child` pairing + the both-directions projection is the Refs mirror's (the §2.9
# one-directional edge: `myelin-refs-service` depends on the producer crates; the CDC models the
# consumer field reads locally). The block-granular replay that REBUILDS the Refs projection from the
# typed table (the TE-7 drift-correction; cold == live) is KN-P20 / P-310 (named).
# REF-P20 / P-336 (M4) lands the SECOND real TE-7 mirror — Issues `issue_relation`. The Refs CONSUMER
# side projects a real `issue.relation.*` typed-lifecycle event into BOTH inverse-paired
# `rel_class='lifecycle'` edges over the WHOLE lifecycle vocabulary
# (`closes`/`blocks`/`blocked_by`/`depends_on`/`parent`/`assigns`/`relates`) via
# `myelin_refs_service::issues_producer::{mirror_issue_relation, project_issue_relation,
# reconverge_issue_relations}` — reusing the REF-P14 `mirror_edges`/`reconverge` (no second engine).
# The consumer-side CDC pair (`cdc_5_5_issue_relation.rs`) proves both-directions traversal is ONE Refs
# query; the lib unit drills (`src/issues_producer/tests.rs`) prove the inverse pairing on every shape
# + the TE-7 SECOND-mirror reconvergence (typed wins; supports ISS-D6); the REAL Postgres proof (the
# inverse-paired lifecycle edges + RLS isolation + reindex byte-parity + the typed-wins reconvergence)
# is `crates/myelin-refs-service/tests/integration_ref_p20_issue_producer.rs` (the `integration`
# feature, proven green against the live dev-stack). The `closes`/`depends_on`/`assigns` inverse token
# stays the `Inverse::None` floor (the owning subsystem's mint).
status = "covered"
# ISS-P17 / P-383 (M4) adds the ISSUES-OWNED PRODUCER half of the SECOND TE-7 mirror: an
# `issue_relation` typed-row write/delete emits `issue.relation.created`/`.removed`
# (`rel_class='lifecycle'`) in the SAME outbox transaction (`myelin_issues::refs_glue::emit_relation_edge`),
# so 0 typed-row-without-edge. The typed table is the SOURCE OF TRUTH; Issues emits the FORWARD edge
# only over the whole lifecycle vocabulary (parent/blocks/blocked_by/closes/depends_on/relates) — the
# inverse pairing + both-directions projection is the Refs mirror's (REF-P20, already named below). The
# CDC models the consumer field reads locally (myelin-issues is a producer LEAF; no inverse token in
# the emitted payload).
cdc = [
    "crates/myelin-git/tests/cdc_5_5_git_lifecycle_edges.rs",
    "crates/myelin-knowledge/tests/cdc_5_4_5_5_5_6_5_7_knowledge_refs_glue.rs",
    "crates/myelin-refs-service/tests/cdc_5_5_issue_relation.rs",
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
]

[[contract]]
row = "5.6"
title = "project(ref, viewer)"
# COVERED by GIT-P18 / P-279 (the git-owned `project(ref, viewer)` half — the SAME provider+consumer
# pair as row 5.2): git's `project` is the ONLY way Refs/Search/Notif read a git artifact, per-viewer
# permission-checked (deny ⇒ Tombstone, 0 title leak). The general Refs resolver bulk (the per-kind
# resolution spine across ALL subsystems) remains the P-159 follow-on (named in row 5.2).
# KN-P19 / P-309 (M3) adds the KNOWLEDGE-OWNED `project(ref, viewer)` half
# (`myelin_knowledge::refs_glue::Projector::project`) — the ONLY way Refs/Search/Notif read a
# Knowledge artifact (no cross-DB), per-viewer permission-checked on the `#sub`-stripped ROOT (a sub
# is never more visible than its parent): a confidential page degrades to a content-free tombstone
# CARRYING THE ROOT, never the title (the project-leak counter = 0). The frozen
# `{title,state,icon,render_hint,sub_anchor?}` shape; mandatory-core (a project leak IS the failure),
# mutation floor ≥ 90% on refs_glue.rs (the permission-first gate + each ladder rung).
status = "covered"
# CI-P25 / P-368 (M4) adds the CI-OWNED `project(ref, viewer)` half
# (`myelin_ci_controlplane::surfacing::Projector::project`) — the ONLY cross-DB read of a CI artifact
# (run/deployment/pipeline), per-viewer permission-checked on the `#sub`-stripped ROOT: a confidential
# run degrades to a content-free tombstone, never the title (0 title leak — the cross-fabric leak
# counter = 0). Backs the chat run unfurl / PR context pane / knowledge embed / inbox / search snippet.
# The provider+consumer pair (cdc_5_1_5_6_5_7_ci_surfacing.rs) proves the projection shape + the
# tombstone-on-deny. Mandatory-core (a project leak IS the failure), mutation floor ≥ 80% on surfacing.rs.
# ISS-P17 / P-383 (M4) adds the ISSUES-OWNED `project(ref, viewer)` half
# (`myelin_issues::refs_glue::Projector::project`) — the ONLY way Refs/Search/Notif read an Issues
# artifact (no cross-DB), per-viewer permission-checked on the `#sub`-stripped ROOT: a CONFIDENTIAL
# issue degrades to a content-free tombstone CARRYING THE ROOT, never the title (the project-leak
# counter = 0 — the ISS-D3 slice re-asserted at the unfurl boundary). The frozen
# `{title,state,category,icon,render_hint,sub_anchor?}` shape; mandatory-core (a project leak IS the
# failure), mutation floor ≥ 90% on refs_glue.rs (the permission-first gate + each ladder rung). The
# provider+consumer pair (cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs) proves the shape + the
# tombstone-on-deny + the chained mint→resolve-per-viewer flow.
cdc = [
    "crates/myelin-git/tests/cdc_5_1_5_2_5_6_git_project.rs",
    "crates/myelin-knowledge/tests/cdc_5_4_5_5_5_6_5_7_knowledge_refs_glue.rs",
    "crates/myelin-ci-controlplane/tests/cdc_5_1_5_6_5_7_ci_surfacing.rs",
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
]

[[contract]]
row = "5.7"
title = "unified #sub sub-artifact scheme (frozen)"
# The frozen `#sub` GRAMMAR + the per-subsystem stable-opaque MINT half of 5.7 is now PROVEN by real
# provider+consumer CDC pairs: Refs owns the ONE grammar (REF-P1 / P-052, the parse/format value type
# in cdc_5_1_artifactref.rs), and each owner registers + mints its declared kinds through that one
# grammar — git (`comment-`/`thread-`/`L<a>-L<b>`, GIT-P4 / P-230) and chat (`message-`/`thread-`,
# CHAT-P2 / P-244). The PROVIDER mints stable opaque sub-ids of its kinds (0 ungrammatical by
# construction); the CONSUMER (Refs) ACCEPTS the registration + classifies every mint + strips it to
# the canonical root. FLOOR (named): the `resolve(ref, viewer, mode)` 4-step tombstone LADDER half of
# 5.7 (the per-kind project/resolution) is the separate row 5.2 (deferred P-159) + the Refs resolver
# bulk (P-164) + the per-kind resolvers (GIT-P18/P-24, the Chat projection spine); this row's
# GRAMMAR+MINT pairs ship here.
status = "covered"
cdc = [
    "crates/myelin-refs/tests/cdc_5_1_artifactref.rs",
    "crates/myelin-git/tests/cdc_5_7_git_sub_mints.rs",
    "crates/myelin-chat/tests/cdc_5_7_chat_sub_mints.rs",
    "crates/myelin-knowledge/tests/cdc_5_7_knowledge_stable_id_mint.rs",
    "crates/myelin-knowledge/tests/cdc_5_4_5_5_5_6_5_7_knowledge_refs_glue.rs",
    "crates/myelin-ci-controlplane/tests/cdc_5_1_5_6_5_7_ci_surfacing.rs",
    # ISS-P17 / P-383 (M4): Issues' owned `comment-`/`b`/`field-`/`row-` `#sub` mints — STABLE opaque
    # ids minted through the ONE refs codec (0 ungrammatical; a tombstone always carries the root). The
    # provider+consumer pair classifies every mint to its declared kind + strips it to the root, and
    # PROVES the 4-step ladder over the mints (a confidential viewer of a #sub gets a tombstone).
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
]
# CI-P25 / P-368 (M4) adds CI's owned `#sub` mints: `step-<n>` (jump-to-failure, resolves
# `CheckStatus.details_ref`) + `check-<context>` + `L<a>-L<b>`, minted through the ONE refs codec
# (the `#step-<n>` mint is byte-identical to `check_emitter::details_ref` — one source of truth). The
# `step-<n>` id is opaque + STABLE across retries (a chat/runbook embed of `#step-3` never dangles).
# KN-P10 / P-300 (M3) adds Knowledge's owned `b<id>` / `h<id>` stable block-id mints — the §2.3
# stability obligation (the `block_id` is stable across edits/moves/collaboration; the move is an
# `order_key` write, NEVER an id re-mint). The provider+consumer pair
# (cdc_5_7_knowledge_stable_id_mint.rs) registers the kinds with Refs, classifies every mint to its
# declared kind (0 ungrammatical), and PROVES the minted `#sub` is byte-identical across a real
# `BlockTree::move_block` (the `moved_block_id_dangles == 0` stable-id gate).
# KN-P19 / P-309 (M3) FILLS the named `b`/`h` `project(ref, viewer)` 4-step-ladder follow-on: the
# Knowledge projector runs the ONE frozen ladder (permission → root → sub {LIVE/MOVED/OUTDATED/GONE}
# → ERASED) over Knowledge's `#sub` anchors — a tombstone ALWAYS carries the root (the embed degrades
# to "this referenced <page>" rather than vanishing); a MOVED block still resolves (the stable
# `block_id` is a tree move, not a 3-way diff). Proven in cdc_5_4_5_5_5_6_5_7_knowledge_refs_glue.rs.

[[contract]]
row = "5.8"
title = "reindex(scope) (Refs)"
status = "deferred"
landing = "P-165"

[[contract]]
row = "5.9"
title = "Git<->CI CheckStatus seam"
status = "covered"
# GIT-P6/P-232 DECLARED the consumer half: the `check_status` projection-table schema keyed
# (commit_oid, context), the monotonic `run_attempt` supersession rule, the `required`-set policy +
# the gate outcome, and the typed `CheckStatus` consumer view that decodes the OPAQUE Bus payload
# (`myelin_git::check_status`) — a COMPILING, not-yet-live seam module against the M2-frozen 5.9 shape,
# with a consumer-half CDC (`crates/myelin-git/tests/cdc_5_9_check_status_consumer.rs`).
# EB-26/P-246 (M3) WIRED THE CONSUMER LEG LIVE: `myelin_git::check_status::CheckStatusConsumer` is now
# an idempotent `myelin_events::EventHandler` over the Bus's per-aggregate-ordered `ci.check.updated`
# carriage (the §4.2 idempotent template) — proven through the Bus consumer runtime (idempotent on
# event_id → 0 dup; per-aggregate-ordered → monotonic supersession; malformed payload dead-letters) in
# `crates/myelin-git/tests/drills_git_d9_check_seam_consumer_leg.rs`, and the carriage itself proven
# over the LIVE NATS JetStream stack in
# `crates/myelin-events/tests/integration_eb26_check_seam_carriage.rs` (envelope conformance +
# broker-dedup + per-aggregate ordering — run with --features integration).
# GIT-P20/P-281 (M3) LANDED THE STORE-BACKED PROJECTION (the data-layer follow-on the seam-floor
# named): `myelin_git::check_status_store::PgCheckStatusProjection` is the LIVE Postgres `check_status`
# table + migration; its `apply` runs the idempotent-on-event_id consumer_dedup guard + the monotonic
# `run_attempt` supersession UPSERT (`WHERE EXCLUDED.run_attempt >= …`, the < drop atomic in SQL) in
# ONE transaction — proven against the dev Postgres stack (GIT-D10 part (a): out-of-order/dup →
# exactly 1 current row/key; idempotent on event_id) in
# `crates/myelin-git/tests/integration_git_d10_check_status_projection.rs` (run with
# --features integration). The supersession-core mutation floor is met (cargo-mutants on
# `check_status.rs` supersedes/apply/gate path: 10 caught, 0 missed). The row stays DEFERRED because
# the seam is not yet live END-TO-END: the consumer leg runs against a SYNTHETIC `ci.check.updated`
# emitter (the X-1 seam-floor) — no real CI PRODUCER yet. The real CI producer wiring (CI emits
# `ci.check.updated` + the rollup `ci.result`) is the M4 co-gate (GIT-D10/CI-D8 end-to-end) — only
# then is the full provider+consumer CDC pair closeable to `covered`.
# GIT-P21/P-282 (M3-G4) LANDED THE MERGE GATE + THE REQUIRED-SET POLICY (the owned half of 5.9 — "Git
# owns what is allowed to land"): `myelin_git::merge_gate` is the bridge the gate fires across — it
# parses a base_ref's branch-protection ruleset `required_contexts` strings (the `provider/name`
# grammar) into typed `CheckContext`s (`parse_required_context`), resolves each against Git's OWN
# `check_status` projection for the PR head_oid, applies the fork-endorsement trust posture (reads
# `trust_tier` OFF the fact, never recomputes; an un-endorsed `untrusted_fork` success is NEUTRAL for
# gating — Δ3), and returns the typed `MergeGateOutcome` (the 0-under-gated-merges decision: a
# missing/stale/un-endorsed required context ALWAYS blocks). It REUSES the per-context gate logic in
# `check_status` (`is_acceptable_satisfaction`) — no duplicate gate (EI-01 §7). The required-set policy
# is GIT's, NOT CI's `required` bool (CI reports facts; Git decides which contexts gate this base_ref) —
# proven in the CDC `crates/myelin-git/tests/cdc_5_9_merge_gate_required_set.rs` (Git's policy overrides
# the CI bool; every non-green posture blocks). The chained e2e
# (`crates/myelin-git/tests/e2e_git_p21_merge_gate.rs`) drives configure → partial → BLOCK → complete →
# ADMIT + the fork-neutral-until-endorsed branch. The LIVE-store gate read
# (`PgCheckStatusProjection::merge_gate`) reads the REAL `check_status` table — proven against the dev
# Postgres stack (0 under-gated merges; fork-neutral; superseding-failure re-block) in
# `crates/myelin-git/tests/integration_git_p21_merge_gate.rs` (run with --features integration). The
# merge-gate mutation floor is MET (cargo-mutants on `merge_gate.rs`: 18 caught / 0 missed / 5 unviable —
# 100% of viable mutants on the gate-decision path).
# The row stays DEFERRED because the seam is not yet live END-TO-END (no real CI PRODUCER until M4).
# GIT-P22/P-284 (M3-G4) LANDED THE FORK / TRUST-TIER ENDORSEMENT GATE (the poisoned-pipeline defence,
# GIT-D10 parts (b)+(c)) — closing the GIT-P21 floor. `myelin_git::fork_gate` ships the two halves the
# merge gate consumed as explicit inputs: (A) the LIVE endorsement RESOLVER (`EndorsementResolver`) runs
# the maintainer's `check(subject, approve_untrusted_ci, repo)` through the LIVE `GitCheckGate` (GIT-P14
# `fork_endorsement_check`, a Strong zookie-stamped read) for each required context whose CURRENT row is
# an un-endorsed `untrusted_fork` success, PRODUCING the `endorsed_contexts` set `evaluate_merge_gate`
# consumes — a fork can NEVER self-green (the check's subject is the maintainer who holds the relation,
# never the fork author → Deny → 0 endorsements); (B) the `fork:<pr_id>` trust-scoped cache confinement
# (`TrustScope` + `ScopedCache`, contract 11.2 C4 / recon §8) — a scope-key convention over the per-tenant
# `myelin_storage::Cache` so an `UntrustedFork` write can NEVER reach the `trusted:` cache scope (the
# scope is DERIVED from the CI-stamped trust tier, never caller-chosen). GIT-D10 (b) fork-neutral + (c)
# endorse-flips are greened in the chained e2e `crates/myelin-git/tests/e2e_git_p22_fork_endorsement.rs`
# (fork author blocked → maintainer endorses → green; re-run-trusted greens with no endorsement; 0 fork
# writes in the trusted scope) + the CDC `crates/myelin-git/tests/cdc_5_9_fork_endorsement.rs` (CI stamps
# the tier, Git reads it off the fact, the endorsement is a plain relation check, the fork author cannot
# self-endorse). The `fork:<pr_id>` confinement is PROVEN against LIVE Valkey in
# `crates/myelin-git/tests/integration_git_p22_fork_cache_confinement.rs` (--features integration: 0 fork
# writes in the trusted scope on the real server). The fork-gate mutation floor is MET (cargo-mutants on
# `fork_gate.rs`: 18 caught / 0 missed / 5 unviable — 100% of viable mutants on the endorsement + cache-
# confinement path). The merge queue (durable workflow, exactly-once merge, the `ci.result` rollup wait)
# is GIT-P23/P-285 (parks on ci.result, exactly-once merge — LANDED).
# EB-27/P-327 (M4) LANDED THE PRODUCER LEG END-TO-END — the row flips to COVERED. CI's producer side
# is `myelin_events::check_seam::{check_updated_draft, rollup_ci_result, ci_result_draft}` (the Bus's
# narrow carriage half: CI emits `ci.check.updated` per (commit_oid, context) + derives + emits the
# rollup `ci.result` the merge-queue waits on). With Git's consumer leg (EB-26), the merge gate +
# fork endorsement (GIT-P21/P22), and the merge-queue durable workflow (GIT-P23/P-285) all live, the
# X-1 seam is now END-TO-END. GIT-D10 / CI-D8 each GREEN in
# `crates/myelin-events/tests/drills_eb27_check_seam_e2e.rs` (out-of-order/dup ci.check.updated →
# run_attempt-monotonic supersession drops stale; fork self-green NEUTRAL until endorsed; a
# doubly-delivered ci.result → the merge-queue wakes EXACTLY ONCE → 0 double-merge). The Bus's role
# stays NARROW (envelope conformance + per-aggregate ordering + at-least-once + the idempotent
# wait_for_signal substrate); CI/Git own the CheckStatus shape, supersession, trust gating, the merge
# gate. The producer/consumer split was kept across bands (M4 producer DEPENDS-ON M3 consumer). The
# producer-leg carriage is ALSO proven over the LIVE NATS JetStream stack in
# `crates/myelin-events/tests/integration_eb27_check_seam_producer.rs` (envelope conformance for the
# checks AND the rollup + broker-dedup of a re-published ci.result + per-aggregate ordering — run with
# --features integration).
# REF-P19/P-335 (M4) CLOSED THE REFS HALF OF X-1 — the SUB-ANCHOR resolution of `check-`/`step-`. Refs
# is a CONSUMER of the CI producer half (the Refs engine is FIXED at M2): `myelin_refs_service::CiOwner`
# maps a CI `#sub` KIND through the ONE ladder (C-6) onto the frozen `live/moved/outdated/gone` state —
# a `check-<context>` → the CURRENT (latest-by-`run_attempt`) check state off the REUSED
# `myelin_git::check_status::CheckStatusProjection` supersession (out-of-order honoured at the sub-anchor
# level — a late lower attempt is dropped, the anchor never regresses); a `step-<n>` `details_ref` →
# the 11.8 SEALED CI LOG SEGMENTS (`myelin_storage::CiLogTier::resolve_step_anchor` — the exact failing
# step's bytes; an unknown/pruned step → GONE with the root run carried; a crypto-shredded segment →
# ERASED, never a wrong serve). Refs does NOT re-build the supersession rule, the merge gate, or the log
# index — it CONSULTS the frozen seams. REF-D9 GREEN on the CI anchors + the out-of-order chained drill
# are proven against the REAL CI producer end-to-end (real KMS DEK, real sealed content-addressed
# segments, real `check_seam` carriage + ordering, the full Refs `ResolveService` chokepoint) in
# `crates/myelin-refs-service/tests/integration_ref_p19_ci_producer.rs`, plus the unit ladder arms in
# `crates/myelin-refs-service/src/ci_producer/tests.rs`. The Refs sub-anchor consumer-side CDC pair is
# `crates/myelin-refs-service/tests/cdc_5_9_ci_sub_anchors.rs`. The NON-Refs half (merge gate, fork
# endorsement, the merge-queue wake) stays the Git+CI deliverable (GIT-D10/CI-D8, above).
# P-FLOW-23/P-346 (M4) CLOSED THE MERGE-QUEUE FLOOR — the durable-execution half of X-1 END-TO-END.
# The merge-queue durable workflow body (`myelin_flow::merge_queue::WfCtx::run_merge_attempt`) was
# built + drilled IN ISOLATION against a MOCK ci.result producer at P-FLOW-19 (the named floor). This
# prompt replaces the mock with the REAL producer wiring `myelin_flow::RealCiResultProducer`: it does
# NOT fabricate a verdict — it DERIVES the rollup from per-context `ci.check.updated` facts through
# CI's REAL `myelin_events::check_seam::{CheckSeamOrder (per-aggregate ordering), rollup_ci_result
# (success iff every required context green)}` after Git's `run_attempt` last-writer-wins supersession
# collapses each context, keyed idempotently on the merge_attempt_id the workflow minted (the
# no-coordination agreement, §4.9). The merge queue — parked across a worker restart — wakes on that
# DERIVED rollup (delivered twice, at-least-once) EXACTLY once and merges EXACTLY once. GIT-D10/CI-D8
# GREEN end-to-end (out-of-order/re-run/dup ci.check.updated → supersession holds, drops stale; a
# superseding failure → dequeue with a humanised reason; a fork self-green / missing required context
# is NEUTRAL for gating — 0 spurious unblock; 0 double-merge, merge-count == 1/attempt) in
# `crates/myelin-flow/tests/drills_flow_d10_x1_seam_e2e.rs` (the merge-queue durable-workflow half;
# the Bus-substrate half is `crates/myelin-events/tests/drills_eb27_check_seam_e2e.rs`). The 5.9
# merge-queue CONSUMER half is now PAIRED with CI's REAL provider half (not the mock) in
# `crates/myelin-flow/tests/cdc_5_9_merge_queue.rs::consumer_reconciles_with_the_real_ci_producer_end_to_end`.
# The merge-queue-in-isolation FLOOR (P-FLOW-19) is PROMOTED to the seam end-to-end (the floor closed).
# CI-P18/P-361 (M4) LANDED THE REAL CI CONTROL-PLANE PRODUCER HALF — the `check_attempt` monotonic
# counter + the FROZEN 5.9 `CheckStatus` assembly in `myelin_ci_controlplane::check_emitter`. CI is the
# SOURCE of `run_attempt`: `BUMP_CHECK_ATTEMPT_SQL` (the `next_attempt + 1 RETURNING next_attempt - 1`
# UPSERT on the `(commit_oid, context)` key) is monotonic — proven against the live dev Postgres in
# `crates/myelin-ci-controlplane/tests/integration_ci_p18_check_attempt_bump.rs` (first dispatch → 1,
# each re-run bumps strictly; per-(commit_oid, context) sequences; the supersession key is the counter,
# never wall-clock). `check_status_payload` assembles the frozen struct EXACTLY (the `summary` is a
# HumanisedRef never a raw string, 7.3; `cost_settled` flips ONLY on settle, X-1; `trust_tier` is
# STAMPED from provenance — CI records a fork run faithfully but NEVER endorses it). The `ci.pipeline`
# body (CI-P15) was RECONCILED to assemble its terminal facts THROUGH this module (ONE producer shape,
# no divergence — EI-01 §7), fixing the prior loose payload so it now decodes BYTE-IDENTICALLY into
# Git's frozen consumer view. The CDC PROVIDER half is below
# (`crates/myelin-ci-controlplane/tests/cdc_5_9_ci_check_producer.rs`): CI's assembled payload decodes
# through Git's REAL `CheckStatusConsumer::decode` (no drift; an incomplete payload is a LOUD decode
# failure). CI-P19/P-362 (M4) CLOSED THE SEAM END-TO-END FROM CI's REAL PRODUCER: the `ci.result`
# rollup SIGNAL (`myelin_ci_controlplane::CiResultSignal` — derives the FROZEN 5.9 verdict via
# `rollup_ci_result`, encodes it references-not-payloads via `encode_ci_result`, delivers it into the
# merge-queue run's `wf_signal` store idempotent on the `idem_token` = merge_attempt_id; a
# doubly-delivered rollup is ONE buffered row → the merge queue wakes EXACTLY ONCE). The GIT-D10/CI-D8
# end-to-end GATE drives CI's REAL `run_ci_pipeline_body` → the per-context `ci.check.updated` facts →
# Git's projection (monotonic supersession, 1 current row/key) → CI's REAL `ci.result` rollup signal →
# Git's `run_merge_attempt` merge queue: (a) out-of-order/dup supersession holds, (b) fork self-green
# NEUTRAL (merge-count == 0), (c) maintainer endorses → green, (d) doubly-delivered ci.result → wakes
# once → merge-count == 1 (0 double-merge). The dated green artifact is in
# `crates/myelin-ci-controlplane/tests/drills_ci_p19_seam_gate.rs`. The seam is frozen to the 5.9 shape
# EXACTLY (no local divergence); external-provider checks are a demand-driven follow-on.
cdc = [
    # the Bus's carriage CDC (the narrow half — envelope + ordering + the wait substrate)
    "crates/myelin-events/tests/cdc_5_9_check_seam_carriage.rs",
    # Git's consumer leg (the projection over the ordered carriage — provider+consumer markers)
    "crates/myelin-git/tests/cdc_5_9_check_status_consumer.rs",
    # Refs' sub-anchor resolution half (the Refs half of X-1 — check-/step- through the one ladder)
    "crates/myelin-refs-service/tests/cdc_5_9_ci_sub_anchors.rs",
    # CI's status-summary PRODUCER half (NOTIF-P23 / P-344, M4): CI's `CheckStatus.summary` is a
    # `HumanisedRef` `(template_key, args)` pair (X-1, never a raw string) that resolves THROUGH
    # Notif's ONE templating surface (7.3 humanise). CI registers its summary templates (the
    # `ci.check.<verdict>` bodies, `{0}` subject slot) on that surface with ZERO Notif change; the
    # producer `(template_key, args)` is byte-faithfully the Git consumer `HumanisedRef`. The
    # end-to-end render (per-viewer resolve → tombstone on deny, NOTIF-D4 0 leak) is the companion
    # integration test `integration_ci_summary_humanises.rs`.
    "crates/myelin-ci-sandbox/tests/cdc_7_6_5_9_ci_notif_summary.rs",
    # The merge-queue CONSUMER half PAIRED with CI's REAL ci.result provider (P-FLOW-23 / P-346, M4):
    # the durable-execution half of X-1 END-TO-END — the merge queue wakes on the rollup CI's REAL
    # producer DERIVES (RealCiResultProducer over CheckSeamOrder + run_attempt supersession +
    # rollup_ci_result), not a fabricated verdict. Closes the P-FLOW-19 merge-queue-in-isolation floor.
    "crates/myelin-flow/tests/cdc_5_9_merge_queue.rs",
    # CI's CONTROL-PLANE PRODUCER half (CI-P18 / P-361, M4): the `check_attempt` monotonic counter +
    # the frozen 5.9 `CheckStatus` assembly. CI assembles the payload through
    # `myelin_ci_controlplane::check_status_payload` (CI never depends on Git in production code — it
    # produces the frozen JSON shape) and this CDC proves it decodes BYTE-IDENTICALLY through Git's
    # REAL `CheckStatusConsumer::decode` (no drift; summary is a HumanisedRef; cost_settled flips only
    # on settle; an untrusted_fork run is recorded faithfully but never endorsed; an incomplete
    # payload is a LOUD decode failure — the seam shape is a real gate).
    "crates/myelin-ci-controlplane/tests/cdc_5_9_ci_check_producer.rs",
    # CI-P19 / P-362 (M4): the X-1 `ci.result` rollup SIGNAL + the GIT-D10/CI-D8 check-seam END-TO-END
    # GATE driven by CI's REAL producer. `myelin_ci_controlplane::CiResultSignal` derives the FROZEN 5.9
    # verdict (`rollup_ci_result`) + delivers it as the durable `ci.result` signal (references-not-
    # payloads via `encode_ci_result`) idempotent on the merge_attempt_id. The drill drives CI's REAL
    # `run_ci_pipeline_body` → Git's projection (monotonic supersession) → CI's real rollup signal →
    # Git's merge queue: 1 current row/key, fork-self-green NEUTRAL (merge-count == 0), endorsed → GREEN,
    # doubly-delivered ci.result → wakes once → merge-count == 1 (0 double-merge). CI is the producer
    # (emits the rollup signal); Git is the gate (the merge performer is Git's). Closes the X-1 seam.
    "crates/myelin-ci-controlplane/tests/drills_ci_p19_seam_gate.rs",
]

# ──────────────── §6 Search (myelin-search) ─────────────────────────────────────────────────────

[[contract]]
row = "6.1"
title = "query(ast, viewer, zookie?, page) -> RankedResults"
# SRCH-P08/P-171 ships the Search-owned PROVIDER half: the permission-aware query pipeline that
# ALWAYS conjoins the `list_objects` `Filter` (the leak-free pre-filter lowered to an `AclFilter`
# conjoined into every engine branch BEFORE scoring; the `search-requires-acl-filter` discipline;
# exactly one `list_objects` per query — no N+1). Its provider+consumer pair is
# crates/myelin-search/tests/cdc_query_pipeline_6_1.rs.
# KN-P21/P-311 (M3) adds the KNOWLEDGE CONSUMER half: `myelin_knowledge::search_feed::kn_search_query`
# drives `query` ALWAYS through the conjoining entry, pinned to the `page` object type — the KN-D5
# re-confirm: a confidential page appears in NEITHER the result NOR the COUNT (0 leak incl. COUNT).
# SRCH-P21/P-339 (M4) adds the ISSUES TIER-3 BOARD-ESCALATION VALVE CONSUMER half: an over-budget
# Issues board escalates to `escalate_to_search`, which drives `query` conjoining the SAME
# `Filter{set_expr}` the OLTP board would have used (carried verbatim, not re-derived). The valve
# parity check proves BYTE-IDENTICAL visible rows across the OLTP board tier and the Search valve
# tier (0 leak divergence between the two ACL pre-filters — both share `pipeline::lower_set_expr`,
# no second interpreter), plus SRCH-D1 on the valve path (0 confidential-issue leak incl. counts).
# ISS-P14/P-380 (M4) adds the ISSUES COST-BOUNDER CONSUMER half: `myelin_issues::plan_board_query`
# is the three-tier escalation decision — a Tier-3 leg (full-text/cold-huge-facet/over-budget)
# returns an `EscalateToSearch` carrying the board's OWN `Filter{set_expr}` (4.3, constructible ONLY
# with it — the `search-requires-acl-filter` discipline is structural), driven into Search's
# `escalate_to_search`. The Issues-side CDC proves the cost-bounder's escalation conjoins the SAME
# filter over the LIVE engine (SECRET absent, one conjoin) — crates/myelin-issues/tests/
# cdc_6_1_issues_tier3_escalation.rs.
status = "covered"
cdc = [
    "crates/myelin-search/tests/cdc_query_pipeline_6_1.rs",
    "crates/myelin-knowledge/tests/cdc_6_3_6_1_6_2_knowledge_search_feed.rs",
    "crates/myelin-search/tests/cdc_tier3_valve_6_1.rs",
    "crates/myelin-issues/tests/cdc_6_1_issues_tier3_escalation.rs",
]

[[contract]]
row = "6.2"
title = "semantic(text|vec, viewer, k, filter_ast?)"
# SRCH-P11/P-174 ships the Search-owned PROVIDER half: the hybrid/semantic query path
# (filter-during-traversal — k VISIBLE neighbours; RRF fusion of the lexical + semantic lists; the
# SAME conjoined ACL filter on both branches so fusion can never introduce a hidden doc). Its
# provider+consumer pair is crates/myelin-search/tests/cdc_semantic_6_2.rs.
# KN-P21/P-311 (M3) adds the KNOWLEDGE CONSUMER half: `myelin_knowledge::search_feed::kn_search_semantic`
# drives `semantic`/RAG (an agent's delegated principal as viewer) ALWAYS through the conjoining
# entry — the KN-D5 vector half re-confirmed over the KN corpus: a confidential page never enters
# the visible-neighbour set (0 leak through the RAG path).
status = "covered"
cdc = [
    "crates/myelin-search/tests/cdc_semantic_6_2.rs",
    "crates/myelin-knowledge/tests/cdc_6_3_6_1_6_2_knowledge_search_feed.rs",
]

[[contract]]
row = "6.3"
title = "declare_indexable(IndexSpec)"
status = "covered"
# GIT-P5/P-231 ships git's owned spec half: git declares its `declare_indexable` code-projection
# spec (`myelin_git::search_projection::git_code_projection_spec` — git/blob, the path/language/
# blob_oid facets, non-semantic, acl_object_type=repo) and Search ADMITS it into a live indexer's
# per-tenant facet union (the build-time registration). The CDC carries BOTH the provider (git
# declares + serializes to the 6.3 wire shape) and consumer (Search admits) sides. The Search engine
# half was frozen at SRCH-P06/P-167; the projection EMITTER that feeds the rows is GIT-P25/P-287.
#
# ISS-P04/P-243 adds the ISSUES slice: Issues declares its `issue.*` facets-projection spec
# (`myelin_issues::declares::issue_facets_projection_spec` — issue/issue, the seven structured
# board/list/search facets state_category/priority/assignee/type_rank/project_id/cycle_id/rank,
# non-semantic, acl_object_type=issue) and Search ADMITS it. The Issues `issue.*` projection EMITTER
# that feeds the rows is ISS-P17.
# SRCH-P17/P-260 adds the KNOWLEDGE slice: KN declares its index specs
# (`myelin_search::kn_page_index_spec` — knowledge/page semantic + the three structured inline-node
# reference facets mention/artifact_ref/embed; `myelin_search::kn_db_row_index_spec` — knowledge/db_row
# non-semantic + the custom DB-field GIN-scan facets priority/owner/due + order_key) and Search ADMITS
# them. KN has no service crate yet, so the owned specs + the page projection builder are modelled in
# the Search consumer crate against the frozen myelin-content taxonomy (the GIT-P5-style emitter-less
# posture). The real-corpus indexing GATE (multilingual + facets + JSONB GIN-scan + vector-in-v1 +
# read-time rollup/formula + SRCH-D1/D3 on the KN corpus) is in
# crates/myelin-search/tests/integration_srch_p17_kn_indexing.rs. The KN projection EMITTER is the M3
# KN service prompt cluster.
# GIT-P25/P-287 adds the git code-projection EMITTER slice (the §9 TE-27 receive-pack post-commit
# hook): git EMITS one git.blob.snapshot per changed blob on a push to an indexed ref
# (`myelin_git::code_projection::CodeProjectionEmitter::emit_for_push`, incremental — emit-count ==
# changed-blob-count) and the per-blob doc lowers to the Search-owned SearchProjection whose facets
# are EXACTLY the declared 6.3 spec's (path/language/blob_oid). The emitter CDC pins both sides; the
# emit GATE is in crates/myelin-git/tests/e2e_git_p287_code_projection_emit.rs.
#
# KN-P17 / P-307 adds the >5% FACET-PROMOTION-THRESHOLD aspect of 6.3 / OQ-C (the Search-owned
# tunable: a facet in MORE than 5% of a collection's view executions over a rolling window promotes
# from a cold GIN scan to a per-facet generated/expression-column index). Knowledge MEASURES the
# trigger (myelin_knowledge::FacetTelemetry — per-(db, facet) view-execution frequency); the CDC
# (cdc_13_3_6_3_knowledge_flex_db.rs::cdc_6_3_*) pins that the executor's threshold == the
# thresholds-file value (one source of truth, `flex_db.facet_promotion_ratio`) and that the trigger
# is STRICTLY >5% (a facet at exactly 5% does NOT promote — the frozen wording, never weakened). The
# promotion ACT (provisioning the generated-column index off the bus) is the NAMED KN-P31 (M5) floor.
# KN-P21 / P-311 (M3) RE-HOMES the Knowledge owned 6.3 surface into the Knowledge SERVICE crate (the
# emitter-less SRCH-P17 model said "when the Knowledge service crate lands it re-homes the specs
# verbatim; the SHAPE does not change"): `myelin_knowledge::search_feed` re-exports the page + db_row
# specs (PROVIDER — Knowledge declares) and Search ADMITS them (CONSUMER), AND drives the `project`
# feed (`feed_project`) + the 6.1/6.2 Filter conjoin (the KN-D5 re-confirm, 0 leak incl. COUNT across
# the FT + semantic/RAG paths). The provider+consumer pair is
# crates/myelin-knowledge/tests/cdc_6_3_6_1_6_2_knowledge_search_feed.rs.
cdc = [
    "crates/myelin-git/tests/cdc_6_3_git_code_projection.rs",
    "crates/myelin-git/tests/cdc_6_5_git_code_projection_emitter.rs",
    "crates/myelin-issues/tests/cdc_6_3_issues_facets_projection.rs",
    # ISS-P17 / P-383 (M4): the LIVE Issues `issue.*` Search projection EMITTER that feeds the rows
    # (`myelin_issues::refs_glue::IssueProjectFetcher`) — the `project(ref)` → SearchProjection push
    # body the incremental indexer fetches through (the SAME cross-DB read; ACL-/restriction-/erasure-
    # safe — a restricted/erased issue projects to Gone, no leak via a result/count/rank). The facet
    # keys are byte-identical to the ISS-P04 declared 6.3 spec (the spec is the schema, the emitter is
    # the row). This fills the named ISS-P04 emitter floor.
    "crates/myelin-issues/tests/cdc_5_4_5_5_5_6_5_7_5_3_6_3_issues_refs_glue.rs",
    "crates/myelin-search/tests/cdc_6_3_kn_index_specs.rs",
    "crates/myelin-knowledge/tests/cdc_13_3_6_3_knowledge_flex_db.rs",
    "crates/myelin-knowledge/tests/cdc_6_3_6_1_6_2_knowledge_search_feed.rs",
    # CI-P26 / P-369 (M4): the CI slice — CI declares its owned `ci/run` IndexSpec
    # (`myelin_ci_controlplane::ci_run_index_spec` — ci/run, acl_object_type=ci_run, the seven struct
    # facets state/trust_tier/env/actor_pseudonym/created_at/repo_ref/commit_oid, SEMANTIC for the
    # failure_summary RAG/dedup field) and Search ADMITS it into a live indexer (the build-time
    # registration). The restriction flag is honoured (`run_doc_is_indexable` — 0 restricted/erased
    # rows indexed). The run-projection EMITTER REUSES project() (§7.2); the SCIP/LSIF find-usages
    # input (6.5) is the NAMED post-CI-M5 follow-on (arch 06 R-3). The CI-log doc spec is ci_log_projection.
    "crates/myelin-ci-controlplane/tests/cdc_6_3_2_6_7_3_ci_surfacing_index.rs",
]

[[contract]]
row = "6.4"
title = "reindex(scope) -> job (Search)"
status = "covered"
# SRCH-P16/P-179 ships Search's OWNED reindex-from-source seam (SearchReindexer::reindex) — the ONLY
# rebuild path (SEARCH-1): wipe -> bus re-emit (2.6, events::reindex) -> *.snapshot through the LIVE
# indexer's index() step, idempotent on the deterministic snapshot event_id, with the cursor store S4
# (throttled/resumable/per-tenant in-flight caps). No "load the index from Postgres" backdoor (the
# no-cross-db lint holds). FLOOR named: the per-owner real `replay` bodies are EB-26/per-owner M3/M4;
# the full-scale reindex-parity (SRCH-D5 at scale, E2E-3) is SRCH-P32 (M5) — this row is the CI-variant
# seam pair (provider: the reindex job; consumer: the live indexer; cold == live).
cdc = ["crates/myelin-search/tests/cdc_6_4_reindex.rs"]

[[contract]]
row = "6.5"
title = "code-search input"
status = "covered"
# GIT-P5/P-231 froze the declare_indexable code-projection SPEC (row 6.3). GIT-P25/P-287 ships the
# EMITTER that feeds the code-search input: on a git.ref.updated to an indexed ref the receive-pack
# post-commit hook (`myelin_git::code_projection::CodeProjectionEmitter`, architecture 02 §9 / TE-27)
# diffs new_tip ∖ last_indexed_oid (the code_projection_cursor) and emits ONE git.blob.snapshot per
# changed blob — the §9 shape (path / detected language / camel·snake-split symbols / literals / blob
# text / tip commit message / blob_oid), incremental (emit-count == changed-blob-count, 0 missed/stale),
# restriction-safe (a restrict-ed subject's body is suppressed), through the outbox so replay re-emits
# the same doc (cold == live). Git emits the projection + supplies the TEXT; Search builds the
# trigram/symbol/path/literal index (no cross-DB). The provider (git emits the §9 shape) + consumer
# (the doc lowers to the Search-owned SearchProjection whose facets match the declared 6.3 spec) CDC
# pair is crates/myelin-git/tests/cdc_6_5_git_code_projection_emitter.rs; the chained-e2e emit GATE
# (push code → per-changed-blob incremental emit) is
# crates/myelin-git/tests/e2e_git_p287_code_projection_emit.rs.
# FLOOR named (GF-3): trigram/lexical code search v1 is what this feeds; AST-aware "find usages" via
# CI-produced SCIP/LSIF indices is the GIT-P33/M5 follow-on. The leak-free list_objects SetExpr
# push-down + code-search pre-filter (GIT-D11) that conjoins this per viewer is GIT-P26/P-288.
cdc = ["crates/myelin-git/tests/cdc_6_5_git_code_projection_emitter.rs"]

# ──────────────── §7 Notifications (myelin-notif) ───────────────────────────────────────────────

# 7.1 COVERED at NOTIF-P5 (P-183): the list_inbox read surface + the C-9 scoped-view filter grammar.
# The provider (Notif owns 7.1 — the ONE inbox, a view is a STRICT SUBSET) + consumer (a subsystem
# scoped view / the CLI inbox list|show reading through the SAME list_inbox path) CDC pair. Step-0
# read authorize (4.2/4.10) drops an unseeable item (held, not leaked, ADR-03). Ranking is NOTIF-P7.
[[contract]]
row = "7.1"
title = "list_inbox"
status = "covered"
cdc = [
    "crates/myelin-notif/tests/cdc_7_1_notif_list_inbox.rs",
    # GIT slice — git (GIT-P31 / P-292, M3-G8) pins that its "Review requests" scoped view is a FILTER
    # over the ONE inbox (`reason = ReviewRequested` AND a git subject), NEVER a second store (C-9). The
    # `git_review_requests_filter` / `matches_review_requests_view` predicate selects a row in the ONE
    # inbox; it does not move it into a parallel store.
    "crates/myelin-git/tests/cdc_5_2_7_3_7_1_git_humanise_resolve.rs",
]

# 7.2 COVERED at NOTIF-P6 (P-184): mark / snooze / mark_all_read — the ONE read-state truth. The
# provider (Notif owns 7.2 — one store, one read-state: a mark in a scoped view is read in the
# unified inbox, 0 divergence; snooze records the until + suppresses from the active inbox;
# mark_all_read(filter) flips EXACTLY the C-9-filtered rows) + consumer (the inbox UI / the CLI
# inbox read|snooze through the SAME read-state path). The durable snooze re-surface timer is the
# named floor NOTIF-P14/P18 (the myelin-flow wheel); here only the until is recorded.
[[contract]]
row = "7.2"
title = "mark / snooze / mark_all_read"
status = "covered"
cdc = [
    "crates/myelin-notif/tests/cdc_7_2_notif_read_state.rs",
]

[[contract]]
row = "7.3"
title = "humanise"
# COVERED: the ONE templating surface (humanise + the ICU-subset formatter + the per-viewer
# tombstone-never-leak render) is owned + built at NOTIF-P9/P-187 (myelin-notif/src/humanise.rs,
# with its unit + NOTIF-D4 leak tests). CHAT-P3 / P-245 ships the CHAT registration slice on top:
# chat REGISTERS its humanise template keys (the card string, the agent-message string, the
# chat.message.mentioned string — NO chat-private string map, OQ-L) and Notif's ONE templating
# surface ADMITS + SERVES them. The CDC carries the PROVIDER (chat registers its keys) + CONSUMER
# (Notif's ONE TemplateStore admits + renders through the ONE formatter) sides. The per-subsystem
# humanise key sets for Git/KN/Issues/CI accrete the same way (each a registration, no Notif change).
status = "covered"
cdc = [
    "crates/myelin-chat/tests/cdc_7_3_chat_humanise_keys.rs",
    # GIT slice — git (GIT-P31 / P-292, M3-G8) ships the resolve-for-unfurls CONSUMER of the ONE
    # humanise surface: `myelin_git::git_resolve::GitRefResolver` is a REAL impl of the frozen
    # `RefResolvePort` (the 5.2 resolve seam humanise binds its slots to) over Git's permission-first
    # `Projector` (5.6). A confidential PR/commit subject → for a viewer lacking `pull` the humanise
    # render is a TOMBSTONE (`a restricted pr`); the title NEVER leaks (NOTIF-D4-class, 0 title leaks).
    # This PROMOTES the GIT-P19 test-local resolve stand-in (the named floor) to a real producer-crate
    # seam — the leak property is now exercised over Git's real projection, zero Notif change. The CDC
    # carries the PROVIDER (Git's resolver returns Projection|Tombstone, permission-first) + CONSUMER
    # (Notif's humanise binds the denied slot to the PII-free tombstone) sides.
    "crates/myelin-git/tests/cdc_5_2_7_3_7_1_git_humanise_resolve.rs",
    # KNOWLEDGE slice — knowledge (KN-P22 / P-312, M3) ships the project-Display → humanise CONSUMER
    # of the ONE humanise surface: `myelin_knowledge::KnowledgeRefResolver` is a REAL impl of the
    # frozen `RefResolvePort` (the 5.2 resolve seam humanise binds its slots to) over Knowledge's
    # permission-first `Projector` (5.6, project Display mode = the humanisation projection Notif uses,
    # architecture 03 §2.2 / OQ-L). A confidential page/block/row subject → for a viewer lacking `read`
    # the humanise render is a TOMBSTONE (`a restricted page`); the title NEVER leaks (NOTIF-D4-class, 0
    # title leaks). Knowledge registers NO second template engine — it feeds its Display projection into
    # the ONE templating surface. This is the SAME acyclic `producer-service -> myelin-notif` edge the
    # Git slice carries (`myelin-notif` is a NAMED leaf consumer; the DAG stays acyclic). The CDC carries
    # BOTH the provider (KN's resolver returns Projection|Tombstone, permission-first, no 2nd engine) and
    # consumer (Notif's humanise binds the denied slot to the PII-free tombstone) sides; it pins the KN
    # reason vocabulary (mentioned/comments/shared/watched) agrees between the 7.6 registration
    # (NOTIF-P20, cdc_7_6_4_9_knowledge_notif_rules.rs) and the 7.3 render — one vocabulary, no drift.
    "crates/myelin-knowledge/tests/cdc_5_2_7_3_knowledge_humanise_resolve.rs",
    # CI slice — CI-P26 / P-369 (M4): CI's `CheckStatus.summary` HumanisedRef `(template_key, args)`
    # (the NOTIF-P23/P-344 `myelin_ci_sandbox::notif_rules` set, RE-EXPORTED through
    # `myelin_ci_controlplane::surfacing_index` so the surfacing surface names the humanise half)
    # resolves through Notif's ONE humanise TemplateStore — every `ci.check.<verdict>` key admits to a
    # registered ICU body with the `{0}` per-viewer subject slot (NOTIF-D4), NEVER a raw string (the X-1
    # invariant is structural — there is no raw-string summary constructor). One vocabulary, no drift.
    "crates/myelin-ci-controlplane/tests/cdc_6_3_2_6_7_3_ci_surfacing_index.rs",
]

# 7.4 COVERED at NOTIF-P10 (P-188): get_prefs / set_prefs — prefs + quiet-hours over the frozen
# myelin-query QueryAst (the matcher = the EventMatcher core 3.4; Notif invents NO second predicate
# language); quiet-hours in the recipient tz; critical/escalated pierce by default (pierce_classes —
# you cannot silence an on-call page). The provider (Notif owns 7.4 — set/get/route) + consumer (the
# router/CLI/a subsystem rule, building the ONE QueryAst matcher; `notify test` previews the SAME
# route decision) CDC pair is the dated green artifact.
[[contract]]
row = "7.4"
title = "get_prefs / set_prefs"
status = "covered"
cdc = [
    "crates/myelin-notif/tests/cdc_7_4_notif_prefs.rs",
]

# 7.5 oncall_now / page: the escalation chain on the durable wheel (the FROZEN §2.4 chain shape;
# ack-as-event). Notif owns the POLICY (chain-walk, at-fire-time target resolution, the critical
# pierce); the DURABILITY is the myelin-flow durable timer wheel + signal (9.1/9.3/9.4), seamed
# behind escalation::DurableWheel (the real engine is P-FLOW-09/P-FLOW-13, a named floor; the
# in-memory InMemoryWheel models effectively-once). The provider+consumer CDC pair + the NOTIF-D7
# (exactly-once page across a kill) / NOTIF-D8 (critical pierces) drills are the dated green artifact.
[[contract]]
row = "7.5"
title = "oncall_now / page (escalation)"
status = "covered"
# The Notif machinery (the FROZEN §2.4 chain shape + oncall_now + page + the durable wheel) is owned +
# frozen at NOTIF-P14/P-192 (cdc_7_5_notif_escalation.rs). NOTIF-P21/P-342 ships the ISSUES consumer
# slice: Issues passes its REAL three-tier SLA escalation chain (team -> project -> org incident lead,
# `myelin_issues::sla_escalation::issue_sla_escalation_policy`) which REPLACES the Notif-defined
# `test_chain` floor. The CDC carries BOTH provider (Issues' real chain is the frozen 3-tier shape) +
# consumer (Notif's EscalationEngine starts + walks it on the durable wheel, exactly-once) sides; the
# ISS-D6 chain-start drill (drill_iss_d6_sla_escalation.rs) proves the breach starts the chain, walks
# it across a kill exactly-once, and the ack halts it. ZERO Notif code change (the inverse-signal).
cdc = [
    "crates/myelin-notif/tests/cdc_7_5_notif_escalation.rs",
    "crates/myelin-issues/tests/cdc_7_5_4_9_issues_sla_escalation.rs",
]

[[contract]]
row = "7.6"
title = "define_notif_rule"
status = "covered"
# The Notif SEAM + the registration verb + the §3.1 ranking table is owned + frozen at NOTIF-P8/P-186
# (the inverse-signal registry: a subsystem registers its reason set by CALLING `register`, zero Notif
# change). ISS-P04/P-243 ships the ISSUES slice on top: Issues declares its reason set
# (`myelin_issues::declares::issue_notif_rules` — SLA at-risk→critical / unblocked→watching /
# approval-requested→critical, each via the frozen `define_notif_rule` verb reconciled against the
# §3.1 table) and Notif ADMITS + ROUTES it. The CDC carries BOTH the provider (Issues declares the
# three reasons at their table-correct bands) and consumer (Notif registry admits + classifies) sides.
# The live trigger/SLA → Signal → inbox WIRING ("My Work") is the ISS-P22 follow-on; the other
# per-subsystem reason sets (Git/KN/Chat/CI) land in NOTIF-P19..P23.
cdc = [
    "crates/myelin-notif/tests/cdc_7_6_notif_define_rule.rs",
    "crates/myelin-issues/tests/cdc_7_6_issues_notif_rules.rs",
    # CHAT slice — chat (CHAT-P3 / P-245) declares its reason set (mentioned->direct /
    # replied->participating / thread_watched->watching / approval_requested->critical, each via the
    # frozen `define_notif_rule` verb reconciled against the §3.1 table) + its OWNED fanout-class
    # decision (arch §4: write-fanout the bounded high-signal `mention` producer vs read-fanout the
    # unbounded ambient set that never write-amplifies). Notif ADMITS + ROUTES it (zero Notif change).
    "crates/myelin-chat/tests/cdc_7_6_chat_notif_rules.rs",
    # GIT slice — git (GIT-P19 / P-263, M3) declares its reason set (review_requested->direct /
    # mentioned->direct / watched->watching, each via the frozen `define_notif_rule` verb reconciled
    # against the §3.1 table) AND wires its REAL PR/repo watcher reverse index behind the frozen
    # `WatcherResolvePort` (4.3/4.10), over the `watcher` relation its frozen ReBAC fragment declares
    # (4.9). Notif ADMITS + ROUTES the rules + READ-FANS-OUT a viewer's ambient Git slice over the real
    # watcher graph (replacing the NOTIF-P13 synthetic fixtures for Git subjects) — zero Notif change.
    # The CDC carries BOTH the provider (Git declares the reasons at table-correct bands + serves only
    # the Git watcher relation) and consumer (Notif registry admits/classifies + read-fanout lowers the
    # InRelation{watcher} push-down, held-not-leaked on revoke/unavailable) sides; it also pins the 4.9
    # watcher-relation slice for the read-fanout half (the fragment-admit slice stays cdc_4_9_git_fragment).
    "crates/myelin-git/tests/cdc_7_6_4_9_git_notif_rules.rs",
    # KNOWLEDGE slice — knowledge (NOTIF-P20 / P-264, M3) declares its reason set (mentioned->direct /
    # comments->participating / shared->direct / watched->watching, each via the frozen
    # `define_notif_rule` verb reconciled against the §3.1 table) AND wires its REAL page/space/row
    # watcher reverse index behind the frozen `WatcherResolvePort` (4.3/4.10), over the `watcher`
    # relation Id's compiled Knowledge ReBAC fragment declares (4.9, on space/page/database_row). Notif
    # ADMITS + ROUTES the rules + READ-FANS-OUT a viewer's ambient KN slice over the real watcher graph
    # (replacing the NOTIF-P13 synthetic fixtures for KN subjects) — zero Notif change, the SECOND
    # producer no harder than the first. The KN registration lives in `myelin-identity-service`
    # (alongside `knowledge_fragment`) for the same §2.9-DAG reason the fragment does: `myelin-content`
    # is the Knowledge data-model leaf and Notif already depends ON content, so a `content -> notif`
    # edge would be a cycle. The CDC carries BOTH provider + consumer sides; it pins the 4.9
    # watcher-relation slice for the read-fanout half (the fragment-admit slice stays
    # cdc_4_9_knowledge_fragment).
    "crates/myelin-identity-service/tests/cdc_7_6_4_9_knowledge_notif_rules.rs",
    # ISSUES slice — issues (NOTIF-P21 / P-342, M4) declares the FULL consumer reason set (assigned->
    # direct / blocked->watching / SLA->critical / unblocked->watching / approval_requested->critical,
    # each via the frozen `define_notif_rule` verb reconciled against the §3.1 table —
    # `myelin_issues::declares::issue_notif_rules`, extended from the ISS-P04 SLA/unblocked/approval
    # slice) AND wires its REAL issue watcher reverse index behind the frozen `WatcherResolvePort`
    # (4.3/4.10 — `myelin_issues::sla_escalation::IssueWatcherIndex`), over the `watcher` relation its
    # frozen ReBAC fragment declares (4.9, on `issue`). Notif ADMITS + ROUTES the rules + READ-FANS-OUT a
    # viewer's ambient "My Work" slice over the real watcher graph (replacing the NOTIF-P13 synthetic
    # fixtures for Issues subjects) — zero Notif change, the THIRD consumer no harder than the first. The
    # 7.6 reason-set CDC is cdc_7_6_issues_notif_rules.rs; the 4.9 watcher read-fanout half + the 7.5
    # real-chain half are in cdc_7_5_4_9_issues_sla_escalation.rs (the C-9 "My Work" invariant is
    # integration_issues_my_work_c9.rs; the ISS-D6 chain-start drill is drill_iss_d6_sla_escalation.rs).
    "crates/myelin-issues/tests/cdc_7_5_4_9_issues_sla_escalation.rs",
    # CI slice — CI (NOTIF-P23 / P-344, M4 — the FINAL M4 consumer accretion) declares its
    # status-summary reason set (a check status change → `state_changed`->watching, via the frozen
    # `define_notif_rule` verb reconciled against the §3.1 table — `myelin_ci_sandbox::ci_notif_rules`)
    # AND registers its `CheckStatus.summary` `HumanisedRef` templates on the ONE humanise surface (7.3
    # — the `ci.check.<verdict>` bodies). Notif ADMITS + ROUTES the rule + admits the templates — zero
    # Notif change, the FOURTH+ consumer no harder than the first. The X-1 invariant the CI summary is a
    # `(template_key, args)` pair (never a raw string) that resolves THROUGH humanise is pinned here +
    # exercised end-to-end (per-viewer resolve → tombstone on deny, NOTIF-D4) in the companion
    # integration_ci_summary_humanises.rs. The live CI Signal-curation emitter is the CI emit follow-on.
    "crates/myelin-ci-sandbox/tests/cdc_7_6_5_9_ci_notif_summary.rs",
]

# 7.7 SPLIT: the HOLDER HALF (registration + locate/export/rectify/restrict + the structural
# references-not-payloads erase) ships NOW at NOTIF-P4 (P-182) with its provider+consumer CDC pair
# (crates/myelin-notif/tests/cdc_7_7_notif_holder.rs) — Notif as holder H13 (NotificationHistory). The
# REPLAY half (`replay(scope, since)` — the inbox rebuilt by reindex-from-source, the only recovery
# path) lands at NOTIF-P17 (P-195); the off-cell-payload erasure residual (X-7 / 10.9) at NOTIF-P27
# (P-469). The holder-half CDC pair is the dated green artifact for row 7.7 here.
[[contract]]
row = "7.7"
title = "PersonalDataHolder (Notif history) + replay"
status = "covered"
cdc = [
    "crates/myelin-notif/tests/cdc_7_7_notif_holder.rs",
]

[[contract]]
row = "7.8"
# COVERED at NOTIF-P16 (P-194): the BODY behind the carrier trait — the idempotent `DeliveryFabric`
# (at-least-once + idempotent on `UNIQUE(tenant, idem_key)` — a retried send collapses to exactly one
# effective delivery), the deterministic mock adapter (`--use-mock`-as-runtime, the §3.6 FLOOR), and
# the redaction discipline (off-cell carries a humanised summary + deep link, `delivery.redacted=true`;
# in-app stays in-cell — 0 off-cell egress, 0 off-cell full-body). The CDC pair pins the PROVIDER (the
# fabric: idempotent + redacted + EU-preferring) against the CONSUMER (a concrete adapter implementing
# the SAME trait — the strategy-pattern swap point the real EU provider, NOTIF-P25/P26, plugs into).
# The NOTIF-D9 drill (crash between provider-ack and ledger-write, retry → exactly 1 effective delivery
# per (item, channel)) is the dated green artifact. FLOOR: the real EU email/push provider (with its
# DPA/sub-processor posture) is NOTIF-P25/P26 ([OPEN — LEGAL]); the durable `notif_delivery` PgStore
# backing is the integration leg.
title = "DeliveryAdapter"
status = "covered"
cdc = ["crates/myelin-notif/tests/cdc_7_8_notif_delivery.rs"]

# ──────────────── §8 Agent fabric (myelin-agent) ────────────────────────────────────────────────

[[contract]]
row = "8.1"
# COVERED at AG-P1 (P-130): the SIGNATURE half — the `ToolSurface` trait (register_tool/resolve) +
# the `ToolDef` value type with its frozen field list (name, subsystem, version, input_schema,
# required_caps, effect_kind, side_effecting, requires_approval, exposed_over_mcp). The CDC pair
# pins the PROVIDER (the platform-owned catalogue: register then resolve round-trips every field)
# against the CONSUMER (a subsystem contributing a ToolDef). The persisted catalogue + the
# per-subsystem requires_approval defaults SEED are AG-P8 (→ P-220); the migration is AG-P2
# (→ P-131) — IMPL follow-ons, not contract-pair gaps; the 8.1 signature pair carries both sides NOW.
#
# KN-P27 (→ P-317, M3) adds the KNOWLEDGE-slice CDC: the KN-domain SOURCE OF TRUTH
# (myelin_knowledge::agent — the tool identity + the required_caps from the frozen myelin-content ReBAC
# carrier + the frozen §6.3 consequential-gate classification) is pinned AGAINST the Fabric ToolDef
# registration (myelin_agent_service::knowledge_tools, AG-P19) — the registered defs MUST carry the
# KN-domain caps + gate byte-for-byte (a rename/gate-drift on either side is a compile/test break).
title = "ToolSurface::register_tool(ToolDef)"
status = "covered"
cdc = [
    "crates/myelin-agent/tests/cdc_8_1_tool_surface.rs",
    "crates/myelin-knowledge/tests/cdc_8_1_knowledge_agent_tools.rs",
    # CI-P26 / P-369 (M4): the CI X-6 slice — CI OWNS its action-consequence classification (X-6, a
    # frozen contract value): `myelin_ci_controlplane::surfacing_tools` ships the COMPLETE CI agent-tool
    # set (run/run_pipeline/cancel_run/retry_run/read_log/read_run/validate/plan = NOT gated;
    # deploy/approve_deploy/rollback/write_secret = gated) with the frozen X-6 requires_approval +
    # effect-kind/read split + the 4.9 required_caps, and `register_ci_tools` registers them into the ONE
    # ToolSurface. The CDC RECONCILES the Fabric seed (`myelin_agent_service::defaults::
    # requires_approval_default`) against CI's owned classification for every CI tool — a gating drift on
    # either side is a test break. CI-P24/P-347 (`myelin_agent_service::ci_tools`) shipped the four
    # privileged/run-pipeline consumer defs; CI-P26 completes the read/run/lifecycle/rollback rows so the
    # frozen X-6 table is whole. ToolHands::exec is ABSENT (the runner itself, X-6 / 05 §HP-5).
    "crates/myelin-agent-service/tests/cdc_8_1_ci_x6_tooldefs.rs",
]

[[contract]]
row = "8.2"
# COVERED at AG-P1 (P-130): the SIGNATURE half — the `EffectApi::apply(&RunCtx, ProposedEffect) ->
# Applied(EventId) | Gated(GateId) | Denied(reason)` outcome shape. The CDC pair pins the PROVIDER
# (plan-then-apply returns exactly one of three outcomes; a gated effect does NOT mutate, AG-8)
# against the CONSUMER (the loop / external MCP / a workflow activity branching on the outcome). The
# eight-step pipeline BODY (AG-D1/D2/D3) is AG-P6 (→ P-218) — an IMPL follow-on, not a pair gap.
#
# KN-P27 (→ P-317, M3) adds the KNOWLEDGE-slice CDC: the KN agent-governance HALF
# (myelin_knowledge::agent — the HITL withhold (no mutation, AG-8) + the per-effect idem_key + the
# reserve/settle bookend the KN public endpoint applies before threading an op through the collab
# protocol with "suggested by agent" attribution) is pinned AGAINST the FROZEN Fabric rules: the
# per-effect idem_key derivation AGREES with myelin_agent_service::hitl_batch::per_effect_idem_key
# (OQ-F — a double-click is one approval), and the KN-D11 chained drill (plans → withheld → approve →
# applied once across a double-click → reserve/settle) is the consumer (0 ungoverned/0 pre-approval/0
# double-apply).
title = "EffectApi::apply (plan-then-apply)"
status = "covered"
cdc = [
    "crates/myelin-agent/tests/cdc_8_2_effect_api.rs",
    "crates/myelin-knowledge/tests/cdc_8_2_knowledge_agent_governance.rs",
]

[[contract]]
row = "8.3"
# COVERED at AG-P1 (P-130): the SIGNATURE half — `AgentRuntime::step(&Conversation) -> UseTools |
# Submit`, the stateless-brain strategy seam. The CDC pair pins the PROVIDER (a deterministic
# scripted brain on the `--use-mock` code path; step is a pure function of the conversation, NO LLM
# SDK) against the CONSUMER (the platform loop reading the StepOutcome). The runtimes are SKELETON
# (AG-P4 → P-216) + MockAgentRuntime (AG-P5 → P-217); LlmAgentRuntime is designed-not-built (AG-P25)
# — IMPL follow-ons, not a pair gap; the 8.3 signature pair carries both sides NOW.
title = "AgentRuntime::step"
status = "covered"
cdc = ["crates/myelin-agent/tests/cdc_8_3_agent_runtime.rs"]

[[contract]]
row = "8.4"
title = "ToolHands::exec (unified sandbox)"
# COVERED at CI-P1 (P-129): the CI runner-seam half — the JobSpec shape + the SandboxBackend/
# FleetProvider trait seam + the four-uniform-guarantee hooks (X-6). The CDC pair pins the
# PROVIDER (the CI runner seam: launch drives isolation-floor → attribution → reserve → settle,
# fail-closed on un-digested image / exhausted wallet) against the CONSUMER (a dispatcher building
# a JobSpec; the agent-fabric case wires the X-6 equivalence ToolHands::exec == launch(JobSpec
# {kind:Agent})). The FULL agent-fabric consumer body (the real ToolHands::exec dispatch path) is
# SHIPPED at AG-P15 (→ P-226): myelin-agent-service::exec::SandboxToolHands realises
# ToolHands::exec as the dispatch of the kind=agent JobSpec onto the unified sandbox, with the
# routing split (only `compute` reaches the sandbox; a mutate/external effect can NEVER build a
# SandboxJob → 0 mutate-via-exec, encoded in the type) + the four guarantees wired by construction.
# Its CDC (cdc_8_4_exec_routing.rs) pins the AGENT-FABRIC CONSUMER half (the runner-seam PROVIDER
# driving the four guarantees in order; the Fabric CONSUMER routing compute → sandbox + refusing a
# mutate). The ZERO-escapes real-kernel GATE proving guarantee #4 is CI-P5 (→ P-239) / AG-P17 (→
# P-229); the Firecracker backend is CI-P2 (→ P-237).
#
# DRILL-AS-GATE consumed at AG-P17 (→ P-229): the CI side ran the real-kernel escape drill on a real
# Firecracker microVM and emitted a GREEN EscapeAttestation (CI-P5 → P-239). The FABRIC half CONSUMES
# that attestation (myelin-agent-service::escape_gate::AgentExecGate — it does NOT re-implement the
# drill nor fork the attestation type) as a FAIL-CLOSED gate on the exec dispatch path: a
# SandboxToolHands / AgentJobDispatcher cannot be constructed without a GREEN AgentExecGate for the
# production backend (ZERO escapes, matching kernel/rootfs/corpus identity) — so no green attestation
# ⇒ no untrusted compute, encoded in the TYPE (never a hardcoded true). The consumer CDC
# (cdc_8_4_escape_gate.rs) proves the Fabric exec path is fail-closed without a green attestation and
# admits with one; the integration test (integration_escape_gate.rs, integration-gated) consumes the
# REAL P-239 artifact. There is NO floor on AG-D4 (ZERO escapes is floor + full answer; a PERMANENT
# GATE re-run on every backend/image/kernel change); the real LlmAgentRuntime against this runner is
# post-M5 (AG-P25). These are follow-ons, not pair gaps.
#
# M4 PROD-IMAGE RE-CONFIRM at AG-P21 (→ P-348): the SAME real escape battery (the CI-P5 / P-239
# adversarial corpus + the production Firecracker launch recipe — REUSED, not forked) is re-run on the
# PRODUCTION CI runner image (the config-resolved runner rootfs — dev↔prod is a config swap,
# MYELIN_REGION=fr-par / prod=Scaleway), emitting a DATED GREEN attestation tagged as the prod-image
# re-confirm (a distinct `prod-image-<date>.json` path + a role residual; the EscapeAttestation type is
# REUSED whole). The drill is `myelin-ci-sandbox/tests/escape_drill_prod_image_reconfirm_test.rs`
# (integration-gated; boots a REAL microVM on this host, ZERO escapes). The CONSUMER CDC
# (cdc_8_4_prod_image_reconfirm.rs) proves the Fabric exec/escape-gate path (AgentExecGate, REUSED)
# admits a green prod-image attestation + is fail-closed without one + treats the prod image as a
# DISTINCT identity (the permanent gate). NAMED residual: production runs on KVM-capable Scaleway
# Elastic Metal (the prod image is re-drilled there at deploy); the CI-side M4-boundary re-confirm is
# CI-P27 / P-370.
#
# M4 CI-SIDE COMMITTED-GATE RE-CONFIRM at CI-P27 (→ P-370): the SAME escape battery (the CI-P5 / P-239
# corpus + the production Firecracker launch recipe — REUSED, not forked) is re-run on the PRODUCTION
# CI runner image as the CI control plane's COMMITTED M4-boundary gate. The drill is
# `myelin-ci-sandbox/tests/escape_drill_ci_committed_gate_reconfirm_test.rs` (integration-gated; boots
# a REAL microVM on this host, ZERO escapes, dated attestation `ci-p27-reconfirm-<date>.json`). It is
# declared as a committed CI job in `myelin_ci_controlplane::permanent_gates::m4_boundary_permanent_
# gates` (an uncommitted re-run is no gate, EI-01 §5) — the COMMITTED-GATE WIRING half (no new drill
# logic). No floor (a PERMANENT GATE); gVisor re-runs it at CI-P28.
status = "covered"
cdc = [
    "crates/myelin-ci-sandbox/tests/cdc_8_4_unified_sandbox.rs",
    "crates/myelin-agent-service/tests/cdc_8_4_exec_routing.rs",
    "crates/myelin-agent-service/tests/cdc_8_4_escape_gate.rs",
    "crates/myelin-agent-service/tests/cdc_8_4_prod_image_reconfirm.rs",
]

[[contract]]
row = "8.5"
# COVERED at AG-P1 (P-130): the SIGNATURE half — `Agent::handle(InboxEvent, &dyn AgentRuntime) ->
# RunOutcome`, the platform-owned bounded multi-turn loop. The CDC pair pins the PROVIDER (the loop
# drives the brain through the `&dyn AgentRuntime` strategy seam — dynamically dispatched + swappable
# for mock/real) against the CONSUMER (the dispatch tier delivering an event + a runtime choice). The
# bounded loop BODY (build_conversation → reserve → step → route → settle) is AG-P4 (→ P-216) — an
# IMPL follow-on, not a pair gap; the 8.5 signature pair carries both sides NOW.
title = "Agent::handle"
status = "covered"
cdc = ["crates/myelin-agent/tests/cdc_8_5_agent_handle.rs"]

[[contract]]
row = "8.6"
# COVERED at AG-P1 (P-130): the SIGNATURE half — `EventInbox::deliver(InboxEvent)`, the platform
# delivery seam (agents don't poll; explicit-first dispatch — a mention notifies, it does not
# auto-spawn a costed run). The CDC pair pins the PROVIDER (the platform delivers a matched event)
# against the CONSUMER (the agent fabric records it). The dispatch-tier wiring is the Bus's (§3.6) +
# the SKELETON consumer body is AG-P4 (→ P-216) — IMPL follow-ons, not a pair gap.
# NOTIF-P22 / P-343 (M4) ships the CHAT half of the EXPLICIT-FIRST boundary: chat decides the
# dispatch CLASS per event (`myelin_chat::glue::agent_dispatch_class` — a casual @agent mention →
# NotifyOnly, an explicit action → ExplicitDispatch) and the Bus dispatch tier
# (`myelin_query::DispatchTier`, §4.7) ENFORCES it: a Mention → `Disposition::NotifiedOnly`, 0
# auto-spawn, 0 reservation (the explicit-first check fires BEFORE reserve/settle). The CDC carries
# the PROVIDER (chat's class decision) + CONSUMER (the dispatch tier's enforcement) sides; the
# CHAT-D17 drill (drill_chat_d17_explicit_first.rs) proves 0 auto-spawn from a casual mention + the
# reserve gate bites even the explicit run. ZERO Notif/Bus code change (the inverse-signal).
title = "EventInbox::deliver"
status = "covered"
cdc = [
    "crates/myelin-agent/tests/cdc_8_6_event_inbox.rs",
    "crates/myelin-chat/tests/cdc_8_6_chat_explicit_first.rs",
]

[[contract]]
row = "8.7"
# COVERED at AG-P1 (P-130): the SIGNATURE half — `run --dry-run(InboxEvent) -> Vec<ProposedEffect>`,
# the plan-then-apply testability lever (the `DryRun` trait). The CDC pair pins the PROVIDER (dry_run
# returns the proposed effects WITHOUT applying any — no mutation) against the CONSUMER (the CLI / a
# test reading the plan). The `run --dry-run` plan-lever BODY is AG-P8 (→ P-220) — an IMPL follow-on,
# not a pair gap; the 8.7 signature pair carries both sides NOW.
title = "run --dry-run -> Vec<ProposedEffect>"
status = "covered"
cdc = ["crates/myelin-agent/tests/cdc_8_7_dry_run.rs"]

[[contract]]
row = "8.8"
title = "AG-7 agent trace"
# COVERED at the GDPR-service seam (cdc_8_8_agent_trace_seam). AG-P19 (→ P-268, M3) adds the FABRIC
# consumer leg: the agent-trace HOLDER seam — run.trace_ref resolves to a CONTENT-ADDRESSED Knowledge
# document reusing the frozen myelin-content 13.1 block model (a blake3:<hex> over a Vec<Block>), and
# the trace registers as the erasable H17 PersonalDataHolder, distinct from the audit log. The drill
# pair below carries the PROVIDER (the Fabric builds + content-addresses the 13.1 trace document) +
# CONSUMER (run.trace_ref reads the content address; the H17 holder erase crypto-shreds it, KN-D12).
# KN-P28 (→ P-318, M3) lands the LIVE KNOWLEDGE-side PRODUCER (`myelin_knowledge::agent::trace`): the
# content-addressed write_agent_trace reusing the block model, the erasable AgentTraceHolder distinct
# from the audit log, and the KN-D12 erase (per-subject DEK crypto-shred, attribution falls back to the
# pseudonym). The Knowledge-slice CDC below pins that PRODUCER agrees with the Fabric CONSUMER seam (the
# same blake3:<hex> over the same 13.1 Vec<Block>) + the GDPR-service H17 id (one name across the seam).
status = "covered"
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_8_8_agent_trace_seam.rs",
    "crates/myelin-agent-service/tests/drills_ag_p19_knowledge_tools.rs",
    "crates/myelin-knowledge/tests/cdc_8_8_knowledge_agent_trace.rs",
]

# ──────────────── §9 Durable workflow (myelin-flow) ─────────────────────────────────────────────

[[contract]]
row = "9.1"
title = "DurableExecutor{start, signal, describe, cancel}"
status = "covered"
# The PROVIDER half (the engine-agnostic start/describe/cancel control surface + the per-effect
# idem_key rule) shipped in myelin-flow (P-203 + siblings); its CDC is cdc_9_1_executor.rs.
# CI-P11 (P-354, M4) adds the CI Trigger & Dispatch CONSUMER half: the reserve/start handoff
# (myelin_ci_dispatch::resolve::reserve_and_start) builds StartSpec{ wf_type: "ci.pipeline",
# input: [snapshot_ref], idem_key } and hands it to DurableExecutor::start (9.1) — a re-delivered
# trigger is ONE run (idempotent on idem_key), started on the references-not-payloads CAS snapshot
# ref. The prior `deferred → P-203` marker was stale (P-203 had shipped the provider + its CDC).
cdc = [
    "crates/myelin-flow/tests/cdc_9_1_executor.rs",
    "crates/myelin-ci-dispatch/tests/cdc_ci_p11_resolve_dispatch.rs",
]

[[contract]]
row = "9.2"
title = "WfCtx core"
status = "deferred"
landing = "P-199"

[[contract]]
row = "9.3"
title = "durable timer wheel"
# Wheel + arm/fire half: P-207 (FLOW-D3 100k floor + live-PG). Cheap disarm/re-arm half: P-210
# (the §6.6 row-update re-arm + disarm), paired with the Issues `stale_after`/SLA consumer fixture.
# P-FLOW-21 / P-266 (M3) CONFIRMS the cheap re-arm UNDER its real Git/Issues + Trigger call sites: the
# ONE documented call-site helper (crates/myelin-flow/src/timer.rs `sla` module — SlaTimerCall +
# sla_timer_id / trigger_stale_timer_id) both producers call, so a re-arm at the Issues/Trigger
# boundary is a SINGLE row update (no ad-hoc key, no second code path). The cdc pair below now routes
# through the helper (not ad-hoc keys). NO new owned row, NO new FLOW drill is owed in M3.
status = "covered"
cdc = ["crates/myelin-flow/tests/cdc_9_3_disarm_rearm.rs"]

[[contract]]
row = "9.4"
title = "durable signal (multi-day HITL)"
status = "deferred"
landing = "P-205"

[[contract]]
row = "9.5"
title = "workflow<->agent mapping"
status = "deferred"
landing = "P-213"

[[contract]]
row = "9.6"
title = "PersonalDataHolder (workflow history) + replay"
status = "deferred"
landing = "P-201"

# P-FLOW-20 / P-265 (M3) — resumable maintenance activities + the history-rewrite invalidation
# fan-out. NO new owned FLOW row + NO new FLOW drill is owed in M3 (roadmap §2 M3 Exit gate — GIT-D9
# is Git's gate). This prompt ships the myelin-flow helper (crates/myelin-flow/src/maintenance.rs):
# (a) WfCtx::run_maintenance / run_heavy_maintenance — Git's GC/repack/bundle-gen/history-rewrite run
# as journaled activities (or SCHEDULE_AND_RUN_JOB long-parks for the heavy ones), so a crash
# mid-repack replays to the un-journaled step (§4.1) with 0 re-executed side effect; (b)
# WfCtx::run_history_rewrite_invalidation — the fan-out over the trust-scoped cache namespaces
# (contract 11.2: Fork/Mirror/CloneBundle) as a journaled sequence replaying from the last journaled
# step. CONSUMES contract 10.6 (history-rewrite audited op — GDPR/Audit-owned, `covered` above) +
# 11.2 (trust-scoped cache namespaces — Storage-owned, `covered` below). The application of the
# existing activity model (§4.4) — NO new engine primitive. The Git GC/repack/bundle-gen/
# history-rewrite CALL SITES are co-built in Git's M3 prompts (GIT-D9 is Git's gate).
# Green artifact: crates/myelin-flow/tests/drills_flow_maintenance.rs — crash-mid-repack-resumes-with-
# no-side-effect + the fan-out-replays-from-last-step (the FLOW-D1 property reused on a maintenance
# workflow), registered into the M0 permanent suite.
#
# P-FLOW-21 / P-266 (M3) — the cheap SLA-timer re-arm CONFIRMED under the Git/Issues + Trigger call
# sites + the merge-queue holds-no-runtime re-green. NO new owned FLOW row + NO new FLOW drill is owed
# in M3 (roadmap §2 M3 — GIT-D9 is Git's gate). This prompt ships the ONE documented call-site helper
# (crates/myelin-flow/src/timer.rs `sla` module — SlaTimerCall + sla_timer_id / trigger_stale_timer_id)
# both producers call, so a re-arm at the Issues/Trigger boundary is a SINGLE row update (no ad-hoc
# key, no second code path — the M3 confirmation that the P-FLOW-14 cheap row op is what the REAL call
# sites hit), and re-greens the P-FLOW-19 merge-queue-in-isolation drill (holds-no-runtime, 0
# double-merge). Green artifacts: crates/myelin-flow/tests/cdc_9_3_disarm_rearm.rs (re-arm-is-row-update
# at the call boundary, now routed through the helper) + crates/myelin-flow/tests/drills_flow_merge_queue.rs
# (the merge-queue-in-isolation re-green). The X-1 seam END-TO-END against CI's real ci.result producer
# is P-FLOW-22 (M4).

# ──────────────── §10 GDPR / Audit / PersonalDataHolder (myelin-gdpr) ───────────────────────────

# 10.1 — the SIGNATURE was frozen at P-GA-01 (`myelin-gdpr`); the BODIES + the GDPR-owned holder
# impls (H18 GDPR's own stores G1–G7 + H16 the audit carve-out) land at P-GA-05 → P-105, which
# ships the provider+consumer CDC pair NOW (the GDPR-owned holder responding + the DSR-orchestrator
# fanning out via `dyn PersonalDataHolder`). The upstream-store orchestration (H6/H8/…) extends
# this at P-GA-06 → P-106; it is not a missing pair (the pair below is a real provider+consumer
# seam), so the row is `covered`.
[[contract]]
row = "10.1"
title = "PersonalDataHolder{locate, export, rectify, restrict, erase}"
# COVERED by the GDPR-owned holder bodies (P-GA-05) AND — extended at REF-P3 / P-120 — the Refs
# side: Refs as holder H12 (a STUB surface at M1; locate/export empty-but-correct, restrict/erase
# no-ops; the real erase = R2-cache purge + Identity pseudonym shred for origin_actor + *.erased
# tombstoning lands in REF-P15). Each CDC file carries its own provider+consumer pair.
status = "covered"
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_10_1_holder_bodies.rs",
    "crates/myelin-refs-service/tests/cdc_10_1_refs_holder.rs",
    # AG-P3 / P-132 — the Agent-Fabric side: the H11 OLTP + H17 trace stores as holders (a
    # REGISTRATION SEAM; locate/export empty-but-correct, erase/rectify/restrict no-ops naming their
    # AG-P23 / AG-P19 follow-on). The provider+consumer pair fans out via `dyn PersonalDataHolder`.
    "crates/myelin-agent-service/tests/cdc_10_1_agent_holder.rs",
    # P-GA-27 → P-256 — the M3 PRODUCER-holder fan-out leg: the orchestrator fans to Git H1 /
    # Knowledge H4 / agent-trace H17 (crypto-shred inline bodies / blocks+db-rows+embeddings / the
    # agent trace), and those holders respond. The Knowledge instance (10.9 by reference) + the H17
    # trace holder body (distinct from audit) light up here; the Git X-7 instance is P-GA-28 → P-257.
    "crates/myelin-gdpr-service/tests/cdc_10_1_producer_holders.rs",
    # GIT-P29 / P-290 — the GIT side of 10.1: the H1 holder BODY (GitPersonalDataHolder) implementing
    # locate/export/rectify/restrict over git + metadata, with the contract-shaped erase(EraseScope)
    # loud-refusing (the EI-01 §1 deviation — no seam bundle on the frozen signature) and pointing at
    # the real §6.1 fan-out erase_fanout (GIT-D2 complete: every git holder hit, residual == the ONE
    # posture 10.9/X-7, backups shredded). The provider+consumer pair fans locate out + drives the
    # real fan-out via the wired EraseHolders bundle.
    "crates/myelin-git/tests/cdc_10_1_10_4_git_holder.rs",
    # KN-P24 / P-314 (M3) — the EXPORT half of 10.1: the Knowledge Export/Import service
    # (`myelin_knowledge::export`) is the Art. 20 lossless-JSON portable-bundle MECHANISM the GDPR
    # `PersonalDataHolder::export` in KN-P25 reuses (`ExportDoc::to_json_bundle`/`from_json_bundle`,
    # byte-faithful round-trip + `render(parse(md))===md` across the boundary). The provider+consumer
    # pair drives the Export service (provider) + the GDPR export(subject) holder pattern (consumer)
    # that assembles the subject's bundle by reusing it (EI-01 §7, one mechanism).
    "crates/myelin-knowledge/tests/cdc_13_2_10_1_knowledge_export_import.rs",
    # CI-P9 / P-352 (M4) — the CI side of 10.1: the CI Control Plane holder (CiHolder, H2) over
    # run-state/logs/artifacts/caches/deployments. locate/export are TYPED (empty-but-correct
    # content-addressed receipts); restrict flips a REAL per-subject flag the CI index/agent/analytics/
    # notif seams read (Art. 18/21); erase is STUBBED to crypto-shred — a typed no-op naming its CI-P32
    # / CI-D3 fan-out follow-on (the per-subject/per-tenant DEK crypto-shred + pseudonym shred +
    # ci.*.erased tombstones; residual = the ONE posture 10.9/X-7, by reference). The CI OLTP store
    # auto-registers through the substrate one door (1.4) + classifies to H2 (0 orphans). The
    # provider+consumer pair fans the DSR out via dyn PersonalDataHolder.
    "crates/myelin-ci-controlplane/tests/cdc_10_1_ci_holder.rs",
    # ISS-P31 / P-385 (M4-I8, the band exit) — the Issues side of 10.1, now the FULL erase BODY: the
    # Issues holder responds to the frozen dyn PersonalDataHolder surface (provider) AND the DSR
    # orchestrator drives IssueEraseFanout across EVERY Issues holder (HolderTarget::ALL — pseudonym map
    # / per-subject free-text DEK / attachment-blob DEK / OLAP+restriction / Search+embeddings / Refs)
    # with a per-holder receipt + post-restore re-erasure (GD-14). The real per-subject DEK is
    # crypto-shredded over the ONE KmsEngine ISS-P07 sealed through; 0 PII keys resurrected post-restore.
    # The third-party residual = the ONE posture 10.9/X-7, by reference. The provider+consumer pair.
    "crates/myelin-issues/tests/cdc_10_1_issues_holder_erase.rs",
]

# 10.2 — the classify-derive (five tags + subject_locator, P-GA-07) PLUS the OQ-H worklog extension
# (P-GA-31 → P-334): the `data_role_default = Restricted` worklog tag is now STRUCTURAL (captured into
# the generated registry), and its consumer (the worklog-analytics default-DENY) ships its CDC pair.
# The worklog `basis = TBD_LEGAL` is the NAMED `[OPEN — LEGAL]` residual (counsel ratifies
# special-category vs elevated + the per-jurisdiction works-council trigger; the structural floor ships
# regardless). So the row stays `covered`.
[[contract]]
row = "10.2"
title = "#[personal_data(...)] classify-derive + the OQ-H worklog tag (data_role_default)"
status = "covered"
cdc = [
    "crates/myelin-gdpr/tests/cdc_10_2_classify_derive.rs",
    "crates/myelin-gdpr/tests/cdc_10_2_worklog_tag.rs",
]

# 10.3 — the data-map / RoPA GENERATOR ships its provider+consumer CDC pair NOW (P-GA-09 → P-109):
# the PROVIDER is `data_map() → Inventory` (the generator walking the `#[personal_data]` registry +
# the auto-registered holder set); the CONSUMER is the DSR-orchestrator resolve-scope-from-the-map
# step that builds the per-holder erase checklist off the generated map (the P-GA-13 → P-111 body
# extends this consumer; the pair below is a real provider+consumer seam). The CI data-map DIFF GATE
# (commit the inventory; fail the build on a change) extends this at P-GA-10 → P-110; the per-store
# content completeness floor is M5 P-GA-32 → P-505. So the row is `covered`.
[[contract]]
row = "10.3"
title = "data_map() / ropa(tenant)"
status = "covered"
cdc = ["crates/myelin-gdpr-service/tests/cdc_10_3_data_map.rs"]

# 10.4 — the DSR orchestrator API + the state machine + the controller/processor posture gate ships
# its provider+consumer CDC pair NOW (P-GA-11 → P-111): the orchestrator (provider) + a DSR-submitting
# caller polling `dsr_status` + an auditor reading `dsr_certificate` (consumer). The per-holder fan-out
# DRIVE + the verifiable receipts (P-GA-12 → P-112), tenant-operability (P-GA-13 → P-113), and the
# Merkle SEAL of the certificate (P-GA-20 → P-119) EXTEND this spine. The DURABLE DEADLINE TIMER leg
# (P-GA-21 → P-148) is NOW SHIPPED (`cdc_10_4_deadline_timer.rs` — the timer arming the
# `sleep_until(deadline − warning_margin)` + the wheel firing the warning Signal + the restart
# survival + the Art. 12(3) extension re-arm), CLOSING the P-GA-14 coarse-deadline floor. All are real
# provider+consumer seams (not a missing pair), so the row is `covered`.
[[contract]]
row = "10.4"
title = "dsr_submit / dsr_status / dsr_certificate"
status = "covered"
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_10_4_dsr_orchestrator.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_4_fanout.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_4_tenant_ops.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_4_deadline_timer.rs",
    # GIT-P29 / P-290 — the GIT-side DSR fan-out leg of 10.4: the §6.1 erasure algorithm over git as
    # holder H1 reaches EVERY git holder (pseudonym map + per-subject DEK bodies + reflog/bitmap/pack
    # backups + search + refs + bus + cache/CDN + erasure ledger) — GIT-D2 complete. The same CDC file
    # pins the per-holder fan-out drive shape the orchestrator takes for H1.
    "crates/myelin-git/tests/cdc_10_1_10_4_git_holder.rs",
]

# 10.5 — the retention engine + consent + sub-processor + legal-hold + the transfer gate. The
# RETENTION leg (tightest-policy-wins + legal-hold-aware suspend-don't-delete) ships its
# provider+consumer CDC pair at P-GA-22 → P-149 (`cdc_10_5_retention_engine.rs`: the engine ⇄ a
# retention caller + a hold caller). The CONSENT / SUB-PROCESSOR / TRANSFER-GATE legs ship NOW at
# P-GA-23 → P-150 (`cdc_10_5_consent_transfer_gate.rs`: the `transfer_allowed` gate ⇄ a transfer
# caller — deny extra-EU by default; the consent registry ⇄ a consent caller — withdrawal
# propagates + may trigger deletion; the sub-processor registry ⇄ a change-notification caller —
# versioned + region + DPA ref + objection). The §5.3 outbound push-mirror residency gate reads the
# SAME `transfer_allowed` policy shipped here; its end-to-end proof is M5 P-GA-35 (GA-11). All are
# real provider+consumer seams, so the row is now `covered`. The MIRROR half (C-4, Tenancy-owned —
# the control-plane residency-boundary decision `mirror_allowed`) ships at P-CP-16 → P-251
# (`cdc_10_5_mirror_gate.rs`: the Git mirror feature ⇄ `mirror_allowed` over the REAL GDPR
# `transfer_allowed` — deny-by-default for a residency-boundary crossing without a recorded lawful
# basis). The control plane owns "does this target cross the residency boundary?"; GDPR owns "is this
# transfer lawful?". The STORAGE HALF of the C6 mirror seam ships at P-ST-25 → P-255
# (`cdc_10_5_mirror_crossing_flag.rs`): Storage FLAGS the residency-boundary crossing (it reports the
# mirror TARGET region into `residency_verify` + counts `mirror_residency_deny{tenant}`) and the
# control plane's `mirror_allowed` gate DENIES the same crossing by default — Storage flags, the
# control plane gates (storage.md §6). Storage authors NO local mirror policy (the deny is at 10.5).
[[contract]]
row = "10.5"
title = "retention / consent / sub-processor / legal-hold / transfer gate / outbound-mirror (C-4)"
status = "covered"
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_10_5_retention_engine.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_5_consent_transfer_gate.rs",
    "crates/myelin-control-plane/tests/cdc_10_5_mirror_gate.rs",
    "crates/myelin-storage/tests/cdc_10_5_mirror_crossing_flag.rs",
]

# 10.6 — the CONSTRUCTION half (the outbox-only audit consumer + the per-tenant hash-chain +
# the Merkle leaves + the minimisation) ships its provider+consumer CDC pair (P-GA-19 → P-062).
# The PROOFS half (inclusion/consistency/signed-tree-head + independent-witness anchoring +
# DSR-receipt seal + the H16 carve-out body) ships its OWN provider(audit authority)+consumer
# (auditor) pair at P-GA-20 → P-119. Both pairs are real provider+consumer seams, so the row is
# `covered`.
[[contract]]
row = "10.6"
title = "tamper-evident audit log"
status = "covered"
# CI-P23 (P-366, M4) adds a CONSUMER of the CT-Merkle PATTERN: CI's sigstore Rekor transparency log
# is the SAME RFC 6962 BLAKE3 Merkle structure (deterministic root over leaf digests + the inclusion
# check) GDPR/Audit's tamper-evident log builds — one Merkle convention platform-wide, no second
# transparency mechanism (the audit-log CODE stays service-internal to gdpr-service; CI reuses the
# structure over its own supply-chain leaves).
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_10_6_audit_log.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_6_audit_proofs.rs",
    "crates/myelin-ci-controlplane/tests/cdc_supply_chain_2_2_10_6_4_7.rs",
]

[[contract]]
row = "10.7"
title = "eDiscovery / legal-hold export"
status = "covered"
cdc = ["crates/myelin-gdpr-service/tests/cdc_10_7_ediscovery_export.rs"]

[[contract]]
row = "10.8"
title = "erasure ledger"
status = "deferred"
# The erasure ledger (PII-free, non-shred-erasable) is OWNED by GDPR/Audit; the full provider+consumer
# pair (GDPR mints + owns the GLOBAL ledger) lands at P-115 (GA-15) — the row stays `deferred` until then.
# The Bus CONSUMES it: EB-16 / P-093 ships the Bus's consumer-side participation (its PII-free
# BusErasureLedger slice + BusHolder::re_erase_after_restore so the key stays destroyed across a restore)
# with its own consumer-side CDC + drill (crates/myelin-events/tests/cdc_10_8_bus_reerase.rs +
# drills_bus_d8_reerase_after_restore.rs). The cross-seam restore TRIGGER is P-ST-14 (P-100).
landing = "P-115"

[[contract]]
row = "10.9"
title = "the ONE free-text / immutable-content erasure posture"
status = "covered"
# P-116 (GA-16): the ONE free-text/immutable erasure posture written ONCE as the single canonical
# artifact (myelin-gdpr-service::posture::CANONICAL_POSTURE) — structural floor (§7.1) + residual
# (§7.2) + the [OPEN — LEGAL] ratified posture (§7.3), instantiated per subsystem BY REFERENCE. The
# CDC pair: the PROVIDER side (the posture as OWNED) is COMPLETE here; the CONSUMER side is the
# by-reference subsystem-section SHAPE (a Git-like reference) — the REAL subsystem-reference
# consumer (over the real M3/M4 docs) lands in P-GA-28 → P-256/P-257 (Git) and P-GA-29/-31
# (CI/Issues/Knowledge/Chat), at which point the `references-it-never-restates` architecture
# assertion fires over the real subsystem docs.
#
# P-118 (GA-18): the pseudonymous-by-default commit-identity PREREQUISITE leg of 10.9 — GDPR's ONE
# critical-path obligation, recorded in M1 as the commit-time prerequisite Git must satisfy in M3,
# before the Git data model freezes (EI-04 §1; gdpr §7.1.2 GIT-1; VISION §3). Ships
# myelin-gdpr-service::commit_prerequisite::COMMIT_IDENTITY_PREREQUISITE (the recorded obligation,
# dated 2026-06-20, expressed in the frozen 4.8 grammar) + commit_actor_holds_only_pseudonym (the
# architecture-test scaffold: a pseudonym-form commit actor passes, a real-identity form fails).
# cdc_10_9_commit_prerequisite.rs carries the provider (GDPR records) + consumer (Git-like
# commit-actor shape) pair. The REAL Git consumer (the scaffold firing over Git's commit codec) lands
# in P-GA-27 / P-GA-28 → P-256/P-257.
#
# P-117 (GA-17): the structural-floor PROOF on the M1 stores (§7.1, contract 10.9) —
# myelin-gdpr-service::structural_floor proves the three levers end-to-end on a faithful M1-store
# model: per-subject DEK crypto-shred (11.4) + pseudonym-map shred (4.8) + the genuinely-new
# `restrict` suppression FLAG every M1 holder HONOURS (no indexing/agent-use/analytics/notification
# while RETAINING storage; reversible — §4.4). cdc_10_9_structural_floor.rs carries the
# `restrict`-honoured-by-M1-holder pair the prompt requires; the residual (third-party mention under
# the author's DEK) is restrict-suppress-only, never crypto-shredded (the documented limit, §7.2).
# The full restriction-into-derived-stores proof (GA-D7) is M2 P-GA-25 → P-152.
#
# P-257 (GA-28): the Git pseudonymous-commit INSTANCE of X-7 (10.9 BY REFERENCE) + GIT-D2. The FIRST
# real subsystem-reference register lands: myelin-gdpr-service::git_instance::GIT_INSTANCE cites the
# canonical anchor and does NOT restate the posture, so the P-GA-16 `references-it-never-restates`
# architecture assertion now FIRES over a real subsystem doc (the consumer half of the 10.9 pair).
# GIT-D2's residual == the ONE platform-posture residual (git_residual_is_the_one_posture, confirmed
# equal — never re-described). The P-GA-18 commit-identity architecture test now FIRES over Git's REAL
# commit codec (myelin_git::commit::Commit::canonical_bytes — pseudonymous-by-construction, GIT-P25):
# commit_actor_holds_only_pseudonym PASSES over the live author/committer lines. The dated green
# artifact is tests/git_d2_pseudonymous_commit.rs (erase → 0 recoverable real identity in the
# immutable bytes, crypto-shred reaches backups, residual == the ONE posture). The audited
# history-rewrite erasure path (the rare commit-body expunge) is the named M5 follow-on P-GA-35 (GA-10).
cdc = [
    "crates/myelin-gdpr-service/tests/cdc_10_9_erasure_posture.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_9_structural_floor.rs",
    "crates/myelin-gdpr-service/tests/cdc_10_9_commit_prerequisite.rs",
]

# ──────────────── §11 Storage — tiers / BlobStore / KMS / reserve-settle / backup-restore ───────

[[contract]]
row = "11.1"
title = "OLTP tier client"
status = "covered"
cdc = ["crates/myelin-storage/tests/cdc_11_1.rs"]

[[contract]]
row = "11.2"
title = "BlobStore{put, get, head, delete}"
status = "covered"
# P-047 (P-ST-03): the content-addressed BlobStore trait + fs-backed floor. The base trait +
# fs floor ship here; the per-blob content-key WRAP is the named floor → P-ST-08 (P-095), the
# object-store backing → P-ST-30 (P-636), the C3/C4/object-backed-pack additions → later prompts.
# P-252 (P-ST-22): the local-disk GIT PACK TIER behind the trait — git packs + loose objects
# addressed THROUGH the BlobStore (so local-disk → object-store is a backing swap, §3.5),
# region-pinned + node-RELOCATABLE placement (the storage face of 12.2), SHA-256 read-side verify
# closing the blob.rs git-object floor, and STOR-D7 on packs (corrupt object detected + recovered
# from replica, 0 silent serve). The object-backed pack/delta + smart-transport is the named M5
# follow-on → P-ST-31 (P-442); the C3 CDN clone/bundle class is the sibling → P-ST-23 (P-254).
# P-253 (P-ST-24): the GIT CRYPTO-SHRED REACH (rows 11.2/11.4 reach) — the per-tenant blob DEK
# (KeyClass::Blob) shreds the reflog / bitmap / pack-tier-backup ciphertext that rides this pack tier,
# live AND in backups by construction (§7.5); the commit-object bytes are the pseudonymous-by-default
# residual (10.9, by reference — NOT byte-mutated; the on-demand audited history-rewrite is 10.6, the
# named follow-on). GitCryptoShredReach IS the BlobShredReach seam the erase orchestrator's step-2
# crypto-shred invokes; GIT-D2 (storage half) green. See also row 11.4. The C6 outbound push-mirror
# residency gate seam is the sibling → P-ST-25 (P-255).
# P-254 (P-ST-23): the within-EU CDN CLONE/BUNDLE class (C3, row 11.2-C3) — a content-addressed blob
# CLASS over the UNCHANGED BlobStore (a blob-class tag + an eligible-edge-set policy, NOT a new store,
# EI-01 §7): the content-address IS the cache-validity check (no staleness model), an EU tenant's
# eligible edge set is within-EU-only (the CdnEdgeSet filter), and the CDN reports into
# `verify_region_pinning` (the new ResidencyStoreClass::CdnEdgeSet variant) so the residency
# attestation covers the CDN edge set (12.4) — a cross-region CDN edge FAILs the SAME aggregation
# (STOR-D5 CDN within-EU, 0 cross-region PII egress via the CDN). The CDN clone class RIDES the
# unchanged base FsBlobStore (the structural "not a new store" assertion). The C6 outbound push-mirror
# residency gate seam is the sibling → P-ST-25 (P-255).
# P-272 (GIT-P11): the GIT-SIDE pack tier on the local-NVMe floor (myelin-git/src/pack_tier.rs) — the
# git subsystem CONSUMES this row's GitPackTier (REUSED, never re-defined). NEW git-side layer: the
# accepted-quarantine → object-DB migration THROUGH the trait (PackTierMigration, closing the
# QuarantineMigration floor receive_pack.rs named GIT-P11 to fill), the commit-graph/reachability-
# bitmap/MIDX maintenance artifacts + their staleness fences (arch 01 §4.1 / 02 §8, each a content-
# addressed blob through the trait), the byte-identical clone round-trip (the GATE: receive-pack →
# store → clone, 0 corruption — proven end-to-end through RefStore::receive with the REAL pack tier),
# and the residency-pin lint green on the pack placement (relocatable, never node-pinned — STOR-5).
# Floors named GF-1 (object-backed packs) / GF-2 (cross-cell) / GF-2b (SHA-256 flip) / GF-4 (Mononoke-
# class) all → GIT-P33. The git consumer-half CDC is cdc_11_2_git_pack_tier_consumer.rs (the REAL
# PackObjectDb drives the storage provider — pins the frozen put/get/placement shape).
# P-284 (GIT-P22): the C4 TRUST-SCOPED CACHE namespaces (row 11.2-C4 — an `UntrustedFork` write cannot
# reach the trusted cache scope, the poisoned-cache defence). `myelin_git::fork_gate::ScopedCache` is the
# `fork:<pr_id>` confinement: a scope-key convention (`TrustScope` → key-prefix) over the UNCHANGED
# per-tenant `myelin_storage::Cache` (NOT a new store, EI-01 §7) — a fork run's keys ALWAYS prefix
# `fork:<pr_id>:` (the scope is DERIVED from the CI-stamped trust tier, never caller-chosen), so a fork
# write can never address a `trusted:`-prefixed key. PROVEN against LIVE Valkey in
# crates/myelin-git/tests/integration_git_p22_fork_cache_confinement.rs (0 fork writes in the trusted
# scope on the real server; per-PR fork isolation; the trusted scope still round-trips). Ties to X-1.
# P-330 (P-ST-28): the STORAGE-side C4 TRUST-SCOPED CACHE namespaces over the T2 BLOB keyspace (row
# 11.2-C4) — the SIBLING of P-284 at a DIFFERENT tier (the durable build-cache artifacts, not the T7
# coordination cache). `myelin_storage::ci_cache_scope::CiCacheNamespace` is the scope-key convention
# `<tenant>/ci/cache/<scope>/...` (`<scope> ∈ {trusted, fork:<pr_id>, branch:<name>}`) over the
# UNCHANGED content-addressed `BlobStore` (a namespace + a write-scope refusal, NOT a new store —
# EI-01 §7; a cache artifact IS a content-addressed blob). The asymmetric C4 rule the prompt mandates:
# an `untrusted_fork` run may READ the `trusted` scope (a cache hit is fine) but its WRITE to `trusted`
# is REFUSED by the blob client (`CacheScopeError::ForkWriteToTrusted`). The scope is stamped from the
# CI-stamped `trust_tier` (an INPUT `TrustTier` — Storage ENFORCES, it does NOT recompute trust; storage
# cannot depend on git, so the tier is handed in off the SAME X-1 fact the git gate reads). Wires the
# `cache_scope_violation{tenant}` signal (D-S11, 0 cross-scope LANDINGS). D-S11 GREEN (poisoned-cache
# attack defeated; 0 fork value readable as trusted; per-PR fork isolation) in
# crates/myelin-storage/tests/stor_d_s11_trust_scoped_cache_isolation_drill.rs. PROVEN against the LIVE
# RustFS object store (the real T2 backing — dev<->prod is a backing SWAP) in
# crates/myelin-storage/tests/integration_backends.rs::c4_trust_scoped_cache_namespaces_over_real_object_store
# (--features integration: a fork write to trusted is refused BEFORE any byte hits the real bucket; the
# fork reads trusted; the fork's own scope round-trips; 0 cross-scope landings). The C5 OLAP
# restriction-flag gate is the sibling → P-ST-29 (P-331). Ties to X-1.
# P-354 (CI-P11, M4): the CI Trigger & Dispatch CONSUMER of the CAS snapshot — the
# definition-resolution (myelin_ci_dispatch::resolve::resolve_snapshot) serialises the resolved,
# matrix-expanded, digest-pinned DAG to canonical JSON and PUTs it as a T2 content-addressed blob
# (the run's reproducible, auditable definition — IDENTICAL to `myelin ci plan`, shift-left). The
# content address round-trips (the consumer reads the SAME bytes back) and is reproducible (the same
# definition → the same BLAKE3 address). Consumer CDC: cdc_ci_p11_resolve_dispatch.rs.
cdc = [
    "crates/myelin-storage/tests/cdc_11_2_blobstore.rs",
    "crates/myelin-storage/tests/cdc_11_2_git_pack_tier.rs",
    "crates/myelin-storage/tests/cdc_11_2_11_4_git_crypto_shred_reach.rs",
    "crates/myelin-storage/tests/cdc_11_2_c3_cdn_clone_class.rs",
    "crates/myelin-storage/tests/cdc_11_2_c4_trust_scoped_cache.rs",
    "crates/myelin-git/tests/cdc_11_2_git_pack_tier_consumer.rs",
    "crates/myelin-git/tests/cdc_11_2_c3_git_bundle_uri.rs",
    "crates/myelin-ci-dispatch/tests/cdc_ci_p11_resolve_dispatch.rs",
    "crates/myelin-ci-controlplane/tests/cdc_11_2_11_4_ci_artifact_cache.rs",
    "crates/myelin-issues/tests/cdc_11_2_issues_attachment_pointer.rs",
]
# P-386 (ISS-P19, M4): the ISSUES consumer of the BlobStore row for ATTACHMENTS —
# `myelin_issues::time_axis::{attach, AttachmentPointer}` PUTs the attachment bytes through the REAL
# fs-backed `FsBlobStore` provider + builds the OLTP-row POINTER. The row holds ONLY the content
# address + per-subject-DEK metadata (`pii_key_ref` `kms://…subject:<id>`) + size + region — 0 bytes
# of the attachment in the row (`AttachmentPointer::row_byte_count() == 0`, structural: no `bytes`
# field; the 0-bytes-in-row GATE artifact). The provider/consumer halves AGREE on the content address
# with no drift (the pointer address == the BlobStore put/get address == BLAKE3(plaintext)); per-tenant
# content-addressed dedup holds + cross-tenant dedup is forgone (residency isolation). FLOOR: the
# burndown/CFD ANALYTICS the time-axis feed serves land in the OLAP read store (ISS-P20 / P-387). The
# real per-blob DEK WRAP at rest is the storage-side P-ST-08 floor (unchanged here — the address is
# plaintext-derived). Consumer CDC: cdc_11_2_issues_attachment_pointer.rs.
# P-365 (CI-P22, M4): the CI CONTROL-PLANE consumer of the C4 trust-scoped cache namespaces (11.2) —
# `myelin_ci_controlplane::artifact_cache::derive_cache_scope` maps a run's CI-stamped `trust_tier`
# (the `ci_run.trust_tier` fact + branch/PR provenance) to the EXACT `(TrustTier, CacheScope, pr_id)`
# triple the storage `CiCacheNamespace::put` enforces against — the CI half of the poisoned-cache
# defence. A fork run derives ONLY its `fork:<pr_id>` scope (a fork with no pr_id is a LOUD error,
# never a fall-through to trusted); a trusted run derives `trusted` / `branch:<name>`. The two halves
# AGREE with no drift (the CDC property). The CI-D6 drill (0 fork→trusted writes) is greened end-to-end
# in drills_ci_p22_fork_cache_poison.rs. CI also consumes the within-EU CDN clone class (C3) + the
# residency-pin on every artifact/cache/CDN write (ArtifactWritePin, 0 cross-region). FLOOR: the full
# erase fan-out (crypto-shred ERASE path) is CI-P32 (P-492). Consumer CDC:
# cdc_11_2_11_4_ci_artifact_cache.rs.
# P-276 (GIT-P15): the GIT-SIDE consumer of the C3 within-EU CDN clone/bundle class — the bundle-URI
# accelerated-clone path (myelin-git/src/shed_clone.rs BundleUriClone) CONSUMES the storage
# CdnCloneClass (REUSED, never re-defined, EI-01 §7): publish a precomputed bundle → advertise a
# bundle-URI → serve a clone BY CONTENT-ADDRESS. The accelerated-clone floor holds (a bundle-URI clone
# round-trips a valid clone); a tampered bundle is REFUSED (0 silent serve — the content-address is the
# cache-validity check). The consumer-half CDC is cdc_11_2_c3_git_bundle_uri.rs. FLOOR: the full
# within-EU CDN class (real edge POP fleet + cache-fill transport) hardens → GIT-P33 (M5).

[[contract]]
row = "11.3"
title = "KMS hierarchy + KeyOrigin trait"
status = "covered"
# P-058 (P-ST-06): the three-level KMS HIERARCHY half — L0 cell root / L1 per-(tenant,region)
# KEK / L2 DEKs (AES-256-GCM, per-tenant + per-subject), the frozen pii_key_ref
# (kms://<tenant>/<dek-epoch>/<class>), rotation-as-envelope-re-wrap, crypto-shred, and the
# STOR-D6 fail-static posture. The CDC pair is an encrypted-store caller wrapping/unwrapping
# through the engine. The SECOND half of this row — the KeyOrigin trait
# (can_derive_plaintext_index()=false structural HYOK enforcement) — is the sibling P-ST-07
# (global P-094); it adds the index-builder consumer to this same row.
cdc = ["crates/myelin-storage/tests/cdc_11_3_kms.rs"]

[[contract]]
row = "11.4"
title = "crypto-shred + GD-4 granularity"
status = "covered"
# P-095 (P-ST-08): the GD-4 classify-driven key-choice RULE half — `key_class_for` routes an
# `erasure=subject` tag to a per-subject DEK and a bulk class to the tenant DEK (cdc_11_4_classify_
# key_choice.rs).
# P-099 (P-ST-09): the erase-ALGORITHM half — the crypto-shred `erase(subject, tenant)` six-step
# mechanism (CryptoShredErase::erase: pseudonym-map shred → KMS.destroy(per_subject_DEK) → Search
# purge+reindex → Refs tombstone → Bus erase → erasure receipt), idempotent (a re-erase is a no-op
# success) and loud-on-partial-failure (the erasure is recorded only when every step succeeded). Step
# 2 is owned in-crate (KmsEngine::destroy_dek); the cross-holder steps are seam traits the DSR
# orchestrator wires. STOR-D4 (the per-subject-DEK-destroy half) is green: erase a subject → attempt
# recovery from the backup snapshot → 0 recoverable (the key is destroyed + excluded from backup,
# §7.5). The CDC pair is the DSR orchestrator wiring the five seams + calling erase
# (cdc_11_4_erase_algorithm.rs). The GD-4 granularity + structural-floor completeness is the sibling
# P-ST-10 (global P-101); the cross-holder reach completeness is P-ST-35 (M5); the post-restore
# re-erasure (STOR-D3) is P-ST-14 (P-100).
# P-253 (P-ST-24): the GIT CRYPTO-SHRED REACH half — extends the step-2 crypto-shred to git's
# structures. GitCryptoShredReach destroys the per-tenant blob DEK (KeyClass::Blob) the reflog /
# bitmap / pack-tier-backup ciphertext seals under (unrecoverable live AND 0 recoverable in backup,
# §7.5) and is wired as the OPTIONAL BlobShredReach seam on EraseHolders (a commit author's erase
# reaches git's structures in the SAME crypto-shred step; a subject with no git content has
# git_reach=None). The commit-object bytes are the pseudonymous-by-default residual (10.9, by
# reference — NOT byte-mutated; on-demand audited history-rewrite = 10.6, the named follow-on). The
# reach is verified-not-assumed (a failed 0-recoverable post-condition is a loud
# EraseError::BlobShredReach, the erasure not recorded). GIT-D2 (storage half) green
# (git_d2_git_crypto_shred_drill.rs). The CDC pair is the DSR orchestrator wiring the git reach into
# erase for a commit author (cdc_11_2_11_4_git_crypto_shred_reach.rs).
cdc = [
    "crates/myelin-storage/tests/cdc_11_4_classify_key_choice.rs",
    "crates/myelin-storage/tests/cdc_11_4_erase_algorithm.rs",
    "crates/myelin-storage/tests/cdc_11_2_11_4_git_crypto_shred_reach.rs",
    "crates/myelin-ci-controlplane/tests/cdc_11_2_11_4_ci_artifact_cache.rs",
    "crates/myelin-issues/tests/cdc_4_8_11_4_issues_pseudonym_dek.rs",
]
# P-365 (CI-P22, M4): the CI CONTROL-PLANE consumer of the per-subject-vs-per-tenant DEK selection
# (11.4) for isolable inline log PII (Storage C1). `select_log_segment_dek` composes the FROZEN
# `myelin_storage::kms::{KeyClass, PiiKeyRef}` grammar (no second vocabulary): an isolable subject →
# `subject:<id>` DEK (the GD-4 individual crypto-shred lever, `kms://<tenant>/<epoch>/subject:<id>`);
# non-isolable → the per-tenant DEK fallback (`kms://<tenant>/<epoch>/tenant`). The SAME selection
# keys an artifact holding isolable subject PII (the `ci.artifact.published` pointer carries the
# `pii_key_ref`, so the erase fan-out reaches the bytes). This BUILDS the per-subject DEK key-selection
# SUBSTRATE; the full erase fan-out (crypto-shred ERASE path that destroys the DEK across every holder)
# is the named M5 follow-on CI-P32 (P-492). Consumer CDC: cdc_11_2_11_4_ci_artifact_cache.rs.
# P-373 (ISS-P07, M4): the ISSUES CONSUMER of the per-subject-DEK free-text columns (11.4 / GD-4).
# `myelin_issues::dek::{encrypt_free_text, IssueFreeText}` seals the free-text `title`/`props`/
# `change_delta`/comment-body under the SUBJECT's per-subject DEK through the ONE shared
# `myelin_storage::encryption::ColumnCryptor` over the P-058 KmsEngine (NO second cryptor, NO parallel
# key store — EI-01 §7): ciphertext + the `kms://<tenant>/<epoch>/subject:<id>` pii_key_ref at rest, 0
# plaintext free-text. `apply_mutation_sealed` threads the REAL per-subject-DEK pii_key_ref onto the
# emitted `issue.created` event (the erase fan-out — ISS-P31 — destroys exactly that key). The erase
# BODY (the crypto-shred that destroys the DEK) is the named ISS-P31 follow-on. Provider/consumer CDC:
# cdc_4_8_11_4_issues_pseudonym_dek.rs (seal under the per-subject DEK → decrypt-while-the-key-lives, 0
# plaintext at rest); the live-Postgres at-rest artifact:
# crates/myelin-issues/tests/integration_iss_p07_subject_dek.rs (0 plaintext free-text in the real
# Postgres bytea column).

[[contract]]
row = "11.5"
title = "backup / restore / cross-seam"
status = "covered"
# P-059 (P-ST-11): the BACKUP half — continuous WAL archiving + base backups + PITR (RPO ≤ 5 min,
# the STOR-D2 floor), the T2 versioned+in-region-replicated object tier, the T3 sealed-segments-as-
# T2-blobs log tier, the structural "T4/T7/derived-indexes NOT backed up — rebuilt from source"
# assertion, and the crypto-shredded-key-excluded-from-backup rule (§7.5). The CDC pair is the ops
# backup caller (archive WAL tail → base backup → snapshot the backed-up tiers at the cross-seam
# offset → read the measured RPO).
# P-060 (P-ST-12): the RESTORE + cross-seam half — restore(to_offset T) lands every tier at ONE
# consistent point (PITR to the seq ≤ T cursor, referenced-ContentHash-presence verification with a
# hard FAIL on a missing hash = the §7.3 silent-corruption case, reindex-from-source derived rebuild
# = derived == source by construction, and KEK restore-except-crypto-shredded = §7.5). Its CDC pair
# is the restore-verify gate's restore caller (cdc_11_5_restore.rs).
# P-061 (P-ST-13): THE HEADLINE — the CI-wired restore-verify GATE (STOR-D1, the permanent gate). The
# RestoreVerifyGate spins a clean target, drives restore(to_offset T), and runs the §7.4 assertions:
# no-loss/checksum-parity (every restored object's bytes re-hash to its BLAKE3 address — the leg the
# bare presence check misses), cross-seam/one-consistent-point (derived == source-replay, checked
# storage-native + cross-validated against the harness SUB-D6 assertion in the drill), and
# erasure-held (a tenant crypto-shredded before the backup stays erased — §7.5). It emits a dated
# green artifact with measured numbers on pass and a typed GateFailure on red; the verdict is
# #[must_use] and run_or_fail_ci() turns a red into a process-failing Err (loud-never-swallowed, no
# `|| true`). Its CDC pair is the CI durability-gate caller (cdc_11_5_restore_verify_gate.rs). This is
# one of the TWO PERMANENT gates (master §4): it re-runs on every store-touching change, forever. The
# post-restore re-erasure (STOR-D3) + the cell-kill RTO (STOR-D2) are P-ST-14 (global P-100).
#
# CI-STORES WIRING at CI-P27 (→ P-370): the CI control plane WIRES its stores into the SAME
# RestoreVerifyGate + CellKillRestore (REUSED, not forked — coherence, EI-01 §7). The committed gate
# `myelin-ci-controlplane/tests/integration_ci_p27_restore_verify_ci_stores.rs` (integration-gated)
# restores the CI OLTP (ci_run/ci_job/job_queue) + the T2 blob tier (artifact/cache_entry) + the T3
# log segments (log_segment) + the CI event-log offset (ci_event_log_offset) to ONE consistent
# cross-seam point (OLTP↔blob↔index↔offset), asserting 0 loss + RPO ≤ 5 min + RTO ≤ 1 h/tenant
# ≤ 4 h/cell (bounds READ from thresholds.toml, never hardcoded), proven GREEN over the live stack and
# loud-never-swallowed via `myelin_ci_controlplane::run_ci_restore_verify_or_fail` (a corrupted CI
# backup FAILs CI). The committed-job manifest is `permanent_gates::m4_boundary_permanent_gates`. No
# floor (a PERMANENT GATE re-run on every CI-store-touching change).
cdc = [
    "crates/myelin-storage/tests/cdc_11_5_backup.rs",
    "crates/myelin-storage/tests/cdc_11_5_restore.rs",
    "crates/myelin-storage/tests/cdc_11_5_restore_verify_gate.rs",
]

[[contract]]
row = "11.6"
title = "OLAP read store"
status = "covered"
# P-104 (P-ST-17): the OLAP read store FRAME — the holder + the CQRS-fed-by-the-bus contract SHAPE
# (11.6 partial). `OlapReadStore` is a per-cell, residency-pinned, idempotent-consumer-fed (dedup on
# `event_id`) analytics read model, populated ONLY by replaying the durable event STREAM — live
# (`apply`, the shape the live bus feed P-ST-18 takes) or cold (`reindex_from_source`) — NEVER by
# scanning OLTP. The no-OLTP-scan backdoor is BOTH a runtime guard (`oltp_scan_path_count == 0`, 0 by
# construction — no code path increments it) AND a structural source assertion (the frame's
# production source carries no `OltpPool`/`from_oltp`/OLTP-`SELECT` construct). Reindex-from-source is
# the ONLY rebuild path (cold == live, shares the live `apply` projection). The OLAP store registers
# as a `PersonalDataHolder` (`OlapStoreHolder`, crypto-shred erasure; restrict = the C5 suppression
# seam). The CDC pair is the bus-fed CQRS feeder (`OlapBusFeeder`, the shape the live feed takes) over
# `OlapEvent::from_envelope` + `apply` (cdc_11_6_olap_frame.rs).
# Floors named: the LIVE bus feed (steady-state idempotent consumer off the durable stream +
# `*.snapshot` replay) is P-ST-18 (global P-145), which COMPLETES this frame.
# C5 — the OLAP restriction-flag gate (P-331 / P-ST-29, M4) is now LANDED (olap_restrict.rs):
# `restrict(subject)` (10.1) propagates into T4 — a restricted subject's rows are EXCLUDED from
# every analytics aggregate (CFD/cycle-time/velocity/delivery-health). `OlapAnalytics` over the frame's
# read model applies the restriction as a QUERY-TIME filter (the subject's rows STAY — a lift makes it
# reappear with no reindex; an erasure removes them); the leak audit intersects each aggregate's
# contributing-subject set with the restriction set → `olap_restricted_subject_leak == 0` (D-S12). A
# COMPLIANCE gate, not a tuning knob — it unblocks the Issues analytics ask without leaking a restricted
# subject. CDC pair = the Issues analytics consumer (cdc_11_6_c5_restriction_gate.rs); drill =
# stor_d_s12_restricted_subject_olap_suppression_drill.rs (over the real live bus feed). REUSES the
# frame's docs + restriction set — never a parallel store (EI-01 §7).
# Floor named (C5): the worklog/productivity/estimate analytics-ELIGIBILITY (OQ-H) is [OPEN — LEGAL]
# (works-council consultation) — Storage ships the `AnalyticsEligibility` SEAM (per-individual rollups
# OFF by default) + the C5 restriction gate REGARDLESS; the LEGAL ratification of which rollups are
# eligible is the named follow-on. The real ClickHouse-class columnar backend lands with the live feed
# (P-ST-18); the frame + the C5 gate are an in-memory-testable MODEL over the existing
# SourceLog/ReindexFromSource primitive + the frozen EventEnvelope, so NO new db/object-store/cache/bus
# trait is touched and no new integration drill is owed.
# P-387 (ISS-P20, M4): the ISSUES CONSUMER side of 11.6 — the Issues OLAP CQRS feed
# (myelin_issues::olap_feed). `IssueOlapConsumer` is the bus `EventHandler` (contract 2.4) whose
# whitelist is the analytics-driving issue.*/sla.*/cycle.* tokens (NEVER `*`); it feeds the SHARED
# `myelin_storage::olap::OlapReadStore` through the SAME `OlapEvent::from_envelope` seam Storage's live
# `OlapBusFeeder` uses (REUSED, never a parallel store — EI-01 §7), NEVER the OLTP issue table (the
# 0-OLTP-read gate, `oltp_read_count == 0` by construction). `IssueOlapAnalytics` reuses Storage's
# frozen CFD/cycle-time/velocity aggregates (a restricted subject EXCLUDED at query time via the C5
# filter) + ADDS the Issues ask's SLA-compliance leg with the SAME restriction filter; `leak_audit`
# (`restricted_subject_leak == 0`) is the restriction-exclusion gate (recon §8: no analytics for a
# restricted subject). `reindex_from` rebuilds the DERIVED feed DRIFT-FREE off Issues' source of truth
# (`IssueReindexSource` → *.snapshot → the SAME handle body — steady-state + recovery share one code
# path, contract 2.6 consumed); the cold rebuild byte-matches the live projection
# (`projection_fingerprint`, the ISS-D8b OLAP-feed reindex-parity). The restriction flag is the SHARED
# holder `RestrictionFlag` (the SAME one the Issues erase fan-out / index / notif seams read).
# Floors named: the Monte-Carlo forecast that reads OLAP throughput samples is ISS-P32 (linear floor →
# Monte-Carlo, ADR-08); the ClickHouse columnar backend is behind OlapReadStore (Storage P-ST-18,
# wired); the OQ-H worklog analytics-eligibility seam is Storage's AnalyticsEligibility ([OPEN — LEGAL]).
# The feed is an in-memory-testable consumer over the existing frozen OLAP read-store API + the frozen
# EventEnvelope — NO new db/object-store/cache/bus trait is touched, so no new integration drill is owed.
landing = "P-331, P-387"
cdc = [
  "crates/myelin-storage/tests/cdc_11_6_olap_frame.rs",
  "crates/myelin-storage/tests/cdc_11_6_c5_restriction_gate.rs",
  "crates/myelin-issues/tests/cdc_11_6_issues_olap_feed.rs",
]

[[contract]]
row = "11.7"
title = "reserve/settle cost gate"
status = "covered"
# P-103 (P-ST-16): the reserve/settle cost gate MECHANISM + the durable per-tenant ledger
# (Storage owns the ledger correctness; the gate policy is Agent's §5.4, the wallet balance is
# Commercial's). `CostLedger::reserve` debits an integer-minor-units amount against the supplied
# wallet balance and REFUSES on no-balance (no balance → no run); `settle` records exactly ONE
# cost event per metered unit with the wholesale ≠ markup split recorded DISTINCTLY (idempotent —
# a double-settle never double-charges); the reserve is the billing cap; `cancel_unstarted` is the
# ONLY teardown and is structurally barred from an InFlight reservation (NEVER interrupt in-flight —
# the inflight_interrupt_count is 0 by construction, no code path increments it). All arithmetic is
# checked integer minor-units (a float cost is unrepresentable). The synthetic-run drill emits a
# green ReserveSettleSignal (cost_events_per_unit == 1, inflight_interrupt_count == 0). The CDC pair
# is an agent/CI run-dispatcher fronting every run with the gate (cdc_11_7_reserve_settle.rs).
#
# P-146 (P-ST-19): the LIVE CONSUMER HALF — the Storage-owned `AgentRunGate` that fronts every
# `AgentRuntime` run + every `SCHEDULE_AND_RUN_JOB`. Reserve-at-dispatch mints a move-only
# `InFlightRun` handle (no balance → no handle → the run never starts); the handle's ONLY exit is
# settle-on-completion; the gate exposes NO API that interrupts an in-flight run (never interrupt
# in-flight is structural). The CDC pair adds the gate-fronting consumer cases to
# cdc_11_7_reserve_settle.rs; the AG-D6 (surge sheds over-budget) / AG-D11 (runaway loop stops at
# the wallet, 0 interrupt) drills emit the dated green AgentRunGateSignal.
# Floors named: the gate FRONTS agent runs in M2 → DONE (P-146); it fronts CI runs in M4 (a
# CI-subsystem run-dispatch consumer — the named M4 follow-on); the real `LlmAgentRuntime` brain is
# designed-not-built (AG-P25); the real Postgres-backed durable ledger lands with the OLTP driver
# (P-S12) — the arithmetic + invariants are complete + testable now over the same OLTP tier whose
# real backend (PgStore) is already proven by the infra-stage integration drills (no NEW
# db/object-store/cache/bus trait is touched by P-146, so no new integration drill is owed).
#
# P-360 (CI-P17, M4): the gate FRONTS CI RUNS — the named M4 follow-on row 11.7 recorded ("it fronts
# CI runs in M4 — a CI-subsystem run-dispatch consumer"). CI builds NO second metering path (arch CI
# 02 §6): the CI metering module (myelin-ci-controlplane/src/metering.rs) REUSES this frozen ledger +
# the engine bookend (9.5) in place. It adds the CI METER: the resource-second taxonomy (Meter — the
# EXACT cost_event.meter CHECK set, the wholesale unit directly comparable to an agent compute call,
# X-6), the CI cost_event schema row (CostEventRow — wholesale + markup SEPARATE columns, kind ∈ {ci,
# agent}, UNIFY/X-6), and CiMeter (reserve_budget refuse-to-start / settle_budget on job.done over the
# SAME BudgetGate/wallet both kinds use). The CI-D5 drill
# (drills_ci_p17_reserve_settle_parity.rs) exhausts ONE wallet → BOTH a CI run AND an agent run
# refuse-start (the parity, never interrupt in flight) → 0 starts past exhaustion, wholesale ≠ markup
# STABLE under a pricing change (one cost_event per metered unit), AND the REAL ci.pipeline body
# refuses-to-start a CI stage when the shared wallet is exhausted by a prior agent reserve.
# FLOOR named: the resource-second → credit/price MARKUP mapping is Commercial's (arch 06 R-2 — CI
# carries the MarkupPolicy seam + owns ONLY the meter + the wholesale column).
cdc = [
    "crates/myelin-storage/tests/cdc_11_7_reserve_settle.rs",
    "crates/myelin-storage/tests/ag_d6_d11_reserve_refusal_drill.rs",
    "crates/myelin-ci-controlplane/tests/drills_ci_p17_reserve_settle_parity.rs",
]

[[contract]]
row = "11.8"
title = "T3 log tier (CI)"
status = "covered"
# P-147 (P-ST-20): the T3 firehose-archive seam — the SEALING + per-tenant-DEK segment encryption
# HALF of 11.8, validated on a NON-CI firehose (a synthetic op-stream). `FirehoseArchiver` (storage.md
# §3.3) rides the 3.5 resume-cursor transport (`myelin_events::Firehose`, P-141 — the SAME `Frame`s a
# live viewer subscribes to) and SEALS frame batches into content-addressed T2 blobs encrypted under
# the per-tenant DEK (a real `DekContentWrap` over `KmsEngine`, P-095/P-058): a sealed segment INHERITS
# T2 crypto-shred — destroying the tenant DEK renders it unrecoverable LIVE and IN BACKUPS (excluded
# from `backup_snapshot`, §7.5). Telemetry: `unencrypted_segment_count == 0`, `segment_content_addressed
# == true`. The durable bus carries only a references-not-payloads `SealedSegment` POINTER, never the
# inline frame bodies (an agent is never woken per log line). The CDC pair is a non-CI op-stream
# producer sealing + resolving through the seam (cdc_11_8_firehose_archive.rs); the gate drill is
# stor_p147_firehose_archive_gate_drill.rs. The T3 archive registers a new residency store class
# (`ResidencyStoreClass::T3FirehoseArchive`) that feeds the SAME `verify_region_pinning` aggregation.
# P-328 (P-ST-26): the C2 HALF of 11.8 — the `(job, step, byte-range)` index + the X-1 `#step-<n>`
# resolution — is now LANDED. `CiLogTier` (ci_log_index.rs) is the CI-keyed instance of the P-ST-20
# `FirehoseArchiver` (the SEALING + per-tenant-DEK mechanism — REUSED wholesale, no second seal path)
# plus the `(job, step, byte-range)` index P-ST-20 named as its M4 follow-on. A CI log frame carries
# `(job, step, chunk-bytes)`; `seal_ci_batch` flushes a content-addressed T2 segment AND records, per
# `(job, step)`, the byte-range that chunk occupies in the reconstructed step log — the
# `(job, step, byte-range) → (segment-blob, offset)` resolver. `resolve_step_anchor` parses the X-1
# `myelin://.../ci/run/<id>#step-<n>` `CheckStatus.details_ref` sub-anchor (5.9 / OQ-D) and reads ONLY
# the indexed segment(s), slicing out EXACTLY the failing step's bytes (the GIT-D10/CI-D8 jump-to-failure);
# an unknown step is a LOUD `CiLogError::UnknownStep`. The CI log segment is a T2 segment under the
# per-tenant DEK, so it inherits T2 crypto-shred (a destroyed DEK renders the step unrecoverable live AND
# in backups) and the `T3FirehoseArchive` residency class (no new variant). The CDC pair is CI (the
# consumer, 5.9) streaming step logs + resolving the details_ref #step-<n> (cdc_11_8_ci_log_index.rs);
# the gate drill is stor_p328_ci_log_step_resolution_drill.rs.
# Floors named: the per-SUBJECT CI-log DEK (C1) is the SIBLING M4 prompt P-ST-27 (a key-class swap on
# the same DekContentWrap seam); the real OLTP `ci_log_index` table (UNIQUE(job,step,seq)) is the
# P-S12/P-S15 backing swap (the in-process map is the index SHAPE); the real broker firehose +
# object-store segment backing are the P-S12 / P-ST-30 floors (inherited from the P-ST-20 archiver).
cdc = [
    "crates/myelin-storage/tests/cdc_11_8_firehose_archive.rs",
    "crates/myelin-storage/tests/cdc_11_8_ci_log_index.rs",
]

# ──────────────── §12 Tenancy & control plane (myelin-tenancy) ──────────────────────────────────

[[contract]]
row = "12.1"
title = "TenantId / Region / ResidencyTag"
status = "deferred"
landing = "P-025"

[[contract]]
row = "12.2"
title = "discover(slug|tenant_id) + placement_of(repo) repo-grain (C-1)"
# COVERED: the tenant-grain `discover` half landed P-081; the repo-grain `placement_of(repo) ->
# {cell_id, group, region, status}` half (C-1: region-pinned, relocatable, NEVER node-pinned) lands
# P-250 (P-CP-15) in myelin-control-plane (src/placement_of_repo.rs). The CDC pair below proves the
# PROVIDER (control-plane `placement_of_repo` + the repo-grain gateway `route_repo`) feeds the
# CONSUMER (the git wire, `git@<cell-endpoint>:tenant/repo.git`, §7.3) WITHOUT a shared type (the DAG
# forbids a myelin-control-plane -> myelin-git edge; the git-wire consumer is modelled in-test over
# the public provider surface, documented deviation EI-01 §1). The GIT residency leg drill
# (repo-grain misroute -> 0 cross-tenant + relocation redirect + cross-region-relocation reject) is
# the dated green artifact. The relocation BYTE-MOVE mechanism is the M5 floor P-CP-22; this row is
# the live routing ANSWER + the redirect-on-relocation PROPERTY.
status = "covered"
cdc = ["crates/myelin-control-plane/tests/cdc_12_2_repo_grain_git_wire.rs"]

[[contract]]
row = "12.3"
title = "place / placement_of"
status = "deferred"
landing = "P-084"

[[contract]]
row = "12.4"
title = "residency_verify -> SignedAttestation"
# COVERED: the control plane (P-085) ships the aggregation+sign half + its in-crate CDC pair, and
# Storage (P-ST-15 / P-102) ships the per-store region REPORTS (the provider) + the
# `myelin storage residency verify <tenant>` admin path. The cross-crate CDC file below proves
# Storage's reports (PROVIDER) feed the control plane's `residency_verify` (CONSUMER) that signs the
# no-global-pool attestation — the two halves agree (green + the cross-region fail leg) WITHOUT a
# shared report type (the DAG forbids a myelin-storage -> myelin-control-plane edge; documented
# deviation, EI-01 §1). The CDN edge set EXTENDS this same row in M3 (P-ST-23 / P-254 — LANDED: the
# ResidencyStoreClass::CdnEdgeSet report feeds the SAME aggregation, the cross-crate CDC maps it to
# the control-plane T2 blob tier; a cross-region CDN edge FAILs the same residency_verify). The C6
# outbound PUSH-MIRROR TARGET EXTENDS this same row in M3 (P-ST-25 / P-255 — LANDED: the
# ResidencyStoreClass::PushMirror report flags the mirror TARGET's region into the SAME aggregation,
# so an extra-EU mirror target FAILs residency_verify WITHOUT a code change; the cross-crate CDC
# `cdc_10_5_mirror_crossing_flag.rs` proves Storage's flag and the control-plane `mirror_allowed` gate
# agree on the crossing). The aggregation shape does not change for any of the three follow-ons.
# The CI-STORE COVERAGE EXTENDS this same row in M4 (P-CP-17 / P-324 — LANDED: the control plane's
# `residency_verify_ci` (RequiredStoreSet::M1AndCi) attests over the M1 stores AND the CI surfaces —
# the CI runner pool / log tier (11.8) / artifact store / cache namespaces (11.2); a wrong-region or
# absent CI store FAILs the SAME aggregation (residency_verify_over), closing the P-085 M1-only named
# partial — the no-global-CI-pool property is attestable per-tenant. The CI-coverage CDC pair below
# proves a CI-coverage attestation provider + an auditor consumer that DEMANDS CI coverage agree; the
# CI-R3 drill (ci_r3_residency_verify_ci_drill.rs) proves the gate goes red on a wrong-region CI
# runner/log/cache. The in-region runner-CLAIM enforcement is the sibling P-CP-18 / P-325.
# The RESIDENCY-PINNED-RUNNERS leg EXTENDS this same row in M4 (P-CP-18 / P-325 — LANDED: P-CP-17 made
# the no-global-CI-pool property ATTESTABLE (a wrong-region CI store FAILs residency_verify); P-CP-18
# makes it ENFORCED AT CLAIM TIME — the control-plane region-pin assertion (RunnerClaimPin::admit_claim)
# admits a CI runner-claim of an EU tenant's run ONLY from an in-region runner (0 out-of-region claims),
# and the residency-pin leg (RunnerClaimPin::pin_ci_store_write, contract 1.6) pins every CI-store write
# (logs/artifacts/caches never leave the region). The CI subsystem owns the runner-claim MECHANISM
# (myelin_ci_sandbox::JobLeaseStore::claim_for_labels, whose predicate already skips out-of-region jobs);
# Tenancy owns the region-pin ASSERTION. The cross-owner CDC pair below proves the CI claim mechanism
# (PROVIDER) and the control-plane region-pin assertion (CONSUMER) AGREE — an out-of-region runner is
# refused from both sides; the CI-R3 runner-claim drill (ci_r3_runner_claim_drill.rs) proves the gate
# goes red on an out-of-region claim. No floor — this completes the CI residency posture begun in P-CP-17.
# The CI-FLEET PROVIDER leg EXTENDS this same row in M4 (CI-P14 / P-357 — LANDED: the EU fleet
# autoscaler (myelin-ci-controlplane/src/fleet.rs) is the PROVISION/WRITE side the CLAIM side (P-CP-18)
# pairs with. The fleet's runner-row write is residency-pinned by RunnerWritePin::admit_runner_write
# (contract 1.6 — `row.region == cell.region`, 0 cross-region runner rows; the CI-side analogue of the
# Bus BusStreamResidency::provision write-boundary), and the fleet REPORTS its pool region via
# FleetResidencyReport (the consumer side of 12.4) into the SAME no-global-pool aggregation — a
# wrong-region fleet report FAILs the per-store match-of-record. The CI-fleet CDC pair below proves the
# fleet's report (PROVIDER) and the auditor's match-of-record demand (CONSUMER) agree; the live
# integration test (integration_ci_p14_fleet.rs, behind --features integration, PROVEN green against the
# dev-stack Postgres) proves the region-pinned runner INSERT applies under RLS and the per-(region,pool)
# autoscale COUNT never aggregates across regions. No floor for the write-boundary; the cell-scale CI-R3
# residency drill at fleet scale stays CI-P31 / CI-M5.
status = "covered"
cdc = [
    "crates/myelin-storage/tests/cdc_12_4_storage_residency_report.rs",
    "crates/myelin-storage/tests/cdc_10_5_mirror_crossing_flag.rs",
    "crates/myelin-control-plane/tests/cdc_12_4_residency_verify_ci_coverage.rs",
    "crates/myelin-control-plane/tests/cdc_ci_r3_runner_claim_region_pin.rs",
    "crates/myelin-ci-controlplane/tests/cdc_12_4_ci_fleet_residency_report.rs",
]

[[contract]]
row = "12.5"
title = "isolation-tier contract"
status = "deferred"
landing = "P-086"

[[contract]]
row = "12.6"
title = "cross-cell PII-free pointer bridge"
status = "covered"
# Frame AUTHORITY is myelin-tenancy (the §2.9 DAG sink, P-CP-02/P-027); EB-14/P-091 pins it from
# the Bus side (re-export on the frozen myelin_events::* path, EI-01 §7 — no second definition) and
# ships the Bus-side serde-conformance CDC pair. The frame is designed-not-built; the cell-local
# resolution BUILD is the M5 follow-on EB-25.
cdc = ["crates/myelin-events/tests/cdc_12_6_crosscell.rs"]

# ──────────────── §13 The shared crates' refined shapes (myelin-content, myelin-query) ──────────

[[contract]]
row = "13.1"
title = "myelin-content taxonomy (frozen)"
# P-234 (KN-P01): the frozen 15-variant Block taxonomy + the markdown-subset inline
# grammar + the three structured nodes, with the WASM render path and the KN-D2 round-trip
# gate. Knowledge PROVIDES the frozen taxonomy; Chat/Issues consume strict subsets (X-2).
# GIT-P17/P-278 adds GIT as a CONSUMER of the frozen subset: PR/review/comment bodies are
# `myelin-content` documents (`myelin_git::body::Body`) round-tripped `render(parse(md)) === md`
# through the ONE editor render path (the KN-D2-class git-body corpus parity, 100%).
# ISS-P10/P-376 adds ISSUES as a CONSUMER of the frozen subset: the issue description body + comments
# are a `myelin-content` block SUBTREE (the consumed Issues SUBSET = the full block set MINUS the
# Knowledge-only db_view/sync_block/toggle, X-2) under a single-author version-token CAS, round-tripped
# `render(parse(md)) === md` through the ONE WASM render path (read + edit use the identical parser —
# no second renderer). `myelin_issues::content` LINKS the frozen Block/Inline (never re-defines a node)
# + the subset validator REJECTS a Knowledge-only node LOUDLY (never a silent drop). The Issues consumer
# CDC (cdc_13_1_issues_content_subset.rs) pins the subset agreement; the ISS-D10 gate
# (roundtrip_iss_d10.rs) is the 100%-round-trip body+comment corpus artifact.
status = "covered"
cdc = [
    "crates/myelin-content/tests/cdc_13_1_taxonomy.rs",
    "crates/myelin-git/tests/cdc_5_4_13_1_git_content_edges.rs",
    "crates/myelin-issues/tests/cdc_13_1_issues_content_subset.rs",
]

[[contract]]
row = "13.2"
# P-235 (KN-P02): the frozen ADF -> myelin-content lossy-node map (the Issues import
# conversion table; lossy nodes named + recorded in the import report, X-2/CR-9).
# Knowledge PROVIDES the table; Issues' import CONSUMES it (builds the named targets +
# records each lossy conversion).
# KN-P24 / P-314 (M3): the Knowledge IMPORT consumer — `myelin_knowledge::import_adf` builds the
# Export/Import service's ADF import against the frozen `myelin_content::MAP`, constructing the named
# target Block for each node and recording every lossy conversion in the ImportReport (named, never
# silent); the degraded content survives. Bounded to EXACTLY the frozen map (the X-2 anti-drift
# anchor). The provider+consumer pair drives the frozen map + the per-import report artifact.
title = "ADF -> myelin-content lossy-map (frozen)"
status = "covered"
cdc = [
    "crates/myelin-content/tests/cdc_13_2_adf_map.rs",
    "crates/myelin-knowledge/tests/cdc_13_2_10_1_knowledge_export_import.rs",
]

[[contract]]
row = "13.3"
# P-235 (KN-P02): the type/grammar half of the 13.3 primitive — the frozen FieldType enum
# (field.rs, P-167), the ViewSpec view-model (view.rs), and the textual QueryAst grammar
# parser (parse.rs) over the ONE Predicate core. Knowledge PROVIDES the shared definitions;
# Issues + Search CONSUME them byte-identically (each owns its executor; the shapes are
# identical, X-3). The order_key/LexoRank half rides the same crate (field.rs) + its unit tests.
#
# P-236 (KN-P03): the order_key/LexoRank half is now a FULL provider+consumer CDC pair — the
# frozen encoding (rank_first/rank_last/rank_between + the 2-char jitter + the 48-char rebalance +
# the created_at+ULID tiebreak) + the SHARED X-3 conformance vector (authored once in
# order_key.rs, replayed byte-identically by Knowledge here and by Issues at ISS-P02/P-241). The
# X-3 anti-drift gate = 0 rank divergences across the shared vector.
#
# P-241 (ISS-P02): Issues now CO-OWNS the 13.3 primitive byte-identical — the genuine consumer leg.
# The myelin-issues crate LINKS myelin-query as a dependency (architecture issue-tracker §1 "Issues
# links them directly — the same bytes Knowledge uses, not a re-implementation") and re-exports the
# four shared shapes from src/query_coown.rs (no second definition; EI-01 §7). The Issues-side CDC
# (cdc_13_3_issues_coown.rs, living IN the issues crate) replays the shared CONFORMANCE_VECTOR and
# serializes the shared ViewSpec/FieldType through Issues' OWN call sites, asserting byte-diff = 0 vs
# Knowledge's frozen outputs. A FieldType rename / ViewSpec reorder / order_key midpoint-or-jitter
# change now fails BOTH co-owners' tests at once. Issues' own AST→store compiler is ISS-P13; the
# order_key CAS reorder is ISS-P09; the co-equal views are ISS-P16 (no data written yet).
#
# KN-P17 / P-307: Knowledge now ships its EXECUTOR over the frozen 13.3 shapes — the flexible-DB
# query lowering (myelin_knowledge::database). The executor CONSUMES the frozen ViewSpec/QueryAst/
# FieldType byte-identically (it never re-defines them) and lowers a view into JSONB ops over
# `db_row.props`, conjoining the `list_objects` SetExpr ACL (4.3) into ONE leak-free view query AND
# the permission-correct COUNT. The typed FieldType validation gate (FieldSchema::validate_props —
# a wrong-typed value is rejected, never coerced) + the two-way relation maintenance (db_relation,
# the TE-7 source of truth) ride here. The executor CDC (cdc_13_3_6_3_knowledge_flex_db.rs) pins the
# agreement; the live JSONB+GIN+SetExpr one-query/0-leak proof is the `--features integration` test
# (integration_kn_d9_flex_db.rs) + the KN-D9 SCHED drill (drill_kn_d9_flex_db.rs).
# KN-P18 / P-308: Knowledge now OWNS the read-time formula/rollup half of 13.3 — the bounded
# FormulaAst evaluator (myelin_knowledge::rollup) over the frozen myelin-query expression core. The
# formula tree REUSES the frozen Literal value space + the SAME static node/depth cost ceilings the
# predicate core enforces (MAX_FORMULA_NODES==MAX_PREDICATE_NODES, MAX_FORMULA_DEPTH==MAX_PREDICATE_DEPTH
# — pinned in the CDC; a drift would be a second expression engine, the EI-01 §7 anti-pattern).
# Formulas/rollups are computed at READ TIME, NEVER stored (KN-3): a rollup over a relation aggregates
# the related rows the viewer may read, CONJOINING the SAME list_objects SetExpr (4.3) the view
# executor uses — 0 rollup leak (a restricted target is never counted/summed, composing with KN-D5);
# a cyclic formula surfaces as #CYCLE via the bounded dependency-graph evaluator, never an infinite
# loop. The rollup CDC (cdc_13_3_4_3_knowledge_rollup.rs) pins the agreement + the #CYCLE + the
# rollup-permission gates; the live permission-filtered rollup conjoin against the dev-stack Postgres
# is the `--features integration` test (integration_kn_d10_rollup.rs) + the KN-D10 SCHED drill
# (drill_kn_d10_rollup.rs). Floor 2 (per-rollup materialised aggregate when read-time recompute is
# MEASURED too slow → the OLAP read store 11.6) is KN-P31 (M5), named.
# ISS-P09 / P-375 (M4): Issues now EXECUTES the order_key half of 13.3 — the server-arbitrated CAS
# reorder (myelin_issues::reorder). A drag-to-reorder bisects a new rank via the frozen byte-identical
# OrderKey::rank_between (midpoint + 2-char jitter, contract 13.3 — NOT re-defined; the shared crate is
# called) and writes it under a CAS on the moved issue's last-seen (order_key, version): a STALE version
# LOSES and re-bases honestly against the authoritative order (0 silent clobber — nothing written,
# nothing emitted), a WIN co-commits issue.reordered (2.2). The 48-char rebalance (myelin_issues::rebalance)
# re-spaces precision-exhausted keys WITHOUT reordering the displayed order. The ISS-D5 drill
# (drill_iss_d5_reorder_zero_clobber.rs) proves it: N writers + an agent re-rank the SAME region ->
# 0 silent clobber (5 distinct-issue moves converge into the (I3,I4) region, jitter keeps keys distinct,
# strict total order), a 6-way same-issue+version contention -> exactly ONE winner / 5 losers re-base
# (bounded 1-retry churn), and the 48-char rebalance preserves the displayed sequence issue-for-issue.
# The reorder provider/consumer CDC pair (same file) pins issue.reordered per-aggregate ordering (2.3)
# + dedup-safe replay (2.5). MUTATION FLOOR (mandatory-core — the order_key CAS IS the silent-clobber
# seam): `cargo mutants -p myelin-issues --file crates/myelin-issues/src/reorder.rs` ≥ 90%; the
# mutation-tested core is the CAS precondition (a stale version must LOSE), the loser-rebases path (a
# lost CAS writes NOTHING + emits NOTHING), the co-commit ordering (emit on the SAME tx — emit-iff-
# committed), and the order-preserving rebalance. The measured % is the CI artifact, registered
# RED-UNTIL-RUN here, never self-asserted (EI-01 §3). FLOOR named: ranking = order_key + server-
# arbitrated CAS; the move-CRDT (Yrs list / Fugue) reusing the byte-identical order_key is the measured
# M5 follow-on (ISS-P32) — the promotion swaps the conflict engine, not the data model.
# ISS-P12 / P-378 (M4): Issues now EXECUTES the QueryAst guard half of 13.3 (and 3.4) — the
# data-driven workflow FSM interpreter (myelin_issues::workflow). A governed transition runs the
# frozen myelin_query::QueryAst guards (WorkflowGuard::predicate — NOT re-defined; the shared engine is
# called) over an IssueContext: a defined-false OR un-evaluable guard BLOCKS with a pre-assembled,
# admin-authored reason (fail-closed — MissingContext → block, never a silent allow), required-fields
# are enforced, and a permitted transition stamps the FIXED state-category (unstarted/started/completed/
# cancelled — the ONE mandatory governance invariant) + stages the post-actions (assign/set-field/link/
# arm-trigger; the arm-trigger carries the frozen myelin_query::EventMatcher = the SAME QueryAst core,
# 3.4). The interpreter is the pure governance decision the ISS-P06 write path drives (the transition
# ABAC — Id.check + the transition CaveatContext, 4.2 — is already wired on apply_mutation); it NEVER
# emits. The ISS-D12 guard-half drill (e2e_iss_p12_workflow_fsm.rs) proves "can't close while blocked_by
# an open issue" → transition blocked + reason + 0 pre-approval mutation; the guard config-shape CDC
# (same file) pins the workflow body round-trip (provider: ISS-P11 scheme author; consumer: the FSM
# interpreter). The flow-determinism lint (1.6) is GREEN over the arm-trigger workflow body
# (lint_workflow_determinism.rs — time read through WfCtx, never a raw clock), with a RED witness.
# MUTATION FLOOR (mandatory-core — the guard is governance-correctness-bearing): `cargo mutants -p
# myelin-issues --file crates/myelin-issues/src/workflow.rs` MET at 26 caught / 0 missed / 10 unviable
# (100% of viable mutants — the guard-evaluation decision, the fixed-category stamp, required-fields,
# and the fail-closed un-evaluable-guard path are all killed). FLOOR named: the CI-red guard half of
# ISS-D12 ("can't mark Done while CI red on the linked PR" — the X-1 CheckStatus + trust_tier off the
# fact, 5.9/Δ10) lands in ISS-P27 (P-394) when the X-1 check-seam closes; the guard SHAPE
# (linked_pr_ci_green_guard) is here and fails closed until then.
# ISS-P16 / P-382 (M4): Issues now BUILDS the co-equal ViewSpec VIEWS over the 13.3 view-model — the
# seven canonical views (board/roadmap/backlog/list/table/calendar/cycle) are each a frozen
# myelin_query::ViewSpec projection over the ONE issue table (myelin_issues::views::IssueView::spec —
# the co-owned shape, never a second view-model). The board↔roadmap co-equality is STRUCTURAL: the
# denormalised type_rank (board ≤ 1, roadmap ≥ 2) partitions the SAME rows. The ISS-D1 same-row gate is
# proven by row id — the e2e (e2e_iss_p16_coequal_views.rs: edit on board → read on roadmap → same id,
# 0 drift) + the LIVE integration drill (integration_iss_p16_coequal_views.rs: one issue table, two
# index-range scans, an UPDATE on the board moves the SAME row id onto the roadmap with the board-set
# date, PROVEN against the dev-stack Postgres). The design-system pass over all seven views (incl. the
# empty/loading/error/permission/tombstone states) is signed off in the design folder. FLOORS named:
# the cross-cell portfolio rollup view is the M5 follow-on (ISS-P32); the real-time board sync is
# ISS-P30 (P-397).
title = "myelin-query primitive (frozen byte-identical)"
status = "covered"
cdc = [
    "crates/myelin-query/tests/cdc_13_3_query_primitive.rs",
    "crates/myelin-query/tests/cdc_13_3_order_key.rs",
    "crates/myelin-issues/tests/cdc_13_3_issues_coown.rs",
    "crates/myelin-issues/tests/drill_iss_d5_reorder_zero_clobber.rs",
    "crates/myelin-issues/tests/e2e_iss_p12_workflow_fsm.rs",
    "crates/myelin-knowledge/tests/cdc_13_3_6_3_knowledge_flex_db.rs",
    "crates/myelin-knowledge/tests/cdc_13_3_4_3_knowledge_rollup.rs",
]

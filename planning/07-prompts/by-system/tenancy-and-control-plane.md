# Phase 7 — Implementation Prompts: Tenancy & Control Plane (cells · region-pinning · the PII-free control plane)

> Phase: `07-prompts/by-system`. The complete ordered set of clean-context, independently-committable coding
> prompts that operationalize the entire **tenancy-and-control-plane** shared-system roadmap
> ([`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md))
> into build tasks. Authored to the ledger template ([`../00-ledger-overview.md`](../00-ledger-overview.md) §2), in
> this system's own band order (M0 → M6). The consolidated index (`01-ledger-index.md`, Phase 7-B) interleaves
> these into the single global `P-<NNN>` sequence; the `P-<NNN>` ids below are this file's **local placeholders**
> (`P-CP-01..P-CP-10`) — the index reassigns stable global ordinals and rewrites DEPENDS-ON to global ids.
> Plain-text identifiers (no backticks-as-emphasis). Markdown only; no git commits by this document or its author.
>
> **Coverage (this file → roadmap):** every Tenancy milestone maps to ≥1 prompt below —
> CP-M0 (the two committed lints `residency-pin` + `control-plane-pii-free` with red+green fixtures, and the
> `myelin-tenancy` partition-key crate) → P-CP-01 + P-CP-02; CP-M1 (the PII-free control-plane registry +
> `discover`/`place`/`placement_of` + two-phase signup + the four-layer region-pinning + `residency_verify` + the
> Pool isolation tier + the frozen-not-live `CrossCellPointer` frame + provision-gating + self-host parity) →
> P-CP-03 + P-CP-04 + P-CP-05; CP-M3 (`placement_of(repo)` live + the outbound-mirror residency gate) → P-CP-06;
> CP-M4 (`residency_verify` CI-store coverage + residency-pinned runners) → P-CP-07; CP-M5 (the cell bulkhead
> under 30× surge + the multi-cell floor follow-on live + live tenant migration / repo relocation + measured
> sizing numbers + restore-verify at cell scale) → P-CP-08 + P-CP-09; CP-M6 (Myelin self-hosts as exactly one
> cell) → P-CP-10. Every named floor is paired with its follow-on: single-cell (P-CP-03) → multi-cell bridge live
> (P-CP-08); avoid-migration-by-sizing (P-CP-03) → live tenant migration / repo relocation (P-CP-09); scripted
> provisioning (P-CP-04) → durable-workflow provisioning (re-confirmed in P-CP-09 once `myelin-flow` lands M2);
> `residency_verify` over M1 stores (P-CP-04) → CI-store coverage (P-CP-07); default-deny mirror gate (P-CP-06) →
> counsel-ratified `transfer_allowed` entries (parallel, legal — named, not built). Date: 2026-06-19.

---

## Canon every prompt in this file assumes (the shared reading set)

Each prompt re-states the precise subset it needs, but all assume the M0 substrate from
[`../00-ledger-overview.md`](../00-ledger-overview.md) §6: the Cargo workspace + the eight glue crates, the
`serve(AppSpec)` harness (three ports, holder auto-registration, ResilientClient, FailStatic, the protected-
human-lane shed order), the transactional outbox + the EventHandler/consumer template, the twelve committed
lints, the contract-coverage scanner, and the failure-injection harness (the 1x/10x/30x load generator + the
scoped-reversible dependency-break injector + the telemetry-assertion library reading contract 1.8). A prompt's
GATE/DRILLS rows are scenarios on that harness; a prompt's DEFINITION OF DONE requires all committed lints green
and the contract-coverage scanner passing.

**The implementation crate.** Tenancy's runtime code lands in two places: the **glue crate `myelin-tenancy`**
(the partition-key types `TenantId`/`Region`/`ResidencyTag` + the `CrossCellPointer` frame — the compile-time
contract carriers every consumer builds against) and a new **service crate `myelin-control-plane`** (the PII-free
registry, `discover`/`place`/`placement_of`/`residency_verify`, the provisioning workflow, the mirror gate, the
discovery cache). The two lints `residency-pin` and `control-plane-pii-free` are authored by Tenancy but hosted in
the substrate/CI lint framework (contract 1.6); their rule + both fixtures are Tenancy's deliverable. Tenancy
implements to the frozen contract shapes — it does not redesign the signatures (EI-01 §1, code-wins-over-docs: if
reality forces a divergence, write it in the crate doc comment and escalate as a whole-workspace contract change).

**The frozen Tenancy architecture** is
[`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md)
(sections cited per prompt); the frozen contracts are
[`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md)
§12 (rows 12.1–12.6) + the co-owned mirror half 10.5 + the consumed rows 1.1/1.4/1.6/1.8, 9.1, 11.3/11.5,
10.4/10.5; the rationale is
[`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md)
(OQ-I cross-cell bridge, §10 the C-1..C-4 residency sharpenings). The Tenancy drills are
[`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md)
rows CP-D1..CP-D8 + the cross-owner residency legs STOR-D5 / CI-R3 / GA-D8 (§4.2) + the F2/F3/F6/F7 families
(§4.1) + E2E-1..E2E-4 (§2), with the strategy in
[`../../05-refined-shared-systems-architecture/testing-strategy/README.md`](../../05-refined-shared-systems-architecture/testing-strategy/README.md).
The band order + the gate invariant is
[`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) (§1 Tier 5 the partition +
residency pin; §2 bands; §4 the gate invariant).

**The two load-bearing facts this whole file sequences around:** (1) Tenancy's two lints are M0 — they ship
*before* the system they constrain exists, so the cross-tenant-leak and control-plane-PII bug-classes are
impossible to compile from the first write (the cheapest ratchet, master §1 Tier 3). (2) The partition + the
four-layer residency pin land in M1 (master Tier 5) and are the M1→M2 go/no-go (CP-D2 + CP-D3), gated behind a
green **STOR-D1** (the silent-data-loss floor, Storage-owned) — no real tenant data is placed until "EU data stays
in EU, no cross-region query path" and "no cross-tenant read" are true by construction *and* restore-verify is
green.

---

### P-CP-01 — The myelin-tenancy partition-key crate: TenantId / Region / ResidencyTag + the CrossCellPointer frame

- **BAND.** M0.
- **ROADMAP MILESTONE.** CP-M0 (the `myelin-tenancy` glue-crate skeleton — the `(tenant, region)` partition-key types frozen as the names/units anchor, 12.1, + the frozen-not-live `CrossCellPointer` frame, 12.6) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M0".
- **DEPENDS-ON.** The M0 substrate prompts that lay down the Cargo workspace + the eight glue-crate skeletons (the `myelin-tenancy` skeleton this fills in) and the `serve(AppSpec)` harness shape. (Substrate-band prompts; the index resolves the exact ids.) This is the first `myelin-tenancy` content prompt; no intra-Tenancy predecessor.
- **CANON DOCS (read these first, in full, before writing any code).**
  - [`../../VISION.md`](../../VISION.md) §1 (one identity/permission model; tenant is the unit), §3 (world-scale + multi-tenant from day 1; GDPR-safe & EU-sovereign by construction; name-your-floors).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §7 (reconcile cross-component contracts at the plan layer — field names AND units agreed up front; a unit mismatch calcifies), §1 (code-wins-over-docs).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §4.1 (the `TenantId`/`Region`/`ResidencyTag` types — `(tenant, region)` the first-class partition key injected by the harness, identical at every isolation tier), §6.1 (the `CrossCellPointer` frame — the four fields `subject (opaque) / type / correlation_id / home_cell`, carries ONLY these four, never payload/PII/authz state; copy the frame byte-exact), the Units header (timestamps RFC-3339 UTC; TTLs/staleness = seconds; resilient-client timeouts = milliseconds; the `(tenant, region)` partition key is the names/units authority).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.1 (the partition-key types), 12.6 (the `CrossCellPointer` frame, frozen — the resolution path is M5, P-CP-08).
- **DELIVERABLE (what to build + exactly where in the repo).** In the glue crate **myelin-tenancy**: (a) the **partition-key types** — `TenantId` (an opaque, non-personal id; NOT a slug, NOT an email — a newtype over an opaque token), `Region` (an enum/newtype of the EU-sovereign regions, with the rule that a `Region` is immutable once assigned), `ResidencyTag` (the per-row residency marker the `residency-pin` lint reads). The canonical shape is `(tenant, region)` = the first-class partition key — frozen here as the names/units anchor every store and every later contract aligns to (ADR-01: a change to this glue type breaks every consumer's build *now*, never silently in prod). (b) The **`CrossCellPointer` frame** as a frozen type with exactly the four fields (`subject: OpaqueSubjectId`, `type: ArtifactType`, `correlation_id: CorrelationId`, `home_cell: CellId`) — the type compiles and ISS/KN/CHAT can build against it, but **no resolution path exists yet** (that is the M5 multi-cell follow-on, P-CP-08). (c) `CellId` + the supporting newtypes (`OpaqueSubjectId`) the frame needs. **Floor named:** the `CrossCellPointer` frame is FROZEN-NOT-LIVE — the type exists so consumers compile and the `control-plane-pii-free` lint guards it, but resolution goes live in M5 (P-CP-08); record this in the crate doc comment with the follow-on pointer.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.1 (`TenantId`/`Region`/`ResidencyTag` partition-key types — the frozen shape), 12.6 (the `CrossCellPointer` frame — type frozen, resolution deferred to P-CP-08). Consumed: none on the hot path (these are the carriers others consume).
- **GATE / DRILLS (quantified; must be green to call this done).** No runtime drill yet (this is a type crate) — the gate is **the workspace compiles with `myelin-tenancy` as a dependency of every consumer crate**, and a doc-test asserts the `CrossCellPointer` frame has exactly four fields and that `subject` is an `OpaqueSubjectId` (never a name/email type). This contributes the type substrate the M0→M1 "contract-coverage scanner passes on the contract set" exit condition leans on. The `control-plane-pii-free` lint (P-CP-02) will assert the frame carries no `is_personal=true` field — wired there.
- **TESTS (required).** Unit: `TenantId` is opaque and not constructible from a personal string accidentally (a type-level test); `Region` is immutable (no setter; a region change is modelled as a new value, never a mutation — a compile-fixture); the `CrossCellPointer` frame round-trips its four fields and rejects (does not compile with) a fifth PII-bearing field. CDC: the provider+consumer pair for 12.1 (a consumer crate opening a store handle parameterised by `(TenantId, Region)`) and 12.6 (a consumer that constructs a `CrossCellPointer` and asserts it holds only the four fields). No mutation floor (pure type definitions; correctness is structural).
- **DEFINITION OF DONE.** The `myelin-tenancy` crate exports the frozen partition-key types + the `CrossCellPointer` frame to the exact §4.1/§6.1 shape; the workspace compiles with it as a dependency; the frozen-not-live frame is named in writing with its follow-on (P-CP-08); CDC + unit tests pass; all twelve committed lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: myelin-tenancy partition-key crate + CrossCellPointer frame`; body lists 12.1 + 12.6 (frame frozen, resolution deferred) implemented, the frozen-not-live frame floor → P-CP-08. Branch first if on default. Co-Authored-By trailer.

---

### P-CP-02 — The two committed lints: residency-pin + control-plane-pii-free, each with red+green fixtures

- **BAND.** M0.
- **ROADMAP MILESTONE.** CP-M0 (the two committed lints in the M0 ratchet 1.6 — `residency-pin` + `control-plane-pii-free`, each with a red-fixture proving it rejects + a green-fixture proving it admits) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M0".
- **DEPENDS-ON.** P-CP-01 (the `myelin-tenancy` partition-key types the `residency-pin` lint reads). The M0 substrate prompts that ship the contract-1.6 lint framework + the M0 lint harness, and the GDPR M0/M1 data-map derive `#[personal_data]` (10.2) that `control-plane-pii-free` runs through. (Substrate/GDPR-band prompts; the index resolves ids.)
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 in full (the ratchet — convert each discipline into a committed mechanical gate; each lint ships a red-fixture proving it rejects + a green-fixture proving it admits; wired loud, never `... || true`; an uncommitted lint is no lint), §2 (a residency leak and a control-plane PII leak are stop-the-bleeding — order by non-negotiability).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency as region-pinning, no cross-region query path — the property `residency-pin` makes structural).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §4.3 (the two lints' exact rules — `residency-pin`: every store a service opens carries the cell's `region`, every write asserts `row.region == cell.region`, a region-mismatched write fails at the boundary; `control-plane-pii-free`: no control-plane registry column is classified `is_personal=true`, run through the generated data-map), §5.3 (the four-layer region-pinning the lint is layer 3 of), §3.3 (the control plane holds ZERO in-region personal data — what `control-plane-pii-free` enforces).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 1.6 (the twelve lints — `residency-pin` + `control-plane-pii-free` are the two Tenancy owns), rows 12.1/12.4 (the residency types the lint constrains), 10.2 (the classify-derive `control-plane-pii-free` reads).
- **DELIVERABLE.** Author the two lints into the contract-1.6 lint framework as committed CI gates, each wired loud-never-swallowed (no `|| true`, no silent filter). (1) **`residency-pin`**: a static check that every store a service opens carries the cell's `region` (threaded by the harness) and every write asserts `row.region == cell.region` — a region-mismatched write fails the build. Ship a **red-fixture** = a write whose `row.region ≠ cell.region` (must fail the build / the lint fires) + a **green-fixture** = a region-pinned write (must admit). (2) **`control-plane-pii-free`**: a static check that no control-plane registry column is classified `is_personal=true` when run through the generated data-map (the GDPR 10.2 derive). Ship a **red-fixture** = a `name`/`email` (PII) column declared on a control-plane table (must fail the build) + a **green-fixture** = a clean PII-free control-plane schema (must admit). Both lints count toward the master M0→M1 "all twelve lints green with both fixtures" go/no-go. (Note: the full *runtime* CP-D1/CP-D3 drills land in P-CP-03/P-CP-05 once the control plane + the write-boundary exist; the *lint legs* — the compile-time rejection — are this M0 deliverable.)
- **CONTRACTS TO IMPLEMENT.** Realized: 1.6 (the two Tenancy-owned lints, with both fixtures each), constraining 12.1 (the partition key), 12.4 (residency attestation), and the control-plane schema (10.2 data-map).
- **GATE / DRILLS.** Both lints **green-with-both-fixtures**: each red-fixture fails the build (the lint fires — a captured expected-failure), each green-fixture passes (the lint admits). This is the **CP-D1 lint leg** (`control-plane-pii-free` red on a PII column) + the **CP-D3 lint leg** (`residency-pin` red on an out-of-region write). Quantified: 2/2 lints emit a green artifact on the green-fixtures and a captured-expected failure on the red-fixtures; they are part of "all twelve lints green" (master M0 exit). **Never weaken a threshold or soften a lint to admit a red-fixture** (EI-01 §3/§5) — a softened lint is a silent gate.
- **TESTS (required).** Unit: the four fixtures (2 red + 2 green) ARE the tests — each asserts the lint's verdict (red-fixture → build fails / lint fires; green-fixture → admits). A test that the `residency-pin` lint reads the cell `region` from the harness-threaded handle, not from a request field. A test that `control-plane-pii-free` runs the control-plane schema through the GDPR data-map and flags an `is_personal=true` column. CDC: n/a (these are lints, not contract RPCs; they are exercised by their fixtures). No separate mutation floor (the lint's correctness is its red+green fixtures).
- **DEFINITION OF DONE.** Both lints wired into CI loud-never-swallowed; all four fixtures present and asserting the correct verdict (2 red fail the build, 2 green admit); the CP-D1 + CP-D3 lint legs emit dated green artifacts; both count toward the twelve-lints-green M0 exit; coverage scanner green; no lint softened to manufacture green. Committed.
- **COMMIT.** Header `P-<NNN> M0: residency-pin + control-plane-pii-free lints with red+green fixtures`; body lists 1.6 (the two Tenancy lints) realized, the CP-D1 + CP-D3 lint legs green (red-fixtures captured-failing, green-fixtures admitting). Co-Authored-By trailer.

---

### P-CP-03 — The PII-free control-plane registry + two-phase signup (discover / place / placement_of) + the placement invariant

- **BAND.** M1.
- **ROADMAP MILESTONE.** CP-M1 (the PII-free control-plane registry — `cell` / `tenant_placement` / `cell_provisioning` tables + the per-cell `local_tenant` directory; `discover`/`place`/`placement_of` PII-free routing; two-phase signup; the HARD placement invariant) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M1".
- **DEPENDS-ON.** P-CP-01 (the partition-key types), P-CP-02 (the two lints constraining the registry — `control-plane-pii-free` guards these tables). The M0 substrate harness (`serve(AppSpec)` + the `region`/`cell_id` injection + the telemetry signal set). The M1 OLTP tier client (Storage P-ST-01, the bounded pool + RLS the registry tables open through). This is the first `myelin-control-plane` prompt.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §1 (EU-sovereign; tenant is the unit), §3 (GDPR-safe by construction; the control plane holds no PII).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (order by non-negotiability — cross-tenant IDOR and a control-plane PII leak are stop-the-bleeding), §3 (prove-it; observability is part of the pass — `placement_count`/`discovery_cache_hit` signals).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency as region-pinning; the immutable-region property the placement invariant enforces).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §3 (the cell + the global control plane — what lives ONLY in the control plane: the PII-free `tenant → {cell_id(s), region}` record, cell inventory, isolation tier, opaque routing token, aggregate utilisation, provisioning state; ZERO in-region personal data), §5.1 (the three PII-free tables — `cell` inventory, `tenant_placement` with `tenant_id` opaque PK + immutable `region` + `home_cell` + `isolation_tier` + non-personal `slug` + `status` + `member_cells[]`, `cell_provisioning` orchestration log; the per-cell `local_tenant` directory; the HARD INVARIANT — every cell in `{home_cell} ∪ member_cells` has `cell.region == tenant_placement.region`, DB trigger + lint, multi-cell single-region by construction), §7.2 (assignment — two-phase signup region-first/identity-captured-inside-the-cell; region-first/isolation-tier-second/capacity-third/stability-always; placement a sticky stored fact never a hot-path hash), §7.3 (discovery — `discover` returns `{cell_id, region, cell_endpoint, ttl_seconds}` keyed by opaque `tenant_id` or non-personal `slug`; client-cached with TTL; a misroute redirect is the correction signal), §4.1 (the `discover`/`place`/`placement_of` signatures, frozen).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.2 (`discover` — tenant-grain here; repo-grain is P-CP-06), 12.3 (`place`/`placement_of`, `member_cells` single-element in v1), 12.1 (the partition key the registry stores), 1.1/1.4/1.8 (harness + holder + telemetry).
- **DELIVERABLE.** In crate **myelin-control-plane**: (1) the **three PII-free registry tables** — `cell` (`cell_id`, immutable `region`, `status`, `isolation_kind`, capacity vector, `utilisation`, `version`, `endpoint`), `tenant_placement` (`tenant_id` opaque PK, immutable `region`, `home_cell`, `isolation_tier`, non-personal `slug`, `status`, `member_cells[]` — single-element in v1), `cell_provisioning` (orchestration log) — plus the per-cell `local_tenant` directory (the cell-local table that maps the cell's own tenants). EVERY column is PII-free (the `control-plane-pii-free` lint guards it; a `name`/`email` column does not compile). (2) the **HARD placement invariant** — a DB trigger (backed by the `residency-pin` lint) that rejects any `tenant_placement` whose `{home_cell} ∪ member_cells` contains a cell with `cell.region ≠ tenant_placement.region`: multi-cell is single-region by construction. (3) **`discover(slug | tenant_id) → {cell_id, region, cell_endpoint, ttl_seconds}`** — PII-free routing keyed by the opaque id / non-personal slug; off the hot path; client-cacheable with the TTL (bounded-staleness fail-static for *routing*). (4) **`place(region, requested_tier) → {tenant_id, home_cell, isolation_tier, cell_endpoint}`** implementing **two-phase signup**: region is chosen FIRST and the `tenant_id` minted PII-free; the human tenant name + admin email are born INSIDE the assigned cell (NOT in the control plane) — assert in a test that `place` never receives or stores a name/email. Assignment is region-first → isolation-tier-second → capacity-third → stability-always; placement is a sticky stored fact (never a hot-path hash). (5) **`placement_of(tenant_id) → {region, home_cell, member_cells, isolation_tier, status}`** — the routing answer; `member_cells` single-element in v1 (the multi-element fan-out is the M5 floor, P-CP-08). (6) The service boots from `serve(AppSpec)` and auto-registers any store as a holder via the harness; emits the telemetry signal set (`cell_utilisation`, `placement_count`, `provision_latency`, `misroute_count`, `discovery_cache_hit`). **Floor named:** SINGLE-CELL — `member_cells` is single-element; resolution is always same-cell; the multi-cell `CrossCellPointer` resolution path goes live in M5 (P-CP-08). **Floor named:** AVOID-MIGRATION-BY-SIZING — a tenant never moves cells in v1 (sizing headroom + sealing); live cell→cell migration is the M5 follow-on (P-CP-09). **Floor named:** the GeoDNS/anycast discovery edge is `[OPEN → P4 (infra)]`; v1 is CP-lookup + client cache. Record all three in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.2 (`discover` tenant-grain — repo-grain deferred to P-CP-06), 12.3 (`place`/`placement_of` with two-phase signup + the placement invariant; `member_cells` single-element floor). Consumed: 1.1/1.4/1.8 (harness, holder, telemetry), 12.1 (the partition key).
- **GATE / DRILLS.** **CP-D2:** a request to a cell for a `tenant_id` it doesn't host → misroute rejected, 0 cross-tenant/cross-cell read, audited. Telemetry: `misroute_count` increments, an audit entry is written, 0 cross-tenant rows. The gateway-rejects-misroute layer (layer 4) is exercised here at tenant grain (the repo-grain leg rides P-CP-06). **CP-D1 (registry leg):** the data-map over the control-plane schema → 0 `is_personal=true` columns (the static leg is P-CP-02; here the live registry schema is asserted to have 0 PII columns). CI. (CP-D3 — the residency write-boundary runtime drill — lands with the four-layer enforcement in P-CP-05.)
- **TESTS (required).** Unit: `place` mints a PII-free `tenant_id` and never receives/stores a name or email (two-phase signup); the placement invariant trigger rejects a `member_cell` in a different region than the tenant; `region` is immutable on both `cell` and `tenant_placement` (no UPDATE path — a region change is modelled as new-tenant-+-DSR); `discover` returns the routing tuple keyed by opaque id / non-personal slug and never an authz answer; `placement_of` returns `member_cells` single-element in v1; the registry schema run through the data-map has 0 `is_personal=true` columns. CDC: provider+consumer pair for 12.2 (a gateway/git-wire caller resolving a cell) and 12.3 (the signup edge calling `place`, a gateway calling `placement_of`). Drill: the CP-D2 scenario on the harness (misroute a tenant_id the cell doesn't host → rejected + audited + 0 cross-tenant read). Mutation floor: the placement-invariant trigger logic + the misroute-rejection path are mandatory-core — state and meet a cargo-mutants mutation-score floor on the registry/routing module.
- **DEFINITION OF DONE.** The three PII-free registry tables + the per-cell `local_tenant` directory exist with 0 `is_personal=true` columns; the HARD placement invariant (single-region by construction) holds; `discover`/`place`/`placement_of` work PII-free with two-phase signup; CP-D2 (misroute 0 + audited) + the CP-D1 registry leg (0 PII columns) emit dated green artifacts; the single-cell / avoid-migration / GeoDNS floors are named in writing with their follow-ons (P-CP-08 / P-CP-09 / infra); CDC + unit tests + the mutation floor pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: PII-free control-plane registry + two-phase signup + placement invariant`; body lists 12.2(tenant-grain)/12.3 implemented, CP-D2 (misroute 0, audited) + CP-D1-registry-leg (0 PII columns) green with measured numbers, the single-cell / avoid-migration / GeoDNS floors → P-CP-08 / P-CP-09 / infra, the mutation score. Co-Authored-By trailer.

---

### P-CP-04 — residency_verify (the M1 store set) + the Pool isolation tier + provision-gating (restore-verify + readiness)

- **BAND.** M1.
- **ROADMAP MILESTONE.** CP-M1 (`residency_verify` over the M1 stores 12.4; the Pool isolation-tier contract 12.5 v1 floor; cell-provisioning gating CP-D6 — a new cell gets no traffic until it passes restore-verify + readiness) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M1".
- **DEPENDS-ON.** P-CP-03 (the registry + placement). Storage M1 restore-verify (P-ST-06, STOR-D1 — the cell-readiness gate + the silent-data-loss floor) and KMS (P-ST-04, crypto-shred for tenant decommission). The M2 `DurableExecutor` (`myelin-flow` 9.1) is NOT yet available — provisioning is a scripted procedure on this floor (the durable workflow is P-CP-09's re-confirmation once `myelin-flow` lands).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §1 (EU-sovereign — the no-global-pool attestation pitch), §3 (name-your-floors — the Pool-tier floor; the scripted-provisioning floor).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; a new cell is not given traffic until restore-verify is green — observability is part of the pass), §1 (name-your-floors).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §5.4 (`residency_verify(tenant_id) → SignedAttestation{every_store_region == tenant.region}` — every store reports the tenant's region; the attestation aggregates; the M1 store set is OLTP/blob/index/KMS, the CI-store coverage is the M4 follow-on), §4.1 (the `residency_verify` signature, frozen — the no-global-pool property attestable), §7.1 (the isolation matrix — Pool/Bridge/Dedicated; the partition key identical at every tier; the Pool tier = shared cell, logical/RLS, the long tail = v1), §7.2 (cell provisioning is a durable workflow gated by restore-verify/readiness before a cell goes `active`).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.4 (`residency_verify` — M1 store set here; CI coverage in P-CP-07), 12.5 (isolation-tier contract — Pool floor), 11.5 (restore-verify, the cell-readiness gate consumed), 11.3 (KMS for decommission), 9.1 (`DurableExecutor` — the durable provisioning the scripted floor will be promoted to).
- **DELIVERABLE.** In crate **myelin-control-plane**: (1) **`residency_verify(tenant_id) → SignedAttestation{every_store_region == tenant.region}`** — the mechanism: every store the tenant uses reports the tenant's region; the attestation aggregates them into a signed, PII-free attestation. The M1 store set is the OLTP tier, the blob tier, the index/search tier, and the KMS (the CI runner/log/artifact/cache coverage is the named M4 partial, P-CP-07 — name it explicitly as covered-in-M4). The attestation is the `residency-attestation` telemetry signal CP-D3/STOR-D5 assert against. (2) **The Pool isolation tier** (12.5) as the v1 floor — the shared-cell, logical/RLS isolation that `residency-pin` + the OLTP RLS guard already enforce; the partition key `(tenant, region)` is identical at this tier. Declare the Bridge (DB-per-tenant) and Dedicated (cell-per-tenant) tiers in the isolation-tier contract but provision them on demand (the partition-key contract is identical across all three — the higher tiers are a provisioning concern, not a redesign). (3) **Cell-provisioning gating (CP-D6)** — a new cell does NOT go `active` / accept any tenant until it **passes restore-verify (Storage 11.5) + readiness**; a failing cell stays `provisioning`. On this floor provisioning runs as a SCRIPTED procedure (the *gating* is M1; the *durability* of the procedure waits on `myelin-flow`, M2 — the durable-workflow promotion is P-CP-09's scope). Tenant decommission = crypto-shred the tenant KEK (Storage 11.3). **Floor named:** the Pool tier is the v1 isolation floor; Bridge + Dedicated are on-demand follow-ons (enterprise / public-sector onboarding). **Floor named:** SCRIPTED provisioning is the floor; the durable-workflow provisioning (ADR-09, `DurableExecutor`) is the M2-engine follow-on, re-confirmed in P-CP-09. **Floor named:** the `residency_verify` store set is the M1 stores only; CI-store coverage is the M4 follow-on (P-CP-07) — `residency_verify` is a NAMED PARTIAL until CI lands. Record all three in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.4 (`residency_verify` over the M1 store set — CI coverage deferred to P-CP-07), 12.5 (the isolation-tier contract — Pool floor, Bridge/Dedicated declared-on-demand). Consumed: 11.5 (restore-verify as the cell-readiness gate), 11.3 (KMS for decommission), 9.1 (the durable executor the scripted floor will be promoted onto).
- **GATE / DRILLS.** **CP-D6:** provision a fresh cell → it passes restore-verify + readiness BEFORE accepting any tenant; a failing cell stays `provisioning` (gets no traffic). Telemetry: the restore-verify + readiness gate result; 0 tenants placed on an unverified cell. SCHED. A `residency_verify` smoke leg: `residency_verify(tenant)` over the M1 stores returns a signed attestation where every store's region == the tenant's region; a store reporting a wrong region makes the attestation FAIL (not silently pass). CI. **Gate-invariant note:** this prompt's "place real data" capability does NOT come online over a red STOR-D1 — restore-verify (P-ST-06) must be green first (master §1 Tier 1).
- **TESTS (required).** Unit: `residency_verify` aggregates every M1 store's region report and FAILS the attestation if any store reports a region ≠ the tenant's region (no silent pass); the attestation is signed and PII-free; the Pool tier uses the identical `(tenant, region)` partition key as Bridge/Dedicated would; a cell stays `provisioning` until restore-verify + readiness pass; tenant decommission crypto-shreds the tenant KEK. CDC: provider+consumer pair for 12.4 (an auditor / `myelin tenant residency verify` caller) and 12.5 (a store opening at the Pool tier with the partition key). Drill: the CP-D6 scenario on the harness (provision a fresh cell, fail its restore-verify → it stays `provisioning`, 0 tenants placed). Mutation floor: the `residency_verify` aggregation + fail-on-mismatch path and the provision-gating logic are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** `residency_verify` over the M1 store set works (signed attestation, fail-on-region-mismatch); the Pool isolation tier is the v1 floor with the identical partition key; cell-provisioning is gated on restore-verify + readiness; CP-D6 (no traffic to an unverified cell) emits a dated green artifact; the Pool-tier / scripted-provisioning / CI-store-coverage floors are named in writing with their follow-ons (on-demand / P-CP-09 / P-CP-07); the place-real-data capability rests on a green STOR-D1; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: residency_verify (M1 stores) + Pool isolation tier + provision-gating`; body lists 12.4(M1-store-set)/12.5(Pool) implemented, CP-D6 (no traffic to unverified cell) green, the Pool / scripted-provisioning / CI-coverage floors → on-demand / P-CP-09 / P-CP-07, the mutation score. Co-Authored-By trailer.

---

### P-CP-05 — The four-layer region-pinning enforced end-to-end + self-host parity + CP-outage blast-radius

- **BAND.** M1.
- **ROADMAP MILESTONE.** CP-M1 (the four-layer region-pinning enforcement 5.3 arch — region immutable + the placement invariant + the `residency-pin` write-boundary check + gateway-rejects-misroute; self-host parity 10 arch; the CP-outage blast-radius win CP-D4) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M1".
- **DEPENDS-ON.** P-CP-02 (the `residency-pin` lint = layer 3), P-CP-03 (the registry + the placement invariant = layers 1+2 + the gateway-rejects-misroute = layer 4), P-CP-04 (`residency_verify` the CP-D3 attestation asserts). Storage M1 (P-ST-06 restore-verify green — the silent-data-loss floor before real data; P-ST-07 the residency-pin write-boundary in the store layer). This is the M1→M2 go/no-go owner for Tenancy.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §1 (EU-sovereign by construction — "EU data stays in EU"), §3 (GDPR-safe & EU-sovereign by construction — data residency is an architectural constraint).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 in full (prove-it: a property does not exist until a test forces the failure and observability watches it survive; gates are quantified; never weaken a threshold; observability is part of the pass), §2 (cross-region PII egress is stop-the-bleeding).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency as region-pinning, NO cross-region query path — the headline property this prompt makes structural).
  - [`../../external-insights/02-platform-substrate.md`](../../external-insights/02-platform-substrate.md) §10 (blast-radius, fail-static — the CP-outage degrades-not-cascades property).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §5.3 in full (the four layers of defence-in-depth — (1) region immutable on cell + tenant, a region change is new-tenant-+-DSR never an UPDATE; (2) the placement invariant trigger keeps every member cell in the tenant's region, multi-cell single-region by construction; (3) the `residency-pin` write-boundary check rejects any `row.region ≠ cell.region` write at the boundary with the cell's region injected by the harness; (4) the gateway REJECTS — does not proxy — a request for a `tenant_id` it doesn't host; NO cross-region query path for personal data), §8 (control-plane scaling — a full control-plane outage is a signup/provisioning outage ONLY; existing tenants keep working entirely within their cells — the headline blast-radius win), §10 (self-host parity — a self-hosted install is EXACTLY one cell of identical artifacts; the control plane is degenerate, discovery/placement return "this cell", the registry is a one-row local table, but the SAME code path runs, the SAME `residency-pin` lint holds, the SAME drills run).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.1 (the partition key), 12.4 (`residency_verify` the attestation), 1.10 (`FailStatic` — the discovery cache fail-static for routing; the CP-down degrade).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows CP-D3 / CP-D4 / STOR-D5 (§4.2) + the F2/F3/F7 families (§4.1).
- **DELIVERABLE.** In crate **myelin-control-plane** + the gateway/harness wiring: (1) **the four-layer region-pinning enforced end-to-end** — wire layers 1+2 (region immutable + the placement invariant trigger, from P-CP-03), layer 3 (the `residency-pin` write-boundary check, from P-CP-02 + Storage's store-layer enforcement P-ST-07 — every write asserts `row.region == cell.region`, the cell's region injected by the harness), and layer 4 (**the gateway rejects — does not proxy — a request for a `tenant_id` it doesn't host**, returning a misroute redirect to the correct cell-endpoint, never serving cross-region). Assert end-to-end that there is **no cross-region query path for personal data**. (2) **Self-host parity** — the degenerate single-cell control plane: discovery/placement trivially return "this cell"; the registry is a one-row local table; but the SAME `myelin-control-plane` code path runs, the SAME `residency-pin` lint holds, the SAME drills run. Build the degenerate-cell config + assert the identical code path. (3) **The CP-outage blast-radius (CP-D4)** — hard-down the control plane and prove already-placed tenants keep serving entirely within their cells (discovery is client-cached, fail-static for routing, contract 1.10); only signup/provisioning degrades. **No floor here** — the residency mechanism is fully built in M1 (the legal posture around region-change-as-DSR and slug PII screening is an `[OPEN — LEGAL]` residual that ships regardless — name it, it is not an engineering gate).
- **CONTRACTS TO IMPLEMENT.** Owns: the end-to-end wiring of the four-layer enforcement over 12.1 (partition key) + 12.4 (attestation). Consumed: 1.10 (`FailStatic` for the discovery-cache routing degrade), 11.5 (restore-verify the real-data gate), Storage P-ST-07 (the store-layer `residency-pin` write boundary).
- **GATE / DRILLS.** **CP-D3:** a write where `row.region ≠ cell.region` → the `residency-pin` write-boundary REJECTS it; `residency_verify` attestation passes for in-region writes. Telemetry: `residency-attestation` (the green artifact), 0 out-of-region writes admitted. CI. **CP-D2** (re-confirmed end-to-end at layer 4): the gateway rejects a misrouted `tenant_id`, 0 cross-tenant/cross-cell read. **STOR-D5 (cross-owner, Tenancy's residency leg):** read/replicate a tenant's data outside its region → IMPOSSIBLE (region in the partition key; `residency-pin` rejects out-of-region writes); 0 cross-region PII egress. Telemetry: `residency-attestation`, 0 egress. SCHED. **CP-D4:** hard-down the control plane → already-placed tenants keep serving; only signup/provisioning degrades. Telemetry: `serving-uptime`, degrade scope = signup/provisioning only. SCHED (F7, fail-static). **These — CP-D2 + CP-D3, gated behind a green STOR-D1 — are the master M1→M2 go/no-go pair Tenancy owns; M2 does not start over a red CP-D2/CP-D3 or STOR-D1.** Never weaken a threshold to pass.
- **TESTS (required).** Unit: an out-of-region write is rejected at the boundary (layer 3) and never reaches the store; the gateway rejects (does not proxy) a `tenant_id` it doesn't host and emits a misroute redirect (layer 4); region is immutable end-to-end (no code path UPDATEs it); the degenerate self-host cell runs the identical code path with a one-row registry; a hard-down control plane leaves an already-placed tenant serving (discovery served from the fail-static cache). CDC: provider+consumer pair for the four-layer enforcement (a store-write consumer asserting the boundary check; a gateway consumer asserting misroute rejection). Drill: CP-D3 (out-of-region write rejected + attestation passes), CP-D2 end-to-end (misroute rejected), STOR-D5 (cross-region egress impossible, 0 egress), CP-D4 (CP-down → serving unaffected, degrade scope signup-only) — each on the harness with the scoped-reversible dependency-break injector. Mutation floor: the write-boundary check (layer 3) + the gateway-rejects-misroute path (layer 4) are mandatory-core — state and meet a mutation-score floor (this is the residency floor — a high bar).
- **DEFINITION OF DONE.** The four-layer region-pinning is enforced end-to-end with no cross-region query path for personal data; self-host parity runs the identical code path on a degenerate one-cell control plane; CP-D3 (residency-pin rejects + attestation passes), CP-D2 (misroute 0 end-to-end), STOR-D5 (0 cross-region egress), CP-D4 (CP-down = signup-only impact) each emit a dated green artifact with measured numbers; the `[OPEN — LEGAL]` region-change/slug-PII residual is named (ships regardless); the place-real-data leg rests on a green STOR-D1; CDC + unit tests + the mutation floor pass; all twelve lints green; coverage scanner green. No threshold weakened. Committed.
- **COMMIT.** Header `P-<NNN> M1: four-layer region-pinning end-to-end + self-host parity + CP-outage blast-radius`; body lists the four-layer enforcement + 12.4 attestation, CP-D2/CP-D3/STOR-D5/CP-D4 green with measured numbers, the `[OPEN — LEGAL]` residual named, the mutation score. Co-Authored-By trailer.

---

### P-CP-06 — placement_of(repo) goes live (repo-granular, relocatable) + the outbound push-mirror residency gate

- **BAND.** M3.
- **ROADMAP MILESTONE.** CP-M3 (`placement_of(repo)` goes live — repo-granular, region-pinned, relocatable, never node-pinned, C-1; the outbound push-mirror residency gate `mirror_allowed`, deny-by-default, C-4) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M3".
- **DEPENDS-ON.** P-CP-03 (`discover`/`placement_of` tenant-grain), P-CP-05 (the four-layer enforcement). Git's M3 data model (the producer subsystem; repos/refs exist) and Git's mirror config feature. GDPR's `transfer_allowed` registry (10.5 structural half, M1; ratified entries on demand). This rides the M3 band with Git.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §1 (EU-sovereign — a PII-bearing byte does not leave the region absent a registered lawful basis).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (cross-residency PII egress via a mirror is stop-the-bleeding — deny-by-default), §5 (make violations loud, never silently swallowed — a refused mirror is loud, not logged-and-allowed).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency as region-pinning, no cross-region query path — extended to repo grain + the mirror boundary).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §5.2 (repo-granular placement — `placement_of(repo) → {cell_id, group, region, status}`; a repo's `cell_id` is its tenant's `home_cell`; `group` is the repo-storage group within the cell; region-pinned + relocatable, NEVER node-pinned — a clone URL encodes the cell, a repo can move cells same-region without its URL identity being a node pin; the git wire discovers + gets a misroute redirect if relocated), §7.3 (the git-wire discovery — `git@<cell-endpoint>:tenant/repo.git` re-discovers on a misroute redirect at repo grain), §7.4 in full (the outbound push-mirror residency gate — `mirror_allowed(tenant_id, mirror_target) → Allow | Deny{reason}`; an extra-EU PII-bearing target is denied by default; consults GDPR's `transfer_allowed`/`subprocessors` registry; the split — the control plane decides "does this target cross the residency boundary?", GDPR owns "is this transfer lawful?"; a crossing push without a `transfer_allowed` entry is REFUSED, not logged-and-allowed).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.2 (`placement_of(repo)` repo-grain, C-1), 10.5 (the `transfer_allowed`/`subprocessors` registry + the outbound-mirror gate co-owned half, C-4).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) §10 (the C-1 repo-granular placement + the C-4 mirror gate rationale).
- **DELIVERABLE.** In crate **myelin-control-plane**: (1) **`placement_of(repo: ArtifactRef) → {cell_id, group, region, status}`** goes LIVE — a repo's `cell_id` is its tenant's `home_cell` (single-cell) / the member cell that homes the repo's workload (multi-cell, M5); `group` is the repo-storage group within the cell (Storage 11.2 object-backed pack tier). **Region-pinned + relocatable, never node-pinned** — the placement is a stored fact (like tenant placement) that can be relocated within its region without a hash recompute, but is NEVER derived from a node hash on the hot path. The git wire (`git@<cell-endpoint>:tenant/repo.git`) discovers the cell via `placement_of(repo)` and gets a **misroute redirect** to the current endpoint if relocated (the discovery fail-static property at repo grain). (2) **`mirror_allowed(tenant_id, mirror_target) → Allow | Deny{reason}`** — the outbound push-mirror residency gate: a mirror target resolving to an extra-EU host for PII-bearing content is **denied by default**; the gate consults GDPR's `transfer_allowed`/`subprocessors` registry (10.5) and permits the transfer ONLY if the registry records a lawful basis for that target. The control plane decides the residency-boundary crossing (it knows the tenant's region + the target's region); GDPR owns the lawful-transfer registry. A crossing push without a `transfer_allowed` entry is **refused** (loud), not logged-and-allowed. **Floor named:** the default-deny gate ships regardless; the counsel-ratified `transfer_allowed` entries that permit a specific extra-EU mirror are `[OPEN — LEGAL]` (Schrems II / Art. 44–49) — one ratified statement per target, parallel (legal), NOT an engineering gate. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.2 (`placement_of(repo)` repo-grain, C-1, live), 10.5 (mirror half — the control-plane residency-boundary decision in `mirror_allowed`, C-4). Consumed: 10.5 (GDPR's `transfer_allowed`/`subprocessors` registry — the lawful-transfer half), the Git data model (repos/refs).
- **GATE / DRILLS.** **GIT residency leg of CP-D2/CP-D3 (rides GIT-D8):** cross-tenant repo access via repo-grain misroute → 0; a relocated repo's clone/push is corrected to the current cell-endpoint (misroute redirect). Telemetry: `misroute_count`, 0 cross-tenant read. CI/SCHED. **The C-4 mirror drill (rides the CP-D3 family):** an outbound mirror to an extra-EU host WITHOUT a `transfer_allowed` entry is **denied**; assert **0 unauthorised cross-residency mirror pushes**. Telemetry: the mirror-gate decision, 0 unauthorised pushes. SCHED.
- **TESTS (required).** Unit: `placement_of(repo)` returns the repo's `cell_id`/`group`/`region`/`status` and is a stored fact, not a node-hash (relocating a repo within-region does not recompute a hash); a relocated repo's clone gets a misroute redirect to the new cell-endpoint; `mirror_allowed` denies an extra-EU PII-bearing target by default and permits ONLY when `transfer_allowed` records a lawful basis; a crossing push without an entry is refused (returns Deny with a reason), not logged-and-allowed. CDC: provider+consumer pair for 12.2 repo-grain (the git wire calling `placement_of(repo)`) and 10.5 mirror half (Git's mirror feature calling `mirror_allowed`). Drill: the GIT residency leg (repo-grain misroute → 0 cross-tenant + relocation redirect) and the C-4 mirror drill (extra-EU mirror without an entry → denied, 0 unauthorised pushes) on the harness. Mutation floor: the `mirror_allowed` deny-by-default decision + the repo-relocation misroute-redirect path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** `placement_of(repo)` is live (repo-granular, region-pinned, relocatable, never node-pinned) with the misroute-redirect-on-relocation property; the outbound-mirror gate denies extra-EU PII-bearing targets by default and consults `transfer_allowed`; the GIT residency leg (0 cross-tenant) + the C-4 mirror drill (0 unauthorised cross-residency pushes) emit dated green artifacts; the `[OPEN — LEGAL]` ratified-entries follow-on is named (ships regardless); CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M3: placement_of(repo) live + outbound push-mirror residency gate`; body lists 12.2(repo-grain, C-1) + 10.5(mirror half, C-4) implemented, the GIT residency leg + C-4 mirror drill green (0 cross-tenant, 0 unauthorised mirror pushes), the `[OPEN — LEGAL]` follow-on named, the mutation score. Co-Authored-By trailer.

---

### P-CP-07 — residency_verify CI-store coverage (the no-global-pool attestation) + residency-pinned runners

- **BAND.** M4.
- **ROADMAP MILESTONE.** CP-M4 (`residency_verify` store-set extension over the CI runner pool + log/artifact/cache stores, C-2 — the no-global-pool property attestable per-tenant; residency-pinned runners CI-R3) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M4".
- **DEPENDS-ON.** P-CP-04 (`residency_verify` over the M1 stores — this extends its store set), P-CP-05 (the four-layer enforcement). CI's M4 runner pool + log/artifact/cache stores reporting region (the consumer subsystem; CI lands in M4). This rides the M4 band with CI.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §1 (EU-sovereign — the no-global-CI-pool claim made attestable).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; observability is part of the pass — a CI runner in the wrong region must FAIL `residency_verify`, not silently pass), §1 (code-wins-over-docs — the attestation reflects the running CI topology).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency as region-pinning — extended to the CI execution surface).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §5.4 (the CI no-global-pool attestation, C-2 — `residency_verify`'s store set now explicitly covers the CI runner pool, the CI log tier (T3 content-addressed segments, Storage 11.8), the CI artifact store, and the CI cache namespaces incl. the trust-tier/branch-scoped namespaces, Storage 11.2; a CI runner that executed a tenant's job in the wrong region would FAIL `residency_verify`; the mechanism is unchanged, only the coverage is pinned), §4.1 (the `residency_verify` signature — the store set explicitly includes the CI runner pool + CI log/artifact/cache region).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.4 (`residency_verify` CI-store coverage, C-2), 11.8 (the CI log tier), 11.2 (the CI cache namespaces).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row CI-R3 (§4.2).
- **DELIVERABLE.** In crate **myelin-control-plane** (extending P-CP-04's `residency_verify`): (1) **extend the `residency_verify` store set** to cover the CI surfaces — the CI runner pool, the CI log tier (T3 content-addressed segments), the CI artifact store, and the CI cache namespaces (including the trust-tier/branch-scoped namespaces) all report the tenant's region into the signed attestation. This closes the named-partial from P-CP-04: the no-global-CI-pool property is now attestable per-tenant. A CI runner / log / artifact / cache in the wrong region makes `residency_verify` FAIL (not silently pass). (2) **Residency-pinned runners (CI-R3)** — wire the control-plane side so that an EU-resident tenant's CI run is claimed ONLY by an in-region runner; logs/artifacts/caches never leave the region (within-EU CDN); `residency-pin` passes on every write the CI run makes. (The CI subsystem owns the runner-claim mechanism; Tenancy owns the residency attestation + the region-pin assertion over the CI stores.) **Floor closed:** this is the follow-on that closes P-CP-04's `residency_verify`-over-M1-stores-only floor — record that the partial is now complete.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.4 (the CI-store-set extension of `residency_verify`, C-2 — completing the partial). Consumed: 11.8 (the CI log tier region report), 11.2 (the CI cache-namespace region report), CI's runner-pool region report.
- **GATE / DRILLS.** **CI-R3 (cross-owner, Tenancy's attestation leg):** an EU-resident tenant's run → claimed only by an in-region runner; logs/artifacts/caches never leave the region (within-EU CDN); `residency_verify` attests over the CI store set; `residency-pin` passes on every write. Telemetry: `residency-attestation` (now covering the CI stores), the `residency-pin` lint green on every CI-store write. SCHED. Assert: a runner/log/artifact/cache reporting a wrong region FAILS `residency_verify` (0 silent pass). The no-global-pool property is now attestable per-tenant.
- **TESTS (required).** Unit: `residency_verify` aggregates the CI runner/log/artifact/cache region reports and FAILS if any reports a region ≠ the tenant's region (no silent pass); the attestation store set explicitly enumerates the four CI surfaces; an EU-resident tenant's run is rejected from an out-of-region runner. CDC: provider+consumer pair for 12.4-with-CI-coverage (the auditor / residency-verify caller now covering the CI stores). Drill: the CI-R3 scenario on the harness (an EU tenant run → in-region runner only; an out-of-region CI store → `residency_verify` fails). Mutation floor: the CI-store region-report aggregation + the fail-on-mismatch path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** `residency_verify`'s store set covers the CI runner pool + log/artifact/cache stores; the no-global-pool property is attestable per-tenant (a wrong-region CI store fails the attestation); residency-pinned runners enforce in-region claim; CI-R3 emits a dated green artifact; the P-CP-04 `residency_verify`-M1-only partial is closed (recorded); CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: residency_verify CI-store coverage + residency-pinned runners`; body lists 12.4(CI-store-set, C-2) implemented, CI-R3 green (no-global-pool attestable, 0 silent pass), the P-CP-04 partial closed, the mutation score. Co-Authored-By trailer.

---

### P-CP-08 — Multi-cell goes live: the CrossCellPointer bridge resolution + cross-cell DSR fan-out + the cell bulkhead under 30× surge

- **BAND.** M5.
- **ROADMAP MILESTONE.** CP-M5 (the multi-cell floor follow-on — the cross-cell PII-free pointer bridge goes LIVE 12.6, cross-cell resolution always cell-local, the cross-cell DSR fan-out mechanism, cross-cell zookie consistency, multi-cell rebalancing; the cell bulkhead under 30× surge CP-D5; multi-cell erasure GA-D8) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M5".
- **DEPENDS-ON.** P-CP-01 (the frozen `CrossCellPointer` frame this lights up), P-CP-05 (the single-cell four-layer enforcement). The M4 band complete (all five subsystems; ISS/KN/CHAT have the bridge consumers awaiting resolution). GDPR's `dsr_submit` iterating `member_cells` (10.4). This is the single-cell → multi-cell floor follow-on (master §5).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — multi-cell is the M5 follow-on to the single-cell floor; world-scale from day 1).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — the FLOOR drills CP-D8 + GA-D8 are now owed; observability is part of the pass), §2 (cross-cell PII leak is stop-the-bleeding — the bridge carries 0 PII).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency — the bridge keeps resolution cell-local, never crosses raw rows).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §6 in full (the cross-cell PII-free pointer bridge — the frozen four-field frame; resolution ALWAYS cell-local: A's gateway holding the viewer's identity asks cell B to `resolve(ref, viewer, mode)` IN B, permission-checked IN B against B's tuples, B returns ONLY the already-rendered already-permission-filtered projection or a tombstone — never raw rows, never PII; ISS portfolio rollup aggregates projections, KN cross-cell collab + CHAT cross-org channels resolve membership/content in the home cell; the DSR orchestrator iterates `member_cells` over this bridge; §6.3 what is designed vs deferred — the cross-cell DSR fan-out mechanism, the per-viewer cross-cell resolution latency budget, cross-cell zookie consistency the hardest sub-problem, multi-cell rebalancing), §7.1 (cell sizing — the multi-dimensional envelope; the binding dimension discovered by measurement).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.6 (the `CrossCellPointer` bridge, now live), 10.4 (`dsr_submit` iterates `member_cells`), 5.2 (Refs `resolve(ref, viewer, mode)` the bridge calls cell-local).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) §OQ-I (the cross-cell bridge frame + the multi-cell floor).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows CP-D5 / CP-D8 / GA-D8 (§4.2) + the F6 surge family (§4.1).
- **DELIVERABLE.** In crate **myelin-control-plane** + the cross-cell resolution path: (1) **the `CrossCellPointer` bridge goes LIVE** — resolution ALWAYS cell-local: cell A's gateway (holding the viewer's identity) asks cell B to `resolve(ref, viewer, mode)` (Refs 5.2) IN B; permission-checked IN B against B's own tuples (`check`/`list_objects`); B returns ONLY the already-rendered, already-permission-filtered projection (or a tombstone) — never raw rows, never PII that should stay in B. The bridge carries ONLY the four frozen fields (`subject`/`type`/`correlation_id`/`home_cell`). Light up the ISS cross-cell portfolio rollup (aggregates projections), KN cross-cell collab + CHAT cross-org channels (resolve membership/content in the home cell). (2) **the cross-cell DSR fan-out mechanism** — the DSR orchestrator iterates `member_cells ∪ home_cell` (GDPR 10.4) over the bridge, merging a complete per-cell receipt set. (3) **cross-cell zookie consistency** — the hardest sub-problem: a zookie minted in the home cell read in a member cell (bounded-staleness, Zanzibar-class); name it explicitly and build the bounded read-through. (4) **multi-cell rebalancing** — move a tenant's workload across member cells (same region). (5) **the cell bulkhead under 30× surge (CP-D5)** — wire the per-cell isolation so a fatal fault / 30× surge in one cell leaves other cells unaffected and a noisy tenant is contained to its cell (the F6 surge family, cross-cell impact 0). **The single-cell floor (P-CP-03) is now promoted** — record that `member_cells` is now multi-element and the bridge resolution is live. **Floor named:** the `[OPEN — LEGAL]` cross-cell bridge residency proof (counsel sign-off that `subject`/`type`/`correlation_id` are not personal data for a tenant) ships regardless of ratification.
- **CONTRACTS TO IMPLEMENT.** Owns: 12.6 (the `CrossCellPointer` bridge resolution — now LIVE). Consumed: 5.2 (Refs `resolve` cell-local), 10.4 (`dsr_submit` `member_cells` iteration), Id's zookie (cross-cell bounded staleness).
- **GATE / DRILLS.** **CP-D8 (FLOOR, now owed):** a cross-cell ref (multi-cell) → the bridge carries ONLY `subject`/`type`/`correlation_id`/`home_cell`; the target resolves per-viewer in the home cell; an unauthorised viewer → tombstone. Telemetry: the PII-free bridge proof, 0 PII across the bridge. SCHED. **GA-D8 (cross-owner FLOOR, Tenancy's multi-cell leg):** multi-cell erasure — the DSR fan-out iterates all `member_cells ∪ home_cell`; a complete merged receipt set; 0 cells missed. Telemetry: per-cell receipt set, 0 cells missed. SCHED. **CP-D5:** fatal fault / 30× surge in one cell → other cells unaffected; noisy tenant contained. Telemetry: cross-cell impact 0. SCHED (F6). Tenancy is the partition under all four E2E scenarios (E2E-1..E2E-4) and the residency attestation in E2E-4 (DSAR fan-out) — those must be green for the band. Never weaken a threshold to pass.
- **TESTS (required).** Unit: the bridge carries exactly the four fields and never a raw row / PII (a fifth PII field does not compile); cross-cell `resolve` permission-checks IN the home cell and returns only the projection / a tombstone for an unauthorised viewer; the DSR fan-out iterates `member_cells ∪ home_cell` and merges a complete receipt set (0 cells missed); a zookie minted in the home cell read in a member cell observes bounded staleness; a 30× surge in one cell leaves another cell's latency within budget (cross-cell impact 0). CDC: provider+consumer pair for 12.6-live (an ISS rollup / KN collab / CHAT channel consumer resolving a cross-cell pointer). Drill: CP-D8 (cross-cell ref → 0 PII, unauthorised → tombstone), GA-D8 (multi-cell erasure → 0 cells missed), CP-D5 (cell bulkhead under 30× surge → cross-cell impact 0) on the harness. Mutation floor: the cell-local-resolution + permission-check-in-B path and the DSR fan-out completeness check are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The `CrossCellPointer` bridge resolution is live (always cell-local, 0 PII across the bridge); the cross-cell DSR fan-out (0 cells missed), cross-cell zookie consistency, and multi-cell rebalancing are built; the cell bulkhead holds under 30× surge (cross-cell impact 0); CP-D8 + GA-D8 + CP-D5 emit dated green artifacts; the single-cell floor is promoted (recorded); the E2E scenarios are green for the band; the `[OPEN — LEGAL]` bridge-residency-proof residual is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. No threshold weakened. Committed.
- **COMMIT.** Header `P-<NNN> M5: multi-cell bridge live + cross-cell DSR fan-out + cell bulkhead under 30× surge`; body lists 12.6(live) implemented, CP-D8/GA-D8/CP-D5 green with measured numbers, the single-cell floor promoted, the `[OPEN — LEGAL]` residual named, the mutation score. Co-Authored-By trailer.

---

### P-CP-09 — Live tenant migration + repo relocation + durable-workflow provisioning + the measured sizing numbers + restore-verify at cell scale

- **BAND.** M5.
- **ROADMAP MILESTONE.** CP-M5 (live tenant migration + repo relocation — the online cell→cell move same-region via reindex-from-source + crypto-shred cut-over, CP-D7; durable-workflow provisioning re-confirmed once `myelin-flow` exists; the measured sizing-band numbers; restore-verify at cell scale STOR-D2) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M5".
- **DEPENDS-ON.** P-CP-04 (the scripted-provisioning floor + the provision-gating this promotes to durable), P-CP-06 (`placement_of(repo)` — repo relocation is this mechanism at repo grain), P-CP-08 (multi-cell live — migration moves across cells). Storage's reindex-from-source (2.6) + KMS crypto-shred (11.3) + restore-verify (11.5). The `DurableExecutor` (`myelin-flow` 9.1, M2 — now available). This is the avoid-migration-by-sizing → live-migration floor follow-on (master §5).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — live migration is the M5 follow-on to avoid-migration-by-sizing; world-scale; quality over plan-adherence).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — CP-D7 is owed; the sizing numbers are MEASURED not predicted; never weaken a threshold), §2 (a migration that loses/leaks data is stop-the-bleeding — 0 loss across-seam).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency — the move lands in-region) + §5 (reindex-from-source — the migration rebuilds derived stores from source, never restores them from their own backup).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §7.2 (live tenant migration — designed-not-built in v1; the v1 floor is avoid migration by sizing headroom + sealing; the follow-on reuses reindex-from-source + crypto-shred cut-over, triggered by a MEASURED hot cell that sealing cannot relieve; cell provisioning is a durable workflow off the hot path, ADR-09), §7.1 (cell sizing — the multi-dimensional capacity envelope; the binding dimension discovered by MEASUREMENT, never predicted, ADR-10; the sizing-band numbers an `[OPEN → P5, measured]` default-to-beat), §5.2 (repo relocation, C-1 — the same migration mechanism at repo grain).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 9.1 (`DurableExecutor{start}` — the durable provisioning/migration workflow), 11.3 (KMS crypto-shred cut-over), 11.5 (restore-verify at cell scale, STOR-D2), 2.6 (reindex-from-source), 12.2/12.3 (`placement_of` updated by the move).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows CP-D7 / STOR-D2 (§4.2).
- **DELIVERABLE.** In crate **myelin-control-plane**: (1) **the online cell→cell move (same region)** as a durable workflow (`myelin-flow` `DurableExecutor`, ADR-09) — reusing **reindex-from-source + crypto-shred cut-over**: copy the tenant's source-of-truth to the target cell, reindex derived stores FROM SOURCE in the target (never restore them from backup), cut over (`placement_of` updated to the target), then **crypto-shred the source** (Storage 11.3). The move lands in-region (the placement invariant holds across the move). **Triggered by a MEASURED hot cell** that sealing cannot relieve (ADR-10, measure-before-shard). (2) **Repo relocation (C-1)** — the same mechanism at repo grain (`placement_of(repo)` updated; the git wire gets a misroute redirect to the new cell-endpoint, P-CP-06). (3) **Durable-workflow provisioning** — promote the P-CP-04 scripted provisioning to the durable workflow (the *gating* on restore-verify + readiness is unchanged; now the procedure is durable). (4) **the measured sizing-band numbers** — replace the conservative defaults with `tenants_max` per cell class + which capacity dimension binds first, set from load-test + per-cell telemetry (the `cell_utilisation` signal); record the measured numbers in the thresholds file. (5) **restore-verify at cell scale (STOR-D2)** — re-confirm RPO ≤ 5 min / RTO ≤ 1h-tenant / ≤ 4h-cell under world-scale load. **The avoid-migration-by-sizing floor (P-CP-03) + the scripted-provisioning floor (P-CP-04) are now promoted** — record both.
- **CONTRACTS TO IMPLEMENT.** Owns: the live-migration + repo-relocation mechanism (over 12.2/12.3 `placement_of` updates) + the durable-workflow provisioning. Consumed: 9.1 (`DurableExecutor` for the durable workflow), 2.6 (reindex-from-source), 11.3 (KMS crypto-shred cut-over), 11.5 (restore-verify at cell scale).
- **GATE / DRILLS.** **CP-D7 (FLOOR, now owed):** migrate a tenant cell→cell (same region) → 0 loss across-seam, lands in-region, source crypto-shredded. Telemetry: the migration receipt, 0 loss, the source key destroyed. SCHED. **STOR-D2 at cell scale (re-confirmed):** RPO ≤ 5 min / RTO ≤ 1h-tenant / ≤ 4h-cell under world-scale load. Telemetry: `backup_rpo_seconds`, restore-time. SCHED. A durable-provisioning leg: provisioning now runs as a durable workflow and remains gated on restore-verify + readiness (CP-D6 re-confirmed under the durable engine). The measured sizing numbers are recorded in the thresholds file (a measured default-to-beat, never predicted). Never weaken a threshold to pass.
- **TESTS (required).** Unit: the migration reindexes derived stores from source in the target (no backup-restore path for derived stores); the cut-over updates `placement_of` atomically; the source is crypto-shredded after the move (the source key is destroyed, the source data unrecoverable); the move lands in-region (the placement invariant holds — a cross-region move does not compile); repo relocation updates `placement_of(repo)` and the git wire redirects; durable provisioning remains gated on restore-verify + readiness. CDC: provider+consumer pair for the migration workflow (the ops/sizing trigger calling the migration) and the durable-provisioning workflow. Drill: CP-D7 (cell→cell migration → 0 loss, source crypto-shredded) + STOR-D2 at cell scale (RPO/RTO under load) on the harness. Mutation floor: the cut-over atomicity + the source-crypto-shred-after-move path + the reindex-from-source-not-backup path are mandatory-core — state and meet a high mutation-score floor (this is a data-move — a high bar).
- **DEFINITION OF DONE.** Live cell→cell migration (0 loss, in-region, source crypto-shredded) + repo relocation + durable-workflow provisioning exist; the sizing-band numbers are measured + recorded in the thresholds file; restore-verify is re-confirmed at cell scale; CP-D7 + STOR-D2-at-cell-scale emit dated green artifacts with measured numbers; the avoid-migration + scripted-provisioning floors are promoted (recorded); CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. No threshold weakened. Committed.
- **COMMIT.** Header `P-<NNN> M5: live tenant migration + repo relocation + durable provisioning + measured sizing + cell-scale restore-verify`; body lists the migration/relocation/durable-provisioning mechanism, CP-D7 (0 loss, source shredded) + STOR-D2-at-cell-scale green with measured RPO/RTO + sizing numbers, the avoid-migration + scripted-provisioning floors promoted, the mutation score. Co-Authored-By trailer.

---

### P-CP-10 — Dogfooding: Myelin self-hosts as exactly one cell + the lints run as Myelin CI

- **BAND.** M6.
- **ROADMAP MILESTONE.** CP-M6 (Myelin self-hosts as exactly one cell — the degenerate control plane serves the team's own tenant; the `residency-pin` + `control-plane-pii-free` lints run as Myelin CI jobs on every Myelin commit; `residency_verify` green on the platform's own data) — [`../../06-roadmaps/shared/tenancy-and-control-plane.md`](../../06-roadmaps/shared/tenancy-and-control-plane.md) §2 "CP-M6".
- **DEPENDS-ON.** P-CP-05 (self-host parity — the degenerate one-cell control plane), P-CP-09 (world-scale-ready; restore-verify + the sizing numbers proven). The M5 band complete (the platform is world-scale-ready and the E2E wedge is proven). The Myelin self-hosting CI graph (the M6 dogfood loop).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §5 (the dogfood loop — Myelin hosts itself; the team's data is real tenant data), §3 (EU-sovereign by construction — the self-host cell stays in the team's region).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — the lints now run as Myelin CI jobs on the platform's own commits; an uncommitted gate is no gate), §1 (code-wins-over-docs; the truth-up pass confirms every PROVEN row rests on a dated green artifact).
  - [`../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md`](../../05-refined-shared-systems-architecture/tenancy-and-control-plane.md) §10 (self-host parity — a self-hosted install is EXACTLY one cell of identical artifacts; the control plane is degenerate but the SAME code path runs; the SAME `residency-pin` lint holds; the SAME drills run; managed-fleet-only features are N/A for self-host by definition, not a gap).
  - [`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) §2 "M6" (the dogfood band; the team's data is real tenant data, so this band does not start over a red restore-verify or DSAR fan-out — Tier 1 + Tier 6).
- **DELIVERABLE.** (1) **Place the Myelin team's own tenant** on the platform: the degenerate single-cell control plane (from P-CP-05) serves it — discovery/placement return "this cell", the registry is a one-row local table, the SAME `myelin-control-plane` code path runs. (2) **Wire the two Tenancy lints as Myelin CI jobs** — `residency-pin` + `control-plane-pii-free` run on EVERY Myelin commit in the self-hosting CI graph (the dogfood loop), loud-never-swallowed. (3) **`residency_verify` green on the platform's own data** — the self-host cell's stores attest the team's region. (4) Confirm **no later-band CP gate is red** (the gate invariant holds end-to-end): a truth-up pass confirms every PROVEN Tenancy row (CP-D1..CP-D8, STOR-D5, CI-R3, GA-D8) rests on a dated green artifact, never a doc claim (code-wins-over-docs). **No new floor** — this is the proof the floors built across M0..M5 hold on the platform's own data.
- **CONTRACTS TO IMPLEMENT.** Owns: none new — this exercises the full Tenancy contract surface (12.1–12.6 + the lints + `residency_verify`) on the platform's own tenant. Consumed: the self-hosting CI graph (the M6 dogfood loop), the M5 world-scale-ready substrate.
- **GATE / DRILLS.** The self-host cell serves the team with `residency_verify` GREEN; the `residency-pin` + `control-plane-pii-free` lints GREEN on the platform's own commits (run as Myelin CI jobs); no later-band CP gate red. Telemetry: `residency-attestation` green on the team's tenant; the lints green in the Myelin CI graph; the truth-up pass confirms 0 red earlier Tenancy gates. SCHED (the dogfood loop runs on the platform's own commits). This is the done-bar for Tenancy — the gate invariant holds end-to-end.
- **TESTS (required).** Unit: the degenerate one-cell control plane runs the identical code path (the same `discover`/`place`/`placement_of`/`residency_verify` over a one-row registry); the two lints run as Myelin CI jobs and fail a fixture commit that introduces a PII column / an out-of-region write (the ratchet still bites on the platform's own code). CDC: n/a (this is an integration/dogfood exercise of the existing contract surface). Drill: `residency_verify` green on the team's tenant; the truth-up pass over the Tenancy gate rows (every PROVEN row → a dated green artifact). No new mutation floor (the mutation gate now runs as a Myelin CI job on every commit per the dogfood loop).
- **DEFINITION OF DONE.** The Myelin team's tenant is placed on the degenerate one-cell self-host control plane running the identical code path; the `residency-pin` + `control-plane-pii-free` lints run green as Myelin CI jobs on every commit; `residency_verify` is green on the platform's own data; the truth-up pass confirms no later-band CP gate is red (the gate invariant holds end-to-end); all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M6: Myelin self-hosts as exactly one cell + the Tenancy lints as Myelin CI`; body lists the team tenant placed on the degenerate self-host cell, the two lints green as Myelin CI jobs, `residency_verify` green on the platform's own data, the truth-up pass confirming 0 red earlier Tenancy gates. Co-Authored-By trailer.

---

## Coverage check (this file → the Tenancy roadmap, no milestone dropped)

| Roadmap milestone | Prompt(s) | Band | Gate(s) proven |
|---|---|---|---|
| CP-M0 (partition-key crate 12.1 + CrossCellPointer frame 12.6) | P-CP-01 | M0 | compile + frame-shape doctest |
| CP-M0 (the two lints `residency-pin` + `control-plane-pii-free`, red+green fixtures) | P-CP-02 | M0 | CP-D1 + CP-D3 lint legs; twelve-lints-green |
| CP-M1 (PII-free registry + `discover`/`place`/`placement_of` + two-phase signup + placement invariant) | P-CP-03 | M1 | CP-D2; CP-D1 registry leg |
| CP-M1 (`residency_verify` M1 stores + Pool isolation tier + provision-gating) | P-CP-04 | M1 | CP-D6; residency_verify smoke |
| CP-M1 (four-layer region-pinning + self-host parity + CP-outage blast-radius) | P-CP-05 | M1 | CP-D3 + CP-D2 e2e + STOR-D5 + CP-D4 (the M1→M2 go/no-go) |
| CP-M3 (`placement_of(repo)` live C-1 + outbound-mirror gate C-4) | P-CP-06 | M3 | GIT residency leg + the C-4 mirror drill |
| CP-M4 (`residency_verify` CI-store coverage C-2 + residency-pinned runners) | P-CP-07 | M4 | CI-R3 |
| CP-M5 (multi-cell bridge live 12.6 + cross-cell DSR fan-out + cell bulkhead 30× surge) | P-CP-08 | M5 | CP-D8 + GA-D8 + CP-D5 + the E2E wedge |
| CP-M5 (live migration + repo relocation + durable provisioning + measured sizing + restore-verify at cell scale) | P-CP-09 | M5 | CP-D7 + STOR-D2-at-cell-scale |
| CP-M6 (Myelin self-hosts as one cell + the lints as Myelin CI) | P-CP-10 | M6 | residency_verify green on own data; truth-up pass |

**Floor → follow-on pairs (each visible, never invisible — EI-04 §4):** single-cell (P-CP-03) → multi-cell bridge
live (P-CP-08); avoid-migration-by-sizing (P-CP-03) → live tenant migration / repo relocation (P-CP-09); scripted
provisioning (P-CP-04) → durable-workflow provisioning (P-CP-09); `residency_verify` over M1 stores (P-CP-04) →
CI-store coverage (P-CP-07); frozen-not-live `CrossCellPointer` frame (P-CP-01) → bridge resolution live (P-CP-08);
default-deny mirror gate (P-CP-06) → counsel-ratified `transfer_allowed` entries (parallel, legal — named, not an
engineering gate); Pool isolation tier (P-CP-04) → Bridge + Dedicated tiers (on demand); CP-lookup+cache discovery
(P-CP-03) → GeoDNS/anycast edge (post-M5, infra — named, not an engineering gate). Every `[OPEN — LEGAL]` residual
(region-change-as-DSR, slug PII screening, bridge residency proof, mirror lawful basis) ships its structural floor
regardless of ratification and is named in the owning prompt.

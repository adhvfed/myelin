# Phase 7 — Implementation Prompts: Storage (tiers · KMS · crypto-shred · backup/restore-verification)

> Phase: `07-prompts/by-system`. The complete ordered set of clean-context, independently-committable coding
> prompts that operationalize the entire **storage** shared-system roadmap
> ([`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md)) into build tasks. Authored to
> the ledger template ([`../00-ledger-overview.md`](../00-ledger-overview.md) §2), in this system's own band
> order (M0 → M6). The consolidated index (`01-ledger-index.md`, Phase 7-B) interleaves these into the single
> global `P-<NNN>` sequence; the `P-<NNN>` ids below are this file's **local placeholders** (`P-ST-01..P-ST-16`)
> — the index reassigns stable global ordinals and rewrites DEPENDS-ON to global ids. Plain-text identifiers
> (no backticks-as-emphasis). Markdown only; no git commits by this document or its author.
>
> **Coverage (this file → roadmap):** every Storage milestone — S-M0 (OLTP tier client + the outbox-bearing
> store + RLS, the fs-backed BlobStore + content-addressed trait, the forward-only-migration + residency-pin
> lints, the migration runner), S-M1 (the keystone band: KMS hierarchy + KeyOrigin, crypto-shred + GD-4
> granularity, OLTP envelope encryption, the backup/restore/cross-seam + restore-verify durability gate, the
> OLAP frame + reserve/settle, the structural GDPR floor), S-M2 (OLAP fed by the bus, reserve/settle fronts
> agent runs, the T3 firehose-archive seam), S-M3 (local-disk git packs behind the trait, the within-EU CDN
> clone class C3, the outbound-mirror residency seam C6, git crypto-shred reach), S-M4 (the CI log tier
> (job,step,byte-range) index C2, per-subject CI-log DEK C1, trust-scoped cache namespaces C4, the OLAP
> restriction-flag gate C5), S-M5 (object-store BlobStore, object-backed git packs, multi-cell migration, the
> full DSAR fan-out, restore-verify at cell scale + the F6 surge family), S-M6 (the restore-verify gate runs on
> Myelin's own commits) — each maps to ≥1 prompt below. Every named floor and its follow-on are paired: the
> fs-BlobStore floor (P-ST-02) → object-store BlobStore (P-ST-13); local-disk git packs (P-ST-09) → object-backed
> packs (P-ST-13); single-cell (P-ST-05) → multi-cell migration (P-ST-14); per-subject DEK for
> free-text/chat/profile (P-ST-04) → + CI inline-PII log segments (P-ST-11). Date: 2026-06-19.

---

## Canon every prompt in this file assumes (the shared reading set)

Each prompt re-states the precise subset it needs, but all assume the M0 substrate from
[`../00-ledger-overview.md`](../00-ledger-overview.md) §6: the Cargo workspace + the eight glue crates, the
`serve(AppSpec)` harness (three ports, holder auto-registration, ResilientClient, FailStatic, the protected-
human-lane shed order), the transactional outbox + the EventHandler/consumer template, the twelve committed
lints, the contract-coverage scanner, and the failure-injection harness (the 1x/10x/30x load generator + the
scoped-reversible dependency-break injector + the telemetry-assertion library reading contract 1.8). A prompt's
GATE/DRILLS rows are scenarios on that harness; a prompt's DEFINITION OF DONE requires all committed lints green
and the contract-coverage scanner passing.

**The implementation crate.** Storage's runtime code lands in a new workspace crate **myelin-storage** (the
tier clients, the KMS adapters, the BlobStore impls, the backup/restore machinery, the restore-verify drill
harness). The cross-crate trait shapes BlobStore and PersonalDataHolder are stubbed by the M0 substrate prompt
P-S01 (in myelin-gdpr per the substrate skeleton); myelin-storage implements them to that frozen shape — it
does not redesign the signatures (EI-01 §1, code-wins-over-docs: if reality forces a divergence, write it in the
crate doc comment and escalate as a whole-workspace contract change).

**The frozen Storage architecture** is
[`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md)
(sections cited per prompt); the frozen contracts are
[`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md)
§11 (rows 11.1–11.8) + the consumed rows 1.1/1.4/1.8, 2.2/2.3/2.4/2.6, 4.3, 10.1/10.2/10.5/10.8, 12.1/12.2/12.4/12.6;
the rationale is
[`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md)
(X-7 erasure posture, C1–C6 in §8/§10, OQ-D, OQ-H, OQ-I). The Storage drills are
[`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md)
rows STOR-D1..STOR-D8 + D-S11/D-S12/D-S13 (§4.2) + E2E-3/E2E-4 (§2/§4.4), with the strategy in
[`../../05-refined-shared-systems-architecture/testing-strategy/README.md`](../../05-refined-shared-systems-architecture/testing-strategy/README.md).
The band order + the gate invariant is
[`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) (§2 bands, §4 the two
permanent gates — STOR-D1/STOR-D2 is one of them).

**The single load-bearing fact this whole file sequences around:** Storage's restore-verify gate (11.5) is the
Tier-1 silent-data-loss floor on the master critical path's M1 node — it must be green **before any subsystem
writes real tenant data**, and it is one of the two permanent gates that re-run forever on every store-touching
change. STOR-D1/STOR-D2 (P-ST-06) gates the whole M1→M2 boundary; every later band re-confirms it.

---

### P-ST-01 — OLTP tier client: harness pool + (tenant,region) RLS guard + the outbox-bearing store

- **BAND.** M0.
- **ROADMAP MILESTONE.** S-M0 (OLTP tier client 11.1 partial — pool + RLS + the outbox table physically lives here) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M0".
- **DEPENDS-ON.** The M0 substrate prompts: P-S01 (workspace + glue crates + the BlobStore/PersonalDataHolder trait stubs), P-S06 (serve(AppSpec) + three ports + the bounded DB pool + the forward-only migration runner), P-S03 (the EventEnvelope + the outbox table shape + OutboxTx::emit + the relay), P-S04 (the EventHandler/consumer template). This is the first myelin-storage prompt; no intra-Storage predecessor.
- **CANON DOCS (read these first, in full, before writing any code).**
  - [`../../VISION.md`](../../VISION.md) §3 (world-scale + multi-tenant from day 1; GDPR-safe by construction; name-your-floors).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (order-by-non-negotiability — silent data loss and cross-tenant IDOR are stop-the-bleeding), §5 (the committed ratchet).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §1.1 (the non-negotiables every tier inherits — tenant is the first column/partition key/blob-prefix component; no cross-tenant query path; tenant from the verified token never the path; one DB per service; the cache is never a source of truth), §3.1 (Tier 1 OLTP: PostgreSQL-class, one database per service, the (tenant,region)-first RLS tenant-scoping guard = the IDOR floor and the tenant-predicate lint target, bounded pools + statement timeouts, the outbox lives here in the SAME transaction as the state change — the anchor of the §7.3 cross-seam consistency point), §2 (the store map T1 row).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.1 (OLTP tier client), 2.2/2.3 (OutboxTx::emit + the outbox table), 12.1 ((tenant,region) partition key), 1.1/1.4/1.8 (harness + holder auto-registration + telemetry).
- **DELIVERABLE (what to build + exactly where in the repo).** In crate myelin-storage: the **OLTP tier client** — a thin, harness-wired layer over the substrate's bounded Postgres pool that every subsystem opens through `serve(AppSpec)` (NOT a hand-rolled connection). (a) The **(tenant, region)-first RLS tenant-scoping guard**: a query layer where every statement against a tenant-owned table carries a `(tenant, region)` predicate sourced from the verified token (never the URL path); a tenant-less query against such a table does not compile (the `tenant-predicate` lint target — the red/green fixtures land in P-ST-03). (b) **Co-location of the outbox in the OLTP database**: the migration that creates the `outbox` table (frozen shape from 2.3 — `(event_id UNIQUE, aggregate, seq, subject, envelope)`, `UNIQUE(aggregate, seq)`) in the SAME service database so an outbox row commits in the same transaction as the state change (this co-location is what makes the §7.3 cross-seam cursor exist — assert in a test that the state write and the outbox insert share one transaction and either both commit or both roll back). (c) **Bounded pools + statement timeouts + per-tenant in-flight caps** read from config, validated at boot, fast-fail on saturation. (d) The store is auto-registered as a PersonalDataHolder via the harness (1.4) — the locate/export/rectify/restrict/erase bodies are the GDPR M1 deliverable; here the registration hook fires. **Floor named:** per-tenant ENVELOPE ENCRYPTION of columns is NOT yet wired — the KMS lands in M1, so columns are plaintext-at-rest on this floor; the M1 prompt P-ST-04 closes this gap; NO real tenant data is written before then (the M1 STOR-D1 gate enforces this). Record this in writing in the crate doc comment.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.1 (OLTP tier client — pool + RLS guard + outbox co-location, the at-rest-encryption half deferred to P-ST-04). Consumed/wired: 2.2/2.3 (the outbox emit path + table, co-located here), 12.1 ((tenant,region) partition key from the verified token), 1.1/1.4/1.8 (harness, holder auto-registration, telemetry).
- **GATE / DRILLS (quantified; must be green to call this done).** The outbox-bearing OLTP tier makes **SUB-D1** (kill between commit and publish → exactly-once-in-effect, 0 ghost / 0 lost) and **BUS-D4** (crash producer between state-commit and publish → emit-iff-committed) green for the OLTP/outbox half — these are master M0 exit gates; Storage owns the OLTP/outbox half. Telemetry: `outbox_depth → 0` after relay, `emit_iff_committed == true`. A scoped IDOR drill: a read whose token-tenant ≠ path-tenant resolves to the token-tenant, 0 path-derived tenants (the §1.1 IDOR floor). CI.
- **TESTS (required).** Unit: the state write + outbox insert share one transaction (both commit / both roll back under an injected mid-tx failure); a tenant-less query against a tenant table fails to compile (a compile-fixture; the lint enforces it externally in P-ST-03); a query carries the (tenant,region) predicate from the token. CDC: the provider+consumer pair for 11.1 (a subsystem-side consumer opening the OLTP client through the harness). Drill harness scenarios for SUB-D1 + BUS-D4 (using the P-S02 injector to kill between commit and publish). Mutation floor: the transactional co-commit path + the RLS predicate derivation are mandatory-core — state and meet a cargo-mutants mutation-score floor ≥ 80% on the OLTP-client module.
- **DEFINITION OF DONE.** The OLTP tier client opens through the harness with the (tenant,region) RLS guard; the outbox table is co-located in the service DB and co-commits with state; bounded pools + timeouts + per-tenant caps wired; the store auto-registers as a holder; SUB-D1 + BUS-D4 emit dated green artifacts for the OLTP/outbox half; the plaintext-at-rest floor is named with its follow-on (P-ST-04); CDC + unit tests + the mutation floor pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: OLTP tier client + (tenant,region) RLS + outbox co-location`; body lists 11.1 (partial) implemented, SUB-D1 (0 ghost/0 lost) + BUS-D4 (emit-iff-committed) with measured numbers, the named plaintext-at-rest floor → P-ST-04, the mutation score. Branch first if on default. Co-Authored-By trailer.

---

### P-ST-02 — BlobStore: the content-addressed trait + the fs-backed floor (BLAKE3 hash-on-write)

- **BAND.** M0.
- **ROADMAP MILESTONE.** S-M0 (BlobStore trait + fs-backed floor 11.2 floor — STOR-5: nothing is node-pinned by construction) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M0".
- **DEPENDS-ON.** P-S01 (the BlobStore trait shape stubbed in myelin-gdpr), P-ST-01 (the myelin-storage crate exists), P-S06 (the harness for holder registration).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the fs floor with the named object-store follow-on), §4 (Rust default; cite proven structures).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; observability is part of the pass — a corrupt blob must emit a detection signal), §7 (one primitive — the narrow trait, fs↔object is a one-line swap).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.2 (the narrow content-addressed put/get/head/delete trait — copy the trait byte-exact; BLAKE3-on-write with a self-describing multihash prefix so SHA-256 can coexist; address by plaintext hash WITHIN a tenant's keyspace, store ciphertext; dedup is per-tenant — cross-tenant dedup deliberately forgone as a residency leak; per-blob random content key wrapped by the tenant/per-subject DEK; immutable-tier erasure = crypto-shred not delete), §2 (the T2 row), §11 (the cited content-addressing prior art — Git/Venti/IPFS CID/BLAKE3).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 11.2 (the BlobStore surface; the C3/C4/object-backed-pack additions are later prompts — implement only the base trait + fs floor here).
- **DELIVERABLE.** In crate myelin-storage: implement the **BlobStore** trait (`put(&[u8]) -> ContentHash`, `get(&ContentHash) -> Vec<u8>`, `head(&ContentHash) -> BlobMeta`, `delete(&ContentHash) -> ()`) to the frozen §3.2 shape, with an **fs-backed implementation** so it runs from the first commit. (a) **BLAKE3 hash-on-write** with a self-describing multihash prefix (so SHA-256 blobs can coexist); the content address IS the hash (Git/Venti model). (b) **Address by plaintext hash within a tenant's keyspace, store ciphertext**: the key path is `<tenant>/...`; dedup is per-tenant only (a cross-tenant identical blob is stored twice — this is deliberate isolation, not a bug; assert it in a test). The per-blob random content-key wrapping is a stub here (the KMS lands M1, P-ST-04 wires the real wrap) — name this floor. (c) **Re-hash-on-read integrity**: `get` re-hashes the bytes and refuses to serve on a content-address mismatch (0 silent serve). (d) Holder-registered via the harness. **Floor named:** the fs-backed BlobStore is the floor; the **object-store (MinIO / Ceph RADOS) BlobStore** is the M5 follow-on (P-ST-13), a one-line backing swap by the trait's design (trigger: the measured single-node blob ceiling). Record this in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.2 (the base BlobStore trait + fs floor; the content-key wrap deferred to P-ST-04; C3/C4/object-backed-pack/object-store deferred to P-ST-09/P-ST-11/P-ST-13). Consumed: 1.4 (holder auto-registration).
- **GATE / DRILLS.** **STOR-D7 (blob integrity floor):** corrupt an fs-BlobStore object → re-hash-on-read detects the content-address mismatch; 0 silent serve. Telemetry: `blob_integrity_fail` increments on the corrupt read, the serve is refused (not a silent wrong-bytes return). CI — runnable the moment the trait exists.
- **TESTS (required).** Unit: put→get round-trips the exact bytes; the content address equals the BLAKE3 multihash; two tenants storing identical bytes get two stored objects (per-tenant dedup, no cross-tenant share); get on a corrupted object refuses to serve and signals blob_integrity_fail. CDC: the provider+consumer pair for 11.2 (a blob-holding service putting/getting through the trait). Drill: the STOR-D7 scenario on the harness (corrupt an object, assert detection + 0 silent serve). Mutation floor: the hash-on-write + re-hash-on-read integrity path is mandatory-core — state and meet the mutation-score floor on the BlobStore module.
- **DEFINITION OF DONE.** The BlobStore trait + fs floor exist to the frozen shape; BLAKE3 hash-on-write + per-tenant-keyspace + store-ciphertext + re-hash-on-read integrity work; STOR-D7 emits a dated green artifact (0 silent serve); the content-key-wrap floor (→ P-ST-04) and the object-store follow-on (→ P-ST-13) are named in writing; CDC + unit tests + mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: BlobStore content-addressed trait + fs-backed floor`; body lists 11.2 (base) implemented, STOR-D7 green (0 silent serve), the content-key-wrap floor → P-ST-04, the fs→object-store follow-on → P-ST-13. Co-Authored-By trailer.

---

### P-ST-03 — The two storage lints (forward-only-migration, residency-pin) + the migration runner, red+green fixtures

- **BAND.** M0.
- **ROADMAP MILESTONE.** S-M0 (the storage lints in the M0 ratchet 1.6 + the forward-only online migration runner) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M0".
- **DEPENDS-ON.** P-S05 (the contract-1.6 lint framework + the M0 lint harness), P-S06 (the forward-only migration runner mechanism + the hot-table declaration mechanism), P-ST-01 (the OLTP tier client the lints constrain).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — each lint ships a red-fixture proving it rejects + a green-fixture proving it admits; wired loud, never `... || true`; an uncommitted lint is no lint), §2 (residency leak + a migration outage are stop-the-bleeding).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.1 (forward-only online migrations — expand→backfill→contract; the hot-table flags each subsystem declares, KN `block`/`db_row`/`doc_op` + all high-write; lock-time measured against a restored copy once restore-verify exists in M1), §1.1 (residency-pinned every store; no cross-region query path), §2 ("migrations are forward-only and online").
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 1.6 (the twelve lints — forward-only-migration + residency-pin are the two Storage-relevant), row 1.5 (forward-only online migrations + hot-table flags), row 12.1/12.4 (residency).
- **DELIVERABLE.** (1) Wire the **forward-only-migration** lint into CI as a committed gate: the migration runner only admits expand→backfill→contract; a destructive/contract-before-backfill or a non-forward migration does not compile. Red-fixture = a migration that drops/rewrites a column in place (rejected); green-fixture = the same change as expand→backfill→contract (admitted). Declare the hot-table flags mechanism is honoured (a migration touching a declared hot table must use the online path). (2) Wire the **residency-pin** lint: no out-of-region write compiles. Red-fixture = a write whose region ≠ the tenant's pinned region (rejected); green-fixture = a region-pinned write (admitted). (3) The **forward-only online migration runner** itself (the expand→backfill→contract machinery in myelin-storage, building on the substrate runner mechanism) — lock-time is measured against a RESTORED copy once restore-verify exists in M1 (the full lock-budget drill STOR-D8 lands in P-ST-08; here the runner exists and admits only the online shape). Wire each lint loud-never-swallowed (no `|| true`).
- **CONTRACTS TO IMPLEMENT.** Realized: 1.6 (the two Storage-relevant lints), 1.5 (the forward-only online migration runner + hot-table declaration), constraining 11.1 (OLTP) + 12.1/12.4 (residency).
- **GATE / DRILLS.** Both lints green-with-both-fixtures: each red-fixture fails the build (the lint fires), each green-fixture passes (the lint admits) — committed CI gates from M0 that stay green forever. Quantified: 2/2 lints emit a green artifact on the green-fixtures and a captured-expected failure on the red-fixtures; they count toward "all twelve lints green" (master M0 exit).
- **TESTS (required).** Unit: the four fixtures (2 red + 2 green) ARE the tests — each asserts the lint's verdict; a unit test that the runner rejects a contract-before-backfill ordering. CDC: n/a (lints + a runner, not a contract RPC; the runner is exercised by the migration tests). No separate mutation floor (the runner's correctness is the migration-safety drill STOR-D8 in P-ST-08).
- **DEFINITION OF DONE.** The two lints are wired into CI loud-never-swallowed; all four fixtures present and asserting the correct verdict; the forward-only online migration runner exists and admits only expand→backfill→contract; all twelve committed lints green on the workspace; coverage scanner green; no threshold weakened (a lint is never softened to admit a red-fixture). Committed.
- **COMMIT.** Header `P-<NNN> M0: storage lints (forward-only-migration, residency-pin) + migration runner`; body lists the two lints with their red+green verdicts and the online migration runner. Co-Authored-By trailer.

---

### P-ST-04 — The KMS hierarchy + KeyOrigin trait + per-tenant/per-subject DEKs + OLTP/blob envelope encryption

- **BAND.** M1.
- **ROADMAP MILESTONE.** S-M1 (KMS hierarchy 11.3 + KeyOrigin trait + OLTP envelope encryption completing 11.1 + per-subject DEK wiring of 11.4) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M1".
- **DEPENDS-ON.** P-ST-01 (the OLTP tier client whose columns this encrypts), P-ST-02 (the BlobStore whose per-blob content keys this wraps), the GDPR M1 prompts that ship 10.2 (`#[personal_data(... erasure ...)]` classify-derive — the per-subject-vs-per-tenant key choice driver) and 10.1 (PersonalDataHolder), co-built in this band.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (GDPR-safe & EU-sovereign by construction — residency + erasability are architectural constraints).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (silent data loss outranks every feature — KMS hard-down must fail-static, never fail-open), §3 (prove-it).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (crypto-shred substrate, erasure-vs-immutability — crypto-shred is destroy-a-key not byte-mutation).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §4 (the three-level envelope-encryption hierarchy — L0 per-cell root HSM/sealed never exported; L1 per-(tenant,region) KEK = the tenant-granularity crypto-shred lever; L2 DEKs AES-256-GCM, per-tenant for bulk, per-subject for the individual-erasure classes; the `pii_key_ref = kms://<tenant>/<dek-epoch>/<class>` travels with every ciphertext — copy the canonical shape byte-exact; key rotation = envelope re-wrap O(keys) not O(data); KMS availability degrades fail-static, hard-down → not-ready never fail-open; a crypto-shredded key is excluded from backup), §6 (the KeyOrigin trait — platform-managed | BYOK | HYOK behind one trait; `can_derive_plaintext_index()=false` is the STRUCTURAL HYOK enforcement — copy the trait byte-exact), §3.1 (the OLTP envelope-encryption seam — personal-data columns under the tenant DEK, free-text/profile columns under a per-subject sub-key), §5.1 (the GD-4 decision rule — `classify(field)` drives the key choice automatically).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.3 (KMS + KeyOrigin), 11.4 (crypto-shred + GD-4 granularity — the per-subject/per-tenant rule), 11.1 (the at-rest-encryption half deferred from P-ST-01), 10.2 (the classify-derive that drives the key choice).
- **DELIVERABLE.** In crate myelin-storage: (1) the **three-level KMS hierarchy** — L0 per-cell root (HSM/sealed, never exported, the most protected key), L1 per-(tenant,region) KEK (destroying it = tenant-granularity crypto-shred, the tenant-offboard lever), L2 DEKs (AES-256-GCM; per-tenant for bulk, per-subject for the free-text/profile/chat-body/agent-memory classes). The `pii_key_ref = kms://<tenant>/<dek-epoch>/<class>`, `<class> ∈ {tenant, subject:<id>, blob}` travels with every ciphertext (in the OLTP row, the blob metadata, the envelope). Key rotation = envelope re-wrap (O(keys), not O(data)); forward-only; a compromised key triggers expand→backfill re-encryption never a rollback. Wire a self-hostable engine (Vault-Transit-class) behind an adapter; KMS availability degrades fail-static (resolved-DEK reads survive a bounded TTL), hard-down → not-ready + shed, NEVER fail-open. (2) the **KeyOrigin trait** (`wrap`, `unwrap`, `can_derive_plaintext_index`, `destroy`) — platform-managed | BYOK | HYOK behind one trait; `can_derive_plaintext_index()=false` is the structural HYOK enforcement Search/Agent must consult. (3) **Wire OLTP + blob envelope encryption**: the personal-data columns of P-ST-01's tables now encrypt under the tenant DEK, free-text/profile columns under a per-subject sub-key — closing the plaintext-at-rest floor P-ST-01 named; the BlobStore per-blob content key from P-ST-02 now wraps under the tenant/per-subject DEK. (4) Wire `classify(field)` (10.2) → key choice: a column tagged `personal-data, erasure=subject` is auto-wired to a per-subject DEK by the harness. **Floor named:** the KMS MECHANISM + the limits are built; the per-content-class HYOK POLICY + the KMIP/external-key-store adapter are `[OPEN → P6/LEGAL]` named follow-ons (parallel-legal) — record in writing. **Floor named:** per-subject DEK covers free-text/profile/chat-body/agent-memory now; the CI inline-PII log segment extension (C1) is the named M4 follow-on (P-ST-11).
- **CONTRACTS TO IMPLEMENT.** Owns: 11.3 (KMS hierarchy + KeyOrigin trait, full), 11.4 (crypto-shred + GD-4 granularity — the per-subject/per-tenant rule + the classify-driven key choice; the erase algorithm itself is P-ST-05), 11.1 (the at-rest envelope-encryption half, completing P-ST-01). Consumed: 10.2 (classify-derive), 10.1 (holder).
- **GATE / DRILLS.** **STOR-D6:** transient KMS outage → resolved-DEK reads survive (bounded TTL); hard-down → not-ready + shed, NEVER fail-open; 0 plaintext-without-key. Telemetry: `fail_static` ratio on the bounded-TTL survival, `0 fail-open` (assert no read returns plaintext when the key is unavailable). A scoped HYOK check: a class marked HYOK reports `can_derive_plaintext_index()=false` (the full Search/Agent skip drill D-S10 lands with Search/Agent). CI. The no-untagged-personal-data lint stays green (a deliberately-untagged PII scratch field fires the lint, then remove it).
- **TESTS (required).** Unit: wrap→unwrap round-trips a DEK under a KEK; destroy(KEK) renders every DEK under it unrecoverable; a per-subject DEK is distinct from the tenant DEK; the pii_key_ref encodes (tenant, dek-epoch, class) exactly; rotation re-wraps without re-encrypting payload; an unavailable KMS yields not-ready + shed, never a plaintext read; `can_derive_plaintext_index()` is false for HYOK, true for platform/BYOK; classify(field=erasure:subject) routes to a per-subject DEK. CDC: provider+consumer pair for 11.3 (an encrypted-store caller wrapping/unwrapping) and 11.4 (the classify-driven key-choice caller). Drill: the STOR-D6 scenario on the harness (inject KMS outage → fail-static then not-ready, 0 fail-open). Mutation floor: the wrap/unwrap/destroy path + the fail-open-prevention branch are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The three-level KMS hierarchy + the KeyOrigin trait exist to the frozen shape; OLTP + blob envelope encryption is wired (the P-ST-01 plaintext floor closed); classify(field) drives the per-subject/per-tenant key choice; STOR-D6 emits a dated green artifact (0 fail-open, 0 plaintext-without-key); the HYOK-policy/KMIP follow-on and the C1 CI-log-DEK follow-on (→ P-ST-11) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: KMS hierarchy + KeyOrigin + per-tenant/per-subject DEKs + envelope encryption`; body lists 11.3/11.4(rule)/11.1(encryption-half) implemented, STOR-D6 green (0 fail-open), the HYOK-policy/KMIP follow-on and the C1 follow-on → P-ST-11, the mutation score. Co-Authored-By trailer.

---

### P-ST-05 — The crypto-shred erase algorithm + GD-4 granularity + the structural GDPR floor

- **BAND.** M1.
- **ROADMAP MILESTONE.** S-M1 (crypto-shred + GD-4 granularity 11.4 structural floor + the erase(subject,tenant) algorithm + the structural GDPR floor X-7) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M1".
- **DEPENDS-ON.** P-ST-04 (the KMS + per-subject DEKs to destroy), the GDPR M1 prompts that ship 10.1 (PersonalDataHolder erase) + 10.8 (the erasure ledger) + the pseudonym-map (Id 4.8 erase), co-built in this band.
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (crypto-shred substrate; erasure-vs-immutability; crypto-shred reaches backups by construction).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; a property does not exist until a test forces the failure).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §5.1 (the GD-4 decision rule table — free-text/profile/chat-body/CI-inline-PII/agent-memory = per-subject DEK; bulk pseudonym-referenced = per-tenant DEK; tenant offboard = the KEK; the rule is the measured-minimum granularity), §5.2 (the erase(subject, tenant) algorithm — pseudonym-map shred → KMS.destroy(per_subject_DEK) → Search purge+reindex → Refs tombstone → Bus erase → erasure receipt; copy the six steps exactly; crypto-shred reaches backups by construction; the search index is the plaintext-derived exception = purge+reindex; reach is verified not assumed), §5.3 (the free-text/immutable residual handled BY REFERENCE to the platform posture 10.9/X-7 — Storage contributes its reach, it does NOT author a Storage-local residual statement).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.4 (crypto-shred + GD-4), 10.1 (PersonalDataHolder erase), 10.8 (erasure ledger), 10.9 (the ONE platform erasure posture — by reference).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) §X-7 (the one erasure posture — instantiated per subsystem by reference, not restated).
- **DELIVERABLE.** In crate myelin-storage: (1) the **`erase(subject, tenant)` algorithm** as the storage-side mechanism behind the DSR orchestrator — the six steps exactly: pseudonym-map shred (calls Id.erase), `KMS.destroy(per_subject_DEK(tenant, subject))` (crypto-shred the free-text/chat/profile/agent-memory ciphertext, live AND in backups — by construction, the backup holds ciphertext under the now-destroyed key), trigger Search purge+reindex (the plaintext-derived exception), trigger Refs tombstone, trigger Bus erase (inline-PII event keys + `*.erased` tombstones), record the erasure receipt to the audit/erasure-ledger holder. (2) The **GD-4 granularity wiring** complete: the per-subject DEK classes (free-text/profile/chat-body/agent-memory — the CI inline-PII log class is named for P-ST-11) and the per-tenant DEK bulk classes, driven by the `classify` tag. (3) The **structural GDPR floor** (with GDPR): per-subject DEK crypto-shred + pseudonym-map shred reach + crypto-shred-reaches-backups-by-construction — the structural half of X-7; the residual posture is handled BY REFERENCE (10.9) — Storage does NOT author a local residual statement; it contributes its structural reach (crypto-shred into backups/reflogs/bitmaps; the history-rewrite crypto-shred half is the Git M3 reach, P-ST-10). **Floor named:** the residual lawful-basis is `[OPEN → P6/LEGAL]` (counsel/DPO ratifies once, for all five subsystems) — the structural floor ships regardless; record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.4 (the erase algorithm + GD-4 granularity, full structural floor). Consumed: 10.1 (PersonalDataHolder erase), 10.8 (erasure ledger receipt), 10.9 (the platform erasure posture, by reference), Id 4.8 (pseudonym shred).
- **GATE / DRILLS.** **STOR-D4:** erase a subject, attempt recovery from backups → per-subject ciphertext unrecoverable (key destroyed + excluded from backup); 0 recoverable PII in any backup. Telemetry: `crypto_shred_lag`, `0 recoverable PII`. The full cross-holder reach (search/OLAP/refs/bus/agent-memory/CDN/backups) is the GA/E2E-4 drill in M5 (P-ST-15); here the per-subject-DEK-destroy unrecoverability is proven. SCHED — the GA-D5 sibling. The full STOR-D3 post-restore re-erasure is greened in P-ST-06 (it needs the restore machinery).
- **TESTS (required).** Unit: the erase algorithm runs the six steps in order and is idempotent (re-erasing an already-erased subject is a no-op, not an error); KMS.destroy renders the per-subject ciphertext unrecoverable (decrypt fails after destroy); the GD-4 rule routes each class to the correct granularity; the search step is purge+reindex (not key-destroy); an erasure receipt is recorded. CDC: provider+consumer pair for 11.4 (the DSR orchestrator calling erase). Drill: the STOR-D4 scenario on the harness (erase → attempt backup recovery → 0 recoverable). Mutation floor: the erase algorithm + the KMS-destroy-reaches-backups path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The erase(subject,tenant) algorithm runs the six steps + is idempotent; GD-4 granularity is wired complete; the structural GDPR floor (per-subject shred + pseudonym reach + backups-by-construction) holds; the residual is handled BY REFERENCE (10.9), no Storage-local residual statement; STOR-D4 emits a dated green artifact (0 recoverable PII in backups); the `[OPEN → P6/LEGAL]` residual-basis follow-on is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: crypto-shred erase algorithm + GD-4 granularity + structural GDPR floor`; body lists 11.4 (full structural floor) implemented, STOR-D4 green (0 recoverable PII in backups), the residual handled by reference (X-7), the `[OPEN → P6/LEGAL]` follow-on, the mutation score. Co-Authored-By trailer.

---

### P-ST-06 — THE HEADLINE: backup/restore + the cross-seam consistency point + the CI-wired restore-verify gate + post-restore re-erasure

- **BAND.** M1.
- **ROADMAP MILESTONE.** S-M1 (backup/restore/cross-seam + restore-verify durability gate 11.5 — the silent-data-loss floor + post-restore re-erasure §7.5) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M1" (THE HEADLINE).
- **DEPENDS-ON.** P-ST-01 (the OLTP tier + the outbox seq = the cross-seam cursor), P-ST-02 (the BlobStore for blob-hash presence verification), P-ST-04 (the KMS, so restore restores tenant KEKs except crypto-shredded ones), P-ST-05 (the erase algorithm, re-applied in re-erasure), the Bus M0 reindex-from-source `*.snapshot` re-emit (2.6), the GDPR erasure ledger (10.8) — co-built in this band.
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (world-scalable from day 1; quality over plan-adherence).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 in full (prove-it: a backup that has never been restored is not a backup; gates resolve to quantified thresholds RPO/RTO; never weaken a threshold or invert an assertion to make a check pass; observability is part of the pass condition; build the failure-injection harness early; every real incident ends by adding a drill), §2 (silent data loss outranks every feature — this is THE Tier-1 floor).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §5 (reindex-from-source — never restore a derived store from its own backup).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §7 in full (§7.1 continuous WAL archiving + base backups → PITR, RPO target ≤ 5 min; object tier versioned + in-region replicated; OLAP + caches + derived indexes NOT backed up — rebuilt; KMS keys backed up only under the cell root while the tenant is live, a crypto-shredded key excluded from backup; RTO target ≤ 1h/tenant ≤ 4h/cell; §7.3 the cross-seam consistency point = the per-aggregate outbox seq / event-log offset — restore OLTP to the WAL position whose outbox rows have seq ≤ T, verify every referenced ContentHash is present, reindex derived stores FROM SOURCE to offset T never from their own backups, restore tenant KEKs except crypto-shredded; §7.4 the CI-wired restore-verify gate — spin a clean target, restore T1/T2/T5, reindex T4/Search/Refs from source, assert no-loss + cross-seam + erasure-held, green artifact on pass or fail CI; §7.5 post-restore re-erasure GD-14 against the erasure ledger — re-apply every erasure completed after the backup's PIT, assert 0 resurrected; §7.6 the backup-window-vs-erasure-SLA residual, `[OPEN → LEGAL]`).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.5 (backup/restore/cross-seam + restore-verify + post_restore_reerase), 2.3 (the outbox seq = the cursor), 2.6 (reindex-from-source), 10.8 (the erasure ledger driving re-erasure).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D1/STOR-D2/STOR-D3 (§4.2), the F3 family (§4.1).
- **DELIVERABLE.** In crate myelin-storage: (1) **continuous archiving + base backups + PITR** — continuous WAL archiving + periodic base backups for the OLTP tier (T1); the object tier (T2) versioned + in-region replicated (content-addressing makes integrity re-hash-verifiable); the log tier (T3) sealed segments are immutable T2 blobs + the range index in T1. (2) **`restore(to_offset T)` to the cross-seam consistency point** — the cross-seam linearisation cursor is the per-aggregate outbox `seq` / event-log offset (the outbox row commits in the same OLTP tx as the state change, so OLTP commit order == event order, established in P-ST-01): PITR-restore OLTP to the WAL position whose outbox rows have `seq ≤ T`; verify every `ContentHash` referenced by restored rows is present in T2 (a referenced-but-missing hash = FAIL, the silent-corruption case); **reindex derived stores FROM SOURCE up to offset T** through the live consumer path (never restore them from their own backups → derived == source by construction); consumers resume at T; restore tenant KEKs EXCEPT any crypto-shredded since the backup. (3) **The CI-wired restore-verify gate** — a CI job: spin a clean target, restore T1/T2/T5, reindex T4/Search/Refs from source to T, assert no-loss (checksum parity) + cross-seam (every restored row's blob hash present + integrity-verified; derived == source-replay) + erasure-held; emit a dated GREEN ARTIFACT on pass, FAIL CI on red. This also produces the production-scale restored copy that online migrations (STOR-D8, P-ST-08) rehearse lock-time against. (4) **Post-restore re-erasure (§7.5)** — every restore runs a mandatory re-erasure pass against the erasure ledger (10.8): for each erasure completed AFTER the backup's PIT, re-apply it (re-destroy the per-subject DEK, re-delete the pseudonym map, re-purge+reindex Search, re-tombstone Refs, re-emit `*.erased`); assert 0 resurrected subjects. **Floor named:** the RPO/RTO numbers (≤ 5 min / ≤ 1h-tenant / ≤ 4h-cell) are the proposed defaults-to-beat measured + set by STOR-D2 here and re-confirmed at cell scale in M5 (P-ST-15); the §7.6 backup-window residual number is `[OPEN → LEGAL]`. Record in writing. **This is one of the two permanent gates (master §4): the restore-verify CI job re-runs on every change touching a store, forever** — wire it so it is loud-never-swallowed.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.5 (backup/restore/cross-seam + restore-verify + post_restore_reerase, full). Consumed: 2.3 (the outbox seq cursor), 2.6 (reindex-from-source for derived rebuild), 10.8 (the erasure ledger for re-erasure), 11.3 (KMS for KEK restore-except-shredded).
- **GATE / DRILLS.** **STOR-D1 (the headline):** rebuild from backups to offset T → 0 loss (checksum parity); OLTP↔blob↔index↔offset at ONE mutually consistent point; 0 dangling refs; cold == live. Telemetry: `restore_verify_pass`, 0 dangling. **STOR-D2:** kill a cell, restore → RPO ≤ 5 min (WAL tail), RTO ≤ 1h/tenant, ≤ 4h/cell. Telemetry: `backup_rpo_seconds`, restore-time. **STOR-D3 (with GA):** erase a subject, restore an OLDER backup → still erased (post-restore re-erasure ran); 0 resurrected. Telemetry: re-erasure receipt, 0 resurrected. All SCHED. **This is the silent-data-loss floor — M2 does not start over a red STOR-D1; STOR-D1/STOR-D2 become the permanent gate.** Never weaken a threshold to pass; a red gate becomes a dated "claimed, not proven" thresholds-file row.
- **TESTS (required).** Unit: restore-to-T lands OLTP at the WAL position whose outbox seq ≤ T; a referenced-but-missing ContentHash makes restore-verify FAIL (not silently pass); a derived store is rebuilt from source not its own backup (there is no backup-restore path for T4/T7); a KEK crypto-shredded since the backup is NOT restored; re-erasure re-applies an erasure completed after the PIT. CDC: provider+consumer pair for 11.5 (the ops/DSR + the CI durability gate callers). Drill harness scenarios for STOR-D1 (rebuild to T, assert 0 loss + cross-seam), STOR-D2 (kill cell, restore, assert RPO/RTO), STOR-D3 (restore older backup, assert 0 resurrected). Mutation floor: the cross-seam-consistency-point logic + the referenced-hash-presence check + the re-erasure pass are mandatory-core — state and meet a mutation-score floor ≥ 85% on the restore module (this is the silent-data-loss floor — the highest bar).
- **DEFINITION OF DONE.** Continuous archiving + PITR + restore(to_offset T) to the cross-seam point + the CI-wired restore-verify gate + post-restore re-erasure exist; STOR-D1 (0 loss, one consistent point, cold==live), STOR-D2 (RPO ≤ 5min / RTO targets met), STOR-D3 (0 resurrected) each emit a dated green artifact with MEASURED numbers; the restore-verify CI job is committed loud-never-swallowed as the permanent gate; the RPO/RTO-measured + §7.6 residual follow-ons are named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. No threshold weakened. Committed.
- **COMMIT.** Header `P-<NNN> M1: backup/restore + cross-seam consistency + restore-verify gate + post-restore re-erasure`; body lists 11.5 (full) implemented, STOR-D1/STOR-D2/STOR-D3 green with measured RPO/RTO + 0 loss + 0 resurrected, the permanent-gate wiring, the measured-number + §7.6 follow-ons, the mutation score. Co-Authored-By trailer.

---

### P-ST-07 — Residency pinning enforced end-to-end + the reserve/settle cost ledger + the OLAP frame

- **BAND.** M1.
- **ROADMAP MILESTONE.** S-M1 (residency enforcement STOR-D5 + reserve/settle cost gate 11.7 mechanism + ledger + OLAP read store frame 11.6 partial) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M1".
- **DEPENDS-ON.** P-ST-01 (the OLTP tier the ledger lives in + the RLS guard), P-ST-03 (the residency-pin lint), P-ST-04 (the KMS for the OLAP holder), the control plane M1 prompts that ship 12.2/12.4 (`placement_of` / `residency_verify`) and 12.1 (the (tenant,region) partition key), co-built in this band; the Commercial wallet (for reserve/settle co-ownership).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (GDPR-safe & EU-sovereign by construction — data residency is an architectural constraint).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; observability), §7 (reconcile cross-component contracts — integer minor-units, wholesale ≠ markup, agreed units up front).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §1.1 (every store residency-pinned; no cross-region query path), §3.4 (the OLAP read store — ClickHouse-class CQRS read model fed ASYNC off the durable event stream via the idempotent consumer, NEVER by scanning OLTP; reindex-from-source the ONLY rebuild path; a holder; residency-pinned; the C5 restriction-flag gate is NAMED but lights up with Issues in M4), §9 (the reserve/settle cost gate row 11.7 — reserve at dispatch / settle on completion / never interrupt in-flight, integer minor-units, wholesale ≠ markup, fronts every agent run + every CI run; Storage holds the durable ledger), the units block (integer minor-units).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.7 (reserve/settle), 11.6 (OLAP read store frame), 12.2/12.4 (placement_of / residency_verify), 12.1 ((tenant,region) partition).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row STOR-D5 (§4.2).
- **DELIVERABLE.** In crate myelin-storage: (1) **Residency pinning enforced end-to-end** — every store reports its tenant's region into `residency_verify` (12.4); the (tenant,region) partition key (12.1) + the residency-pin lint (P-ST-03) make a cross-region read/replicate impossible by construction. Wire the `myelin storage residency verify <tenant>` admin path that proves region pinning (the CDN edge set + mirror targets are added in P-ST-09/P-ST-10). (2) **The reserve/settle cost gate mechanism + the durable per-tenant ledger** (11.7) — reserve-at-dispatch (no balance → no run), settle-on-completion, NEVER interrupt in-flight; integer minor-units; wholesale ≠ markup; the durable ledger lives in Storage's OLTP tier (one cost event per metered unit). The gate is exercised the moment agent runs (M2, P-ST-08) and CI runs (M4) exist — co-owned with the Agent gate + the Commercial wallet; Storage owns the durable ledger correctness. (3) **The OLAP read store frame (11.6 partial)** — the holder registration + the CQRS-fed-by-the-bus contract shape (the consumer template the OLAP store will use; it is NOT yet fed by a live stream — that is P-ST-08); residency-pinned + crypto-shred-capable (one tenant's OLAP rows live in that tenant's cell, not a global warehouse); reindex-from-source wired as the ONLY rebuild path (no "read OLTP into ClickHouse" backdoor). **Floor named:** the C5 restriction-flag gate is NAMED but lights up with Issues analytics in M4 (P-ST-12); worklog analytics-eligibility is `[OPEN → LEGAL]` (OQ-H). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.7 (reserve/settle mechanism + ledger — the durable-ledger half; the gate fronts agent runs in P-ST-08, CI runs in M4), 11.6 (OLAP frame + holder + CQRS contract shape — the C5 gate deferred to P-ST-12). Consumed: 12.2/12.4 (placement_of / residency_verify), 12.1 (partition key).
- **GATE / DRILLS.** **STOR-D5:** read/replicate a tenant's data outside its region → impossible by construction (the residency-pin lint rejects out-of-region writes; the partition key carries the region); 0 cross-region PII egress. Telemetry: `residency_verify` attestation, 0 egress. SCHED — the CP-D3 storage face. Reserve/settle: the ledger records one cost event per metered unit and never interrupts an in-flight run (the full agent-run drill is P-ST-08); assert the reserve-at-dispatch/settle-on-completion invariant on a synthetic run.
- **TESTS (required).** Unit: a cross-region write is rejected (the lint + partition key); residency_verify attests the tenant's single region; the reserve/settle ledger reserves at dispatch, refuses on no-balance, settles on completion, never interrupts in-flight; integer-minor-units arithmetic (no float); wholesale ≠ markup is recorded distinctly; the OLAP holder registers + the CQRS consumer shape exists; the OLAP store has no OLTP-scan backdoor. CDC: provider+consumer pairs for 11.7 (an agent/CI run-dispatcher) and 11.6 (the bus-feeding consumer). Drill: the STOR-D5 scenario on the harness (attempt cross-region read → 0 egress). Mutation floor: the reserve/settle ledger arithmetic (never-interrupt-in-flight, integer minor-units) is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** Residency is enforced end-to-end (STOR-D5 green, 0 cross-region egress, dated); the reserve/settle ledger reserves/settles/never-interrupts with integer minor-units; the OLAP frame + holder + CQRS shape exist with reindex-from-source as the only rebuild path; the C5-gate and OQ-H follow-ons are named (→ P-ST-12); CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: residency pinning + reserve/settle ledger + OLAP frame`; body lists 11.7(ledger)/11.6(frame) implemented + residency enforced, STOR-D5 green (0 cross-region egress), the C5/OQ-H follow-ons → P-ST-12, the mutation score. Co-Authored-By trailer.

---

### P-ST-08 — Online-migration safety on a restored copy + OLAP fed by the bus (reindex-from-source) + reserve/settle fronts agent runs

- **BAND.** M2.
- **ROADMAP MILESTONE.** S-M2 (OLAP fed by the bus 11.6 + reserve/settle fronts agent runs 11.7 + the T3 firehose-archive seam prep + STOR-D8 online migration on the restored copy) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M2".
- **DEPENDS-ON.** P-ST-06 (the restored prod-scale copy STOR-D8 rehearses against + the cross-seam point), P-ST-07 (the OLAP frame + the reserve/settle ledger), P-ST-03 (the migration runner), the Bus/KN M2 prompts that ship the consumer template + `*.snapshot` replay (2.4/2.6) and the firehose resume-cursor transport (3.5), the Agent fabric M2 (the first reserve/settle consumer).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; the failure-injection harness; observability), §4 (exercise the real thing — chain mutations; reindex-from-cold on a real stream).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §5 (reindex-from-source — the derived store rebuilds via the live consumer path only, no bespoke recovery reader).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.4 (the OLAP store fed async off the durable stream via the idempotent consumer dedup-on-event_id, never scanning OLTP; reindex-from-source the only rebuild path), §3.3 (the T3 log/firehose archive — Storage owns the DURABLE archive of the firehose not the live ephemeral fan-out; the durable bus carries only pointer events; the CI-specific `(job,step,byte-range)` index is built with CI in M4 but the sealing mechanism + per-tenant-DEK segment encryption are validated here on a non-CI firehose), §9 (reserve/settle fronts every agent run), §3.1 (forward-only online migration — lock-time measured against a RESTORED copy, now that restore-verify exists).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.6 (OLAP fed by the bus), 11.7 (reserve/settle fronting agent runs), 2.4/2.6 (consumer template + reindex-from-source), 3.5 (firehose resume-cursor transport).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D8 (§4.2), BUS-D5 / the F4 family (§4.1).
- **DELIVERABLE.** In crate myelin-storage: (1) **The OLAP read store fed by the bus** — the idempotent consumer (dedup on `event_id`) populating the ClickHouse-class CQRS read model off the durable stream, NEVER by scanning OLTP; reindex-from-source (2.6) wired as the ONLY rebuild path (no "read OLTP into ClickHouse" backdoor — assert there is no such code path). (2) **Reserve/settle fronts agent runs** — the gate (ledger from P-ST-07) now sits in front of every `AgentRuntime` run + every `SCHEDULE_AND_RUN_JOB`: reserve-at-dispatch → no balance → no run; settle on completion; never interrupt in-flight. (3) **The T3 firehose-archive seam (prep for 11.8)** — the durable archive of the firehose (sealed segments → T2 content-addressed blobs, inheriting T2 encryption + crypto-shred) specified against the M2 resume-cursor transport (3.5); validate the sealing mechanism + the per-tenant DEK segment encryption HERE on a non-CI firehose (e.g. a synthetic op-stream); the CI-specific `(job,step,byte-range)` index (C2) is built with CI in M4 (P-ST-11). (4) **Online-migration safety on the restored copy** — exercise expand→backfill→contract on the prod-scale RESTORED copy STOR-D6 produces, under load, measuring lock-time against budget. **Floor named:** no new Storage floor in M2; the carried floors (fs-BlobStore → P-ST-13, single-cell → P-ST-14) remain named.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.6 (OLAP fed by the bus — completing the frame), 11.7 (reserve/settle fronting agent runs — the live consumer half), the T3 archive seam (prep for 11.8). Consumed: 2.4/2.6 (consumer template + reindex-from-source), 3.5 (firehose resume-cursor transport).
- **GATE / DRILLS.** **The OLAP derived-store rebuild is reindex-from-source-only** — `reindex(scope)` rebuilds the read model byte-matching live; no bespoke recovery reader (the F4 family on the OLAP holder, the storage face of BUS-D5). Telemetry: reindex-parity hash matches. SCHED. **Reserve/settle never interrupts in-flight** — exercised by the agent-run drills (AG-D6/AG-D11 reserve refusals); Storage owns the ledger correctness (one cost event per metered unit). CI. **STOR-D8:** expand→backfill→contract on a restored prod-scale copy under load → no blocking lock beyond budget; 0 downtime. Telemetry: `lock-wait p99`, 0 downtime. SCHED — uses the restored copy STOR-D6 produces. **STOR-D1/STOR-D2 must remain green** (the permanent gate; any M2 store change re-runs them).
- **TESTS (required).** Unit: the OLAP consumer dedups on event_id; a reindex(scope) rebuilds the OLAP read model byte-matching live; there is no OLTP-scan path (a compile/structural assertion); reserve refuses on no-balance and never interrupts an in-flight run; a firehose segment seals to a content-addressed T2 blob under the per-tenant DEK. CDC: provider+consumer pairs for 11.6 (the bus-feeding + the analytics-reading sides) and 11.7 (the agent-run dispatcher). Drill harness scenarios for STOR-D8 (online migration on the restored copy under load) + the F4 OLAP reindex-parity. Mutation floor: the idempotent-consumer dedup + the reserve-never-interrupt path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The OLAP store is fed by the bus (reindex-from-source only, no OLTP backdoor); reserve/settle fronts agent runs (never interrupts in-flight); the T3 firehose-archive seam is validated on a non-CI firehose; STOR-D8 emits a dated green artifact (lock within budget, 0 downtime); the OLAP reindex-parity is green; STOR-D1/STOR-D2 remain green (re-run); the carried floors stay named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: OLAP fed by the bus + reserve/settle fronts agent runs + STOR-D8 online migration`; body lists 11.6/11.7(live) implemented + the T3 archive seam, STOR-D8 green (lock within budget, 0 downtime) + OLAP reindex-parity green + STOR-D1/D2 re-run green, the mutation score. Co-Authored-By trailer.

---

### P-ST-09 — Git storage floor: local-disk packs behind the BlobStore trait + the within-EU CDN clone/bundle class (C3)

- **BAND.** M3.
- **ROADMAP MILESTONE.** S-M3 (local-disk git packs behind the trait 11.2 STOR-5 floor + the within-EU CDN clone/bundle class C3) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M3".
- **DEPENDS-ON.** P-ST-02 (the BlobStore trait the packs are addressed through), P-ST-07 (residency_verify for the CDN edge set), the control plane prompts that ship repo-granular `placement_of` + `residency_verify` (12.2/12.4), the Git subsystem M3 (the consumer of the pack tier).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §3 (world-scale git storage — authoritative bytes on local disk first; the object-backed transition is a sequenced backing swap; never pin repos to a node).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it), §1 (name-your-floors).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.5 (the git object-backing seam STOR-5 — git packs + loose objects addressed THROUGH the BlobStore trait so local-disk → object-store-backed packs is a backing swap not a rewrite; relocatable placement, repo-granular placement_of, region-pinned NOT node-pinned; the object-backed pack impl is the M5/Git deliverable), §3.2 (the C3 within-EU CDN clone/bundle blob class — content-addressed like any T2 blob so an edge cache is a pure content-address cache, no staleness model; residency-respecting, the CDN edge set within-EU-only for an EU tenant; it rides the existing trait, a blob-class tag + an eligible-edge-set policy not a new store; residency_verify covers the CDN edge set).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 11.2 (the object-backed pack/delta seam + the within-EU CDN clone/bundle class), 12.2 (repo-granular relocatable placement_of), 12.4 (residency_verify covers the CDN edge set).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D5 (residency, extended), STOR-D7 (on git packs).
- **DELIVERABLE.** In crate myelin-storage (consumed by the Git subsystem): (1) **Local-disk git pack/object storage behind the BlobStore trait** — git packs + loose objects addressed through the trait (P-ST-02), with relocatable placement (repo-granular `placement_of`, region-pinned, NOT node-pinned, 12.2). **Floor named:** packs on local disk behind the trait is the floor; the **object-backed pack/delta management + smart-transport** is the M5 follow-on (P-ST-13), a backing swap by the trait's design (trigger: the single-node ceiling measured, GIT-D4). The relocatability constraint is DECIDED now; the impl is M5. (2) **The within-EU CDN clone/bundle blob class (C3)** — a content-addressed blob class over the BlobStore for hot-repo/clone-storm acceleration; residency-respecting (the CDN edge set is within-EU-only for an EU tenant; the content-address IS the cache-validity check — no staleness model to get wrong); the control plane's `residency_verify` covers the CDN edge set. It is a blob-class tag + an eligible-edge-set policy, NOT a new store — the base BlobStore is unchanged. (3) Add the CDN edge set + the (later) mirror targets to the `myelin storage residency verify` attestation. Record the local-disk-packs floor → object-backed-packs follow-on (P-ST-13) in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.2 (the local-disk git pack tier behind the trait + the C3 CDN clone/bundle class; the object-backed pack impl deferred to P-ST-13). Consumed: 12.2 (repo-granular relocatable placement_of), 12.4 (residency_verify covers the CDN edge set).
- **GATE / DRILLS.** **STOR-D5 (residency, extended):** the C3 CDN edge set is within-EU → 0 cross-region PII egress via the CDN. Telemetry: residency-attestation includes the CDN edge set, 0 egress. SCHED. **STOR-D7 on git packs:** corrupt a pack object → content-address re-hash detects + recovers from replica/backup; 0 silent serve. Telemetry: `blob_integrity_fail` on the corrupt pack, 0 silent serve. CI. **STOR-D1/STOR-D2 remain green** on the now-git-bearing stores (the permanent gate re-runs).
- **TESTS (required).** Unit: a git pack is addressed through the BlobStore trait (not a raw filesystem path the object-store swap cannot relocate); placement_of returns a repo's region-pinned but node-relocatable placement; a CDN clone bundle is content-addressed (the address IS the validity check); the CDN edge set for an EU tenant contains only within-EU edges; a corrupted pack object is detected on read. CDC: provider+consumer pair for 11.2 (the Git pack-tier consumer). Drill harness scenarios for STOR-D5 (CDN within-EU) + STOR-D7 (corrupt pack detection). Mutation floor: the trait-addressing of packs (relocatability) + the within-EU-edge-set filter are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** Git packs are addressed through the BlobStore trait (node-relocatable, region-pinned); the within-EU CDN clone/bundle class is content-addressed + residency-respecting; the residency attestation includes the CDN edge set; STOR-D5 (CDN within-EU, 0 egress) + STOR-D7 (pack integrity, 0 silent serve) emit dated green artifacts; STOR-D1/STOR-D2 remain green; the local-disk-packs → object-backed-packs follow-on (P-ST-13) is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M3: local-disk git packs behind the trait + within-EU CDN clone class (C3)`; body lists 11.2 (local-disk floor + C3) implemented, STOR-D5/STOR-D7 green on git packs + STOR-D1/D2 re-run green, the local-disk → object-backed-packs follow-on → P-ST-13, the mutation score. Co-Authored-By trailer.

---

### P-ST-10 — Git crypto-shred reach + the outbound push-mirror residency gate seam (C6)

- **BAND.** M3.
- **ROADMAP MILESTONE.** S-M3 (git crypto-shred reach into reflogs/bitmaps/pack-tier backups + the outbound-mirror residency gate seam C6) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M3".
- **DEPENDS-ON.** P-ST-05 (the crypto-shred erase algorithm), P-ST-09 (the git pack tier whose backups this reaches), P-ST-07 (residency_verify), the GDPR/CP prompts that ship `transfer_allowed` (10.5) + the pseudonym grammar/erase (Id 4.8), the Git subsystem M3 (the consumer of the push-mirror feature).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — pseudonymous-by-default commits never bake erasable PII; the git half of the crypto-shred posture).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §6 (C6 — the outbound push-mirror residency gate: a mirror targeting an extra-EU host for PII-bearing content is denied by default; the GATE lives at GDPR/Audit `transfer_allowed` 10.5 + the control plane, Storage's role is to keep mirror-source blobs content-addressed + encrypted and to REPORT the mirror target region into residency_verify — Storage flags the crossing, the control plane allows/denies), §5.3 (the structural reach — crypto-shred into reflogs/bitmaps/pack-tier backups via the per-tenant blob DEK; the commit-object-byte residual is the 10.9 posture; the pseudonym-map shred reach), §5.2 (the erase reach is verified not assumed).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.2/11.4 (the crypto-shred reach into git structures), 10.5 (`transfer_allowed` — the outbound-mirror gate, owned by GDPR/CP; Storage flags the crossing), 12.4 (residency_verify reflects the mirror target), 10.9 (the residual posture, by reference), 10.6 (the audited history-rewrite erasure path — the named on-demand follow-on).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows GIT-D2 (storage half), D-S13 (outbound-mirror residency deny, §4.2).
- **DELIVERABLE.** In crate myelin-storage: (1) **Crypto-shred reach into git's structures** — per-tenant blob DEK shreds reflogs, bitmaps, and pack-tier backups (those ARE shreddable via the per-tenant blob DEK); the commit-object-byte residual is the 10.9 posture (pseudonymous-by-default commits, X-7 — already decided before Git's data model froze). Wire the erase(subject) reach (P-ST-05) to include the git reflog/bitmap/pack-tier-backup shred. The audited history-rewrite erasure path (10.6, changed-hash consequence) is the NAMED on-demand follow-on (M5/on-demand). (2) **The outbound push-mirror residency gate seam (C6)** — Storage FLAGS the crossing: it keeps mirror-source blobs content-addressed + encrypted, and REPORTS the mirror target region into `residency_verify` (12.4) so the no-extra-EU-PII property is attestable; the actual allow/deny is the control-plane/`transfer_allowed` gate (10.5) — Storage does NOT author a local mirror policy. Wire the `mirror_residency_deny{tenant}` telemetry signal (must be 0 for an ungated extra-EU mirror). Lands as the Git push-mirror feature lands. **Floor named:** pseudonymous-by-default commits is the floor; the audited history-rewrite erasure path (10.6) is the on-demand M5 follow-on — record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (the storage half): the crypto-shred reach into reflogs/bitmaps/pack-tier-backups (11.2/11.4 reach), the C6 mirror-crossing flag + the residency_verify report. Consumed: 10.5 (`transfer_allowed` — the gate Storage flags into), 12.4 (residency_verify), 10.9 (the residual posture, by reference), Id 4.8 (pseudonym shred).
- **GATE / DRILLS.** **GIT-D2 (storage half):** erase a commit author → crypto-shred reaches backups/reflogs/bitmaps; residual == the ONE platform posture (pseudonymous-by-default). Telemetry: crypto-shred reach receipt, residual == documented posture. SCHED. **D-S13 (outbound-mirror residency deny, C6):** an extra-EU mirror target for an EU tenant's PII-bearing repo → deny-by-default (the gate at 10.5) + `residency_verify` reflects no extra-EU PII path. Telemetry: `mirror_residency_deny{tenant}`, 0 PII to an ungated extra-EU mirror. SCHED — gate at 10.5. **STOR-D1/STOR-D2 remain green.**
- **TESTS (required).** Unit: erase(subject) shreds the git reflog/bitmap/pack-tier-backup ciphertext under the per-tenant blob DEK; the commit-object-byte residual is left to the pseudonymous-by-default posture (not byte-mutated); a mirror config to an extra-EU host reports the crossing into residency_verify; the mirror_residency_deny signal fires for an ungated extra-EU mirror. CDC: provider+consumer pair for the C6 flag (Storage reports → control plane gates). Drill harness scenarios for GIT-D2 (storage half) + D-S13 (mirror deny). Mutation floor: the git-structure crypto-shred reach is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** Crypto-shred reaches git reflogs/bitmaps/pack-tier-backups; the commit-object residual is handled by the pseudonymous-by-default posture (by reference); the C6 mirror crossing is flagged into residency_verify (the deny lives at 10.5); GIT-D2 (storage half) + D-S13 emit dated green artifacts; STOR-D1/STOR-D2 remain green; the history-rewrite follow-on (10.6) is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M3: git crypto-shred reach + outbound-mirror residency gate seam (C6)`; body lists the git crypto-shred reach + the C6 flag implemented, GIT-D2 (storage half) + D-S13 green + STOR-D1/D2 re-run green, the history-rewrite follow-on (10.6), the mutation score. Co-Authored-By trailer.

---

### P-ST-11 — The CI log tier: the (job,step,byte-range) index (C2) + per-subject CI-log DEK (C1)

- **BAND.** M4.
- **ROADMAP MILESTONE.** S-M4 (T3 CI log tier 11.8 / C2 the (job,step,byte-range) index + per-subject CI-log DEK 11.4 / C1) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M4".
- **DEPENDS-ON.** P-ST-08 (the T3 firehose-archive seam + the resume-cursor transport this seals from), P-ST-04 (the KMS for per-subject DEKs), P-ST-05 (the erase algorithm extended to CI log segments), the CI subsystem M4 (the C1/C2 consumer; CI stamps trust + provides the `CheckStatus` seam 5.9).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §5 (event-volume seam — CI is the heaviest log producer; the durable archive carries pointer events, an agent is never woken per log line), §1 (per-subject crypto-shred granularity).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.3 (C2 — the CI log tier (job,step,byte-range) index frozen: firehose frames appended to a current segment, sealed segments flush to T2 as content-addressed blobs inheriting T2 encryption + crypto-shred; an OLTP byte-range index maps `(job, step, byte-range) → (segment-blob, offset)` for tail + range-read; keying by (job,step) is what lets the X-1 `CheckStatus.details_ref` `#step-<n>` sub-anchor resolve to the exact failing step's bytes — the jump-to-failure path; C1 — per-subject DEK for CI inline-PII log segments where the subject's PII is isolable, fall back to the per-tenant DEK + the 10.9 residual where not isolable), §5.1 (the GD-4 rule extended to CI inline-PII log segments).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.8 (the T3 CI log tier + the (job,step,byte-range) index), 11.4 (the C1 per-subject CI-log DEK extension), 3.5 (the resume-cursor transport the archive seals from), 5.9 (the `CheckStatus.details_ref` `#step-<n>` the index resolves).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) §OQ-D (the `#step-<n>` resolution).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row STOR-D4 (+ C1 assertion).
- **DELIVERABLE.** In crate myelin-storage (consumed by CI): (1) **The T3 CI log tier (C2)** — seal firehose frames into T2 content-addressed segments (inheriting T2 encryption + crypto-shred) + build the OLTP **`(job, step, byte-range)` index** mapping to `(segment-blob, offset)` for tail + range-read. This index IS the resolver behind the X-1 `CheckStatus.details_ref` `#step-<n>` jump-to-failure sub-anchor (`myelin://.../ci/run/<id>#step-<n>` → the exact failing step's log bytes). It rides the M2 resume-cursor transport (3.5, P-ST-08). (2) **The per-subject CI-log DEK (C1)** — where a CI log segment's inline PII is isolable to one subject, encrypt it under that subject's DEK so erasure crypto-shreds exactly their log content (live AND backups); where NOT isolable (interleaved free-text from many), fall back to the per-tenant DEK + the 10.9 residual posture (Storage does not invent a CI-local residual statement). Extend the P-ST-05 erase reach to the CI log segments. **Floor named:** the C1 per-tenant-fallback for non-isolable interleaved CI-log PII is the documented residual (per 10.9), not a floor-with-follow-on — record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.8 (the T3 CI log tier + the (job,step,byte-range) index, full), 11.4 (the C1 per-subject CI-log DEK extension). Consumed: 3.5 (the resume-cursor transport), 5.9 (the `CheckStatus.details_ref` resolution).
- **GATE / DRILLS.** **STOR-D4 + C1 assertion:** erase a subject incl. CI log segments → unrecoverable in backups; 0 recoverable PII (the CI-D3 storage face). Telemetry: `crypto_shred_lag`, 0 recoverable. SCHED. **The `details_ref` `#step-<n>` resolves to the exact failing step's bytes** via the (job,step,byte-range) index (the storage realisation of GIT-D10/CI-D8's jump-to-failure). Telemetry: the resolved byte-range matches the step's bytes. CI. **STOR-D1/STOR-D2 remain green** on the now-CI-log-bearing stores (T2/T3 are the heaviest; the permanent gate re-runs at this newly-heavy scale).
- **TESTS (required).** Unit: a firehose frame seals to a content-addressed T2 segment under the correct DEK; the (job,step,byte-range) index resolves `(job, step)` to the exact `(segment-blob, offset)`; a `#step-<n>` anchor resolves to the failing step's bytes; an isolable-PII CI log segment encrypts under the subject's DEK (erasable individually); a non-isolable segment falls back to the per-tenant DEK + the documented residual. CDC: provider+consumer pair for 11.8 (CI as the consumer). Drill harness scenarios for STOR-D4+C1 (erase incl. CI logs → 0 recoverable) + the `#step-<n>` resolution. Mutation floor: the byte-range index resolution + the per-subject-isolability key choice are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The CI log tier seals frames into content-addressed T2 segments + the (job,step,byte-range) index resolves `#step-<n>` to the exact bytes; per-subject CI-log DEK where isolable, per-tenant fallback + documented residual where not; STOR-D4+C1 (0 recoverable) + the `#step-<n>` resolution emit dated green artifacts; STOR-D1/STOR-D2 remain green; the C1 per-tenant-fallback residual is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: CI log tier (job,step,byte-range) index (C2) + per-subject CI-log DEK (C1)`; body lists 11.8/11.4(C1) implemented, STOR-D4+C1 green + the #step-<n> resolution green + STOR-D1/D2 re-run green, the C1 per-tenant-fallback residual, the mutation score. Co-Authored-By trailer.

---

### P-ST-12 — Trust-scoped CI cache namespaces (C4) + the OLAP restriction-flag gate (C5)

- **BAND.** M4.
- **ROADMAP MILESTONE.** S-M4 (trust-scoped cache namespaces 11.2 / C4 + the OLAP restriction-flag gate 11.6 / C5) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M4".
- **DEPENDS-ON.** P-ST-02 (the BlobStore the cache namespaces live in), P-ST-07 (the OLAP frame the C5 gate extends), the CI subsystem M4 (stamps `trust_tier` off run provenance — the C4 consumer), the GDPR `restrict(subject)` (10.1) + Issues analytics M4 (the C5 consumer).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (the poisoned-cache attack is stop-the-bleeding — a fork must not poison the trusted cache), §3 (prove-it; quantified gate; `cache_scope_violation` = 0).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.2 (C4 — the trust-tier/branch-scoped cache namespaces: the scope-key convention `<tenant>/ci/cache/<scope>/...`, `<scope> ∈ {trusted, fork:<pr_id>, branch:<protected_branch_name>}`; an `untrusted_fork` run may READ the trusted scope but may only WRITE its own `fork:` scope — a write to `trusted` is REFUSED by the blob client; trust is decided by CI/Git off run provenance, Storage enforces the write-scope rule it does not recompute trust; the structural half of the X-1 poisoned-pipeline defence), §3.4 (C5 — the OLAP store honours the restriction flag: `restrict(subject)` propagates into T4, a restricted subject's rows are EXCLUDED from analytics aggregates CFD/cycle-time/velocity/delivery-health; a COMPLIANCE gate not a tuning knob; the OQ-H worklog analytics-eligibility is `[OPEN — LEGAL]`).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.2 (the C4 trust-scoped cache namespaces), 11.6 (the C5 restriction-flag gate), 10.1 (`restrict(subject)`).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows D-S11 (trust-scoped cache isolation), D-S12 (restricted-subject OLAP suppression, §4.2).
- **DELIVERABLE.** In crate myelin-storage: (1) **Trust-scoped cache namespaces (C4)** — the scope-key convention `<tenant>/ci/cache/<scope>/...`, `<scope> ∈ {trusted, fork:<pr_id>, branch:<protected_branch>}` over the per-tenant blob keyspace; an `untrusted_fork` run may READ the `trusted` scope (cache hits are fine) but may only WRITE its own `fork:<pr_id>` scope — a write to `trusted` is REFUSED by the blob client (the scope is stamped from the run's `trust_tier`, which CI stamps from run provenance; Storage ENFORCES the write-scope rule, it does NOT recompute trust). The structural half of the poisoned-cache defence. Wire the `cache_scope_violation{tenant}` telemetry signal (must be 0). (2) **The OLAP restriction-flag gate (C5)** — `restrict(subject)` (10.1) propagates into T4: a restricted subject's rows are EXCLUDED from analytics aggregates (CFD/cycle-time/velocity/delivery-health); the OLAP consumer applies the restriction flag as a filter at query time, the subject's contribution withheld until restriction lifts or erasure completes. A compliance gate, not a tuning knob — it unblocks the Issues analytics ask without leaking a restricted subject. Wire the `olap_restricted_subject_leak` telemetry signal (must be 0). **Floor named:** the worklog/productivity/estimate analytics-eligibility (OQ-H — works-council consultation trigger) is `[OPEN — LEGAL]`; Storage ships the gate regardless — record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.2 (the C4 trust-scoped cache namespaces), 11.6 (the C5 restriction-flag gate). Consumed: 10.1 (`restrict(subject)`), the CI `trust_tier` stamp.
- **GATE / DRILLS.** **D-S11 (trust-scoped cache isolation, C4):** an `untrusted_fork` run writes a cache entry → lands only in `fork:<pr_id>`; a trusted run never reads it as `trusted`-scoped. Gate: 0 cross-scope cache writes; `cache_scope_violation` = 0. CI — the CI-D6 storage face. **D-S12 (restricted-subject OLAP suppression, C5):** `restrict(subject)`; run CFD/cycle-time/velocity → the subject's contribution is absent. Gate: `olap_restricted_subject_leak` = 0. CI. **STOR-D1/STOR-D2 remain green.**
- **TESTS (required).** Unit: an untrusted_fork write to `trusted` is refused (the blob client rejects); an untrusted_fork write to its own `fork:<pr_id>` succeeds; an untrusted_fork run can READ the trusted scope; the scope is stamped from trust_tier (Storage does not recompute trust); a restricted subject's rows are excluded from each analytics aggregate; the restriction lifts → the subject reappears; `cache_scope_violation` and `olap_restricted_subject_leak` are both 0 in the happy path and fire on a violation. CDC: provider+consumer pairs for 11.2-C4 (the CI cache consumer) and 11.6-C5 (the Issues analytics consumer). Drill harness scenarios for D-S11 + D-S12. Mutation floor: the write-scope-refusal branch + the restriction-flag filter are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The trust-scoped cache namespaces refuse a fork write to `trusted` (allow read); the OLAP restriction-flag gate excludes a restricted subject from every analytics aggregate; D-S11 (`cache_scope_violation` = 0) + D-S12 (`olap_restricted_subject_leak` = 0) emit dated green artifacts; STOR-D1/STOR-D2 remain green; the OQ-H worklog follow-on is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: trust-scoped cache namespaces (C4) + OLAP restriction-flag gate (C5)`; body lists 11.2-C4/11.6-C5 implemented, D-S11 (cache_scope_violation 0) + D-S12 (olap_restricted_subject_leak 0) green + STOR-D1/D2 re-run green, the OQ-H follow-on, the mutation score. Co-Authored-By trailer.

---

### P-ST-13 — The floor follow-ons: object-store BlobStore + object-backed git packs

- **BAND.** M5.
- **ROADMAP MILESTONE.** S-M5 (object-store BlobStore 11.2 + object-backed git packs 11.2 / STOR-5 — the named M0/M3 floor follow-ons) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M5" + §3 (the hard-problem ledger).
- **DEPENDS-ON.** P-ST-02 (the fs-BlobStore floor this swaps), P-ST-09 (the local-disk git packs behind the trait this backs with the object store), the measured ceilings (the single-node blob ceiling; GIT-D4) that trigger the follow-ons, the Git subsystem M5 deliverable (the object-backed pack impl — chunking/delta-base selection/serving from the object tier is co-owned with Git).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §3 in full (world-scale git storage — the explicit sequenced transition from node-local disk to the object store; delta/pack management, sharding, replication, smart-transport; early choices did not pin repos to a node).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (the floor's promotion is itself drilled — a backing swap, not a rewrite, by the trait's design), §3 (prove-it under measured pressure).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §3.2 (the S3-compatible object store — MinIO or Ceph RADOS Gateway, self-hostable EU-deployable, behind the narrow content-addressed trait so fs↔object is a one-line swap; the S3 REST API is the portable contract, no proprietary global managed object service), §3.5 (the object-backed pack/delta management + smart-transport = the Git P6 deliverable; the relocatability constraint was DECIDED in M3, the impl is M5), §8 (measure-before-shard — the swap is triggered by the measured single-node ceiling, not predicted).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 11.2 (the object-store BlobStore + the object-backed pack/delta seam).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D7 (blob integrity on the object store), GIT-D4 (the monorepo/single-node ceiling, with Git).
- **DELIVERABLE.** In crate myelin-storage: (1) **Object-store BlobStore (11.2 follow-on)** — swap the fs-backed floor (P-ST-02) for a MinIO / Ceph RADOS Gateway backing behind the SAME BlobStore trait (a one-line backing swap by design; the S3 REST API is the portable contract — no proprietary global managed object service). Trigger: the measured single-node blob ceiling. The content-address + per-tenant-keyspace + store-ciphertext + re-hash-on-read-integrity semantics are unchanged — assert STOR-D7 stays green on the object backing. (2) **Object-backed git packs (11.2 / STOR-5 follow-on, EI-04 §3)** — authoritative git bytes move from node-local disk (P-ST-09) to the object store: delta/pack management, sharding, replication, smart-transport, the within-EU CDN clone/bundle class (P-ST-09 C3) fully wired against the object backing. This is the explicit sequenced transition EI-04 §3 insisted on; early choices did NOT pin repos to a node (P-ST-09's repo-granular relocatable placement). Trigger: GIT-D4 (the single-node ceiling measured). The object-backed pack IMPL (chunking, delta-base selection, serving from the object tier) is co-owned with the Git subsystem — Storage provides the object-backing + the trait; Git provides the pack/delta algorithms. **Floor promoted:** the fs-BlobStore and local-disk-packs floors are now their full answers; what remains designed-not-built (the object-backed pack impl specifics) is named in the honesty register. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 11.2 (the object-store BlobStore + the object-backed pack/delta seam — the full M5 answer; the pack-algorithm impl co-owned with Git). Consumed: the measured ceilings (the trigger).
- **GATE / DRILLS.** **STOR-D7 on the object store:** corrupt an object-store object → content-address re-hash detects + recovers from a replica; 0 silent serve (the integrity property survives the backing swap). CI. **GIT-D4 (the trigger, with Git):** the single-node ceiling is measured + the object-backed packs serve clone p99 within budget. SCHED. **STOR-D1/STOR-D2 remain green** on the now-object-backed stores (the permanent gate re-runs at the new backing). **The F6 surge family on the storage lanes** is the sibling P-ST-15.
- **TESTS (required).** Unit: the object-store BlobStore put/get/head/delete round-trips through the S3 REST API behind the unchanged trait; the fs→object swap is a backing change only (the trait's consumers are untouched — a structural assertion); an object-backed git pack is served from the object tier (not node-local disk); a corrupted object-store object is detected on read + recovered from a replica. CDC: provider+consumer pair for 11.2 (the object-store backing). Drill harness scenarios for STOR-D7 on the object store + GIT-D4 (with Git). Mutation floor: the object-store integrity path + the backing-swap-preserves-semantics path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The object-store BlobStore is wired behind the unchanged trait; object-backed git packs serve from the object tier (relocatable, region-pinned); STOR-D7 stays green on the object backing; GIT-D4 (the ceiling measured, clone p99 within budget) is greened with Git; STOR-D1/STOR-D2 remain green; the floors are promoted (the remaining pack-impl specifics named in the honesty register); CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: object-store BlobStore + object-backed git packs (the floor follow-ons)`; body lists 11.2 (object-store + object-backed packs) implemented, STOR-D7 green on the object store + GIT-D4 green + STOR-D1/D2 re-run green, the promoted floors, the mutation score. Co-Authored-By trailer.

---

### P-ST-14 — Multi-cell: the cross-cell pointer bridge live + cell→cell migration (the single-cell floor follow-on)

- **BAND.** M5.
- **ROADMAP MILESTONE.** S-M5 (multi-cell 12.6 + cell→cell migration — the single-cell M1 floor follow-on; the FLOOR drills CP-D7/GA-D8) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M5" + §3.
- **DEPENDS-ON.** P-ST-04 (the per-cell KEK + per-cell backup machinery), P-ST-06 (the restore/cross-seam machinery the migration uses), P-ST-05 (the crypto-shred erase for the source-cell shred), the control plane prompts that ship the cross-cell PII-free pointer bridge frame (12.6, M1 frame → M5 live) + `member_cells` (10.4), the GDPR DSR fan-out (10.4) that iterates `member_cells`.
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (residency — region-pinning; multi-region is a sequenced floor follow-on, not bolted on).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §2 "S-M5" multi-cell (the cross-cell PII-free pointer bridge goes live; cell→cell migration same-region with 0 loss + source crypto-shredded; DSR fan-out iterates `member_cells`; the FLOOR drills GA-D8/CP-D7/CP-D8 are now owed and run; Storage built the per-cell KEK + per-cell backup machinery in M1, the cell→cell migration is M5), §7.3 (the cross-seam consistency point the migration restores to).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 12.6 (the cross-cell PII-free pointer bridge — `CrossCellPointer{subject opaque, type, correlation_id, home_cell}`, resolution always cell-local), 10.4 (the DSR fan-out iterates `member_cells`), 11.5 (the restore/cross-seam machinery the migration uses).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) §OQ-I (the multi-cell pointer bridge frame).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows CP-D7 (cell→cell migration, FLOOR), GA-D8 (multi-cell erasure, FLOOR, §4.2).
- **DELIVERABLE.** In crate myelin-storage (with the control plane + GDPR): (1) **The cross-cell PII-free pointer bridge goes live (12.6)** — the storage half: a cross-cell pointer carries an OPAQUE subject + type + correlation_id + home_cell; resolution is ALWAYS cell-local (the home cell renders + permission-checks; only the projection crosses — no PII crosses the cell boundary). (2) **Cell→cell migration (same region)** — migrate a tenant's stores cell→cell with 0 loss across the seam (restore the cross-seam consistency point §7.3 into the target cell), landing IN-REGION, with the SOURCE crypto-shredded after the move (the source KEK destroyed). (3) **The DSR fan-out iterates `member_cells`** — extend the erase/DSR reach to iterate all `member_cells ∪ home_cell` so a multi-cell erasure produces a complete per-cell receipt set with 0 cells missed. **Floor promoted:** single-cell (M1) is now multi-cell; the FLOOR drills CP-D7/GA-D8 are now owed + run. Trigger: cross-cell rollup/collab/cross-org demand (OQ-I). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (the storage half): the cell→cell migration (restore-to-consistent-point into the target cell + source crypto-shred), the multi-cell erase fan-out iterating `member_cells`. Consumed: 12.6 (the cross-cell pointer bridge frame), 10.4 (the DSR fan-out + `member_cells`), 11.5 (the restore/cross-seam machinery).
- **GATE / DRILLS.** **CP-D7 (FLOOR):** cell→cell migration (same region) → 0 loss across-seam, in-region, source crypto-shredded. Telemetry: migration receipt, 0 loss, source-shred confirmed. SCHED. **GA-D8 (FLOOR):** multi-cell erasure fan-out iterates all `member_cells ∪ home_cell`; complete receipt set; 0 cells missed. Telemetry: per-cell receipt set, 0 missed. SCHED. **STOR-D1/STOR-D2 remain green** across the cell boundary (the permanent gate re-runs).
- **TESTS (required).** Unit: a cross-cell pointer carries only an opaque subject (no PII crosses); resolution is cell-local; a cell→cell migration restores the cross-seam consistency point into the target cell with 0 loss + lands in-region; the source cell is crypto-shredded after the move (the source KEK destroyed, source ciphertext unrecoverable); the multi-cell erase iterates every member_cell ∪ home_cell with a per-cell receipt. CDC: provider+consumer pair for the cell→cell migration + the multi-cell erase fan-out. Drill harness scenarios for CP-D7 + GA-D8. Mutation floor: the migration's restore-to-consistent-point + source-shred path + the member_cells iteration are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The cross-cell PII-free pointer bridge is live (no PII crosses; resolution cell-local); cell→cell migration is 0-loss + in-region + source-crypto-shredded; the DSR fan-out iterates all member_cells with a complete receipt set; CP-D7 + GA-D8 emit dated green artifacts (0 loss, 0 cells missed); STOR-D1/STOR-D2 remain green across cells; the single-cell floor is promoted; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: multi-cell pointer bridge live + cell→cell migration (the single-cell follow-on)`; body lists the cell→cell migration + the multi-cell erase fan-out implemented, CP-D7 (0 loss, source shredded) + GA-D8 (0 cells missed) green + STOR-D1/D2 re-run green, the promoted single-cell floor, the mutation score. Co-Authored-By trailer.

---

### P-ST-15 — World-scale hardening: restore-verify at cell scale + the F6 surge family + the full DSAR fan-out (E2E-4) + E2E-3 storage half

- **BAND.** M5.
- **ROADMAP MILESTONE.** S-M5 (restore-verify at cell scale STOR-D2 + the F6 surge family on the storage lanes + the full DSAR crypto-shred fan-out E2E-4 + the E2E-3 storage half) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M5".
- **DEPENDS-ON.** P-ST-06 (the restore-verify gate this re-confirms at cell scale), P-ST-11 (the CI log holder — the heaviest), P-ST-12 (the C4 cache namespaces + reserve/settle per-tenant fairness for the surge), P-ST-13 (the object-store backing under surge), P-ST-14 (multi-cell), all five subsystems on the substrate (M4 green) + the complete H1–H18 holder set (so the crypto-shred reach is complete), the GDPR DSR orchestrator (E2E-4 owner).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it under 1×/10×/30× surge; observability is part of the pass), §2 (the protected-human-lane shed order — human lane holds, agent/CI sheds), §4 (chain mutations end-to-end — the whole-system E2E wedge).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §5 (reindex-from-source — E2E-3 cold-reindex == live), §1 (crypto-shred reaches every holder incl. backups — E2E-4).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §2 "S-M5" world-scale hardening (the 30× surge on the storage lanes — CI artifact storm one tenant doesn't starve another, reserve/settle per-tenant fairness + C4 cache namespaces + the cell bulkhead; restore-verify at cell scale under world-scale load; online-migration-under-load at prod scale), §5.2 (the full DSR crypto-shred fan-out across all H1–H18 holders incl. backups + post-restore re-erasure), §7 (restore-verify + cross-seam + re-erasure — the E2E-4 spine + the E2E-3 derived-store half).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.5 (restore-verify at cell scale), 11.7 (reserve/settle per-tenant fairness under surge), 10.4 (the DSR fan-out).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D2 (at cell scale), the F6 surge family (SUB-D3 / GIT-D6 / CI-D2), E2E-4 (DSAR fan-out, §2.4/§4.4), E2E-3 (§4.4).
- **DELIVERABLE.** In crate myelin-storage + the drill harness: (1) **Restore-verify at cell scale** — re-confirm STOR-D1/STOR-D2 under world-scale load (RPO/RTO held under surge); the restore-verify CI job (P-ST-06, the permanent gate) re-runs at cell scale. (2) **The F6 surge family on the storage lanes** — the 30× surge: a CI artifact storm by one tenant does not starve another (reserve/settle per-tenant fairness P-ST-07 + the C4 cache namespaces P-ST-12 + the cell bulkhead); the protected human lane holds within budget, the agent/CI lanes shed (429 + Retry-After); cross-tenant impact 0. The storage faces of SUB-D3 / GIT-D6 (clone surge + CDN hit) / CI-D2 (CI surge). (3) **The full DSAR / crypto-shred fan-out (E2E-4 spine)** — every holder now exists (incl. CI logs, agent memory, chat bodies, vectors), so the crypto-shred reach is COMPLETE: crypto-shred reaches every H1–H18 holder incl. backups (STOR-D4 across all holders), post-restore re-erasure covers every holder (STOR-D3), residual == the ONE documented posture (10.9). (4) **The E2E-3 storage half** — cold-reindex == live for the derived stores (OLAP/Search/Refs rebuilt from source). **Floor promoted:** by M5 the floors are promoted; what remains designed-not-built (HYOK per-content-class policy + KMIP adapter `[OPEN → P6/LEGAL]`, the column-store seam measured-trigger, the generated projection-feeder index measured-trigger) is named in the honesty register. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (the proof side): the cell-scale restore-verify re-confirmation, the F6 storage-lane surge proof, the full DSAR crypto-shred fan-out (the storage spine of E2E-4), the E2E-3 derived-store half. Consumed: 11.5 (restore-verify), 11.7 (reserve/settle fairness), 10.4 (the DSR fan-out).
- **GATE / DRILLS.** **STOR-D2 at cell scale re-confirmed** (RPO/RTO under world-scale load). Telemetry: `backup_rpo_seconds`, restore-time at cell scale. SCHED. **The F6 surge family on the storage lanes** (human lane holds, agent/CI sheds, cross-tenant impact 0) — SUB-D3 / GIT-D6 / CI-D2. Telemetry: shed-counts per principal-kind, cross-tenant-impact 0. SCHED. **E2E-4 (DSAR fan-out)** — Storage is the spine: crypto-shred reaches every holder incl. vectors incl. backups (STOR-D4 across all holders), post-restore re-erasure (STOR-D3), residual == the one documented posture. Gate: 0 holders missed; 0 recoverable PII; certificate sealed. SCHED. **E2E-3 (storage half):** cold-reindex == live for the derived stores (OLAP/Search/Refs rebuilt from source). Telemetry: reindex-parity hash, 0 drift. SCHED. Never weaken a threshold to pass; a red gate becomes a dated "claimed, not proven" row.
- **TESTS (required).** Unit/integration: a 30× CI artifact storm by tenant A leaves tenant B's storage lanes within budget (the cell bulkhead + per-tenant fairness); the human lane holds while the agent/CI lanes shed under surge; the DSAR fan-out hits every H1–H18 holder incl. backups + vectors (0 missed, 0 recoverable); cold-reindex of OLAP/Search/Refs byte-matches live. CDC: the E2E-4 + E2E-3 scenarios are whole-system; Storage's CDC is the holder-coverage + reindex-parity contributions. Drill harness scenarios for STOR-D2 at cell scale, the F6 surge family, E2E-4, E2E-3. Mutation floor: re-run the mandatory-core mutation gate on the restore + crypto-shred + reserve/settle modules at this scale (the floors set in P-ST-06/P-ST-05/P-ST-07 hold).
- **DEFINITION OF DONE.** STOR-D2 at cell scale re-confirmed; the F6 surge family green on the storage lanes (human holds, agent/CI sheds, cross-tenant 0); E2E-4 (0 holders missed, 0 recoverable PII, certificate sealed) + E2E-3 storage half (cold-reindex == live) emit dated green artifacts; the remaining designed-not-built items are named in the honesty register; CDC + unit/integration tests + the mutation floors pass; lints green; coverage scanner green. No threshold weakened. Committed.
- **COMMIT.** Header `P-<NNN> M5: restore-verify at cell scale + F6 storage surge + DSAR fan-out (E2E-4) + E2E-3 storage half`; body lists the cell-scale restore-verify + the F6 surge proof + E2E-4 (0 holders missed, 0 recoverable) + E2E-3 (cold==live) green, the named honesty-register items, the mutation floors held. Co-Authored-By trailer.

---

### P-ST-16 — Dogfood: the restore-verify gate runs on Myelin's own commits + the truth-up pass

- **BAND.** M6.
- **ROADMAP MILESTONE.** S-M6 (the restore-verify CI job runs on the platform's own stores + the truth-up pass) — [`../../06-roadmaps/shared/storage.md`](../../06-roadmaps/shared/storage.md) §2 "S-M6".
- **DEPENDS-ON.** P-ST-15 (M5 green — restore-verify at cell scale + the full DSAR fan-out + multi-cell drilled), the Myelin git hosting + CI live (M3/M4 dogfooded), the dogfood-loop infrastructure (the platform hosts itself).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (the code wins over the docs — the truth-up pass re-syncs every PROVEN row to a dated green artifact; a claim that outlives its verification misleads the next agent), §3 (prove-it on real data — the team's own data is real tenant data; every incident adds a drill).
  - [`../../05-refined-shared-systems-architecture/storage.md`](../../05-refined-shared-systems-architecture/storage.md) §2 "S-M6" (the restore-verify CI job runs on the platform's own commits — Myelin's own monorepo, CI logs, issues, docs are now real tenant data under the same backup/restore/crypto-shred machinery; the every-incident-adds-a-drill loop files a Myelin issue + a reproducing storage drill for any storage incident), §7 (the restore-verify gate).
  - [`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) §2 M6 (you do not dogfood real team data onto a substrate whose restore-verify and DSAR fan-out are not green — Tier 1 + Tier 6), §4 (the permanent gate).
- **DELIVERABLE.** (1) **The restore-verify CI job runs on the platform's own stores** — wire the restore-verify gate (P-ST-06, the permanent gate) into the Myelin self-hosting CI graph so it runs on the platform's own commits: Myelin's own monorepo, CI logs, issues, docs are now real tenant data under the same backup/restore/crypto-shred machinery; the gate emits its dated green artifact on the dogfood data. (2) **The every-incident-adds-a-drill loop** — wire the loop so any storage incident discovered during dogfooding files a Myelin issue + a reproducing storage drill that joins the harness and re-runs forever (the T-3 register_drill hook). (3) **The truth-up pass** — confirm every PROVEN Storage row (every STOR-D*, D-S11/D-S12/D-S13, the floor follow-ons) rests on a DATED green artifact; no earlier-band storage gate is red (the gate invariant holds end-to-end; code-wins-over-docs — if a doc claims a property the code does not hold, fix the doc).
- **CONTRACTS TO IMPLEMENT.** No new contract — this wires the restore-verify gate (11.5) onto the dogfood data + confirms the gate invariant. Consumed: 11.5 (the restore-verify gate, now on real Myelin data).
- **GATE / DRILLS.** **The restore-verify gate green on the platform's own stores** (STOR-D1/STOR-D2 on real Myelin data). Telemetry: `restore_verify_pass` on the dogfood commits. **No earlier-band storage gate is red** — the truth-up pass confirms every PROVEN Storage row rests on a dated green artifact (the gate invariant holds end-to-end). SCHED.
- **TESTS (required).** Integration: the restore-verify CI job runs on the Myelin self-hosting CI graph + emits a dated green artifact on real platform data; a synthetic storage incident files a Myelin issue + registers a reproducing drill that re-runs; the truth-up pass enumerates every PROVEN Storage row and asserts each has a dated green artifact (a row without one is a loud failure, not a silent pass). No new mutation floor (re-runs the existing mandatory-core gates as Myelin CI jobs on every Myelin commit — the dogfood loop).
- **DEFINITION OF DONE.** The restore-verify gate runs green on the platform's own stores; the every-incident-adds-a-drill loop is wired; the truth-up pass confirms 0 red earlier-band storage gates + every PROVEN row dated; the restore-verify permanent gate + the mandatory-core mutation gate run as Myelin CI jobs on every Myelin commit; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M6: restore-verify gate on Myelin's own commits + truth-up pass`; body lists the restore-verify gate green on dogfood data, the every-incident-adds-a-drill loop wired, the truth-up pass (0 red earlier-band storage gates, every PROVEN row dated). Co-Authored-By trailer.

---

## Coverage map (this file → roadmap milestones, for the Phase 7-B index)

Every Storage roadmap milestone maps to ≥1 prompt; every band-boundary Storage drill is greened by some prompt's GATE/DRILLS:

| Roadmap milestone | Band | Prompt(s) | Key drills greened |
|---|---|---|---|
| S-M0 (OLTP tier + outbox + RLS) | M0 | P-ST-01 | SUB-D1, BUS-D4 (OLTP/outbox half) |
| S-M0 (BlobStore trait + fs floor) | M0 | P-ST-02 | STOR-D7 (blob integrity) |
| S-M0 (storage lints + migration runner) | M0 | P-ST-03 | forward-only-migration + residency-pin lints |
| S-M1 (KMS + KeyOrigin + envelope encryption) | M1 | P-ST-04 | STOR-D6 (KMS fail-static, 0 fail-open) |
| S-M1 (crypto-shred erase + GD-4 + structural GDPR floor) | M1 | P-ST-05 | STOR-D4 (0 recoverable PII in backups) |
| S-M1 (backup/restore/cross-seam + restore-verify — THE HEADLINE) | M1 | P-ST-06 | STOR-D1, STOR-D2, STOR-D3 (the silent-data-loss floor, the permanent gate) |
| S-M1 (residency + reserve/settle + OLAP frame) | M1 | P-ST-07 | STOR-D5 (0 cross-region egress) |
| S-M2 (OLAP fed by the bus + reserve/settle fronts agent runs + T3 seam) | M2 | P-ST-08 | STOR-D8, OLAP reindex-parity (F4); STOR-D1/D2 re-run |
| S-M3 (local-disk git packs behind the trait + CDN class C3) | M3 | P-ST-09 | STOR-D5 (CDN within-EU), STOR-D7 (git packs) |
| S-M3 (git crypto-shred reach + outbound-mirror seam C6) | M3 | P-ST-10 | GIT-D2 (storage half), D-S13 (mirror deny) |
| S-M4 (CI log tier index C2 + per-subject CI-log DEK C1) | M4 | P-ST-11 | STOR-D4+C1, the #step-<n> resolution; STOR-D1/D2 re-run |
| S-M4 (trust-scoped cache C4 + OLAP restriction gate C5) | M4 | P-ST-12 | D-S11 (cache_scope_violation 0), D-S12 (olap_restricted_subject_leak 0) |
| S-M5 (object-store BlobStore + object-backed git packs) | M5 | P-ST-13 | STOR-D7 on object store, GIT-D4 (with Git) |
| S-M5 (multi-cell + cell→cell migration) | M5 | P-ST-14 | CP-D7 (FLOOR), GA-D8 (FLOOR) |
| S-M5 (restore-verify at cell scale + F6 surge + DSAR E2E-4 + E2E-3) | M5 | P-ST-15 | STOR-D2 at cell scale, F6 family, E2E-4, E2E-3 storage half |
| S-M6 (restore-verify on Myelin's own commits + truth-up) | M6 | P-ST-16 | restore-verify green on dogfood data; 0 red earlier-band gates |

**Floor → follow-on pairs (each named, the gap visible):** fs-BlobStore (P-ST-02) → object-store BlobStore (P-ST-13); local-disk git packs (P-ST-09) → object-backed packs (P-ST-13); per-subject DEK for free-text/chat/profile (P-ST-04/P-ST-05) → + CI inline-PII log segments C1 (P-ST-11); single-cell (P-ST-04/P-ST-06/P-ST-07) → multi-cell + cell→cell migration (P-ST-14); plaintext-at-rest (P-ST-01) → envelope encryption (P-ST-04); pseudonymous-by-default commits (P-ST-10) → audited history-rewrite (10.6, on-demand follow-on); the C5 restriction gate named (P-ST-07) → live with Issues (P-ST-12). The `[OPEN → P6/LEGAL]` items (the HYOK per-content-class policy + KMIP adapter, the one free-text/immutable residual posture, the OQ-H worklog classification, the §7.6 backup-window residual number) ship their structural floor regardless and are flagged to counsel/DPO in parallel (named in P-ST-04/P-ST-05/P-ST-12/P-ST-15).

**The two permanent gates Storage carries forward (master §4):** STOR-D1/STOR-D2 (restore-verify) is Storage's — it is greened in P-ST-06 (M1) and re-confirmed by P-ST-08 (M2), P-ST-09/P-ST-10 (M3), P-ST-11/P-ST-12 (M4), P-ST-13/P-ST-14/P-ST-15 (M5), P-ST-16 (M6) — every band that touches a store re-runs it, forever. It never moves to "done."

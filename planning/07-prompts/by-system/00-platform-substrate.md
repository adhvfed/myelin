# Phase 7 — Prompt Ledger: Platform Substrate & Foundations (`myelin-substrate` + the glue crates)

> Phase: `07-prompts/by-system`. The complete ordered set of implementation prompts that operationalize the
> **00-platform-substrate** shared system's entire Phase-6 roadmap into clean-context, independently-committable
> coding tasks. Each prompt follows the template in
> [`../00-ledger-overview.md`](../00-ledger-overview.md) §2 EXACTLY. Authored against the FROZEN sequence:
> master bands [`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) (M0..M6 +
> the gate invariant), the substrate roadmap
> [`../../06-roadmaps/shared/00-platform-substrate.md`](../../06-roadmaps/shared/00-platform-substrate.md), the
> refined architecture [`../../05-refined-shared-systems-architecture/00-platform-substrate.md`](../../05-refined-shared-systems-architecture/00-platform-substrate.md)
> and the contracts [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md)
> §1 (owned) + §2/§3.5 (carried). Drills:
> [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md)
> §4.2 (SUB-D1..SUB-D10). Doctrine: VISION §3/§7, EI-01 (§1 code-wins-over-docs + name-your-floors, §2
> order-by-non-negotiability, §3 prove-it-or-it-isn't-real, §5 the ratchet), EI-04 (§2 resume-cursor-first, §5
> untrusted-execution). Plain text identifiers throughout (no backticks-as-emphasis where a name stands alone).
> The global `P-<NNN>` ids are assigned by the Phase-7-B index; this file uses provisional ordinals in the
> substrate's own band order — the index interleaves these with every other system's prompts.
> Date: 2026-06-19.
>
> **The shape of this system.** The substrate is the ROOT of the dependency DAG — it has no upstream (its only
> "dependency" is the failure-injection harness, which it also builds). Its core lands almost entirely in M0
> (the workspace, the eight glue crates, the harness, the outbox, the twelve lints, `serve(AppSpec)`, the
> resilience primitives, the scanner + thresholds). Five later prompts complete-and-prove properties whose proof
> needs a system that does not exist in M0: fail-static (M1, against a real Identity hiccup), restore-verify
> cross-seam integrity (M1, with Storage), the firehose backpressure half (M2/M4), the surge family +
> migration-under-load (M5), and the dogfood loop (M6). The substrate's correctness floors (the outbox, the
> twelve lints, the harness) are NOT staged — they are absolute from M0; its named floors are scale/value/legal
> deferrals only (which blob backend, which budget number, which staleness value).

---

## Coverage map (this file → the substrate roadmap milestones)

| Roadmap milestone | Prompt ids (provisional) |
|---|---|
| SUB-M0 (= master M0; the core) | P-S01 (workspace + glue crates) · P-S02 (failure-injection harness + telemetry signals) · P-S03 (envelope + outbox + relay) · P-S04 (consumer template + dedup) · P-S05 (twelve lints, red+green fixtures) · P-S06 (`serve(AppSpec)` + three ports + migrations) · P-S07 (resilient client + Retry-After) · P-S08 (fail-static mechanism + shed lane + bounded-everything) · P-S09 (contract-coverage scanner + thresholds file + overlay primitives) · P-S10 (M0 exit drills SUB-D1/D2/BUS-D4/D5/D7/D8/D9 + harness self-test) |
| SUB-M1 (master M1) | P-S11 (fail-static proven vs Identity, SUB-D4) · P-S12 (restore-verify cross-seam half, SUB-D6) |
| SUB-M2 (master M2) | P-S13 (firehose backpressure half + D-11 substrate half) |
| SUB-M4 (master M4) | P-S14 (cross-language shim enforced + firehose under connection-storm) |
| SUB-M5 (master M5) | P-S15 (SUB-D3 surge family) · P-S16 (SUB-D10 migration-under-load + restore-verify at cell scale + per-target tuning) |
| SUB-M6 (master M6) | P-S17 (dogfood: lints + scanner + mutation gate as Myelin CI jobs) |

Total: 17 prompts. Bands: M0 ×10, M1 ×2, M2 ×1, M4 ×1, M5 ×2, M6 ×1. Every SUB-M0..SUB-M6 milestone and every
SUB-D1..SUB-D10 drill + the D-11 firehose half is greened by at least one prompt below.

---

### P-S01 — Stand up the Cargo workspace and the eight glue-crate skeletons

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (the workspace + glue-crate skeleton slice) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 4 + §1.1.
- **DEPENDS-ON.** none (this is a root prompt of M0; the whole DAG roots here).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../VISION.md` §4 (Rust default; the workspace `.gitignore` is pre-seeded for Cargo + cargo-mutants) and §3 (name-your-floors).
  - `../../external-insights/01-process-and-quality-doctrine.md` §5 (the ratchet — a gate must be committed) and §7 (keep the architecture coherent; abstract at the third copy).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §2 (the shared crates — responsibility + trait/type surface, the whole section), §2.9 (the dependency root, no cycles — the crate DAG root-last), §2.6 (`myelin-substrate` harness crate).
  - `../../05-refined-shared-systems-architecture/contract-index.md` §0 (the clusters) + §1 header (the substrate cluster) + the "Units (frozen)" block in the doc header.
  - `../00-ledger-overview.md` §6 (the repo/workspace conventions every prompt assumes — the eight glue crates as compile-time contract carriers).
- **DELIVERABLE (what to build + exactly where in the repo).** A single Cargo workspace at the repo root (`Cargo.toml` `[workspace]`). Create the eight glue crates as library crates with their public trait/type surface stubbed to the FROZEN signatures (compiling, `todo!()`-bodied where an impl lands in a later prompt): `myelin-tenancy`, `myelin-identity`, `myelin-events`, `myelin-refs`, `myelin-content`, `myelin-query`, `myelin-agent`, `myelin-gdpr`; plus the substrate crates `myelin-client` (the `ResilientClient` home) and `myelin-substrate` (the harness home). Wire the inter-crate dependency edges in the EXACT root-last order of architecture §2.9 (`myelin-tenancy` → `myelin-identity` → `myelin-events`/`-refs`/`-content`/`-query` → `myelin-agent`/`-gdpr` → `myelin-client` → `myelin-substrate`) — a dependency that would create a cycle must not compile. Each crate gets a top-of-file doc comment naming its owning architecture doc + contract-index cluster. Place the type/trait stubs only for cross-crate surfaces (architecture §2.1–§2.7): `EventEnvelope`, `OutboxTx`, `ArtifactRef` (in `myelin-events`); `Principal`/`PrincipalKind`/`AuthzClient`/`Consistency` (in `myelin-identity`); `Refs` (in `myelin-refs`); `AgentRuntime`/`ToolHands`/`ToolSurface` (in `myelin-agent`); `PersonalDataHolder`/`BlobStore` (in `myelin-gdpr`); `TenantId`/`Region` (in `myelin-tenancy`). **Floor named:** the bodies are stubs; each later prompt names which it fills. This prompt ships the skeleton only.
- **CONTRACTS TO IMPLEMENT.** The TYPE/TRAIT SHAPES (not bodies) of contract-index rows 1.1 (`serve` signature stub), 1.9 (`ResilientClient::call` signature), 1.10 (`FailStatic<T>` struct), 2.1 (`EventEnvelope`), 2.2 (`OutboxTx::emit`), 4.2/4.3/4.5 (`AuthzClient`), 5.1 (`ArtifactRef`), 8.1/8.3/8.4 (`AgentRuntime`/`ToolHands`/`ToolSurface`), 10.1/11.2 (`PersonalDataHolder`/`BlobStore`) — each to the frozen shape in architecture §2. Do not redesign a signature; if reality forces a divergence, write it down in the crate doc comment and escalate (EI-01 §1).
- **GATE / DRILLS (quantified; must be green to call this done).** `cargo build --workspace` and `cargo test --workspace` succeed (green artifact = a clean build log). The dependency DAG is acyclic by construction — assert it with a test that the crate graph has no cycle and `myelin-identity` depends on nothing above `myelin-tenancy` (architecture §2.9 — identity is a sink). No quantified runtime drill at this prompt (it ships skeleton, not behaviour); state this floor explicitly.
- **TESTS (required).** Unit: one compile-asserting test per glue crate that the public surface exists with the frozen field names/units (e.g. `EventEnvelope.occurred_at: Timestamp`, costs as integer minor-units). A `crate-graph-acyclic` test (the build-layer realisation of the `no-cross-sync-cycle` lint, which P-S05 ships as the real lint). No CDC pair yet (the contracts have no behaviour to verify here — named: CDC pairs land with each contract's impl prompt).
- **DEFINITION OF DONE.** The workspace and the ten crates exist and compile; the frozen trait/type shapes are present; the crate-graph-acyclic test passes; the stubbed bodies are named as a floor with their filling prompt; committed.
- **COMMIT.** Header `P-<NNN> M0: Cargo workspace + eight glue-crate skeletons`. Body: lists the ten crates, the acyclic-DAG assertion, and the named stub floor + filling prompts. Branch first if on default. End with the required `Co-Authored-By:` trailer.

---

### P-S02 — Build the failure-injection harness and the telemetry-assertion library (Tier 0, the unit of proof)

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 0 — the failure-injection harness slice) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 1.
- **DEPENDS-ON.** P-S01.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../VISION.md` §3 (prove-it / honesty about uncertainty).
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (prove-it-or-it-isn't-real — *the* heart of the discipline; the failure-injection harness built EARLY: a load generator that multiplies traffic 1×/10×/30× and mixes principal types, a scoped reversible way to break a dependency, assertions read from production telemetry; observability is part of the pass condition).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §10.2 (the telemetry signal set — RED/USE per principal-kind, consumer-lag, outbox-depth, breaker-state, fail-static ratios, shed-counts, causal-depth, firehose frame-lag) and §11 intro (the green-artifact rule + the failure-injection seam = a scoped reversible dependency break, T-3).
  - `../../05-refined-shared-systems-architecture/contract-index.md` row 1.8 (telemetry signal set — every drill asserts against this set; no signal = no provable drill).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.1 (the nine reusable families) + §4.2 (the SUB drills this harness will drive).
- **DELIVERABLE (what to build + exactly where in the repo).** A new crate `myelin-harness` (test-support, in the workspace). It contains: (a) the **1×/10×/30× load generator** — a driver that issues traffic at a multiplier with a configurable mix of the five principal kinds (human / agent / service / CI / external-MCP) and the named per-surface storm profiles (CI-surge, collab op-stream, connection-storm, agent-mention-storm — OQ-K); (b) the **scoped-reversible dependency-break injector** — `break_dependency(name, scope)` / `restore_dependency(name, scope)` that severs one named dependency (Identity, the broker, a downstream) for one named scope without taking the rig down; (c) the **telemetry-assertion library** — a typed reader over the contract-1.8 survival-signal set with `assert_signal(name, predicate)` (e.g. `outbox_depth == 0`, `cross_tenant_count == 0`, `fail_static_ratio`); (d) the **every-incident-adds-a-drill loop** wiring (T-3): a `register_drill(scenario)` hook that a reproducing drill joins and re-runs forever. The telemetry signal set itself is exported on the metrics-health port — but that port is opened by `serve` (P-S06); here the harness reads an in-memory signal source so it is testable before `serve` exists.
- **CONTRACTS TO IMPLEMENT.** Contract-index 1.8 (telemetry signal set — the harness is its first consumer; the producer side is wired into `serve` in P-S06). The harness is the machinery under EVERY later drill in the whole ledger.
- **GATE / DRILLS (quantified; must be green to call this done).** The **harness self-test** (master §2 M0 exit; roadmap SUB-M0 exit): inject one fault via `break_dependency`, drive one unit of load, read one telemetry assertion that reads green — the unit-of-proof drilling itself. Quantified: the assertion library returns a typed green/red, never a swallowed pass (no `... || true`; EI-01 §5). Green artifact = the self-test scenario emitting a dated PASS row.
- **TESTS (required).** Unit: the load generator hits the configured multiplier ±tolerance and the principal mix; `break_dependency`/`restore_dependency` are reversible (a broken dep restores cleanly); `assert_signal` fails loudly on a red signal (a test that an inverted assertion is rejected, EI-01 §3). The harness self-test scenario is itself a committed test.
- **DEFINITION OF DONE.** The three harness pieces + the incident-loop hook exist and compile; the harness self-test emits a dated green artifact; the assertion library is loud-never-swallowed; committed.
- **COMMIT.** Header `P-<NNN> M0: failure-injection harness + telemetry-assertion library`. Body: the 1×/10×/30× generator, the scoped-reversible injector, the signal-set reader, the harness self-test green artifact. Co-Authored-By trailer.

---

### P-S03 — The transactional outbox, the canonical envelope, and the relay (the silent-data-loss floor)

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 1 — the transactional outbox slice) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 2.
- **DEPENDS-ON.** P-S01, P-S02.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../VISION.md` §3 (name-your-floors; quality over plan-adherence).
  - `../../external-insights/01-process-and-quality-doctrine.md` §2 (silent data loss outranks every feature — stop-the-bleeding first) and §3 (prove-it: zero messages lost across a reconnect).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §2.1 (`myelin-events` — the `EventEnvelope` restated to the frozen anchor; `OutboxTx::emit(draft, cause)` the ONLY emit path, causality correct-by-construction; the `ArtifactRef` type) and §2.10 (the canonical envelope field list + units — the X-5 names/units authority).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 2.1 (`EventEnvelope`), 2.2 (`OutboxTx::emit` — no `publish_now`), 2.3 (`outbox` table — `UNIQUE(aggregate, seq)` per-aggregate ordering, relay `FOR UPDATE SKIP LOCKED`, stable ULID for broker dedup, dead-letter after bounded retries), 2.7 (crypto-shred/tombstone on the log; inline-PII events envelope-encrypted with `pii_key_ref`). Read `00-reconciliation-decisions.md` X-5 for the names/units rationale.
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 rows SUB-D1, SUB-D2, BUS-D4.
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-events`: (a) finalise the `EventEnvelope` struct to the frozen field list (architecture §2.1 + §2.10 — `event_id` ULID, `type_`, `schema_ver`, `tenant`, `region`, `actor`, `subject` ArtifactRef, `aggregate`, `causation_id`, `correlation_id`, `caused_by`, `depth`, `contains_personal_data`, `data_role`, `visibility`, `pii_key_ref`, `occurred_at`, `recorded_at`, `payload`) with the frozen units (timestamps RFC-3339 UTC, costs integer minor-units, TTLs seconds, `pii_key_ref = kms://<tenant>/<dek-epoch>/<class>`); (b) implement `OutboxTx::emit(draft, cause)` deriving causality correct-by-construction (root carries; `causation_id = cause.event_id`; `depth = cause.depth + 1`) — there is intentionally NO `publish_now` in the API; (c) the `outbox` table migration (`(event_id UNIQUE, aggregate, seq, subject, envelope)`, `UNIQUE(aggregate, seq)`); (d) the **relay** — claims unsent rows with `FOR UPDATE SKIP LOCKED` (safe across replicas), stamps the stable ULID, publishes to the broker abstraction, marks sent, dead-letters after bounded retries. The broker is a trait (an in-process fake for tests; the real adapter is the Bus's M0 deliverable — name this boundary as the seam). **Floor named:** single-region event log on a general-purpose DB (roadmap §3 floor table) — the column-store seam is the post-M5 follow-on, added only when volume is measured.
- **CONTRACTS TO IMPLEMENT.** Owned: 2.1, 2.2, 2.3, 2.7 (envelope-encryption hook only — the KMS hierarchy is Storage's M1 11.3). The envelope is frozen here as the names/units anchor every later contract aligns to (X-5).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D1** (kill service between commit and publish → exactly-once-in-effect, **0 ghost, 0 lost**; outbox-depth drains, dedup ledger absorbs) — telemetry signals `outbox_depth → 0`, `ghost_count == 0`, `lost_count == 0`, CI. **BUS-D4** (crash producer between state-commit and publish → event delivered, never without state; outbox **emit-iff-committed**) — signal `emit_iff_committed == true`, CI. (SUB-D2 needs the consumer template P-S04; it is greened there.) Never weaken a threshold to pass; a red gate becomes a dated "claimed, not proven" thresholds-file row.
- **TESTS (required).** Unit: `emit` derives causality correctly (depth + parent + root); the relay claims with `FOR UPDATE SKIP LOCKED` and is idempotent under a re-claim; there is NO `publish_now` symbol (a compile-fixture test the API has no fire-and-forget path — the `no-raw-publish` lint, P-S05, enforces this externally). CDC pair: provider + consumer contract test for 2.1/2.2/2.3 against the frozen envelope shape (the contract-coverage scanner, P-S09, fails the build if absent). Drill harness scenarios for SUB-D1 + BUS-D4 (using the P-S02 injector to kill between commit and publish). Mutation floor: this is a mandatory-core module (the emit path) — state and meet a cargo-mutants mutation-score floor ≥ 80% on the outbox/relay module.
- **DEFINITION OF DONE.** The envelope + `emit` + the `outbox` table + the relay exist and compile to the frozen shapes; SUB-D1 and BUS-D4 emit dated green artifacts; the CDC pair + unit tests + the mutation floor pass; the single-region floor is named with its follow-on; committed.
- **COMMIT.** Header `P-<NNN> M0: transactional outbox + canonical envelope + relay`. Body: contracts 2.1/2.2/2.3/2.7; SUB-D1 (0 ghost/0 lost) + BUS-D4 (emit-iff-committed) with measured numbers; the single-region floor + post-M5 column-store follow-on. Co-Authored-By trailer.

---

### P-S04 — The idempotent event-consumer template and the dedup ledger

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 1 — the idempotent-consumer template slice) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 2 (the `EventHandler` template half).
- **DEPENDS-ON.** P-S03.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §2 (silent data loss first) and §4 (chain mutations end-to-end; real sessions chain, that's where bugs live).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §5 (the event-consumer template + the seven encoded rules: idempotent on `event_id` via `consumer_dedup`; ack-after-enqueue; whitelist subjects never `*`; bind-durable-by-name; terminate poison; bounded prefetch; lag as a first-class metric) and §5.3 (causality through the consumer — `emit(draft, cause = Some(incoming))`).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 2.4 (`EventHandler` template + `HandleOutcome`), 2.5 (`consumer_dedup` ledger `(consumer, event_id)` PK), 2.8 (schema-evolution upcasters, forward-only).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D2.
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-events`: the `EventHandler` trait (`subjects() -> &'static [SubjectPattern]` whitelist — never `*`; `handle(ev) -> HandleOutcome`) + `HandleOutcome { Done | NonRetryable(Reason) | Retry(Backoff) }`; the consumer runtime that encodes the seven rules (idempotent on `event_id` via the `consumer_dedup` ledger `(consumer, event_id)` PK; ack-after-enqueue; reject a `*` subscription; bind-by-name; dead-letter poison; bounded prefetch; export consumer-lag as a metric); the `consumer_dedup` table migration; the `(type, from_ver) → to_ver` upcaster registry (2.8, pure fns at consume, forward-only). Wire the lag + dead-letter counts into the telemetry signal set (contract 1.8).
- **CONTRACTS TO IMPLEMENT.** Owned: 2.4, 2.5, 2.8.
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D2** (drop broker mid-stream → **0 lost** across reconnect by bind-by-name + dedup; a slow subject does NOT head-of-line-block others) — signals `consumer_lag` recovers, `lost_count == 0`, no HoL stall, CI. Re-confirm SUB-D1 end-to-end through a consumer (the dedup ledger absorbs the redelivery → 0 dup).
- **TESTS (required).** Unit: a `*` subscription is rejected at registration; a redelivered `event_id` is a no-op (the ledger absorbs it); a poison message dead-letters and does not burn the redelivery budget; a slow subject does not block a fast one (bounded prefetch). CDC pair for 2.4/2.5. Drill scenario for SUB-D2 (the P-S02 injector drops the broker mid-stream). Tests CHAIN: emit → drop broker → reconnect → re-consume, asserting 0 lost + 0 dup over the sequence (EI-01 §4, the sequence property). Mutation floor ≥ 80% on the consumer-runtime module (mandatory core).
- **DEFINITION OF DONE.** The template + ledger + upcasters exist and compile; SUB-D2 emits a dated green artifact and SUB-D1 re-confirms 0 dup through a consumer; CDC + chained tests + mutation floor pass; committed.
- **COMMIT.** Header `P-<NNN> M0: idempotent event-consumer template + dedup ledger`. Body: 2.4/2.5/2.8; SUB-D2 (0 lost / no HoL) with measured numbers. Co-Authored-By trailer.

---

### P-S05 — The twelve committed architecture lints, each with a red + green fixture

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 3 — the twelve lints / the ratchet) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 3.
- **DEPENDS-ON.** P-S01, P-S03, P-S04 (the lints scan the crate surfaces these shipped).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §5 (the ratchet — convert each discipline into a committed mechanical gate; an uncommitted gate is no gate; loud, never silently swallowed — replace `... || true`).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §2.11 (the enforced architecture lints — the full table: `no-cross-db`, `no-raw-publish`, `tenant-predicate`, `no-host-exec`, `forward-only-migration`, `no-cross-sync-cycle`, `residency-pin`, `control-plane-pii-free`, `search-requires-acl-filter`, `no-llm-in-platform`, `no-untagged-personal-data`, `flow-determinism`; the four most load-bearing).
  - `../../05-refined-shared-systems-architecture/contract-index.md` row 1.6 (the twelve lints; each ships with a red-fixture that proves it rejects + a green-fixture that proves it admits).
  - `../../06-roadmaps/00-master-sequencing.md` §1 Tier 3 (the committed ratchet) + the M0 exit gate (all twelve lints green with both fixtures).
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-substrate` (or a dedicated `myelin-lints` crate inside it): the twelve architecture lints as `cargo`-level architecture tests / clippy-style checks, wired into CI **loud-never-swallowed** (no `... || true`; a violation fails the build). For EACH of the twelve, ship a **red-fixture** (a code sample the lint MUST reject — the build fails on it) and a **green-fixture** (a sample it MUST admit). Implement to the EXACT rule text of architecture §2.11 (e.g. `tenant-predicate`: every query-builder call carries a `TenantId` bound, a tenant-less query fails to compile; `no-raw-publish`: no bus publish outside `OutboxTx::emit`; `no-host-exec`: no host-execution path bypassing `ToolHands::exec`; `no-untagged-personal-data`: every PII-carrying field is `#[personal_data(...)]`-tagged). Some lints (`search-requires-acl-filter`, `flow-determinism`, `control-plane-pii-free`) target code that does not exist yet — ship the lint + its fixtures now (the rule and its red/green proof), so the gate is live before the consumer ships; name this as the lint's floor (it tightens as the targeted code lands).
- **CONTRACTS TO IMPLEMENT.** Owned: 1.6 (the twelve lints — this prompt makes the ratchet real).
- **GATE / DRILLS (quantified; must be green to call this done).** **All twelve lints green** with both fixtures: each red-fixture causes a build failure (proves it rejects), each green-fixture builds clean (proves it admits) — 12/12 reject + 12/12 admit. Green artifact = a CI run showing all twelve lint jobs green + the fixture matrix. Wired loud (a lint that silently passes is a defect).
- **TESTS (required).** The fixture matrix IS the test: a meta-test runs each red-fixture and asserts a compile/lint failure, each green-fixture and asserts success. A regression test that removing any lint's wiring fails the meta-test (the ratchet cannot be silently un-wired). No CDC pair (lints are not cross-system contracts with a consumer side; they are the gate UNDER every contract).
- **DEFINITION OF DONE.** All twelve lints exist, are wired into CI loud-never-swallowed, and pass the 12×(red-reject + green-admit) fixture matrix; lints targeting not-yet-existing code are named as floors that tighten on their consumer; committed.
- **COMMIT.** Header `P-<NNN> M0: twelve committed architecture lints + red/green fixtures`. Body: the twelve lints by name; the 12/12-reject + 12/12-admit matrix; the floor lints + their tightening triggers. Co-Authored-By trailer.

---

### P-S06 — `serve(AppSpec)`: boot, migrate, relay, consumers, three ports, graceful drain

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 6-precondition — the service shell) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 4 (the `serve` + ports + migrations half).
- **DEPENDS-ON.** P-S03, P-S04 (relay + consumers are wired by `serve`).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §7 (abstract at the third copy — the harness IS that abstraction; identical plumbing, visible logic).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §3.1 (the one call — `serve(AppSpec{ name, config, migrations, public, internal, consumers, holders, outbox })`; boot → migrate → relay → consumers → three ports → graceful drain), §3.2 (config env-first, validated at boot, fail fast), §3.3 (DB pool + outbox publisher; bounded pool, statement timeout, fast-fail on saturation; read-replica awareness), §3.4 (`PersonalDataHolder` auto-registration — every store the harness opens), §3.5 (telemetry + the three ports), §3.6 (what the harness deliberately does NOT do), §4 (the three-surface topology — public/internal/metrics-health; public↔internal is a security boundary; tenant from token never URL), §4.3 (liveness ≠ readiness), §9 (forward-only online migrations runner; expand→backfill→contract; §9.4 hot-table declaration mechanism).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 1.1 (`serve(AppSpec)`), 1.2 (three-surface topology + tenant-from-token), 1.3 (liveness ≠ readiness), 1.4 (`PersonalDataHolder` auto-registration mechanism), 1.5 (forward-only migrations + hot-table flags).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 rows SUB-D7 (cross-tenant IDOR), SUB-D9 (liveness ≠ readiness).
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-substrate`: the `AppSpec` struct + `serve(AppSpec) -> Result<()>` owning the lifecycle (boot → run migrations → start the outbox relay → start consumers → open the three ports → serve → graceful drain: stop intake, finish in-flight, ack-then-exit; non-zero on failed boot). The **three-surface topology**: a public surface (gateway-fronted, identity+causality headers injected, **tenant from the verified token never the URL path** — a mismatch is rejected + audited as an IDOR), an internal RPC surface (trust boundary; re-authorize every call), a metrics-health surface (export the contract-1.8 signal set; **liveness = not-wedged, readiness = can-serve-correct-traffic** — a dead critical dependency reports not-ready and sheds, never healthy-but-failing; startup = not-ready-not-killed). Config env-first + validated at boot. The bounded DB pool (statement timeout, fast-fail on saturation). `holders: AppSpec::auto` — auto-register every store the harness opens as a `PersonalDataHolder` (the mechanism; the exhaustive H1–H18 list is GDPR's M1 deliverable). The forward-only migration runner + the hot-table declaration mechanism (`forward-only-migration` lint reads the declaration). A hello-world test service that boots from `serve`, emits one event through the outbox, a consumer dedups it, and the metrics port answers (the M0 "first runnable").
- **CONTRACTS TO IMPLEMENT.** Owned: 1.1, 1.2, 1.3, 1.4 (mechanism), 1.5 (runner + declaration mechanism).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D7** (cross-tenant read via path-tenant ≠ token-tenant → **0**; the `tenant-predicate` lint catches a tenant-less query at compile time) — signals `misroute_count == 0` + lint green, CI. **SUB-D9** (kill a critical dependency → instance reports not-ready + sheds; liveness does NOT restart-storm) — signals `readiness` flips, no liveness churn, CI.
- **TESTS (required).** Unit: a URL-path tenant ≠ token tenant is rejected + audited (SUB-D7 mechanism); readiness flips on a severed critical dep while liveness stays up (SUB-D9); graceful drain finishes in-flight before exit; every store opened auto-registers as a holder. CDC pair for 1.1/1.2/1.4. Drill scenarios for SUB-D7 (path-tenant spoof) and SUB-D9 (P-S02 injector kills a critical dep). The hello-world service is an end-to-end boot test (boot → emit → consume → metrics). Mutation floor ≥ 75% on the lifecycle + tenant-from-token modules.
- **DEFINITION OF DONE.** `serve(AppSpec)` + the three ports + the migration runner + holder auto-registration exist and compile; SUB-D7 and SUB-D9 emit dated green artifacts; the hello-world boot test + CDC + mutation floor pass; the exhaustive-holder-list dependency on GDPR M1 is named; committed.
- **COMMIT.** Header `P-<NNN> M0: serve(AppSpec) + three-surface topology + migration runner`. Body: 1.1–1.5; SUB-D7 (misroute 0) + SUB-D9 (readiness flips, no restart-storm) with measured numbers; the holder-list M1 follow-on. Co-Authored-By trailer.

---

### P-S07 — The shared resilient inter-service client (`myelin-client`) + `Retry-After` honouring

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 6-precondition — the resilience primitives, resilient-client half) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 4 (`ResilientClient`).
- **DEPENDS-ON.** P-S01, P-S02.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (build the failure-injection harness early; cheap drills in CI) and §5 (loud, never swallowed).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §6 (the shared resilient client — the four primitives, all mandatory + on-by-default: per-call timeout/deadline-propagation; circuit breaker closed→open→half-open never-retry-through-a-tripped-breaker; bounded-concurrency bulkhead fast-fails never queues unbounded; jittered retry idempotent-calls-only full-jitter), §6.2 (`Retry-After` honouring — our clients MUST honour it as the floor of backoff; a hard requirement on the agent runtime + CLI too), §6.3 (defaults — timeouts in ms; breaker as a failure ratio + min request count; bulkhead an integer cap; backoff base ms full jitter; per-target values are each consumer's call).
  - `../../05-refined-shared-systems-architecture/contract-index.md` row 1.9 (`ResilientClient::call(target, req, idem)` — timeout + breaker + bulkhead + jittered-retry-idempotent-only; honours `Retry-After`).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D5.
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-client`: `ResilientClient::call<R>(target, req, idem: Idempotency) -> Result<R>` with the four mandatory on-by-default primitives — (1) per-call timeout (in ms; deadlines propagate); (2) circuit breaker (closed→open→half-open, never retry through a tripped breaker); (3) bounded-concurrency bulkhead per target (saturation fast-fails, never queues unboundedly); (4) jittered retry for `Idempotency::Idempotent` calls only, full jitter (Brooker 2015) — a `NonIdempotent` call is never retried. **`Retry-After` honouring**: respect the header as the floor of backoff. Export breaker-state, bulkhead-rejections, and `Retry-After` issuance into the contract-1.8 signal set. Ship one **default per-target value set** (the M0 floor); the per-target tuned values are the named M5 follow-on (roadmap §3 floor table). **Floor named:** default per-target values (M0) → per-target tuned values measured by the surge/latency drills (M5, P-S16).
- **CONTRACTS TO IMPLEMENT.** Owned: 1.9.
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D5** (trip a downstream breaker under load → callers fail fast, NO retry through the tripped breaker, honour `Retry-After`, no amplification) — signals `breaker_state == open` while callers fail-fast, `retry_through_tripped == 0`, `retry_after_honoured == true`, CI.
- **TESTS (required).** Unit: a tripped breaker rejects without calling through; a `NonIdempotent` call is never retried; full-jitter backoff never exceeds the `Retry-After` floor downward (it respects it as the floor); a saturated bulkhead fast-fails rather than queueing. CDC pair for 1.9 (provider = a fake downstream that trips; consumer = the client). Drill scenario for SUB-D5 (P-S02 trips a downstream breaker under load). Mutation floor ≥ 80% on the breaker + retry modules (mandatory core — a retry-storm amplifier is a cascade).
- **DEFINITION OF DONE.** `ResilientClient::call` + `Retry-After` honouring exist and compile to the frozen shape; SUB-D5 emits a dated green artifact; CDC + mutation floor pass; the default-per-target floor is named with its M5 tuning follow-on; committed.
- **COMMIT.** Header `P-<NNN> M0: resilient inter-service client + Retry-After honouring`. Body: 1.9; SUB-D5 (fail-fast, no amplification, Retry-After honoured) with measured numbers; the per-target-value floor + M5 follow-on. Co-Authored-By trailer.

---

### P-S08 — Fail-static mechanism, the protected-human-lane shed order, and bounded-everything

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (Tier 6-precondition — fail-static mechanism + the shed order) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 4 (`FailStatic<T>` + the shed lane).
- **DEPENDS-ON.** P-S06 (the shed lane runs at the public surface), P-S02.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §2 (order by non-negotiability — a shared-dependency cascade is a platform-wide kill) and §3 (name your floors; record needs-human-verification honestly).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §7.1 (bounded everything — every queue and pool is bounded; an unbounded queue = unbounded latency = indistinguishable from down), §7.2 (the principal-aware limiter + protected human lane — shed order: speculative → batch/CI → agent → human-last; `429 + Retry-After`; per-tenant so one tenant's surge doesn't shed another's humans), §7.3 (why this order — promise strength), §7.4 (agent-generated load — bounded dispatch pool drops over-cap never forks; causal-depth ceiling; shared-root tripwire), §7.5 (bounded predicate evaluation), §7.6 (the per-surface shed-budget v1 floor table — CI-surge / collab op-stream / connection-storm / agent-mention-storm), §8 (fail-static — distinguish fail-closed from fail-static; `FailStatic<T>{ fresh_ttl, static_max }`; `Answer<T> = Fresh | Static(degraded) | Closed`; stale-while-revalidate; never fail open), §8.2 (the staleness bound — `static_max ≤ revocation-SLA ≥ agent-token-TTL`; the VALUE W is `[OPEN — LEGAL]`, DPO-ratified, L-1).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 1.10 (`FailStatic<T>`), 1.11 (protected-human-lane shed order + per-surface shed budgets), 4.11 (the Id-usage fail-static bound; `[OPEN — LEGAL]` ratification).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D8.
- **DELIVERABLE (what to build + exactly where in the repo).** In crate `myelin-substrate`: (a) `FailStatic<T>{ fresh_ttl: Seconds, static_max: Seconds }` with `get(key, refresh) -> Answer<T>` (`Fresh | Static(degraded) | Closed`): within `fresh_ttl` → fresh; between → stale + degraded marker + background refresh (stale-while-revalidate); past `static_max` → fail closed — never fail open; the constraint `static_max ≤ revocation-SLA ≥ agent-token-TTL` is enforced in the constructor (a value violating it does not construct); the VALUE W is read from the thresholds file (P-S09) and flagged `[OPEN — LEGAL]`. (b) The **principal-aware shed lane** at the public surface: read `Principal.kind` + run-class, reserve the protected human lane, apply the shed order (speculative → batch/CI → agent → human-last) with `429 + Retry-After`, **per-tenant** (one tenant's surge does not shed another's humans). (c) **Bounded everything**: every queue/pool fast-fails (sheds) rather than growing latency unboundedly. (d) The agent-load caps: the bounded dispatch pool (drops over-cap, never forks), the causal-depth ceiling (reads `EventEnvelope.depth`), the shared-causal-root-within-a-window tripwire. (e) The §7.6 per-surface shed-budget **v1 floor table** (named floors, tuned by drills at M5). **Floors named:** the shed-budget numbers (M0 floor table → tuned numbers M5, P-S15); the fail-static value W (`[OPEN — LEGAL]` → DPO-ratified, parallel/legal). Fail-static is PROVEN against a real Identity hiccup in M1 (P-S11) — here only the mechanism + the SUB-D8 loop-guard machinery are drilled.
- **CONTRACTS TO IMPLEMENT.** Owned: 1.10 (mechanism + constraint), 1.11 (shed order + v1 budget floor table). Carried: 4.11 (the Id-usage bound — the constraint; the value is DPO-ratified).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D8** (adversarial agent→agent loop → the depth ceiling (12/16) + the shared-root tripwire + the bounded pool halt it) — signals `causal_depth` histogram bounded, `tripwire_fired`, `dispatch_pool_drops` (over-cap dropped, never forked), CI. (The full agent-loop proof re-runs in M2 with the agent fabric; the substrate ships and drills the machinery here.) The fail-static SUB-D4 proof is deferred to M1 (P-S11) — named, not skipped.
- **TESTS (required).** Unit: `FailStatic` serves fresh/stale/closed at the boundaries and never escalates access (never fails open); a constructor with `static_max > revocation-SLA` is rejected; the shed order sheds in the right priority and the human lane is shed last + per-tenant; the dispatch pool drops over-cap rather than forking; the depth ceiling + tripwire halt a constructed loop. CDC pair for 1.10/1.11. Drill scenario for SUB-D8 (P-S02 drives an agent→agent loop). Mutation floor ≥ 80% on the fail-static + shed-order modules (mandatory core — a fail-open is catastrophic).
- **DEFINITION OF DONE.** `FailStatic`, the shed lane, bounded-everything, and the loop-guard machinery exist and compile; SUB-D8 emits a dated green artifact; the SUB-D4 fail-static proof is named as deferred to M1; CDC + mutation floor pass; the shed-budget + value-W floors are named with their follow-ons; committed.
- **COMMIT.** Header `P-<NNN> M0: fail-static mechanism + protected-human-lane shed order + bounded-everything`. Body: 1.10/1.11/4.11; SUB-D8 (depth ceiling + tripwire + bounded pool) with measured numbers; the shed-budget floor (→ M5) + value-W `[OPEN — LEGAL]` (→ DPO) + the SUB-D4-deferred-to-M1 note. Co-Authored-By trailer.

---

### P-S09 — The contract-coverage scanner, the versioned thresholds file, and the shared overlay/state primitives

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (the committed-gate machinery — scanner + thresholds + overlay primitives) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 5.
- **DEPENDS-ON.** P-S03, P-S04, P-S07 (the scanner reads the contract surfaces these shipped).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §5 (the ratchet — an uncommitted gate is no gate; make violations loud; a contract violation dropped silently is a multi-day misdiagnosis) and §4 (the off-screen-picker / clipped-dialog / unreachable-control bug-classes — foreclose them at the design-system layer) and §3 (the thresholds file; a red gate becomes a "claimed, not proven" row, never edited green).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §10.2 (the telemetry signal set the thresholds key on).
  - `../../05-refined-shared-systems-architecture/contract-index.md` §1 + §2 (the contract rows the scanner enforces have a provider + consumer CDC pair).
  - `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 item 5 (contract-coverage scanner; the shared overlay/state primitives built BEFORE any feature consumes them; the versioned thresholds file holding every Q32 default-to-beat — N=5min revocation, 30× surge, W=5min fail-static, RPO ≤ 5min, RTO ≤ 1h/tenant ≤ 4h/cell, depth ceilings 12/16).
  - `../00-ledger-overview.md` §6 (the contract-coverage scanner fails the workspace if any contract-index row lacks provider + consumer CDC; the thresholds file is where a GATE number is read from).
- **DELIVERABLE (what to build + exactly where in the repo).** (a) The **contract-coverage scanner** (a CI tool in `myelin-substrate` or a workspace `xtask`): it reads the contract-index rows and FAILS the workspace if any row lacks BOTH a provider-side and a consumer-side CDC test — loud, never swallowed. (b) The **versioned thresholds file** (one file, e.g. `thresholds.toml` at the workspace root): every Q32 default-to-beat as a named, dated row (revocation N = 5 min, surge multiplier = 30×, fail-static W = `[OPEN — LEGAL]` placeholder with the `≤ revocation-SLA ≥ agent-token-TTL` constraint, RPO ≤ 5 min, RTO ≤ 1h/tenant ≤ 4h/cell, causal-depth ceilings 12/16, per-surface shed budgets v1 floors). A drill reads its threshold from here; a red gate becomes a "claimed, not proven" scorecard row IN THIS FILE — never edited green. (c) The **shared overlay/state primitives** (the design-system layer floor, testing-strategy §5): the popover/dialog/picker/focus-trap primitives that foreclose the off-screen-picker, clipped-dialog, and focus-leak bug-classes — built once here so no feature hand-rolls them (EI-01 §7 abstract-at-the-third-copy, applied pre-emptively because the doctrine names these exact recurring bugs). If the frontend stack is not yet chosen, ship the primitive CONTRACT (the API + the bug-class invariants the primitives must hold) and name the implementation as a floor tied to the first frontend-bearing subsystem (M3+).
- **CONTRACTS TO IMPLEMENT.** This prompt makes the GATE MACHINERY real that every later prompt's DEFINITION OF DONE leans on (the scanner) and the THRESHOLDS SOURCE every drill reads. No new contract-index row is owned; it enforces all of §1–§13.
- **GATE / DRILLS (quantified; must be green to call this done).** The **contract-coverage scanner passes** on the (still-small) contract set shipped so far (every row with a provider has a CDC pair, or is explicitly marked not-yet-implemented with its landing prompt) — green artifact = a scanner run with 0 uncovered rows that claim coverage. The thresholds file parses and every M0 drill reads its threshold from it (no hardcoded magic number in a drill). The overlay-primitive invariant tests pass (or, if the impl is floored, the invariant CONTRACT is committed + named).
- **TESTS (required).** Unit: the scanner FAILS loudly on a deliberately-uncovered row (a red-fixture for the scanner itself — it must reject); the thresholds file round-trips and a missing threshold is a loud error not a default; the overlay primitives hold their invariants (a picker never opens off-screen; a dialog is never clipped; focus is trapped) OR the contract test asserts the invariant set exists. No CDC pair (the scanner is meta-gate, not a cross-system contract).
- **DEFINITION OF DONE.** The scanner + thresholds file + overlay primitives (or their floored contract) exist and are wired into CI loud-never-swallowed; the scanner passes with 0 falsely-claimed coverage; every M0 drill reads its threshold from the file; any floored overlay impl is named with its follow-on; committed.
- **COMMIT.** Header `P-<NNN> M0: contract-coverage scanner + thresholds file + overlay primitives`. Body: the scanner (fails on uncovered rows), the thresholds file (the Q32 defaults), the overlay primitives or their floored contract + follow-on. Co-Authored-By trailer.

---

### P-S10 — Green the M0 exit gate: SUB-D1/D2/BUS-D4/D5/D7/D8/D9 + all twelve lints + the harness self-test

- **BAND.** M0.
- **ROADMAP MILESTONE.** SUB-M0 (the M0 exit gate — the consolidated band-boundary proof) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 exit gate + `../../06-roadmaps/00-master-sequencing.md` §2 M0→M1 row.
- **DEPENDS-ON.** P-S02, P-S03, P-S04, P-S05, P-S06, P-S07, P-S08, P-S09.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §2 (the gate invariant — no later band done over a red earlier gate) and §3 (PROVEN not CLAIMED; observability is part of the pass condition; never weaken a threshold to make a check pass).
  - `../../06-roadmaps/00-master-sequencing.md` §2 M0 exit gate + §4 (the M0→M1 boundary row: SUB-D1/SUB-D2/BUS-D4 + all twelve lints + the harness self-test).
  - `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M0 exit gate (the full list incl. SUB-D5/D7/D8/D9) + §5 (the green-artifact rule).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 rows SUB-D1, SUB-D2, BUS-D4, SUB-D5, SUB-D7, SUB-D8, SUB-D9.
- **DELIVERABLE (what to build + exactly where in the repo).** A consolidated **M0 exit-gate scorecard** (a CI workflow + a committed scorecard artifact under the testing tree, e.g. `testing/scorecards/sub-m0.md`) that runs and records, each as a DATED green artifact reading the telemetry signal set (contract 1.8): SUB-D1 (0 ghost/0 lost), SUB-D2 (0 lost/no HoL), BUS-D4 (emit-iff-committed), SUB-D5 (fail-fast/Retry-After), SUB-D7 (misroute 0 + lint), SUB-D8 (depth ceiling + tripwire), SUB-D9 (readiness flips/no restart-storm), all twelve lints green with both fixtures, the contract-coverage scanner pass, and the harness self-test. This prompt does NOT re-implement the drills (they live with their feature prompts P-S03..P-S09); it WIRES them into one band-boundary gate, asserts each emits a dated green artifact, and records any red as a "claimed, not proven" thresholds-file row (never edited green). It is the build-layer realisation of the master M0→M1 gate invariant.
- **CONTRACTS TO IMPLEMENT.** None new — this is the gate-aggregation prompt. It MAKES THE M0 BAND BOUNDARY a single green/red signal (the permanent gates SUB-D1/D2 + BUS-D4 re-run on every emit-path change from here on).
- **GATE / DRILLS (quantified; must be green to call this done).** ALL of: SUB-D1, SUB-D2, BUS-D4, SUB-D5, SUB-D7, SUB-D8, SUB-D9 green (each with its quantified threshold + telemetry signal as above); 12/12 lints green with both fixtures; the contract-coverage scanner passes; the harness self-test passes. Green artifact = the dated `sub-m0` scorecard with every row PROVEN. A single red row blocks M1 (the gate invariant) — record it honestly, do not soften it.
- **TESTS (required).** The scorecard workflow itself is a committed CI gate. A meta-test: removing any drill from the scorecard or flipping any threshold green-without-proof fails the gate (the ratchet cannot be gamed). No CDC pair.
- **DEFINITION OF DONE.** The M0 exit-gate scorecard exists, is wired into CI, and every row emits a dated green artifact (or is honestly recorded as claimed-not-proven, blocking M1); the permanent gates SUB-D1/D2/BUS-D4 are marked re-run-forever; committed.
- **COMMIT.** Header `P-<NNN> M0: M0 exit-gate scorecard (SUB-D1/D2/BUS-D4/D5/D7/D8/D9 + 12 lints + harness self-test)`. Body: each drill greened with its measured number; the twelve lints; the scanner pass; the permanent-gate marking. Co-Authored-By trailer.

---

### P-S11 — Prove fail-static against a real Identity hiccup (SUB-D4)

- **BAND.** M1.
- **ROADMAP MILESTONE.** SUB-M1 (fail-static proven) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M1 (the fail-static-proven slice).
- **DEPENDS-ON.** P-S08 (the `FailStatic` mechanism), P-S10 (M0 green), and the Identity authz-client prompts that ship `authenticate`/`check`/`list_objects`/zookie (Identity's M1 ledger — the substrate wires `FailStatic` into Identity's read path; this prompt MUST start after Identity's authz client is mergeable).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (prove-it — a property is not real until a drill forces the failure and observability watches the system survive) and §2 (a shared-dependency cascade kills the platform — fail-static, not fail-closed, is the availability default).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §8 (fail-static — full), §8.2 (the staleness bound `static_max ≤ revocation-SLA ≥ agent-token-TTL`; the zookie bypass — a security-sensitive read passes the zookie so it BYPASSES the fail-static cache), §8.3 (interaction with readiness — fail-static handles a TRANSIENT hiccup; readiness handles a SUSTAINED outage).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 1.10 (`FailStatic<T>`), 4.10 (`Consistency`/zookie — zookie-stamped reads bypass the cache; the authz reverse index honours the zookie revision watermark), 4.11 (the Id-usage bound; `[OPEN — LEGAL]` value W).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D4 (Id-hiccup → already-authenticated survives within W; revoked denied when window closes).
- **DELIVERABLE (what to build + exactly where in the repo).** In `myelin-substrate` (and the thin authz-client wiring in `myelin-identity`'s consumer surface): wire the M0 `FailStatic<T>` mechanism into the Identity authz read path so that on a transient Identity hiccup, already-authenticated traffic is served the coarse "actor still active / coarse grants" answer within `static_max` (never an escalation of access — never fail open). Enforce the **zookie bypass**: a security-sensitive read carrying a zookie BYPASSES the fail-static cache (contract 4.10) and forces a fresh read past the watermark. Read the value W from the thresholds file (`[OPEN — LEGAL]` placeholder; the mechanism + constraint ship regardless). Export the fresh/stale/closed ratios into the telemetry signal set. **Floor named:** the value W remains `[OPEN — LEGAL]` (DPO-ratified) — the mechanism is proven here regardless of the final number.
- **CONTRACTS TO IMPLEMENT.** Consumed: 4.10 (the zookie bypass + watermark — Identity owns the index; the substrate wires the bypass), 4.11 (the bound). Owned (proven here): 1.10 against a real dependency.
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D4** (inject a transient Identity hiccup → already-authenticated traffic survives on the coarse cache within W; a revoked actor is denied once the window closes; an agent token (life == run life) expires inside the window; a zookie-stamped read bypasses the cache) — signals: fail-static fresh/stale/closed ratios read green, staleness never exceeds `static_max ≤ revocation-SLA`, `revoked_after_window_denied == true`, CI. This contributes to the master M1→M2 boundary (mirror of Identity's ID-D2). Never weaken W to pass — if the chosen W is unproven, record it claimed-not-proven.
- **TESTS (required).** Unit: within W a hiccup serves stale + degraded (never open); past W it fails closed; a zookie read bypasses the cache; a revoked actor is denied once the window closes. CDC pair for 1.10 against the Identity authz client (provider = Identity hiccuping; consumer = the substrate cache). Drill scenario for SUB-D4 (P-S02 injects an Identity hiccup; the harness asserts the survival signals). Tests CHAIN: authenticate → hiccup → serve-stale → revoke → window-closes → deny (the sequence property, EI-01 §4). Mutation floor ≥ 80% on the fail-static-wiring module.
- **DEFINITION OF DONE.** Fail-static is wired into the Identity read path with the zookie bypass; SUB-D4 emits a dated green artifact; the value-W `[OPEN — LEGAL]` floor is named; CDC + chained + mutation tests pass; committed.
- **COMMIT.** Header `P-<NNN> M1: fail-static proven against a real Identity hiccup (SUB-D4)`. Body: 1.10/4.10/4.11; SUB-D4 (survives within W, revoked denied at window close) with measured numbers; the value-W `[OPEN — LEGAL]` follow-on. Co-Authored-By trailer.

---

### P-S12 — The restore-verify cross-seam half (SUB-D6, the silent-data-loss floor, with Storage)

- **BAND.** M1.
- **ROADMAP MILESTONE.** SUB-M1 (the restore-verify cross-seam half + the exhaustive-holder confirmation) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M1 (the restore-verify slice).
- **DEPENDS-ON.** P-S02 (the injector/assertion machinery), P-S10 (M0 green), and the Storage M1 prompts that ship the backup/restore + WAL/PITR + the blob/index/offset seams (Storage's M1 ledger — the substrate supplies the injection/assertion half; this prompt MUST start after Storage's backup/restore is mergeable). Also the GDPR M1 holder-list prompt (for the exhaustive-holder confirmation).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §2 (silent data loss outranks every feature — the restore-verify gate is a CI job, not an aspiration) and §3 (RPO/RTO are quantified thresholds; a target you cannot measure is not a gate).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §11 row D-6 (restore + cross-seam integrity — rebuild from backups; OLTP rows ↔ blob ↔ search index ↔ event-log offsets restore to one consistent point, no row → missing blob) + §9.2 (measure lock time against a restored copy — ties to the restore machinery) + §3.4 (`PersonalDataHolder` auto-registration — the M1 exhaustive H1–H18 list).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 11.5 (backup/restore/cross-seam integrity — Storage owns the restore-verify CI job; the substrate owns the failure-injection + telemetry half) + 1.4 (holder auto-registration; the exhaustive list is GDPR's M1 deliverable). Read `00-reconciliation-decisions.md` for STOR-4 / ADR-18.
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D6 (= STOR-D1/STOR-D2: rebuild from backups → no loss; one consistent cross-seam point; RPO ≤ 5 min, RTO ≤ 1h/tenant ≤ 4h/cell).
- **DELIVERABLE (what to build + exactly where in the repo).** In `myelin-harness` + `myelin-substrate`: the substrate's half of the restore-verify gate — the **failure-injection + telemetry-assertion machinery** that drives SUB-D6: trigger a rebuild-from-backups via Storage's restore path, then assert (via the telemetry-assertion library) that the rebuild lands at ONE consistent cross-seam point — OLTP rows ↔ blob ↔ search index ↔ event-log offsets (no row pointing at a missing blob), 0 loss, and that RPO ≤ 5 min / RTO ≤ 1h-per-tenant / ≤ 4h-per-cell (thresholds read from the thresholds file). Storage owns the WAL+PITR restore and the `restore-verify` CI job; the substrate supplies the drill machinery that makes it PROVABLE. Also confirm the `PersonalDataHolder` auto-registration mechanism is exercised against the real H1–H18 holder set as Identity/Storage/GDPR stores come online (the `no-untagged-personal-data` lint goes red on any untagged PII field — GA-D5 mirror).
- **CONTRACTS TO IMPLEMENT.** Carried (substrate half): 11.5 (the injection/assertion half — Storage owns the gate). Consumed/confirmed: 1.4 (the exhaustive-holder mechanism against the real H1–H18 set).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D6 / STOR-D1 / STOR-D2** (rebuild from backups → **0 loss**; OLTP↔blob↔index↔offsets ONE consistent point; **RPO ≤ 5 min, RTO ≤ 1h/tenant ≤ 4h/cell**) — telemetry signal `restore-verify-pass`, SCHED. **This is the silent-data-loss floor and a PERMANENT gate (re-run on every store-touching change); M2 does NOT start over a red STOR-D1.** Never weaken RPO/RTO to pass — a red row is a dated claimed-not-proven scorecard entry.
- **TESTS (required).** The restore-verify drill scenario (the substrate's injection/assertion half driving Storage's restore). Unit: the cross-seam consistency assertion catches a deliberately-injected row→missing-blob mismatch (the assertion must reject an inconsistent rebuild). A holder-completeness test: every store the harness opens is in the H1–H18 set (no orphan store). CDC pair for 11.5 (provider = Storage restore; consumer = the substrate assertion). This is a SCHED drill (expensive) — wired into the scheduled gate, with a cheaper CI smoke variant at small scale.
- **DEFINITION OF DONE.** The restore-verify injection/assertion half exists; SUB-D6/STOR-D1/STOR-D2 emit dated green artifacts at single-tenant scale (cell-scale re-confirm is the M5 follow-on, P-S16); the exhaustive-holder mechanism is confirmed; the permanent-gate marking + the M5 cell-scale follow-on are named; committed.
- **COMMIT.** Header `P-<NNN> M1: restore-verify cross-seam half (SUB-D6, the silent-data-loss floor)`. Body: 11.5 (substrate half) + 1.4 confirmation; SUB-D6/STOR-D1/STOR-D2 (0 loss, one cross-seam point, RPO/RTO) with measured numbers; the permanent-gate marking + the M5 cell-scale follow-on (P-S16). Co-Authored-By trailer.

---

### P-S13 — The firehose resume-cursor backpressure half goes live (SUB-M2, the D-11 substrate half)

- **BAND.** M2.
- **ROADMAP MILESTONE.** SUB-M2 (the firehose backpressure half) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M2.
- **DEPENDS-ON.** P-S08 (bounded-everything + the shed order), P-S12 (M1 green), and the Bus M2 prompt that ships the firehose resume-cursor protocol (contract 3.5 — the Bus owns `subscribe`/`resume`/`scope` + the zero-loss-replay half; the substrate rides its bounded/shed discipline; this prompt MUST start after the Bus firehose protocol is mergeable).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/04-hard-problems.md` §2 (build the durable resume-cursor transport FIRST — the CRDT slots into it; a dropped connection must lose nothing).
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (zero messages lost across a reconnect is a quantified gate).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §7.7 (the firehose resume-cursor seam — the substrate's backpressure role: per-connection in-flight frame caps; a slow consumer dropped to `resync_required` never buffered unboundedly; scope as a bounded selector never `*`; the per-surface shed budgets apply to frames) + §7.1 (bounded-everything generalised to streaming) + §10.2 last row (firehose per-(stream,scope) frame-lag + `resync_required` count survival signal).
  - `../../05-refined-shared-systems-architecture/contract-index.md` row 3.5 (the firehose transport + resume-cursor protocol — `subscribe(stream, scope, cursor?)`, per-(stream,scope) monotonic `seq`, `resume(stream, scope, last_seq)` backfills `(last_seq, now]` then live, `resync_required` → `*.snapshot` fallback; scope a bounded selector never `*`).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §11 row D-11 (firehose reconnect-loses-zero-ops — the substrate owns the bounded-queue half).
- **DELIVERABLE (what to build + exactly where in the repo).** In `myelin-substrate` (the firehose backpressure layer that rides the Bus's 3.5 protocol): (a) **per-connection in-flight frame caps** — a subscription's frame buffer is bounded; over-cap sheds in the firehose's own bounded queue; (b) **slow-consumer drop to `resync_required`** — a slow consumer falls back to a full `*.snapshot` replay (the cold-rebuild path, NAMED not silent) rather than the transport growing memory; (c) **scope as a bounded selector, never `*`** — a 50k-row board paginates its scope (visible window + margin); the firehose delivers only that slice's frames; (d) the **per-surface shed budgets** (§7.6) applied to frames — presence/speculative frames shed before message delivery; agents shed before humans. Export the per-(stream,scope) frame-lag + `resync_required` count into the telemetry signal set. The Bus owns the zero-loss-replay half (3.5); the substrate owns the bounded-and-sheds half.
- **CONTRACTS TO IMPLEMENT.** Carried (substrate half): 3.5 (the bounded/shed half — per-connection caps, slow-consumer→`resync_required`, scope-bounded selector). The Bus owns the resume-cursor + zero-loss-replay half.
- **GATE / DRILLS (quantified; must be green to call this done).** The **substrate's firehose survival signals read green** under a hot-stream drill: per-(stream,scope) frame-lag bounded; an over-retention gap yields `resync_required` → `*.snapshot` (named, not silent); a slow consumer is dropped (not buffered unboundedly). This is the substrate's half of the D-11 reconnect-loses-zero-ops drill (the Bus owns the zero-loss-replay assertion; the substrate owns bounded-and-sheds-never-unbounded-memory) — signals `firehose_frame_lag` bounded, `resync_required_count` accurate, memory bounded, CI. Re-confirmed under real connection-storm load in M4 (P-S14).
- **TESTS (required).** Unit: an over-cap subscription sheds rather than growing memory; a slow consumer is dropped to `resync_required` (memory stays bounded); a `*` scope is rejected (bounded selector only); presence frames shed before message frames. CDC pair for the substrate half of 3.5 (provider = the Bus firehose; consumer = the substrate bounded layer). Drill scenario for the D-11 substrate half (P-S02 drops a firehose subscription on a hot stream). Mutation floor ≥ 75% on the frame-buffer/shed module.
- **DEFINITION OF DONE.** The firehose backpressure half exists and compiles; the substrate's firehose survival signals read green under a hot-stream drill; the D-11 substrate half is proven; the M4 connection-storm re-confirm is named; CDC + mutation tests pass; committed.
- **COMMIT.** Header `P-<NNN> M2: firehose resume-cursor backpressure half (D-11 substrate half)`. Body: 3.5 (substrate half); the bounded/shed firehose drill greened with measured frame-lag + `resync_required`; the M4 connection-storm follow-on (P-S14). Co-Authored-By trailer.

---

### P-S14 — Enforce the cross-language shim (if Chat diverges) + the firehose under connection-storm

- **BAND.** M4.
- **ROADMAP MILESTONE.** SUB-M4 (cross-language shim enforced + firehose under connection-storm) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M4.
- **DEPENDS-ON.** P-S13 (the firehose backpressure half), and the Chat M4 prompts (the connection tier — if Chat diverges to a non-Rust tier, TE-21) + the Knowledge M3 collab prompts (the hot-doc op-stream). This prompt MUST start after Chat's connection tier and KN's collab stream are mergeable.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §5 (an uncommitted gate is no gate — the shim cannot be quietly dropped at a language boundary) and §7 (reconcile cross-component contracts at the plan layer; a non-negotiable dropped at a boundary calcifies).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §3.7 (the cross-language harness shim — the frozen divergence contract; the SEVEN non-negotiables a non-Rust subsystem must satisfy: three-surface topology, liveness ≠ readiness, no fire-and-forget emit, `PersonalDataHolder` registration, the resilient-client behaviour + `Retry-After`, the principal-aware shed order, forward-only online migrations) + §7.6 (the connection-storm + collab op-stream budgets).
  - `../../05-refined-shared-systems-architecture/contract-index.md` row 1.7 (the cross-language harness shim — the contract a non-Rust subsystem must satisfy; Chat connection tier likely, TE-21) + 3.5 (the firehose under hot-stream load).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2/§4.3 rows for CHAT-D1/CHAT-D13/CHAT-D14 (the firehose path: resume 0 lost/0 dup; co-commit; idempotent send) — the substrate asserts its survival signals hold under these.
- **DELIVERABLE (what to build + exactly where in the repo).** (a) **Enforce the cross-language harness shim** IF AND ONLY IF Chat diverges to a non-Rust connection tier: a conformance test suite (in `myelin-harness`) that asserts the divergent tier provides all SEVEN non-negotiables to the SAME guarantee as the Rust harness — three-surface topology, liveness ≠ readiness, no fire-and-forget emit (the outbox pattern in the divergent language too), `PersonalDataHolder` registration, the resilient-client behaviour + `Retry-After` honouring, the principal-aware shed order, forward-only online migrations. A no-op (a recorded N/A) if Chat stays Rust — but the shim CANNOT be quietly dropped. (b) **Firehose backpressure under connection-storm**: re-confirm the P-S13 bounded/shed half under Chat's connection-storm budget and KN's hot-doc collab op-stream budget (§7.6 floors), using the P-S02 connection-storm + collab-op-stream storm profiles. **Floor named:** the shim is `specified` from M0 (P-S08/§3.7) → `enforced` here iff Chat diverges (roadmap §3 floor table).
- **CONTRACTS TO IMPLEMENT.** Carried/enforced: 1.7 (the cross-language shim — enforced here iff Chat diverged), 3.5 (the substrate's bounded/shed half re-confirmed under connection-storm).
- **GATE / DRILLS (quantified; must be green to call this done).** **The cross-language shim's seven non-negotiables green** in the divergent tier (only if Chat diverged) — signal `shim-conformance` green ×7. **The firehose under connection-storm**: the bounded/shed half holds under Chat's connection-storm + KN's hot-doc budgets — signals `shed-counts/lane`, `firehose_frame_lag` bounded, contributing to CHAT-D1/CHAT-D13/CHAT-D14 (the substrate asserts its survival signals; Chat owns the end-to-end resume-0-lost/0-dup + co-commit + idempotent-send drill), CI. Contributes to the master M4→M5 boundary.
- **TESTS (required).** The shim conformance suite (seven non-negotiables, each a committed test; an N/A recorded loudly if Chat stays Rust — silent skip is the failure, EI-01 §4). The connection-storm drill scenario (P-S02 drives the connection-storm + collab-op-stream profiles; the harness asserts the survival signals). CDC pair for 1.7 (if enforced). Mutation floor ≥ 75% on any new shim-enforcement code.
- **DEFINITION OF DONE.** The shim is enforced (seven non-negotiables green) or recorded N/A loudly; the firehose half holds under connection-storm (survival signals green, contributing to CHAT-D1/D13/D14); the specified→enforced floor is named; tests pass; committed.
- **COMMIT.** Header `P-<NNN> M4: cross-language shim enforced + firehose under connection-storm`. Body: 1.7 (seven non-negotiables or N/A) + 3.5 connection-storm; the survival signals greened; the specified→enforced floor. Co-Authored-By trailer.

---

### P-S15 — World-scale: the 30× surge family (SUB-D3) + tune the per-surface shed budgets

- **BAND.** M5.
- **ROADMAP MILESTONE.** SUB-M5 (the surge family) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M5 (the SUB-D3 slice).
- **DEPENDS-ON.** P-S08 (the shed lane + the v1 budget floor table), P-S14 (M4 green). The F6 surge family runs across all owners; the substrate is one owner — this prompt MUST start after M4 (all five subsystems on the substrate).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (the load generator multiplies traffic 1×/10×/30× and mixes principal types; the human lane within budget is the quantified pass) and §2 (per-tenant blast radius — one tenant's surge unaffects another).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §7.2/§7.6 (the shed order + the per-surface budget v1 floor table — now TUNED by the drill, measured-not-predicted) + §11 row D-3.
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 1.11 (the per-surface shed budgets — the v1 floors become measured numbers), 1.8 (the surge survival signals — shed-counts/lane, per-tenant RED).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 row SUB-D3 (30× agent surge one tenant → human lane holds, agent sheds, others unaffected).
- **DELIVERABLE (what to build + exactly where in the repo).** Drive the **30× surge family** (SUB-D3, part of the F6 family across all owners) using the P-S02 load generator at 30× on one tenant with the agent/CI mix: assert the **protected human lane holds** (within its latency budget), the **agent lane sheds** (`429 + Retry-After`, and the clients honour it — §6.2), and **cross-tenant impact is 0** (another tenant's humans are unaffected). Then **tune the §7.6 per-surface shed-budget numbers** against the measured drill results — the v1 floor table becomes measured numbers, written into the thresholds file (the named floor is now a measured value). Record the tuned numbers as a dated thresholds-file update (never edited green without the drill).
- **CONTRACTS TO IMPLEMENT.** Owned (proven + tuned): 1.11 (the shed budgets, now measured). Consumed: 1.8 (the surge survival signals).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D3** (30× agent surge one tenant → human lane within budget, agent sheds with `429 + Retry-After`, cross-tenant impact **0**) — signals `shed-counts/lane`, per-tenant RED (the surged tenant's agents shed, its humans hold, another tenant's RED unchanged), SCHED. Contributes to the master M5→M6 boundary (the F6 surge family). Never relax the human-lane budget to pass — record a missed budget as claimed-not-proven.
- **TESTS (required).** The SUB-D3 drill scenario (P-S02 at 30×, agent/CI mix, one tenant). Assert the three properties (human lane holds, agent sheds, cross-tenant 0) read from the telemetry signal set. A regression test that a budget tuned BELOW the human-lane floor fails the gate (you cannot tune the human lane into starvation). SCHED frequency; a cheaper 10× CI smoke variant.
- **DEFINITION OF DONE.** SUB-D3 emits a dated green artifact (human lane holds, agent sheds, cross-tenant 0); the per-surface shed-budget numbers are tuned to measured values in the thresholds file; the v1-floor→measured follow-on is closed; committed.
- **COMMIT.** Header `P-<NNN> M5: 30x surge family (SUB-D3) + tuned per-surface shed budgets`. Body: 1.11 tuned; SUB-D3 (human lane holds, agent sheds, cross-tenant 0) with measured numbers + the tuned budget values. Co-Authored-By trailer.

---

### P-S16 — World-scale: online-migration-under-load (SUB-D10) + restore-verify at cell scale + per-target client tuning

- **BAND.** M5.
- **ROADMAP MILESTONE.** SUB-M5 (online-migration-under-load + restore-verify at cell scale + the resilient-client per-target tuning) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M5 (the SUB-D10 + cell-scale slices).
- **DEPENDS-ON.** P-S06 (the migration runner), P-S07 (the resilient client), P-S12 (the restore-verify half), P-S15 (M5 surge tuning, same band).
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §3 (expensive drills run scheduled; the quantified thresholds — lock-wait p99, 0 errored writes, 0 downtime).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §9 (forward-only online migrations; §9.1 expand→backfill→contract; §9.2 measure lock time against a restored production-scale copy — the lock-time-against-a-restore rule ties the migration runner to the restore machinery) + §11 row D-10 + §6.3 (the resilient-client per-target values — the auth hot path tighter than a batch indexer, measured by the surge/latency drills).
  - `../../05-refined-shared-systems-architecture/contract-index.md` rows 1.5 (forward-only migrations under load), 11.5 (restore-verify at cell scale), 1.9 (resilient-client per-target tuning).
  - `../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md` §4.2 rows SUB-D10 (expand→backfill→contract on a restored prod-scale copy under load → no blocking lock beyond budget; 0 downtime) + SUB-D6/STOR-D2 (restore-verify at cell scale).
- **DELIVERABLE (what to build + exactly where in the repo).** (a) **Online-migration-under-load** (SUB-D10): run an expand→backfill→contract migration on a **restored production-scale copy under load** (using the P-S12 restore machinery + the P-S02 load generator); assert no blocking lock beyond budget (lock-wait p99 from the thresholds file) and 0 errored writes / 0 downtime. This ties the migration runner (1.5) to the restore-verify machinery (the §9.2 lock-time-against-a-restore rule). (b) **Restore-verify re-confirmed at cell scale** (SUB-D6 / STOR-D2): RPO/RTO held under world-scale load — the cell-scale follow-on named in P-S12. (c) **Tune the resilient-client per-target values** (1.9): the auth hot path gets a tighter timeout than a batch indexer — measured by the surge/latency drills, written into the thresholds file (the M0 default-per-target floor becomes measured values — the follow-on named in P-S07).
- **CONTRACTS TO IMPLEMENT.** Owned/proven: 1.5 (under load), 11.5 (at cell scale), 1.9 (per-target tuned).
- **GATE / DRILLS (quantified; must be green to call this done).** **SUB-D10** (expand→backfill→contract on a restored prod-scale copy under load → lock-wait p99 within budget, **0 errored writes, 0 downtime**) — SCHED. **SUB-D6 / STOR-D2 at cell scale** (RPO/RTO held under world-scale load) — SCHED. Both contribute to the master M5→M6 boundary. The substrate also participates in all four M5 E2E scenarios (each boots on `serve`, emits via the outbox, reads the telemetry signal set — the substrate's job is to be INVISIBLE, no scenario fails on a substrate signal); assert no E2E scenario fails on a substrate signal.
- **TESTS (required).** The SUB-D10 drill scenario (migration on a restored copy under load). The cell-scale restore-verify drill (SUB-D6/STOR-D2 at scale). A regression test that a per-target value tuned looser than the measured latency budget fails the gate. Assert the four E2E scenarios emit no substrate-signal failure. SCHED frequency; cheaper CI smoke variants where feasible.
- **DEFINITION OF DONE.** SUB-D10 + SUB-D6/STOR-D2-at-cell-scale emit dated green artifacts; the resilient-client per-target values are tuned to measured numbers; no E2E scenario fails on a substrate signal; the M5 floors (per-target values, cell-scale restore) are closed; committed.
- **COMMIT.** Header `P-<NNN> M5: online-migration-under-load (SUB-D10) + restore-verify at cell scale + per-target client tuning`. Body: 1.5/11.5/1.9; SUB-D10 (lock-wait p99, 0 downtime) + SUB-D6/STOR-D2 at cell scale (RPO/RTO) with measured numbers; the tuned per-target values; the E2E-invisible confirmation. Co-Authored-By trailer.

---

### P-S17 — Dogfood: run the lints, the contract-coverage scanner, and the mutation gate as Myelin CI jobs

- **BAND.** M6.
- **ROADMAP MILESTONE.** SUB-M6 (the dogfood loop) — `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M6.
- **DEPENDS-ON.** P-S05 (the lints), P-S09 (the scanner), P-S16 (M5 green), and the CI subsystem M4 prompts + the M6 self-hosting prompts (the Myelin CI graph the substrate jobs run on). This prompt MUST start after the self-hosting CI graph is mergeable.
- **CANON DOCS (read these first, in full, before writing any code).**
  - `../../external-insights/01-process-and-quality-doctrine.md` §1 (the code wins over the docs; the truth-up pass re-syncs docs to what the code does; date every status note) and §5 (the ratchet runs on the builders' own work).
  - `../../05-refined-shared-systems-architecture/00-platform-substrate.md` §2.11 (the twelve lints) + §10 (the telemetry baseline — the dogfood loop reads these on Myelin's own commits).
  - `../../06-roadmaps/shared/00-platform-substrate.md` §2 SUB-M6 (the lints + scanner + mandatory-core mutation gate run as Myelin CI jobs on every Myelin commit; the every-incident-adds-a-drill loop files a Myelin issue + a reproducing drill; the harness drives the surge/restore/migration drills as part of the self-hosting CI graph).
  - `../../06-roadmaps/00-master-sequencing.md` §2 M6 + §4 M6-done row (the self-hosting CI graph green; the truth-up pass confirms 0 red earlier gates).
  - `../00-ledger-overview.md` §6 (the cargo-mutants mutation gate runs as a Myelin CI job on every Myelin commit — the dogfood loop reads the one-prompt-one-commit ledger).
- **DELIVERABLE (what to build + exactly where in the repo).** Wire the substrate's ratchet into the self-hosting Myelin CI graph (a Myelin CI pipeline definition): the twelve architecture lints + the contract-coverage scanner + the mandatory-core cargo-mutants mutation gate run as **Myelin CI jobs on every Myelin commit** (the dogfood loop is live). Wire the **every-incident-adds-a-drill loop** to file a Myelin issue + a reproducing drill for any substrate incident (the T-3 loop, now on Myelin's own tracker). Make the harness drive the substrate's surge/restore/migration drills (SUB-D3/D6/D10) as part of the self-hosting CI graph. Run a **truth-up pass** (EI-01 §1): confirm every substrate PROVEN row rests on a dated green artifact (not a doc claim); where a doc and the code disagree, fix the doc.
- **CONTRACTS TO IMPLEMENT.** None new — this wires the M0 ratchet (1.6, the scanner, the mutation gate) to run on Myelin's own commits. It closes the dogfood loop.
- **GATE / DRILLS (quantified; must be green to call this done).** **The Myelin self-hosting CI graph is green** on the platform's own commits (the twelve lints + the scanner + the mutation gate run there). **No earlier substrate gate is red** (the truth-up pass confirms every substrate PROVEN row rests on a dated green artifact — the gate invariant holds end-to-end). Green artifact = a Myelin CI run on a Myelin commit showing the substrate ratchet green + a dated truth-up scorecard with 0 red earlier rows. Contributes to the master M6 done-bar.
- **TESTS (required).** The self-hosting CI pipeline IS the test: a Myelin commit triggers the lints + scanner + mutation gate; a deliberately-violating commit is rejected (the ratchet rejects on Myelin's own work). The incident-loop produces a Myelin issue + a reproducing drill on a simulated incident. The truth-up pass is a committed scorecard. No CDC pair.
- **DEFINITION OF DONE.** The lints + scanner + mutation gate run as Myelin CI jobs on every Myelin commit and reject a violating commit; the incident-loop files a Myelin issue + drill; the truth-up pass confirms 0 red earlier substrate gates; committed.
- **COMMIT.** Header `P-<NNN> M6: dogfood the substrate ratchet (lints + scanner + mutation gate on Myelin's own commits)`. Body: the self-hosting CI ratchet green; the incident-loop wired; the truth-up scorecard (0 red earlier gates). Co-Authored-By trailer.

---

## Digest

**17 prompts**, covering every SUB-M0..SUB-M6 milestone and every SUB-D1..SUB-D10 drill + the D-11 firehose
substrate half, with no gap:

- **M0 (10 prompts, P-S01..P-S10):** the workspace + eight glue crates (P-S01); the failure-injection harness +
  telemetry-assertion library, Tier 0 the unit of proof (P-S02); the transactional outbox + canonical envelope +
  relay, the silent-data-loss floor, SUB-D1/BUS-D4 (P-S03); the idempotent consumer template + dedup ledger,
  SUB-D2 (P-S04); the twelve committed lints with red+green fixtures, the ratchet (P-S05); `serve(AppSpec)` +
  three-surface topology + migration runner + holder auto-registration, SUB-D7/D9 (P-S06); the resilient client +
  Retry-After, SUB-D5 (P-S07); fail-static mechanism + the protected-human-lane shed order + bounded-everything,
  SUB-D8 (P-S08); the contract-coverage scanner + thresholds file + overlay primitives (P-S09); the consolidated
  M0 exit-gate scorecard, SUB-D1/D2/BUS-D4/D5/D7/D8/D9 + 12 lints + harness self-test (P-S10).
- **M1 (2 prompts, P-S11..P-S12):** fail-static proven against a real Identity hiccup, SUB-D4 (P-S11); the
  restore-verify cross-seam half + the exhaustive-holder confirmation, SUB-D6/STOR-D1/STOR-D2, the silent-data-loss
  floor and a permanent gate (P-S12).
- **M2 (1 prompt, P-S13):** the firehose resume-cursor backpressure half + the D-11 substrate half.
- **M4 (1 prompt, P-S14):** the cross-language shim enforced (if Chat diverges) + the firehose under
  connection-storm, contributing to CHAT-D1/D13/D14.
- **M5 (2 prompts, P-S15..P-S16):** the 30× surge family + tuned per-surface shed budgets, SUB-D3 (P-S15);
  online-migration-under-load + restore-verify at cell scale + per-target client tuning, SUB-D10 +
  SUB-D6/STOR-D2-at-cell-scale (P-S16).
- **M6 (1 prompt, P-S17):** dogfood — the lints + scanner + mutation gate as Myelin CI jobs on Myelin's own
  commits + the truth-up pass.

**The two substrate-owned permanent gates** (re-run forever, never "done"): the outbox 0-loss/0-ghost floor
(SUB-D1/SUB-D2/BUS-D4, P-S03/P-S04, re-run on every emit-path change) and the restore-verify gate
(SUB-D6/STOR-D1/STOR-D2, P-S12, re-run on every store-touching change). **Named floors** (each with its
follow-on prompt): fs-backed BlobStore → object-store (M5); per-surface shed-budget v1 table → tuned numbers
(P-S15); resilient-client default per-target values → tuned values (P-S16); single-region event log →
column-store seam (post-M5, when volume is measured); fail-static value W → DPO-ratified (parallel/legal);
cross-language shim specified (P-S08) → enforced (P-S14). The substrate's correctness floors (the outbox, the
twelve lints, the harness) are NOT staged — absolute from M0.

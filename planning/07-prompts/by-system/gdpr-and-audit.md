# Phase 7 — Implementation Prompts: GDPR / Audit (the PersonalDataHolder spine · DSR orchestrator · tamper-evident audit log · the ONE free-text/immutable erasure posture)

> Prompt count: 16 (first pass) → 38 (this finer-grained pass). Every first-pass prompt's coverage is preserved;
> the increase is bundled single-deliverables split out, not padding.

> Phase: `07-prompts/by-system`. The complete ordered set of clean-context, independently-committable coding
> prompts that operationalize the entire **gdpr-and-audit** shared-system roadmap
> ([`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md)) into build tasks.
> Authored to the ledger template ([`../00-ledger-overview.md`](../00-ledger-overview.md) §2), in this system's
> own band order (M0 → M6). The consolidated index (`01-ledger-index.md`, Phase 7-B) interleaves these into the
> single global `P-<NNN>` sequence; the `P-<NNN>` ids below are this file's **local placeholders**
> (`P-GA-01..P-GA-38`) — the index reassigns stable global ordinals and rewrites DEPENDS-ON to global ids.
> Plain-text identifiers (no backticks-as-emphasis). Markdown only; no git commits by this document or its author.
>
> **The shape of this system, and what it means for these prompts.** GDPR/Audit is a **policy + orchestration**
> layer that **owns no mechanism** (arch §1.2): the crypto-shred mechanism is Storage's (KMS / per-subject DEK,
> 11.3/11.4), the pseudonym-indirection lever is Identity's (4.8), every store's erasure is *that store's*
> `PersonalDataHolder::erase`. GDPR decides **whether, when, and prove**; the owning store decides **how**. So
> these prompts split structural-then-orchestral-then-fan-out across M0 → M5, mirroring the holder list coming
> online: the lint + the hook in M0; the trait + the data map + the DSR orchestrator + the tamper-evident audit
> log + the erasure ledger + the structural erasure floor in M1; the deadline timer + restriction-into-analytics
> + per-derivative erasure in M2; the producer holders (Git/KN) + the Git pseudonymous-commit X-7 instance in M3;
> the consumer holders (CI/Issues/Chat) + the per-subject CI-log DEK + the worklog classification in M4; the full
> H1–H18 fan-out + multi-cell + the two new mechanisms + the E2E-4 flagship in M5; the dogfood in M6.
>
> **Coverage (this file → roadmap):** every GDPR/Audit milestone — GA-M0 (P-GA-01..P-GA-03), GA-M1
> (P-GA-04..P-GA-20), GA-M2 (P-GA-21..P-GA-26), GA-M3 (P-GA-27, P-GA-28), GA-M4 (P-GA-29, P-GA-30, P-GA-31), GA-M5
> (P-GA-32..P-GA-36), GA-M6 (P-GA-37, P-GA-38) — each maps to ≥1 prompt below. Every named floor and its follow-on
> are paired: the coarse DSR deadline tracking (P-GA-14) → durable deadline timer (P-GA-21); single-cell DSR
> fan-out (P-GA-14) → multi-cell `member_cells` iteration (P-GA-33); the M1-holder erasure floor (P-GA-05/P-GA-06)
> → full H1–H18 fan-out (P-GA-32, GA-D1); per-tenant CI-log DEK floor → per-subject CI-log DEK (P-GA-29); the
> `[OPEN — LEGAL]` residual posture (written once in P-GA-16, referenced per subsystem in
> P-GA-27/P-GA-28/P-GA-29/P-GA-30/P-GA-31) → counsel/DPO ratification (parallel-legal, named, never blocking the
> build). Date: 2026-06-19.

---

## Canon every prompt in this file assumes (the shared reading set)

Each prompt re-states the precise subset it needs, but all assume the M0 substrate from
[`../00-ledger-overview.md`](../00-ledger-overview.md) §6: the Cargo workspace + the eight glue crates, the
`serve(AppSpec)` harness (three ports, holder auto-registration via contract 1.4, ResilientClient, FailStatic,
the protected-human-lane shed order), the transactional outbox + the EventHandler/consumer template, the twelve
committed lints, the contract-coverage scanner, and the failure-injection harness (the 1x/10x/30x load
generator + the scoped-reversible dependency-break injector + the telemetry-assertion library reading contract
1.8). A prompt's GATE/DRILLS rows are scenarios on that harness; a prompt's DEFINITION OF DONE requires all
twelve committed lints green and the contract-coverage scanner passing.

**The implementation crate.** GDPR/Audit's runtime code lands in two places. (a) The glue crate **myelin-gdpr**
(ADR-01, a compile-time contract carrier) holds the cross-crate trait shapes every store and consumer compiles
against: the `PersonalDataHolder` trait, the `#[personal_data(...)]` classify-derive + the five-tag enum, the
`SubjectRef`/`TenantId`/`EraseScope`/`Receipt` types, the `data_map()`/`ropa()` signatures, the DSR
submit/status/certificate signatures, the audit-proof signatures. These are frozen in M0 so consumers compile
before the bodies exist. (b) A new runtime crate **myelin-gdpr-service** holds the GDPR/Audit service itself —
the DSR orchestrator, the data-map generator build step, the audit-log consumer + Merkle/STH machinery, the
retention/consent/sub-processor engine, the erasure ledger, the eDiscovery export. The service boots from an
`AppSpec` (it does not hand-roll a `main`). GDPR implements its contracts to the frozen shape; a needed shape
change is a whole-workspace contract PR, escalated and written down, not a local divergence (EI-01 §1,
code-wins-over-docs: if reality forces a divergence, write it in the crate doc comment and escalate).

**The frozen GDPR/Audit architecture** is
[`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md)
(sections cited per prompt); the frozen contracts are
[`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md)
§10 (rows 10.1–10.9 owned) + the consumed rows 1.1/1.4/1.8, 2.1/2.2/2.7, 4.8, 11.2/11.3/11.4/11.5/11.6, 6.4/5.8,
9.2/9.3/9.4, 12.3/12.6; the rationale is
[`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md)
(X-7 the erasure posture, X-5 the names/units `data_role` anchor, OQ-G, OQ-H worklog, OQ-I cross-cell bridge,
§9 history-rewrite, §10 outbound-mirror gate). The GDPR drills are
[`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md)
rows GA-D1..GA-D8 + GA-10/GA-11 (§4.2) + STOR-D3/STOR-D4/ID-D8 (the GA-faces) + E2E-3/E2E-4 (§2/§4.4), with the
strategy in
[`../../05-refined-shared-systems-architecture/testing-strategy/README.md`](../../05-refined-shared-systems-architecture/testing-strategy/README.md).
The band order + the gate invariant is
[`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) (§2 bands, §4 the gate
invariant; STOR-D3/STOR-D4 — the restore-verify/re-erasure line GDPR rides — is one of the two permanent gates).

**The single load-bearing fact this whole file sequences around:** GDPR/Audit owns no mechanism, so its
dependency on the things it orchestrates is a **fan-out-completeness** dependency (GA-D1 "0 holders missed" is a
property of the *generated data map*, and it can only be PROVEN complete once every holder H1–H18 exists — M5),
**not** a build-order blocker for its own spine (M1). Its four hard upstream blockers are all M0/M1: the M0
harness auto-registration hook (1.4), the M0 outbox (2.2, the audit log rides it), the M1 Identity pseudonym-map
lever (4.8), the M1 Storage KMS/crypto-shred/restore floor (11.3/11.4/11.5). The X-7 erasure posture is GDPR's
**one critical-path obligation**: it must be frozen in M1 (P-GA-16), before the Git data model freezes in M3
(pseudonymous-by-default commits).

**The `[OPEN — LEGAL]` discipline (EI-04 §4, VISION §3).** Several GDPR surfaces have a structural floor that
ships on the engineering clock and a residual that counsel/DPO ratifies in parallel. The rule binding every
such prompt: **the structural floor ships regardless of legal ratification; counsel ratification gates
publishing a posture as ratified, never building the floor.** A prompt that ships a floor names the floor and
its follow-on in writing; a `[TBD_LEGAL]` basis tag is a named residual, never a blocker. Date every status
note.

---

### P-GA-01 — The myelin-gdpr glue-crate skeleton: the PersonalDataHolder trait signature + the core types

- **BAND.** M0.
- **ROADMAP MILESTONE.** GA-M0 (the myelin-gdpr glue-crate skeleton as a compile-time contract carrier — the frozen trait + type names consumers compile against) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M0".
- **DEPENDS-ON.** The M0 substrate prompts: the workspace + glue-crate skeleton prompt (lays down myelin-gdpr as an empty contract carrier), the EventEnvelope/outbox prompt (the frozen 2.1 `data_role` envelope field this anchors to). No intra-GDPR predecessor — this is the first GDPR prompt.
- **CANON DOCS (read these first, in full, before writing any code).**
  - [`../../VISION.md`](../../VISION.md) §3 (GDPR-safe & EU-sovereign by construction — data-subject rights are architectural constraints, not bolted-on features; name-your-floors).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (code-wins-over-docs — the glue crate is the frozen contract carrier; a divergence is escalated, not silent).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.1 (the five-operation contract — `locate`/`export`/`rectify`/`restrict`/`erase`; the trait shape), §1 (the two legal postures + the `data_role` = tenant-content | platform-operational classification = the X-5 names/units anchor threaded through the envelope).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (PersonalDataHolder, the trait shape — bodies deferred), 2.1 (the EventEnvelope `data_role`/`contains_personal_data`/`pii_key_ref` fields, the X-5 anchor).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) X-5 (the names/units `data_role` reconciliation point).
- **DELIVERABLE (what to build + exactly where in the repo).** In glue crate myelin-gdpr: the **glue-crate skeleton as a compile-time contract carrier** — the trait signature `PersonalDataHolder{locate, export, rectify, restrict, erase}` (bodies are M1 P-GA-05, the signature frozen now), the `SubjectRef`/`TenantId`/`EraseScope`/`Receipt` types, and the `data_role` enum (`TenantContent` | `PlatformOperational`) anchored to the frozen 2.1 envelope field. Consumers across the workspace compile against these names before any body exists.
  **Floor named:** the trait bodies are M1 (P-GA-05); the derive macro names land in P-GA-02; the lint in P-GA-03. Record in the crate doc comment.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.1 (the trait signature only, bodies deferred to P-GA-05). Consumed/wired: 2.1 (the `data_role` envelope field, X-5 anchor).
- **GATE / DRILLS (quantified; must be green to call this done).** The workspace compiles with the frozen trait + type names present; the `data_role` enum serializes to the frozen 2.1 envelope field. No drill fires here (this is a compile-time contract carrier); the gate is "the workspace compiles + all twelve lints green". Telemetry: the glue crate is part of the M0 "workspace compiles clean" gate.
- **TESTS (required).** Unit: a test that the `PersonalDataHolder` trait signature is object-safe / usable as the consumers need; a test that the `data_role` enum serializes to the frozen 2.1 envelope field; a test that `SubjectRef`/`TenantId`/`EraseScope`/`Receipt` round-trip serialize. CDC: none owned with a body here (10.1's CDC pair lands with the bodies in P-GA-05); the consumer-compiles-against-signature property is the gate.
- **DEFINITION OF DONE.** The myelin-gdpr glue skeleton compiles with the frozen trait signature + the four core types + the `data_role` enum; the enum serializes to the frozen 2.1 field; the deferred-to-M1 follow-ons (the derive names → P-GA-02, the lint → P-GA-03, the trait bodies → P-GA-05) are named in writing; unit tests pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: myelin-gdpr glue skeleton — PersonalDataHolder signature + core types`; body lists 10.1(sig)/2.1(anchor) wired, the named M1 follow-ons (P-GA-02/P-GA-03/P-GA-05). Branch first if on default. Co-Authored-By trailer.

---

### P-GA-02 — The #[personal_data] derive-attribute names + the five-tag enum names (frozen so consumers compile)

- **BAND.** M0.
- **ROADMAP MILESTONE.** GA-M0 (the `#[personal_data(...)]` classify-derive *attribute names* + the five-tag enum names frozen so every store compiles before the macro body exists) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M0".
- **DEPENDS-ON.** P-GA-01 (the myelin-gdpr skeleton + the core types).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the derive names freeze in M0; the body is M1).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — the tag names are what the lint will enforce; freezing them now means "we forgot a store/field is a *structural* failure" the moment the lint lands).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.1 (the `#[personal_data(...)]` classification — the five-tag shape: category / role / basis / retention / erasure / subject_locator).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.2 (the classify-derive — the attribute + the five-tag enum names; the macro body deferred to P-GA-04).
- **DELIVERABLE.** In glue crate myelin-gdpr: the `#[personal_data(category, role, basis, retention, erasure, subject_locator)]` derive macro **attribute names + the five-tag enum *names*** (the macro body that emits the registry entry is M1 P-GA-04; the names are frozen now so consumers compile). Define the five enum types' *names + variant names* to the frozen §2.1 shape (the full variant bodies / parsing land in P-GA-04). A store can write `#[personal_data(...)]` and reference the enum variants now; it will not compile correctly against drift later.
  **Floor named:** the derive emits nothing yet (a no-op / placeholder) — the body that emits the registry entry is M1 (P-GA-04); the lint that enforces tagging is M0 (P-GA-03). Record in the crate doc comment.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.2 (the derive-macro attribute name + the five-tag enum names only; the macro body + variant parsing deferred to P-GA-04).
- **GATE / DRILLS.** The workspace compiles with the `#[personal_data(...)]` attribute + the five enum names present; a fixture store can apply the attribute + reference each enum variant name. No drill fires (compile-time carrier); the gate is "the workspace compiles + the attribute parses its five tag keys + twelve lints green".
- **TESTS (required).** Unit: a test that the derive attribute parses the five tag keys (category/role/basis/retention/erasure/subject_locator) without erroring; a test that each of the five enum names + their variant names exist and round-trip serialize. CDC: none with a body here (10.2's CDC pair lands with the macro body in P-GA-04).
- **DEFINITION OF DONE.** The `#[personal_data]` derive-attribute names + the five-tag enum names are frozen to the §2.1 shape; a fixture store applies the attribute + references the variants; the deferred macro body (P-GA-04) + the lint (P-GA-03) are named in writing; unit tests pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: #[personal_data] derive-attribute names + five-tag enum names`; body lists 10.2(names) frozen, the macro-body → P-GA-04 and lint → P-GA-03 follow-ons. Co-Authored-By trailer.

---

### P-GA-03 — The no-untagged-personal-data lint (with red+green fixtures), committed as a CI gate

- **BAND.** M0.
- **ROADMAP MILESTONE.** GA-M0 (the committed ratchet floor — the `no-untagged-personal-data` lint with red+green fixtures, one of the twelve committed lints) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M0".
- **DEPENDS-ON.** P-GA-02 (the `#[personal_data(...)]` attribute the lint enforces presence of), the contract-1.6 lint-framework prompt (the M0 lint harness + the red/green-fixture convention).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (data-subject rights are architectural constraints — a forgotten store is a structural failure).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — each lint ships a red-fixture proving it rejects + a green-fixture proving it admits; wired loud, never `... || true`; an uncommitted gate is no gate), §2 (a PII leak / a forgotten store is stop-the-bleeding, ordered before any feature).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.1 (the `no-untagged-personal-data` lint = "we forgot a store/field is a *structural* failure").
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 1.6 (the twelve lints — `no-untagged-personal-data` is GDPR's), 10.2 (the tag the lint requires).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) X-7 (the erasure posture this lint is the ratchet floor of).
- **DELIVERABLE.** The **`no-untagged-personal-data` lint** wired into CI as a committed gate (one of the twelve): a field of a personal-data-bearing type that lacks a `#[personal_data(...)]` tag **fails the build**. Ship a **red-fixture** (a PII-typed field with no tag → build fails, the lint fires) and a **green-fixture** (a tagged field → admits). Wired loud-never-swallowed (no `|| true`).
- **CONTRACTS TO IMPLEMENT.** Owns: 1.6 (the `no-untagged-personal-data` lint, realized with both fixtures).
- **GATE / DRILLS (quantified; must be green to call this done).** **GA-D5** (CI) — add an untagged personal-data field → the `no-untagged-personal-data` lint fails the build; the build is red on untagged PII. Both fixtures green: the red-fixture fails (lint fires, captured-expected failure), the green-fixture passes (lint admits). Telemetry: the lint is part of the "all twelve lints green with both fixtures" master M0 → M1 exit gate. **Never soften the lint to admit the red-fixture** (EI-01 §3).
- **TESTS (required).** Unit: the two fixtures (red+green) ARE the tests — each asserts the verdict (red fails, green passes). CDC: none (the lint's correctness is the GA-D5 fixture set).
- **DEFINITION OF DONE.** The `no-untagged-personal-data` lint is wired into CI loud-never-swallowed with both fixtures asserting the correct verdict; GA-D5 emits a dated green artifact (build red on untagged PII, both fixtures green); the lint joins the twelve; unit tests pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M0: no-untagged-personal-data lint (red+green fixtures), committed CI gate`; body lists 1.6 implemented, GA-D5 green (build red on untagged PII, both fixtures). Co-Authored-By trailer.

---

### P-GA-04 — The PersonalDataHolder auto-registration hook (contract 1.4) + the holder-registered architecture test

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the `PersonalDataHolder` auto-registration contract frozen into the harness — a store opened outside the harness fails the `holder-registered` architecture test; the holder list cannot drift below the data map) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-01 (the trait signature the registry stores), the `serve(AppSpec)` harness prompt (the store-opening hook the auto-registration wires into).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the registration plumbing ships now; the holder bodies are P-GA-05).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — an uncommitted gate is no gate; the architecture test is committed loud).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.1 (the harness auto-registers every store; a store opened outside the harness fails the `holder-registered` architecture test — the holder list cannot drift below the data map).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 1.4 (PersonalDataHolder auto-registration), 10.1 (the trait the registry holds).
- **DELIVERABLE.** The **PersonalDataHolder auto-registration hook** in the harness contract (contract 1.4): the harness, opening any store, auto-registers it as a holder into a process-local holder registry; a store opened OUTSIDE the harness fails the `holder-registered` architecture test (ship that architecture test + a violating fixture). The *implementations* of the five operations are M1 (P-GA-05) — here the registration plumbing + the enforcement test exist.
  **Floor named:** the registry holds placeholder entries until the holder bodies (P-GA-05) + the data-map generator that walks it (P-GA-07) land. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns/wires: 1.4 (the harness auto-registration hook + the `holder-registered` architecture test). Consumed: 10.1 (the trait the registry holds).
- **GATE / DRILLS.** The `holder-registered` architecture test: a store opened outside the harness fails CI (captured-expected failure); a harness-opened store passes. Telemetry: the architecture test joins the M0 → M1 gate set. Quantified: 100% of harness-opened stores auto-register; a store opened outside the harness is a build failure.
- **TESTS (required).** Unit: the violating fixture (a store opened outside the harness → architecture test fails) + the conforming fixture (a harness-opened store → registers + passes) ARE the tests. CDC: the provider+consumer pair for 1.4 (the harness registering a store + a store-opening service consuming the hook).
- **DEFINITION OF DONE.** The auto-registration hook + the `holder-registered` architecture test exist and fire on a store opened outside the harness; both fixtures assert the correct verdict; the holder-body follow-on (P-GA-05) + the data-map-generator follow-on (P-GA-07) are named in writing; CDC + unit tests pass; all twelve lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: PersonalDataHolder auto-registration hook + holder-registered architecture test`; body lists 1.4 wired, the holder-bodies → P-GA-05 and data-map → P-GA-07 follow-ons. Co-Authored-By trailer.

---

### P-GA-05 — The PersonalDataHolder trait bodies + the GDPR-owned holders (H18 own stores + H16 audit carve-out)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the `PersonalDataHolder` trait bodies 10.1 + GDPR's own-store holders H18/H16 — the holders GDPR owns directly) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-01 (the trait signature), P-GA-04 (the auto-registration hook the GDPR holders register through), the Storage M1 prompts that ship 11.3/11.4 (KMS + per-subject/per-tenant DEK — the crypto-shred mechanism GDPR's own stores use).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — crypto-shred = destroy a key; GDPR owns POLICY+ORCHESTRATION, Storage owns the crypto-shred MECHANISM).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it; every holder op returns a receipt — completion is provable, not asserted).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.1 (the five-operation contract — `locate`/`export`/`rectify`/`restrict`/`erase`; `erase` is purge/crypto-shred/pseudonymise, never hide; each op returns a receipt hash-linked into the audit log; the no-cross-store-read law — the orchestrator NEVER reaches into a store, it calls the holder contract), §3.2 (H18 GDPR's own stores G1–G7; H16 the audit carve-out — retain the lawfully-needed minimised pseudonym record, expire via audit-key crypto-shred).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the trait bodies + the H18/H16 holders), 11.3/11.4 (the crypto-shred mechanism GDPR's own tables use), 1.4 (auto-registration).
- **DELIVERABLE.** In crate myelin-gdpr (the trait default machinery) + myelin-gdpr-service: implement the **`PersonalDataHolder` trait bodies** to the frozen §3.1 shape, and the **GDPR-owned holder implementations**:
  - **H18 (GDPR's own stores G1–G7)** — GDPR owns these directly: `locate`/`export`/`rectify`/`restrict`/`erase` over the DSR/consent/RoPA tables (crypto-shred per-tenant/-subject DEK; consent G5 = per-subject).
  - **H16 (the audit carve-out)** — `erase` is a **carve-out** (retain the lawfully-needed minimised pseudonym record, expire via audit-key crypto-shred — the carve-out scope is `[OPEN — LEGAL]` GD-5, named, the structural carve-out ships). The carve-out machinery integrates with the audit log in P-GA-19.
  - Every holder op returns a `Receipt` (content-addressed); the hash-link into the audit log is wired in P-GA-20 (on this prompt's floor the receipt is constructed + content-addressed, the Merkle seal lands with P-GA-20).
  **Floor named:** only the GDPR-owned holders implement here; the upstream-store orchestration (H6/H8/H9/H10/H14/H15 + the canonical erase order) is P-GA-06; the producer holders H1/H4/H17 (M3, P-GA-27) and the consumer holders H2/H3/H5 (M4, P-GA-29/P-GA-30) light up as their stores ship. GA-D1 (0 holders missed) is the M5 gate (P-GA-32). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.1 (the trait bodies + the H18/H16 holder impls). Consumed: 11.3/11.4 (crypto-shred for GDPR's own tables), 1.4 (auto-registration).
- **GATE / DRILLS.** The GDPR-owned-holder erasure floor: a subject seeded into the GDPR own-store tables → `erase` crypto-shreds + post-erase `locate` returns 0 recoverable PII over the GDPR-owned holders; H16's carve-out retains only the minimised pseudonym record (never breaks the chain). Telemetry: `erasure_fanout_coverage` over the GDPR-owned holder set reads 100%. CI for the synchronous holder ops.
- **TESTS (required).** Unit: each GDPR-owned holder's `erase` is idempotent (a re-driven erase over an already-erased subject is a no-op returning the same receipt); a holder op returns a receipt recording the destroyed key epoch; the orchestrator never reads a store directly (an architecture test that GDPR-service has no `myelin-storage`/owner-DB import path — the no-cross-store-read law); H16 erase retains the minimised carve-out record, never breaks the chain. CDC: provider+consumer pair for 10.1 (the orchestrator calling a holder + a holder responding). Mutation floor: the GDPR-owned-holder `erase` + the H16 carve-out path are mandatory-core — state and meet a cargo-mutants mutation-score floor on the holder module.
- **DEFINITION OF DONE.** The `PersonalDataHolder` trait bodies + the H18/H16 holder impls exist to the frozen shape; every holder op returns a content-addressed receipt recording the destroyed key epoch; the no-cross-store-read law is enforced by an architecture test; the GDPR-owned-holder erasure floor is green (0 recoverable over GDPR's own holders); the upstream-orchestration follow-on (P-GA-06) + the producer/consumer holder follow-ons (P-GA-27/P-GA-29/P-GA-30) + the GA-D1 M5 gate (P-GA-32) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: PersonalDataHolder trait bodies + GDPR-owned holders (H18 + H16 carve-out)`; body lists 10.1 (bodies + H18/H16) implemented, the GDPR-owned erasure floor green, the upstream-orchestration → P-GA-06 and GA-D1 → P-GA-32 follow-ons, the mutation score. Co-Authored-By trailer.

---

### P-GA-06 — The upstream-store holder orchestration (H6/H8/H9/H10/H14/H15) + the canonical erase order + resumable receipts

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the M1-shared-layer holder orchestration 10.1 — GDPR registers + calls the upstream stores' `erase` impls in the canonical erase order; the resumable receipt) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-05 (the trait bodies + the receipt shape), the Identity M1 prompts that ship 4.8 (`erase(subject)` + `resolve_pseudonym` + the frozen pseudonym grammar — the erasure lever + H14 authz-tuples + H15 identity), the Storage M1 prompts that ship 11.3/11.4 (the crypto-shred mechanism H6/H10), the Bus M0/M1 prompts (H8 bus erase + `*.erased` tombstones), the cache prompts (H9).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — delete the identity not the fact; Identity owns the pseudonym-map shred LEVER, Storage owns the crypto-shred MECHANISM).
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §4 (chain mutations end-to-end — test the chained fan-out ordering, not a single holder).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.1 (the no-cross-store-read law — the orchestrator NEVER reaches into a store, it calls the holder contract), §3.2 (the holder list — H6 blob, H8 bus, H9 cache, H10 backup, H14 authz-tuples incl. the reverse index, H15 identity incl. the pseudonym map; each holder's erasure mechanism column), §4 (the canonical erase order).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the holder orchestration + the canonical erase order), 4.8 (the Identity erase + pseudonym lever — H14/H15), 11.3/11.4 (the crypto-shred mechanism — H6/H10), 2.2/2.7 (the bus erase + `*.erased` tombstones — H8).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **holder ORCHESTRATION over the M1-shared-layer upstream stores** (the stores own their `erase` impls; GDPR registers + calls them) for **H6 (blob), H8 (bus), H9 (cache), H10 (backup), H14 (authz-tuples incl. the reverse index), H15 (identity incl. the pseudonym map)**. Wire the canonical **erase order** (Identity.erase pseudonym-map FIRST → KMS.destroy per-subject DEK → … → record receipt) as the orchestrator's holder-call sequence; each holder call is **idempotent + resumable**, returns a **receipt recording the destroyed key epoch + the purge cursor** (so erasure is independently checkable against the KMS key-destruction log).
  **Floor named:** these M1-shared-layer holders implement here; the full erasure proof over them (post-erase `locate` = 0 incl. backups) is P-GA-09; the producer holders H1/H4/H17 (M3, P-GA-27) and the consumer holders H2/H3/H5 (M4, P-GA-29/P-GA-30) light up as their stores ship. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.1 (the M1-shared-layer holder orchestration + the canonical erase order + the resumable receipt). Consumed: 4.8 (Identity pseudonym-map shred + erase, H14/H15), 11.3/11.4 (crypto-shred, H6/H10), 2.2/2.7 (bus erase + tombstones, H8).
- **GATE / DRILLS.** The M1-holder orchestration floor: a subject seeded into the M1-shared-layer holders → fan-out hits every *existing* holder in the canonical order (Identity first); each call returns a resumable receipt. Telemetry: `erasure_fanout_coverage` over the M1 holder set reads 100% of *existing* holders; `crypto-shred-lag` on the key destruction. CI for the synchronous holder-call ordering (the backup-recovery proof is P-GA-15).
- **TESTS (required).** Unit: the canonical erase order calls Identity.erase BEFORE any downstream holder (so downstream sees only the pseudonym); each M1 holder's orchestration call is idempotent (a re-driven erase is a no-op returning the same receipt); a holder op returns a receipt recording the destroyed key epoch + the purge cursor; the orchestrator never reads a store directly (the no-cross-store-read architecture test extends to the upstream holders). CDC: provider+consumer pair for 10.1 (the orchestrator calling an upstream holder + the holder responding). Mutation floor: the canonical-erase-order sequencing + the idempotent-resumable receipt path are mandatory-core — state and meet the mutation-score floor on the holder-orchestration module.
- **DEFINITION OF DONE.** The M1-shared-layer holder orchestration (H6/H8/H9/H10/H14/H15) exists; the canonical erase order (pseudonym-map first) is wired; every holder call is idempotent + resumable + returns a receipt recording the destroyed key epoch; the no-cross-store-read law is enforced; the M1-holder orchestration floor is green (100% of existing holders hit in order); the erasure-proof follow-on (P-GA-09) + the producer/consumer holder follow-ons (P-GA-27/P-GA-29/P-GA-30) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: upstream-store holder orchestration (H6/H8/H9/H10/H14/H15) + canonical erase order`; body lists 10.1 (M1 holders) implemented, the orchestration floor green, the erasure proof → P-GA-09 and holder follow-ons → P-GA-27/P-GA-29/P-GA-30, the mutation score. Co-Authored-By trailer.

---

### P-GA-07 — The #[personal_data] classify-derive macro body + the five-tag enum, applied across every M1 store

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the `#[personal_data]` classify-derive macro body + the five-tag enum 10.2, applied across every M1 store's PII fields) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-02 (the derive-attribute names + the five-tag enum names frozen), P-GA-03 (the lint that forces completeness).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — the derive feeds the lint that makes "we forgot a field" impossible at compile time).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.1 (the five tags answer the five questions every rights pipeline asks — category {ContactInfo | Identifier | Content | Behavioural | SpecialCategory(...)} / role {TenantContent (processor) | PlatformOperational (controller)} / basis {Contract | LegitimateInterest(lia_ref) | Consent(consent_id) | LegalObligation} / retention {TenantPolicy | Fixed | UntilContractEnd | AuditCarveOut} / erasure {Pseudonymise | CryptoShred(key_class) | PurgeReindex | CarveOut}; `subject_locator` makes `locate(subject)` structural — copy the enum variants byte-exact).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.2 (the classify-derive — the five-tag enum + the worklog/behavioural extension deferred to P-GA-31).
- **DELIVERABLE.** In crate myelin-gdpr: the **`#[personal_data(...)]` derive macro body** — it emits, for every tagged field, a **generated registry entry** (field path, owning store, the five tag values, the `subject_locator` expression) into a compile-time-collected inventory the data-map generator (P-GA-09) walks. The **five-tag enum** is implemented to the frozen §2.1 variants (copy byte-exact). The **`subject_locator`** makes `locate(subject)` structural — the macro generates the locator accessor a holder's `locate` uses. Apply the derive across every M1-shared-layer store's PII fields (the `no-untagged-personal-data` lint from P-GA-03 forces completeness — an untagged PII field will not compile).
  **Floor named:** the `SpecialCategory`→DPIA marker is P-GA-08; the worklog `Behavioural`/restricted-by-default extension (OQ-H) lands with Issues in M4 (P-GA-31) — named here. The `erasure = CryptoShred(key_class)` tag drives the per-subject-vs-per-tenant key choice (the Storage GD-4 rule, consumed not owned here). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.2 (the derive-macro body + the five-tag enum + the subject_locator; the SpecialCategory→DPIA marker deferred to P-GA-08, the worklog extension deferred to P-GA-31).
- **GATE / DRILLS.** **GA-D5** (CI) stays green (the derive feeds the lint): a tagged field compiles + emits a registry entry; an untagged PII field fails the build (the P-GA-03 lint, re-confirmed). Telemetry: the registry entry count grows with every tagged field; the lint stays in the "twelve lints green" gate. Quantified: every M1-shared-layer store's PII fields are tagged (0 untagged PII fields compile).
- **TESTS (required).** Unit: the derive emits the correct registry entry for each of the five category variants, each role, each basis, each retention, each erasure; the `subject_locator` accessor resolves a subject from a row; the derive over a field of a PII type with a missing tag triggers the lint (a compile-fail fixture). CDC: provider+consumer pair for 10.2 (the derive emitting a registry entry + the data-map generator consuming it — the consumer half lands in P-GA-09; stub the consumer here). Mutation floor: the tag-parsing + registry-emission path is mandatory-core — state and meet the mutation-score floor on the derive macro.
- **DEFINITION OF DONE.** The `#[personal_data]` derive emits a complete registry entry for every tag combination; the five-tag enum matches the frozen §2.1 variants byte-exact; the `subject_locator` makes `locate` structural; the derive is applied across every M1-shared-layer store (the lint forces it); GA-D5 stays green; the SpecialCategory→DPIA follow-on (P-GA-08) + the worklog extension (P-GA-31) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: #[personal_data] classify-derive macro body + five-tag enum, applied across M1 stores`; body lists 10.2 (derive + enum) implemented, GA-D5 green, the SpecialCategory→DPIA → P-GA-08 and worklog extension → P-GA-31, the mutation score. Co-Authored-By trailer.

---

### P-GA-08 — The SpecialCategory → DPIA router (the data-map-diff marker into the DPIA gate)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the `SpecialCategory` DPIA router 10.2/§2.3 — a `SpecialCategory`-tagged field emits a marker the data-map diff surfaces into the DPIA gate) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-07 (the derive macro body + the five-tag enum, into which the SpecialCategory marker hooks).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — the DPIA gate fires on a data-map diff a DPO reads).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.3 (the DPIA gate fires on a data-map diff introducing a new `SpecialCategory` flow / a new agent capability over personal data / large-scale monitoring).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.2 (the classify-derive — the SpecialCategory marker).
- **DELIVERABLE.** In crate myelin-gdpr: the **`SpecialCategory` tag routes into the DPIA gate** — a field tagged `category = SpecialCategory(...)` emits a marker into the generated inventory that the data-map diff (the CI diff gate, P-GA-10) surfaces to the DPIA router. The router records the new SpecialCategory flow as a DPIA-required change (the DPIA adjudication is a human/DPO call surfaced, not auto-decided).
  **Floor named:** the worklog `Behavioural`/SpecialCategory worklog route (OQ-H) lands with Issues in M4 (P-GA-31), reusing this marker — named here. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.2 (the SpecialCategory→DPIA marker).
- **GATE / DRILLS.** A `SpecialCategory`-tagged field emits the DPIA marker (assert the marker appears in the generated inventory); the CI data-map diff (P-GA-09) routes a new SpecialCategory flow into the DPIA gate. Telemetry: the DPIA marker fires on a SpecialCategory addition. Quantified: 100% of SpecialCategory-tagged fields emit the marker.
- **TESTS (required).** Unit: a `SpecialCategory` tag emits the DPIA marker; a non-SpecialCategory tag does not; the marker is surfaced into the inventory the diff gate reads. CDC: provider+consumer pair for the SpecialCategory-marker leg of 10.2 (the derive emitting the marker + the DPIA router consuming it — the diff-gate consumer is P-GA-09). Mutation floor: the SpecialCategory-detection + marker-emission path is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** A `SpecialCategory`-tagged field emits the DPIA marker into the inventory; the DPIA router records the new flow as DPIA-required; the worklog SpecialCategory route follow-on (P-GA-31) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: SpecialCategory → DPIA router (data-map-diff marker)`; body lists 10.2 (DPIA marker) implemented, the worklog route → P-GA-31. Co-Authored-By trailer.

---

### P-GA-09 — The data-map / RoPA generator (walk every schema + registered holder → the machine-readable inventory)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the data-map / RoPA generator 10.3 — the build step that walks every schema + every registered holder and generates the machine-readable inventory + the RoPA projection) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-07 (the derive emits the registry entries the generator walks), P-GA-04 (the registered holders the generator enumerates).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (code-wins-over-docs — the generated map is the source of truth, regenerated every build; a hand-written list drifts).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.2 (the generated data map + RoPA — a build step walks every schema + every registered holder and GENERATES the machine-readable inventory: what PII exists, where, role/basis/category, retention, locator, residency; regenerated every build; it is the substrate for RoPA (Art. 30), erasure fan-out (Art. 17), breach scoping (Arts. 33–34), access (Art. 15)).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.3 (`data_map() → Inventory`; `ropa(tenant) → ProcessingActivities` — generated).
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) the **data-map generator build step** — it walks the compile-time-collected registry (from P-GA-07's derive) + the registered holder list (from P-GA-04's auto-registration) and **generates a machine-readable inventory** (`data_map() → Inventory`): every PII field, its owning store/holder, the five tags, the subject_locator, the residency region. This is the substrate for fan-out — *the map, not a hand-written list, drives erasure*, so GA-D1's "0 holders missed" is a property of the generated map (a holder that exists but has no map entry is a coverage failure). (2) `ropa(tenant) → ProcessingActivities` — the RoPA projection grouped by processing activity (the legal text is `[OPEN — LEGAL]`, DPO-ratified; the GENERATION ships here).
  **Floor named:** the map's *content completeness* grows per store as holders ship (M3 Git/KN, M4 CI/Issues/Chat) — the map is COMPLETE (and GA-D1 provable end-to-end) only at M5 (P-GA-32). The CI diff gate that surfaces a change is P-GA-10. The RoPA legal text is `[OPEN — LEGAL]` (DPO ratifies). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.3 (`data_map()` + `ropa()` generation). Consumed: 10.2 (the registry entries), 10.1 (the registered holders), 1.4 (auto-registration).
- **GATE / DRILLS.** The generated inventory is the artifact: 0 PII fields/holders absent from the generated map (every registered holder + every tagged field appears). Telemetry: the inventory entry count = the tagged-field count + the holder count; a holder present in the registry but absent from the map is surfaced. CI for the generation (the diff gate is P-GA-10).
- **TESTS (required).** Unit: the generator emits an inventory entry for every tagged field + every registered holder; a field present in the registry but absent from a holder is surfaced (no holder-without-map / map-without-holder drift); the RoPA projection groups by processing activity. CDC: provider+consumer pair for 10.3 (the DSR orchestrator consuming `data_map()` to build the fan-out checklist — the consumer is P-GA-13). Mutation floor: the registry-walk + the inventory-emission path are mandatory-core — state and meet the mutation-score floor on the generator.
- **DEFINITION OF DONE.** The data-map generator walks every schema + every registered holder and generates the machine-readable inventory; `ropa()` projects it grouped by activity; 0 fields/holders absent from the map; the CI diff gate (P-GA-10) + the per-store completeness floor (M5 P-GA-32) + the RoPA `[OPEN — LEGAL]` text are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: data-map/RoPA generator (schema + registered-holder walk → inventory)`; body lists 10.3 implemented (0 fields/holders absent), the CI diff gate → P-GA-10, the completeness floor → P-GA-32, the RoPA legal text `[OPEN — LEGAL]`. Co-Authored-By trailer.

---

### P-GA-10 — The CI data-map diff gate + the DPIA-route on reclassification

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the CI data-map diff gate 10.3 — the generated inventory is committed; a build that changes it fails CI with the diff surfaced until a DPO reviews; a new SpecialCategory flow routes into the DPIA gate) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-09 (the generated inventory the diff compares), P-GA-08 (the SpecialCategory marker the diff routes into the DPIA gate).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (the ratchet — the CI diff is the committed gate a DPO reads; a reclassification that slips through silently is the failure).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.2 (regenerated every build, diffed in CI so a DPO sees any reclassification), §2.3 (the DPIA gate fires on a diff introducing a new SpecialCategory flow).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.3 (the CI-diffed inventory).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **CI data-map diff gate** — the generated inventory (P-GA-09) is committed; a build that changes it (a new PII field, a reclassification, a holder added/removed) **fails CI with the diff surfaced** until reviewed (a DPO sees the change). A new `SpecialCategory` flow routes the diff into the DPIA gate (the marker from P-GA-08).
  **Floor named:** the map's content completeness grows per store as holders ship — the diff surfaces each new holder's PII fields; the map is COMPLETE only at M5 (P-GA-32). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.3 (the CI diff gate). Consumed: 10.2 (the SpecialCategory marker).
- **GATE / DRILLS.** **GA-D5** (CI, the data-map-diff face) — add an untagged personal-data field → the lint fails the build AND the data-map diff surfaces the new field; a reclassification (e.g. role tenant-content → platform-operational) fails CI with the diff shown until reviewed. Telemetry: the diff is the gate; a `SpecialCategory` addition routes into the DPIA gate (assert the DPIA marker fires). Quantified: a changed inventory is a CI failure until reviewed; an unchanged one passes.
- **TESTS (required).** Unit: the diff gate fails on a changed inventory + passes on an unchanged one; a SpecialCategory addition emits the DPIA marker into the diff route. CDC: provider+consumer pair for the diff-gate leg of 10.3 (the generator emitting the inventory + the diff gate consuming it). Mutation floor: the diff-comparison + the DPIA-route path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The CI data-map diff gate fails on any reclassification/new-field/holder-change until reviewed; a SpecialCategory addition routes into the DPIA gate; GA-D5 (the diff face) emits a dated green artifact; the per-store completeness floor (M5 P-GA-32) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: CI data-map diff gate + DPIA-route on reclassification`; body lists 10.3 (diff gate) implemented, GA-D5 (diff face) green, the completeness floor → P-GA-32. Co-Authored-By trailer.

---

### P-GA-11 — The DSR orchestrator API + the state machine + the controller/processor posture gate

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the DSR orchestrator state machine 10.4 — `dsr_submit`/`dsr_status`/`dsr_certificate`; the received→validated→fanned-out→awaiting-holders→verified→completed machine; the controller-vs-processor posture gate) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-09 (the data map the orchestrator will read), the GDPR-service AppSpec boot (the `serve(AppSpec)` harness prompt).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — the canonical erase order; pseudonym-map first means every downstream holder sees only the opaque pseudonym).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 (the state machine — received → validated → fanned-out → {awaiting-holders} → verified → completed; deadline = now + 1 month; validate + decide posture; legal-hold gate), §1 (the controller-vs-processor posture — refuse a Myelin-initiated erase of tenant content absent instruction).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (`dsr_submit`/`dsr_status`/`dsr_certificate`), 10.3 (`data_map()` it will read).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **DSR orchestrator API + the state machine** — `dsr_submit(kind, subject, scope, posture) → dsr_id`, `dsr_status(dsr_id) → {state, deadline, checklist}`, `dsr_certificate(dsr_id) → MerkleProvenBundle` (the certificate Merkle-seal is wired in P-GA-20; the signatures + the state machine ship here). (1) The **state machine** received → validated → fanned-out → {awaiting-holders} → verified → completed — total + ordered (no skipping awaiting-holders). (2) **Validate + decide posture** (controller vs processor): refuse a Myelin-initiated erase of *tenant content* absent tenant instruction or offboarding (the posture gate). (3) The deadline is set to now + 1 month on submit (COARSE tracking here — the durable timer is M2 P-GA-21).
  **Floor named:** the per-holder checklist + the resumable fan-out + the receipts are P-GA-12; tenant-operability is P-GA-13; the durable deadline timer is M2 (P-GA-21, GA-D4). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.4 (the DSR API + the state machine + the posture gate; the checklist/receipts deferred to P-GA-12, tenant-operability to P-GA-13, the timer to P-GA-21). Consumed: 10.3 (`data_map()`).
- **GATE / DRILLS.** The state machine transitions are total + ordered; the posture gate refuses a Myelin-initiated erase of tenant content. Telemetry: `dsr_state` transitions observable; the posture-refusal is a captured-expected denial. CI for the synchronous state machine + the posture gate.
- **TESTS (required).** Unit: the state machine transitions are total + ordered (no skipping awaiting-holders); the posture gate refuses a Myelin-initiated erase of tenant content + admits a tenant-instructed one; `dsr_submit` sets the deadline to now+1month. CDC: provider+consumer pair for 10.4 (a DSR-submitting caller + the orchestrator; an auditor consuming `dsr_certificate`). Mutation floor: the state-machine transitions + the posture gate are mandatory-core — state and meet the mutation-score floor on the orchestrator module.
- **DEFINITION OF DONE.** The DSR orchestrator API + the state machine + the controller/processor posture gate exist to the frozen shape; the deadline is set coarse on submit; the checklist/receipts (P-GA-12), tenant-operability (P-GA-13), and durable timer (P-GA-21) follow-ons are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: DSR orchestrator API + state machine + controller/processor posture gate`; body lists 10.4 (API + state machine + posture) implemented, the checklist/receipts → P-GA-12, tenant-operability → P-GA-13, the durable timer → P-GA-21, the mutation score. Co-Authored-By trailer.

---

### P-GA-12 — The data-map-driven per-holder checklist + the resumable fan-out + verifiable receipts + the legal-hold gate

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the DSR durable resumable per-holder checklist 10.4 — resolved from the data map; the canonical erase order; the verifiable content-addressed receipts; the legal-hold gate wired) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-11 (the orchestrator state machine the checklist drives), P-GA-09 (the data map the checklist is resolved from), P-GA-06 (the holder contract + the canonical erase order the fan-out calls), P-GA-05 (the receipt shape). (Forward note, not a hard edge: the audit-log Merkle seal of these receipts is wired later in P-GA-20, co-built in this band — this prompt constructs the content-addressed receipt; P-GA-20 seals it into the tree.)
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — a verifiable receipt recording the destroyed key epoch + the purge cursor makes "we erased it" independently checkable), §4 (chain mutations end-to-end — a DSR is a sequence; test the chained fan-out).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 (resolve scope FROM THE DATA MAP → a per-holder checklist with the per-field erasure mechanism; legal-hold gate; fan out through the holder contract, each call idempotent + resumable, the durable checklist IS the state — a crashed orchestrator re-drives only un-receipted holders; the canonical erase order; collect + verify receipts), §4.2 (verifiable receipts — `receipt = sign(hash(request_id ∥ holder ∥ scope ∥ outcome ∥ key_epoch_destroyed? ∥ timestamp))`, appended to the per-tenant audit Merkle tree).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the checklist + receipts), 10.1 (the holder contract it fans out to), 10.3 (`data_map()` it reads), 4.8 (the pseudonym lever, fan-out step 1).
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) **Resolve scope from the data map** (P-GA-09) → a per-holder checklist with the per-field erasure mechanism — *the map drives fan-out*, "we forgot the search index" is impossible. (2) The **legal-hold gate**: an active `legal_hold` suspends erasure + retention-expiry for the held scope (the engine is P-GA-22; the gate is wired here, fail-safe-to-suspend); access/portability still proceed. (3) **Fan out** through the holder contract (P-GA-06) using the canonical erase order; each call **idempotent + resumable** — the **durable per-holder checklist IS the state**, a crashed orchestrator re-drives only un-receipted holders. (4) **Collect + verify receipts**; construct a **DSR completion receipt** (content-addressed + signed, `receipt = sign(hash(request_id ∥ holder ∥ scope ∥ outcome ∥ key_epoch_destroyed? ∥ timestamp))`) — the seal into the per-tenant audit Merkle tree is wired in P-GA-20.
  **Floor named:** the receipt seal into the Merkle tree is P-GA-20; the end-to-end erasure proof (0 recoverable + worker-kill resumability) is P-GA-14; the multi-cell `member_cells` iteration is M5 (P-GA-33, GA-D8). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.4 (the data-map-driven checklist + the resumable fan-out + the receipts + the legal-hold gate). Consumed: 10.1 (the holder contract), 10.3 (`data_map()`), 4.8 (the pseudonym lever, fan-out step 1).
- **GATE / DRILLS.** The checklist is built FROM the data map (not a hard-coded list); a crashed-mid-fan-out orchestrator re-drives only un-receipted holders (0 double-erase). Telemetry: `erasure_fanout_coverage` = 100% of existing holders; the receipt set per DSR. CI for the synchronous fan-out + the resumability (the 0-recoverable + backup proof is P-GA-14).
- **TESTS (required).** Unit: the checklist is built FROM the data map (not a hard-coded list); a crashed-mid-fan-out orchestrator re-drives only un-receipted holders (idempotent); the receipt encodes (request_id, holder, scope, outcome, key_epoch, timestamp) exactly; the legal-hold gate defers a held-scope erase while letting access proceed. CDC: provider+consumer pair for 10.4 (the orchestrator fanning to holders + collecting receipts). Mutation floor: the resumable-checklist + the receipt-construction + the legal-hold-gate paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The data-map-driven per-holder checklist + the resumable fan-out + the verifiable receipts + the legal-hold gate exist; the fan-out is data-map-driven; a worker kill re-drives only un-receipted holders (0 double-erase); the receipt-seal follow-on (P-GA-20), the erasure proof (P-GA-14), and the multi-cell follow-on (P-GA-33) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: data-map-driven per-holder checklist + resumable fan-out + receipts + legal-hold gate`; body lists 10.4 (checklist + receipts + hold gate) implemented, the receipt-seal → P-GA-20 and the erasure proof → P-GA-14 and multi-cell → P-GA-33, the mutation score. Co-Authored-By trailer.

---

### P-GA-13 — DSR tenant-operability: Art. 28 tenant-facing DSR + tenant offboarding (EraseScope::Tenant) + restrict/rectify/portability surfaces

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the DSR tenant-operability 10.4 §4.4 — operable by Myelin (controller) and by/for tenants (Art. 28); tenant offboarding = `erase(EraseScope::Tenant)`; restriction/rectification/portability surfaces) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-12 (the fan-out + checklist tenant-operability exposes), P-GA-11 (the orchestrator API).
- **CANON DOCS.**
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.4 (tenant-operability — operable by Myelin (controller) and by/for tenants (Art. 28); tenant offboarding = `erase(EraseScope::Tenant)`; restriction; rectification via reindex-from-source; portability).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the tenant-operability surfaces), 10.1 (the holders the offboarding fans over).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) OQ-I (the cross-cell bridge frame — single-cell tenant offboarding is the floor here; multi-cell is M5).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **DSR tenant-operability surfaces** — (1) the orchestrator exposed to **tenants for *their* data subjects** (Art. 28, processor-side DSR); (2) **tenant offboarding** = `erase(EraseScope::Tenant)` (destroy the tenant KEK ⇒ whole tenant unrecoverable), fanned over the holder list; (3) the **restriction / rectification / portability surfaces** wired through the orchestrator (the `restrict` honoured-everywhere proof is M2 P-GA-25; the reindex-from-source rectification fan-out is M2 P-GA-24).
  **Floor named:** single-cell tenant offboarding ships here; the multi-cell `member_cells` iteration over the cross-cell bridge is the M5 follow-on (P-GA-33, GA-D8). The full restriction-into-derived-stores proof is M2 (P-GA-25). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.4 (the tenant-operability surfaces — Art. 28 DSR, offboarding, restrict/rectify/portability entry points). Consumed: 10.1 (the holders offboarding fans over).
- **GATE / DRILLS.** Tenant offboarding fans `EraseScope::Tenant` over the holder list (destroy the tenant KEK ⇒ whole tenant unrecoverable). Telemetry: `erasure_fanout_coverage` over the tenant scope = 100% of existing holders; the offboarding certificate. CI for the synchronous offboarding fan-out.
- **TESTS (required).** Unit: a tenant-initiated DSR over the tenant's own data subjects is admitted (Art. 28); tenant offboarding fans `EraseScope::Tenant` over the holder list; the restriction/rectification/portability surfaces route through the orchestrator. CDC: provider+consumer pair for the tenant-operability leg of 10.4 (a tenant-facing DSR caller + the orchestrator). Mutation floor: the offboarding-fan-out + the Art-28-scoping path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The DSR tenant-operability surfaces exist (Art. 28 tenant-facing DSR + tenant offboarding + restrict/rectify/portability entry points); tenant offboarding fans `EraseScope::Tenant` over the holder list; the multi-cell follow-on (P-GA-33) + the full restriction proof (P-GA-25) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: DSR tenant-operability — Art. 28 DSR + tenant offboarding + restrict/rectify/portability surfaces`; body lists 10.4 (tenant-operability) implemented, the multi-cell → P-GA-33 and full restriction → P-GA-25, the mutation score. Co-Authored-By trailer.

---

### P-GA-14 — The M1 DSR erasure floor proof: data-map-driven fan-out → 0 recoverable + worker-kill resumability (the coarse-deadline floor)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the M1 DSR floor proven — a subject seeded into the M1 stores → `dsr_submit` → data-map-driven fan-out → post-erase `locate` = 0 recoverable PII → certificate; worker-kill resumability; the coarse 1-month deadline floor) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-12 (the resumable fan-out + checklist + receipts), P-GA-06 (the M1-shared-layer holder orchestration the fan-out hits), P-GA-05 (the GDPR-owned holders).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — 0 recoverable is the green artifact, observed via `locate`), §4 (chain mutations end-to-end — the chained DSR fan-out, not a single holder).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 (the data-map-driven fan-out; the durable checklist re-drives only un-receipted holders), §4.1 step 6 (the coarse deadline tracked synchronously here; the durable timer is M2).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the DSR fan-out), 10.1 (the holders), 10.3 (the data map driving the checklist).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D1 (the M1 face — a subject's PII fan-out → 0 recoverable; full H1–H18 is M5).
- **DELIVERABLE.** In crate myelin-gdpr-service: prove the **M1 DSR erasure floor end-to-end on the M1 stores** — a subject seeded into the M1-shared-layer + GDPR-owned holders → `dsr_submit` → the data-map-driven fan-out hits every *existing* holder → post-erase `locate` returns 0 recoverable PII → a certificate seals (the Merkle seal rides P-GA-20). Prove the **worker-kill resumability** drill: kill the orchestrator mid-fan-out → on restart it re-drives only un-receipted holders, 0 double-erase, 0 missed holder. The **1-month deadline is COARSE here** (synchronous/checklist tracking — no durable timer yet).
  **Floor named:** the durable nearing-deadline Signal arms in M2 on the `myelin-flow` wheel (P-GA-21, GA-D4) — named. The backup-recovery proof (0 recoverable in backups) is P-GA-15; the full H1–H18 fan-out is M5 (P-GA-32). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: the M1-scale proof of 10.4 (data-map-driven fan-out + resumability; the deadline-timer deferred to P-GA-21, multi-cell to P-GA-33). Consumed: 10.1 (the holders), 10.3 (the data map).
- **GATE / DRILLS.** The M1 DSR floor proven (the M1 face of GA-D1): a subject seeded into the M1 stores → `dsr_submit` → fan-out hits every *existing* holder → post-erase `locate` returns 0 recoverable PII → a certificate seals. A **worker-kill resumability** drill: kill the orchestrator mid-fan-out → on restart it re-drives only un-receipted holders, 0 double-erase, 0 missed holder. Telemetry: `erasure_fanout_coverage` = 100% of existing holders; `dsr_deadline_margin` tracked (coarse). CI for the synchronous fan-out + the resumability.
- **TESTS (required).** Unit/drill: the chained M1 DSR scenario on the harness (submit → fan out → verify → seal) returns 0 recoverable PII over existing holders; the worker-kill resumability scenario (kill mid-fan-out → restart → 0 double-erase, 0 missed). CDC: the drill harness scenario IS the proof; reuse the 10.4 CDC pair from P-GA-12. Mutation floor: covered by P-GA-12's orchestrator floor; no new core module here.
- **DEFINITION OF DONE.** The M1 DSR floor emits a dated green artifact (0 recoverable PII over existing holders); a worker kill re-drives only un-receipted holders (0 double-erase, 0 missed); the coarse-deadline floor (→ P-GA-21), the backup proof (→ P-GA-15), and the full-fan-out floor (→ P-GA-32) are named in writing; the drill scenario passes; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: M1 DSR erasure floor proof — data-map fan-out → 0 recoverable + worker-kill resumability`; body lists the M1 DSR floor + worker-kill resumability green (0 recoverable, 0 double-erase, 0 missed) with measured numbers, the coarse-deadline floor → P-GA-21, the backup proof → P-GA-15, the full fan-out → P-GA-32. Co-Authored-By trailer.

---

### P-GA-15 — The erasure ledger (PII-free, non-shred-erasable) + post-restore re-erasure + the crypto-shred-reaches-backups / restore-resurrects-nothing proof

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the erasure ledger 10.8 — the PII-free record of every completed erasure driving Storage's `post_restore_reerase`; the silent-data-loss-meets-erasure floor) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-12 (the DSR orchestrator that writes a ledger entry on completion), P-GA-06 (the holder erase that supplies the destroyed key epochs), P-GA-14 (the M1 erasure floor the backup proof extends), the Storage M1 prompts that ship 11.5 (backup/restore cross-seam + `post_restore_reerase`) and 11.3/11.4 (the per-subject DEK whose destruction the ledger records).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (silent data loss outranks every feature — a restore that resurrects an erased subject IS a data-loss-class failure; the restore-verify/re-erasure line is a permanent gate), §3 (prove-it — a re-erasure receipt is the green artifact).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.2 (H10 backups — crypto-shred by construction: key destroyed ⇒ ciphertext unrecoverable, + bounded retention + post-restore re-erasure), §4 (the canonical erase order records the destroyed key epoch), the §1.2 ownership split (Storage owns the restore MECHANISM `post_restore_reerase`; GDPR owns the LEDGER that drives it).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.8 (the erasure ledger — PII-free opaque subject + holders/keys shredded; drives post-restore re-erasure GD-14), 11.5 (backup/restore cross-seam + `post_restore_reerase`), 11.3/11.4 (the destroyed key epoch the ledger records).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows STOR-D3 (erase; restore an older backup → still erased, 0 resurrected), STOR-D4 (erase; recover from backups → per-subject ciphertext unrecoverable, 0 recoverable), ID-D8 (restore to a consistent point → no resurrected grants past an erasure; post-restore re-erasure runs).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **erasure ledger** (contract 10.8) — a **PII-free, non-shred-erasable** record of every completed erasure: the **opaque subject id** + the holders erased + the **key epochs destroyed** (no PII, so it is itself a recursive holder that does NOT crypto-shred away — it must survive to drive re-erasure). On a DSR completion (P-GA-12), the orchestrator writes the ledger entry. Wire the ledger as the source Storage's `post_restore_reerase` (11.5) reads: when a backup is restored to an offset *before* a subject's erasure, the restore machinery reads the ledger and **re-erases** the subject so a restore never resurrects erased PII. Prove the M1 silent-data-loss-meets-erasure floor end-to-end on the M1 stores: erase → backup-recovery yields 0 recoverable PII (key destroyed) → restoring an older backup lands the subject **still erased** (re-erasure ran), 0 resurrected.
  **Floor named:** these drills run at M1 scale here; they re-run at cell scale at M5 (P-GA-32, under world-scale load). The full H1–H18 fan-out (GA-D1) is M5. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.8 (the erasure ledger). Consumed: 11.5 (`post_restore_reerase` — Storage owns the restore mechanism; GDPR supplies the ledger it reads), 11.3/11.4 (the destroyed key epochs), 10.4 (the DSR completion that writes the ledger entry).
- **GATE / DRILLS.** **STOR-D4 (GA face)** (SCHED) — erase a subject; attempt recovery from backups → per-subject ciphertext unrecoverable (key destroyed, excluded from backup); 0 recoverable PII in any backup. **STOR-D3 / ID-D8 (GA face)** (SCHED) — erase; restore an *older* backup → the subject is still erased (post-restore re-erasure ran from the ledger); 0 resurrected. Telemetry: `crypto-shred-lag` on the destruction; the re-erasure receipt is the green artifact; `erasure_fanout_coverage` over the M1 holder set = 100%. This is the GDPR half of the master M1 → M2 STOR-D4/GA-D5 exit line (the permanent restore-verify/re-erasure gate). SCHED (whole-cell restore drill).
- **TESTS (required).** Unit: the ledger entry is PII-free (an architecture test that no PII-typed field appears in the ledger schema — it is the one holder that must NOT be shred-erasable); a DSR completion writes a ledger entry recording the opaque subject + holders + key epochs; the ledger is idempotent (re-completing the same DSR does not duplicate the entry). CDC: provider+consumer pair for 10.8 (the orchestrator writing the ledger + Storage's `post_restore_reerase` reading it). Drill: the STOR-D4-GA-face + STOR-D3/ID-D8-GA-face scenarios on the harness (erase → recover-from-backup = 0 recoverable; erase → restore-older-backup = still erased, re-erasure receipt). Mutation floor: the ledger-write + the re-erasure-trigger path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The erasure ledger exists, is PII-free + non-shred-erasable, and records every completed erasure; `post_restore_reerase` reads it and re-erases on a pre-erasure restore; STOR-D4-GA-face (0 recoverable in backups) + STOR-D3/ID-D8-GA-face (0 resurrected, re-erasure receipt) emit dated green artifacts; the cell-scale re-run (M5) and the full GA-D1 fan-out (M5) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: erasure ledger + post-restore re-erasure + crypto-shred-reaches-backups proof`; body lists 10.8 implemented, STOR-D4-GA-face (0 recoverable) + STOR-D3/ID-D8-GA-face (0 resurrected) green with measured numbers, the cell-scale re-run → P-GA-32, the mutation score. Co-Authored-By trailer.

---

### P-GA-16 — The ONE free-text/immutable erasure posture (X-7) written once as the single canonical artifact

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the ONE free-text/immutable erasure posture 10.9 written once — the keystone X-7 artifact: structural floor + documented residual limit, instantiated per subsystem BY REFERENCE) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-05 (the holder erase + the per-subject DEK crypto-shred wiring), the Identity M1 prompts that ship 4.8 (the pseudonym-map shred + the frozen pseudonym grammar `<pseudonym>@<tenant>.noreply`), the Storage M1 prompts that ship 11.4 (the per-subject DEK).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the structural floor ships regardless of legal ratification; the residual is flagged, never pretended-solved; date every status note).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — the event-log half is "workable" (per-subject DEK + pseudonym shred), the git-history / third-party free-text half is the hard residual; delete the identity not the fact).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §7 (the ONE free-text/immutable erasure posture — the keystone X-7 artifact: §7.1 the structural floor; §7.2 the residual (third-party free-text PII typed by someone else, encrypted under the AUTHOR's DEK not the subject's); §7.3 the ratified engineering posture `[OPEN — LEGAL]`; §7.4 instantiation per subsystem BY REFERENCE — ONE posture, each subsystem references it, never restated five times).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.9 (the ONE posture — structural floor + documented residual limit, instantiated per subsystem by reference, `[OPEN — LEGAL]`).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) X-7 / OQ-G (the erasure posture decision).
- **DELIVERABLE.** In crate myelin-gdpr-service: **the ONE free-text/immutable erasure posture written ONCE** (contract 10.9) — a single canonical artifact (a documented module in myelin-gdpr-service + the doc text it generates) that states: the **structural floor** (the engineering answer for the overwhelming majority — per-subject DEK crypto-shred + pseudonym-map shred + `restrict`); the **residual** (third-party / immutable-byte free-text PII authored by others, encrypted under the author's DEK — NOT crypto-shreddable by the subject's key); the documented lawful-basis limit + best-effort `rectify`/tombstone + the standing `restrict` guarantee. **Tagged `[OPEN — LEGAL]`** for the residual lawful-basis ratification (ONE statement, not five) — the structural floor ships regardless; counsel ratification gates publishing the posture as ratified. This is the SINGLE source the M3/M4 subsystem instances reference (P-GA-28/P-GA-29/P-GA-31) — never restate.
  **Floor named:** the structural floor PROOF on the M1 stores is P-GA-17; the pseudonymous-commit prerequisite for Git is P-GA-18; the audited history-rewrite erasure path is the M5 follow-on (GA-10, P-GA-35); the residual lawful-basis ratification is parallel-legal. Record in writing, dated.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.9 (the ONE posture written once — the canonical artifact). Consumed: 4.8 (the pseudonym-map shred lever, referenced), 11.4 (the per-subject DEK, referenced).
- **GATE / DRILLS.** The posture artifact exists as ONE canonical document the M3/M4 subsystems will reference. Telemetry: the artifact is the green artifact (a single source). CI: an architecture test that the posture is a SINGLE source (subsystem docs reference it, never restate it — checked when M3/M4 land). The residual is `[OPEN — LEGAL]`, named not gated.
- **TESTS (required).** Unit: the posture artifact is a SINGLE source (an architecture test that subsystem docs reference it, never restate it — the test scaffolding lands here, the assertions fire when M3/M4 reference it); the residual (author's-DEK-encrypted third-party mention) is documented as correctly NOT crypto-shredded by the subject's key (the documented limit). CDC: provider+consumer pair for 10.9 (the posture as owned + a subsystem referencing it — the subsystem-reference consumer lands in P-GA-28; stub it here). Mutation floor: not core logic (a documented artifact); no mutation floor — named.
- **DEFINITION OF DONE.** The ONE free-text/immutable erasure posture is written ONCE as the single canonical artifact (structural floor + residual + the documented lawful-basis limit); the residual is tagged `[OPEN — LEGAL]` (one statement) with the structural floor shipping regardless; the structural-floor-proof (P-GA-17), pseudonymous-commit prerequisite (P-GA-18), and history-rewrite follow-on (P-GA-35) are named in writing; CDC + unit tests pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: the ONE free-text/immutable erasure posture (X-7) written once`; body lists 10.9 (the canonical artifact) implemented, the `[OPEN — LEGAL]` residual (one statement), the floor-proof → P-GA-17, the pseudonymous-commit prerequisite → P-GA-18, the history-rewrite follow-on → P-GA-35. Co-Authored-By trailer.

---

### P-GA-17 — The structural erasure floor proven on the M1 stores (per-subject DEK shred + pseudonym-map shred + restrict suppression)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the structural erasure floor X-7 §7.1 — per-subject DEK crypto-shred of self-authored free-text + pseudonym-map shred of identity + `restrict` suppression, proven end-to-end on the M1 stores) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-16 (the ONE posture this floor instantiates), P-GA-05/P-GA-06 (the holder erase + the per-subject DEK crypto-shred wiring), P-GA-12 (the `restrict` surface in the orchestrator), the Identity M1 prompts that ship 4.8 (the pseudonym-map shred), the Storage M1 prompts that ship 11.4 (the per-subject DEK).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the structural floor ships and is proven; the residual is flagged).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — the event-log half is workable via per-subject DEK + pseudonym shred).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §7.1 (the structural floor built now — per-subject DEK crypto-shred of self-authored free-text; pseudonym-map shred of identity; structural holder coverage + `restrict`), §4.4 (`restrict(subject, on)` — no indexing / agent-use / analytics / notification while retaining storage; reversible).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.9 (the structural floor of the ONE posture), 4.8 (the pseudonym lever), 11.4 (per-subject DEK), 10.1 (the holders that honour `restrict`).
- **DELIVERABLE.** In crate myelin-gdpr-service + the holder impls: **the structural erasure floor (X-7 §7.1), working end-to-end on the M1 stores** — per-subject DEK crypto-shred of self-authored free-text (the lever Storage provides); pseudonym-map shred of identity (the lever Identity provides, fan-out step 1); **`restrict(subject)` suppression** = a per-subject suppression flag every holder honours: no indexing, no agent-use, no analytics, no notification, while RETAINING storage, reversible (the M1 holders honour it now; Search/Refs/Notif/OLAP honour it in M2, P-GA-25). Prove the floor works on the M1 stores.
  **Floor named:** the full restriction-into-derived-stores proof (GA-D7) is M2 (P-GA-25); the M1 holders honour `restrict` now. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.9 (the structural floor proven on M1 stores). Consumed: 4.8 (the pseudonym-map shred lever), 11.4 (the per-subject DEK), 10.1 (the holders that honour `restrict`).
- **GATE / DRILLS.** The structural floor proven on M1 stores: a subject's self-authored free-text crypto-shreds via per-subject DEK (0 recoverable); identity erases via pseudonym-map shred (downstream sees only the pseudonym); `restrict` suppresses processing on the M1 holders (the full restriction proof GA-D7 is M2, P-GA-25). Telemetry: the per-subject-DEK destruction + the pseudonym-map shred emit their key-shred signals; the `restrict` suppression flag is observable on the M1 holders. CI for the floor wiring.
- **TESTS (required).** Unit: per-subject DEK shred of self-authored free-text renders it unrecoverable; pseudonym-map shred leaves immutable bytes holding only the `<pseudonym>@<tenant>.noreply` form; `restrict` sets the suppression flag the M1 holders honour (reversible); the residual (author's-DEK-encrypted third-party mention) is `restrict`-suppressed, never indexed/agent-read/analysed (the documented limit from P-GA-16). CDC: reuse the 10.1/10.9 CDC pairs; add the `restrict`-honoured-by-M1-holder pair. Mutation floor: the `restrict`-suppression-flag + the residual-classification path are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The structural erasure floor (per-subject DEK shred + pseudonym-map shred + `restrict`) works end-to-end on the M1 stores; the residual is `restrict`-suppressed per the documented limit; the full restriction-into-derived proof (P-GA-25) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: structural erasure floor proven on M1 stores (per-subject DEK + pseudonym shred + restrict)`; body lists 10.9 (structural floor) proven on M1 stores, the residual `restrict`-suppressed, the full restriction proof → P-GA-25, the mutation score. Co-Authored-By trailer.

---

### P-GA-18 — The pseudonymous-by-default commit-identity prerequisite recorded for Git M3 (X-7's critical-path obligation)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the pseudonymous-by-default commit-identity requirement recorded as the commit-time prerequisite Git must satisfy in M3, before the git data model freezes — GDPR's ONE critical-path obligation) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-16 (the ONE posture), the Identity M1 prompts that ship 4.8 (the frozen pseudonym grammar `<pseudonym>@<tenant>.noreply`).
- **CANON DOCS.**
  - [`../../VISION.md`](../../VISION.md) §3 (name-your-floors — the prerequisite is decided NOW, before the git data model freezes).
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (the git-history immutable hash must never bake erasable PII — the history-rewrite path is disruptive, so prevent the bake at commit time).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §7.1 (commits pseudonymous-by-default so the immutable hash never bakes erasable PII).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.9 (the prerequisite recorded), 4.8 (the pseudonym grammar).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) X-7 / OQ-G (the pseudonymous-by-default commit prerequisite frozen before the Git data model).
- **DELIVERABLE.** In crate myelin-gdpr-service (the recorded prerequisite + the architecture-test scaffolding): **the pseudonymous-by-default commit-identity requirement** recorded as the **commit-time prerequisite Git must satisfy in M3** (decided NOW, before the git data model freezes, EI-04 §1): the audit/commit actor is the frozen pseudonym grammar `<pseudonym>@<tenant>.noreply` (4.8) so the immutable bytes never bake erasable PII. This is GDPR's ONE critical-path obligation; Git (P-GA-27 + the Git subsystem) consumes it. Ship the prerequisite as a recorded contract obligation + the architecture-test scaffolding that will assert (in M3) that Git commits hold only the pseudonym form.
  **Floor named:** the M3 enforcement (Git commits hold only the pseudonym) lands with the Git instance (P-GA-27); the architecture test fires there. Record in writing, dated.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.9 (the pseudonymous-commit prerequisite recorded as a Git-M3 obligation). Consumed: 4.8 (the pseudonym grammar).
- **GATE / DRILLS.** The prerequisite is recorded as a Git-M3 obligation before the git data model freezes; the architecture-test scaffolding exists (it asserts in M3 that commits hold only `<pseudonym>@<tenant>.noreply`). Telemetry: the recorded obligation is the green artifact (a dated note). CI: the architecture-test scaffold is wired (firing deferred to P-GA-27).
- **TESTS (required).** Unit: the architecture-test scaffolding exists + references the frozen pseudonym grammar (it asserts in M3); a fixture commit-actor in the pseudonym form passes, a real-identity form fails (the scaffold's verdict). CDC: provider+consumer pair for the prerequisite leg of 10.9 (GDPR recording the obligation + Git consuming it — the Git consumer fires in P-GA-27). Mutation floor: the pseudonym-form check is mandatory-core — state and meet the mutation-score floor on the check.
- **DEFINITION OF DONE.** The pseudonymous-by-default commit-identity prerequisite is recorded for Git M3 (before the git data model freezes); the architecture-test scaffolding exists + references the frozen pseudonym grammar; the M3 enforcement follow-on (P-GA-27) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: pseudonymous-by-default commit-identity prerequisite recorded for Git M3`; body lists 10.9 (the prerequisite) recorded, the architecture-test scaffold wired, the M3 enforcement → P-GA-27. Co-Authored-By trailer.

---

### P-GA-19 — The tamper-evident audit log core: outbox-only audit consumer + per-tenant hash-chain + Merkle leaves + minimisation

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the tamper-evident audit log 10.6 — the audit consumer as an outbox-only infra subscription; the per-tenant hash-chain whose entries are Merkle-tree leaves; minimised pseudonym actors) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** the Bus M0 prompts (the outbox + the EventEnvelope — the audit consumer is an infra subscription on the outbox), the Identity M1 prompt that ships 4.8 (the frozen pseudonym grammar — the actor minimisation form).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §5 (written via the outbox only — coverage is a property of the bus, not a per-service good intention).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §6.1–§6.2 (construction — ONE tamper-evident append-only log records every human AND agent action; a per-tenant hash-chain whose entries are also Merkle-tree leaves; written via the outbox only — the audit consumer is an infra subscription, no service writes the log directly; minimised — actor/on_behalf_of/subject are pseudonymous IDs/ArtifactRefs never payloads, actor uses the frozen `<pseudonym>@<tenant>.noreply` grammar; causality-carried correlation_id/causation_id = the "why did this happen" walk; deliberately NOT a blockchain).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.6 (the audit log construction — the hash-chain + Merkle leaves; the proofs deferred to P-GA-20), 2.1/2.2 (the EventEnvelope + the outbox the log rides), 4.8 (the pseudonym grammar).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **tamper-evident audit log core** (contract 10.6, the construction half). (1) The **audit consumer** — an infra subscription on the M0 outbox: every action-bearing event (human + agent) is appended to the log; **no service writes the log directly** (coverage is a bus property — assert no direct-write path exists). (2) The **per-tenant hash-chain** whose entries are also **Merkle-tree leaves**. (3) **Minimised**: `actor`/`on_behalf_of`/`subject` are pseudonymous IDs / ArtifactRefs, NEVER payloads; `actor` uses the frozen `<pseudonym>@<tenant>.noreply` grammar (4.8). (4) **Causality-carried** (correlation_id / causation_id) — the audit log IS the "why did this happen" walk. Deliberately **NOT a blockchain** (no global byzantine consensus; a residency problem if replicated off-cell).
  **Floor named:** the CT-style proofs (inclusion/consistency/STH) + the independent-witness anchoring + the DSR-receipt seal + the H16 carve-out body are P-GA-20; the history-rewrite audited op (10.6 §6.6, GA-10) is M5 (P-GA-35). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.6 (the audit-log construction — the outbox consumer + the per-tenant hash-chain + the Merkle leaves + the minimisation; the proofs/STH/witness deferred to P-GA-20). Consumed: 2.1/2.2 (the EventEnvelope + the outbox the audit consumer subscribes to), 4.8 (the pseudonym grammar).
- **GATE / DRILLS.** An appended action produces a hash-chain entry + a Merkle leaf; the actor is the minimised `<pseudonym>@<tenant>.noreply` form (never a payload); no service writes the audit log except the outbox consumer (coverage is a bus property). Telemetry: `audit_append_lag` reads green (the audit-log health SLO). CI for the construction + the minimisation + the outbox-only property (the tamper-detection proof is P-GA-20).
- **TESTS (required).** Unit: an appended action produces a hash-chain entry + a Merkle leaf; the actor is the minimised `<pseudonym>@<tenant>.noreply` form, never a payload; an architecture test that NO service writes the audit log except the outbox consumer (coverage is a bus property); correlation_id/causation_id are carried. CDC: provider+consumer pair for 10.6 (an action-taking service emitting via the outbox + the audit consumer appending). Mutation floor: the hash-chain-append + the Merkle-leaf construction paths are mandatory-core — state and meet the mutation-score floor on the audit-log module.
- **DEFINITION OF DONE.** The audit log core (the outbox-only audit consumer + the per-tenant hash-chain + the Merkle leaves + the minimisation + the causality carry) is written via the outbox only; entries are minimised (pseudonym actors, never payloads); `audit_append_lag` reads green; the proofs/STH/witness (P-GA-20) + the H16 carve-out body (P-GA-20) + the history-rewrite op (P-GA-35) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: tamper-evident audit log core — outbox-only consumer + per-tenant hash-chain + Merkle leaves + minimisation`; body lists 10.6 (construction) implemented, `audit_append_lag` green, the proofs/STH/witness → P-GA-20 and history-rewrite → P-GA-35, the mutation score. Co-Authored-By trailer.

---

### P-GA-20 — The audit CT-style proofs + signed tree heads + independent-witness anchoring + DSR-receipt seal + the H16 carve-out body (GA-D3)

- **BAND.** M1.
- **ROADMAP MILESTONE.** GA-M1 (the audit log CT-style inclusion/consistency proofs + signed tree heads + the independent-witness anchoring 10.6; the DSR receipts sealing into the tree; the audit carve-out H16 body) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1".
- **DEPENDS-ON.** P-GA-19 (the audit-log core — the hash-chain + Merkle leaves the proofs prove over), P-GA-12 (the DSR receipts that seal into the tree), P-GA-05 (the H16 carve-out holder shell whose body lands here).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — a tamper that breaks the chain + fails the consistency proof + mismatches the witness IS the green artifact; observability is part of the pass).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §6.2–§6.3 (CT-style inclusion + consistency proofs (RFC 6962); the Trillian/CT model with signed tree heads, self-hosted in-cell; the STH anchored to an independent witness (RFC-3161 TSA / a different cell's notary) seeing only an opaque root — residency-safe), §6.4 (the audit carve-out H16 — retain the lawfully-needed minimised pseudonym record, expire via audit-key crypto-shred; the carve-out does NOT weaken tamper-evidence: never rewrite an entry, the real identity lived in Id's erasable pseudonym map), §6.5 (distinct from agent trace + telemetry).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.6 (`inclusion_proof`/`consistency_proof`/`signed_tree_head`; history-rewrite audited op deferred to P-GA-35), 10.4 (the DSR receipts that seal into the tree).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D3 (retroactively edit/delete an audit entry → chain breaks + consistency proof vs published STH fails + witness mismatches; tamper detected 100%) + the E2E-3 audit-tamper leg.
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) **CT-style proofs** — `inclusion_proof(action) → MerklePath` ("this action is in the log"), `consistency_proof(t1, t2) → Proof` ("the log wasn't forked/rewritten between two signed tree heads"), `signed_tree_head(tenant) → STH`. (2) The **independent-witness anchoring**: the STH is periodically anchored to an external witness (RFC-3161 TSA / a different cell's notary) seeing only the **opaque root hash** (no PII crosses — residency-safe), so even a fully-compromised cell cannot rewrite history undetectably. (3) Wire the **DSR receipts (P-GA-12) to seal into the per-tenant Merkle tree** (closing the P-GA-12 seal floor). (4) The **H16 carve-out** body (the shell from P-GA-05): identity erasure is the pseudonym-map shred (never an entry rewrite — that breaks the chain); the entry retains only the minimised opaque-pseudonym record, expiring via audit-key crypto-shred at retention end (carve-out scope `[OPEN — LEGAL]` GD-5).
  **Floor named:** GA-D3 runs at M1 audit-surface scale here; it re-runs at cell scale under world-scale audit volume at M5 (P-GA-35, the E2E-3 audit leg). The history-rewrite audited op (10.6 §6.6, GA-10) is M5 (P-GA-35). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.6 (the three proofs + the STH + the witness anchoring + the H16 carve-out body; the history-rewrite op deferred to P-GA-35). Consumed: 10.4 (the DSR receipts that seal into the tree).
- **GATE / DRILLS.** **GA-D3** (SCHED, partial-at-M1-scale) — retroactively edit/delete an audit entry → the hash-chain breaks + the consistency proof against the published STH fails + the external witness mismatches. **Tamper detected 100%** over the M1 audit surface. Telemetry: `sth_publish_age` reads green (the audit-log health SLO); the tamper-detection proof is the green artifact. SCHED.
- **TESTS (required).** Unit: `inclusion_proof` verifies against the STH; `consistency_proof` verifies between two STHs; a tampered entry breaks the chain AND fails the consistency proof AND mismatches the witness (the GA-D3 property — three independent detections); the witness sees only an opaque root (no PII); the H16 carve-out retains the minimised record without rewriting an entry; a DSR receipt seals into the tree. CDC: provider+consumer pair for 10.6 (an auditor consuming `inclusion_proof`/`consistency_proof`/`signed_tree_head`). Drill: the GA-D3 tamper scenario on the harness (edit an entry → assert all three detections). Mutation floor: the Merkle-proof + the consistency-proof verification paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The three CT-style proofs + STH + independent-witness anchoring exist; DSR receipts seal into the tree; the H16 carve-out retains-without-rewriting; GA-D3 emits a dated green artifact (tamper detected 100% via three independent detections); `sth_publish_age` reads green; the cell-scale re-run (M5) + the history-rewrite op (P-GA-35) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M1: audit CT proofs + STH + independent-witness anchoring + DSR-receipt seal + H16 carve-out`; body lists 10.6 (proofs + witness + carve-out) implemented, GA-D3 green (tamper detected 100%, three detections) with measured numbers, the cell-scale re-run → P-GA-35, the mutation score. Co-Authored-By trailer.

---

### P-GA-21 — The DSR deadline durable timer + the nearing-deadline warning Signal (on the myelin-flow wheel)

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (the DSR 1-month deadline durable timer + the nearing-deadline warning Signal 10.4 — replacing the M1 coarse-tracking floor) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M2".
- **DEPENDS-ON.** P-GA-11/P-GA-14 (the DSR orchestrator whose coarse deadline this replaces), the Workflow M2 prompts that ship 9.3 (the durable timer wheel — `sleep_until` on the minute-bucket index) and 9.2/9.4 (durable activity + signal), the Signals M2 prompt (the `Signal` the warning fires).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — a timer that fires to-the-second across a restart, observed via the signal; 0 silent misses), §7 (one primitive — we do NOT reinvent durable timers; the DSR deadline rides the same wheel as SLA timers and Trigger `stale_after`).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 step 6 (track the deadline on a durable timer — the same `myelin-flow` timer wheel as SLA timers; a nearing-deadline emits a warning `Signal`; the certificate seals on completion).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the deadline timer leg), 9.3 (the durable timer wheel), 9.2/9.4 (durable activity + signal).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D4 (open a DSR → the durable timer fires a warning Signal before the 1-month deadline; the certificate seals on completion; 0 silent misses).
- **DELIVERABLE.** In crate myelin-gdpr-service: replace the M1 coarse deadline-tracking floor (P-GA-14) with the **durable deadline timer** — on `dsr_submit`, arm a `sleep_until(now + 1 month)` on the `myelin-flow` timer wheel (9.3); a **nearing-deadline** point (a default-to-beat margin, set from the thresholds file) fires a **warning `Signal`** (9.4) so the deadline is never silently missed; the timer is durable + resumable (survives an orchestrator restart — it is wheel state, not in-process state). On completion the **certificate seals** (P-GA-20). Wire the deadline-extension-to-3-months-for-complex case (recorded reason) as a re-arm.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.4 (the deadline-timer leg, completing the P-GA-14 floor). Consumed: 9.3 (the durable timer wheel), 9.2/9.4 (durable activity + signal), 10.6 (the certificate seal).
- **GATE / DRILLS.** **GA-D4** (SCHED) — open a DSR → the durable timer fires a warning Signal before the 1-month deadline; the certificate seals on completion. **0 silent misses.** A restart-resilience leg: kill the orchestrator between arm and fire → on restart the timer still fires (wheel state survives). Telemetry: `dsr_deadline_margin` is what the warning Signal fires on; the `DSR-timer fire` + the `sealed cert` are the green artifacts. SCHED.
- **TESTS (required).** Unit: arming a DSR schedules a wheel entry at now+1month; the nearing-deadline margin fires the warning Signal; a restart between arm and fire still fires (the wheel is durable); the extension-to-3-months re-arms with a recorded reason; the certificate seals on completion. CDC: provider+consumer pair for the deadline-timer leg of 10.4 (the orchestrator arming the timer + the wheel firing the signal). Drill: the GA-D4 scenario on the harness (open DSR → assert the warning Signal fires before deadline + the cert seals; kill-and-restart → still fires). Mutation floor: the arm/fire/re-arm timer path is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The DSR deadline durable timer replaces the M1 coarse floor; it arms on submit, fires a nearing-deadline warning Signal, survives a restart, and re-arms on extension; the certificate seals on completion; GA-D4 emits a dated green artifact (0 silent misses); `dsr_deadline_margin` reads green; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: DSR deadline durable timer + nearing-deadline warning Signal`; body lists 10.4 (deadline timer) implemented, GA-D4 green (0 silent misses) with measured numbers, the M1 coarse-tracking floor closed. Co-Authored-By trailer.

---

### P-GA-22 — The retention engine: tightest-policy-wins merge + legal-hold-aware suspend-don't-delete (GA-D6)

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (the retention engine 10.5 — tightest-policy-wins merge; legal-hold-aware suspend-don't-delete) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M1"/"GA-M2" (the retention engine lands in the M1 spine; proven in M2).
- **DEPENDS-ON.** P-GA-12 (the legal-hold gate wired in the orchestrator the engine backs), P-GA-05 (the §3 erasure mechanisms expiry uses).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §2 (silent data loss outranks every feature — a legal-hold must fail-safe-to-suspend, never auto-delete), §3 (prove-it — 0 held-scope deletions, observed).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §5.1 (the retention engine — tightest-policy-wins merge, deterministic + recorded; legal-hold-aware suspend-don't-delete).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.5 (the retention engine leg).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D6 (set a hold; submit an erase → deferred-by-hold, resumes on lift; 0 held-scope deletions).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **retention engine** (10.5, the retention leg) — **tightest-policy-wins merge** (the most restrictive policy that does not violate a legal-retention floor; deterministic + recorded which input won); **legal-hold-aware suspend-don't-delete** (an active hold suspends retention-expiry + erasure for the held scope; on lift the deferred deletion resumes; expiry uses the §3 erasure mechanisms).
  **Floor named:** GA-D6 runs at M2 scale here; it re-confirms at cell scale at M5 (P-GA-35). The consent/sub-processor/transfer-gate legs of 10.5 are P-GA-23. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.5 (the retention engine + the legal-hold-aware suspend leg; consent/sub-processor/transfer deferred to P-GA-23). Consumed: 10.1 (the holders expiry fans over), 10.4 (the legal-hold gate it backs).
- **GATE / DRILLS.** **GA-D6** (SCHED, M5-confirm) — set a hold; submit an erase → erasure deferred-by-hold (not run), resumes on hold-lift; 0 held-scope deletions. Telemetry: the hold-defer receipt is the green artifact; the tightest-wins decision is recorded (which input won). SCHED.
- **TESTS (required).** Unit: tightest-policy-wins merge picks the most restrictive non-floor-violating policy + records which won; a legal hold suspends an erase (fail-safe-to-suspend, never auto-deletes); a held erase resumes on lift; expiry uses the §3 erasure mechanisms. CDC: provider+consumer pair for 10.5 (a retention/hold caller + the engine). Drill: the GA-D6 scenario on the harness (set a hold → submit erase → assert deferred, 0 held-scope deletions → lift → resumes). Mutation floor: the tightest-wins merge + the legal-hold suspend paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The retention engine (tightest-wins + legal-hold-aware suspend-don't-delete) exists + records which policy won; GA-D6 emits a dated green artifact (0 held-scope deletions); the consent/sub-processor/transfer legs (P-GA-23) + the cell-scale re-confirm (P-GA-35) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: retention engine — tightest-policy-wins merge + legal-hold-aware suspend-don't-delete`; body lists 10.5 (retention leg) implemented, GA-D6 green (0 held-scope deletions) with measured numbers, the consent/transfer legs → P-GA-23. Co-Authored-By trailer.

---

### P-GA-23 — The consent registry + the sub-processor registry + the transfer_allowed gate (deny extra-EU by default)

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (the consent G5 + sub-processor G6 registries + the `transfer_allowed` gate 10.5 — deny extra-EU by default; the future real-LLM backend is one such gated adapter) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M2".
- **DEPENDS-ON.** P-GA-22 (the retention engine the registries sit alongside), P-GA-05 (the consent G5 per-subject DEK holder).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — `transfer_allowed` denies extra-EU by default, observed).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §5.2 (consent G5 per-subject-keyed + withdrawal propagation; sub-processors G6 versioned + region + DPA ref + objection workflow; no PII leaves the EU/EEA by default; `transfer_allowed` denies extra-EU by default).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) row 10.5 (the consent/sub-processor/transfer-gate legs).
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) the **consent registry** (G5, per-subject DEK — versioned, timestamped, granular, withdrawable; withdrawal propagates, may trigger deletion); (2) the **sub-processor registry** (G6, versioned + region + DPA ref + change-notification/objection workflow); (3) the **`transfer_allowed` gate** (deny extra-EU by default; the future real-LLM backend is one such gated, EU-preferring, swappable adapter).
  **Floor named:** the outbound push-mirror gate's POLICY (`transfer_allowed`) ships here; the Git mirror seam it gates is M3/M4 and the gate is PROVEN at M5 (GA-11, P-GA-35). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.5 (the consent + sub-processor registries + the `transfer_allowed` gate). Consumed: 10.1 (the consent G5 holder).
- **GATE / DRILLS.** `transfer_allowed` denies extra-EU by default + admits within-EU; consent withdrawal propagates (+ may trigger deletion); the sub-processor registry is versioned + region-tagged + DPA-ref-bearing. Telemetry: the transfer-gate decision (0 default extra-EU) is the green artifact; the consent-withdrawal propagation is observable. CI for the gate + the registries.
- **TESTS (required).** Unit: consent withdrawal propagates + may trigger deletion; `transfer_allowed` denies extra-EU by default + admits within-EU; the sub-processor registry records region + DPA ref + the objection workflow. CDC: provider+consumer pairs for the consent/sub-processor/transfer legs of 10.5 (a transfer caller + the gate; a consent caller + the registry). Mutation floor: the `transfer_allowed` deny-by-default + the consent-withdrawal-propagation paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The consent + sub-processor registries + the `transfer_allowed` gate exist; `transfer_allowed` denies extra-EU by default + admits within-EU; consent withdrawal propagates; the outbound-mirror gate proof follow-on (P-GA-35) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: consent registry + sub-processor registry + transfer_allowed gate (deny extra-EU by default)`; body lists 10.5 (consent/sub-processor/transfer) implemented, the transfer gate denies extra-EU by default, the outbound-mirror proof → P-GA-35. Co-Authored-By trailer.

---

### P-GA-24 — The per-derivative erasure fan-out: Search purge+reindex (incl. embeddings) + Refs tombstone + reindex-from-source rectification (GA-D2)

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (per-derivative erasure — Search purge+reindex incl. embeddings, Refs tombstone, reindex-from-source rectification 6.4/5.8/2.6 — wired as the orchestrator's per-holder erase calls for the M2 derived stores) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M2".
- **DEPENDS-ON.** P-GA-12 (the orchestrator's per-holder erase calls), the Search M2 prompts that ship 6.4 (purge+reindex incl. embeddings), the Refs M2 prompts that ship 5.8 (tombstone) + 2.6 (reindex-from-source replay), the Notif M2 prompts (the humanised `[erased user]`).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §5.3 (reindex-from-source — rectification fans out via replay, never patched-in-place-and-drift; embeddings re-identify so they are purged not hidden).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.4 (rectification via reindex-from-source; portability), §3.1 H7 (Search purge+reindex incl. embeddings), §3.2 H12/H13 (Refs tombstone; Notif purge).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 6.4 (Search purge+reindex), 5.8 (Refs tombstone), 2.6 (reindex-from-source), 10.1 (the holders the per-derivative erase calls).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows GA-D2 (the subject's docs AND embeddings purged+reindexed out, not hidden; 0 hits, 0 embedding re-identification), REF-D5 (refs tombstone, 0 recoverable, no resolve-500), NOTIF-D6 (inbox humanises to `[erased user]`).
- **DELIVERABLE.** In crate myelin-gdpr-service: the **per-derivative erasure fan-out** — wire these as the orchestrator's per-holder erase calls for the M2 derived stores: Search `purge+reindex` (incl. embeddings — purged-not-hidden, they re-identify); Refs `tombstone` (rebuildable projections); Notif purge → humanised `[erased user]`; **rectification fan-out via reindex-from-source** (never patched-in-place — the derived store rebuilds from source).
  **Floor named:** the `restrict` suppression into these same derived stores (GA-D7) is P-GA-25; the agent-trace H17 seam is P-GA-26. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): the per-derivative erase fan-out leg of 10.1. Consumed/wired: 6.4 (Search purge+reindex), 5.8 (Refs tombstone), 2.6 (reindex-from-source).
- **GATE / DRILLS.** **GA-D2** (SCHED, with Search) — the subject's docs AND embeddings purged+reindexed out (not hidden); 0 hits, 0 embedding re-identification (SRCH-D4 is its Search face). Plus the cross-owner instances: **REF-D5** (refs tombstone, 0 recoverable, no resolve-500), **NOTIF-D6** (inbox humanises to `[erased user]`). Telemetry: the embedding-purge receipt is the green artifact. SCHED.
- **TESTS (required).** Unit: Search erase purges embeddings (not hidden — a re-identification attempt returns 0); Refs erase tombstones (0 recoverable, no resolve-500); Notif humanises mentions to `[erased user]`; rectification fans out via reindex-from-source (the derived store rebuilds, never patched-in-place). CDC: provider+consumer pairs for 6.4/5.8/2.6 (the orchestrator fanning to Search/Refs + the reindex-from-source consumer). Drill: the GA-D2 + REF-D5 + NOTIF-D6 scenarios on the harness. Mutation floor: the purge-not-hide (embeddings) + the reindex-from-source paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** Per-derivative erasure (Search purge+reindex incl. embeddings, Refs tombstone, Notif humanise, reindex-from-source rectification) works as the orchestrator's per-holder erase calls; GA-D2 (0 embedding re-identification) + REF-D5 + NOTIF-D6 emit dated green artifacts; the `restrict`-into-derived proof (P-GA-25) + the agent-trace seam (P-GA-26) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: per-derivative erasure fan-out — Search purge+reindex (incl. embeddings) + Refs tombstone + reindex-from-source`; body lists 6.4/5.8/2.6 wired, GA-D2 + REF-D5 + NOTIF-D6 green with measured numbers, the restrict-into-derived → P-GA-25 and agent-trace seam → P-GA-26, the mutation score. Co-Authored-By trailer.

---

### P-GA-25 — restrict suppression into the derived stores (Search/Refs/Notif/Agents/OLAP) — GA-D7

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (`restrict` suppression into Search/Refs/Notif/Agents/OLAP 11.6 GA-9 — the flag built in P-GA-17 is now honoured by the derived stores; 0 processing of a restricted subject) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M2".
- **DEPENDS-ON.** P-GA-17 (the `restrict` suppression flag the derived stores now honour), P-GA-24 (the per-derivative fan-out the restriction rides), the OLAP read model (11.6), the Search/Refs/Notif/Agent M2 prompts that honour the flag.
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — 0 processing of a restricted subject, observed).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.4 (`restrict` — no indexing/agent-use/analytics(incl. OLAP)/notification; reversible).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 11.6 (OLAP honours the restriction flag), 10.1 (the holders that honour `restrict`).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D7 (restrict → no indexing/agent-use/analytics/notification while storage retained; reversible; 0 processing of a restricted subject).
- **DELIVERABLE.** In crate myelin-gdpr-service: **`restrict` suppression into the derived stores** (11.6, GA-9) — the flag built in P-GA-17 is now honoured by **Search (no indexing), Refs, Notif, Agents, and OLAP (no analytics)** — 0 processing of a restricted subject, reversible. Wire the suppression as the orchestrator's `restrict` fan-out into the M2 derived stores.
  **Floor named:** the full multi-cell restriction is M5 (rides P-GA-32/P-GA-33); restriction honoured by the M2-existing derived stores ships here. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): the `restrict`-fan-out leg of 10.1 into the derived stores. Consumed: 11.6 (OLAP honours `restrict`).
- **GATE / DRILLS.** **GA-D7** (CI) — restrict a subject → no indexing/agent-use/analytics/notification while storage retained; reversible. **0 processing of a restricted subject.** Telemetry: the restriction-suppression proof is the green artifact (0 processing observed across Search/Refs/Notif/Agents/OLAP). CI.
- **TESTS (required).** Unit: `restrict` suppresses indexing/agent-use/analytics/notification across the derived stores (reversible); OLAP honours the restriction flag (no analytics for a restricted subject); un-restricting reverses the suppression. CDC: provider+consumer pair for 11.6 (the OLAP consumer honouring `restrict`) + the Search/Refs/Notif/Agent restriction consumers. Drill: the GA-D7 scenario on the harness (restrict → assert 0 processing across all five derived stores → un-restrict → restored). Mutation floor: the restriction-suppression-across-derived-stores path is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** `restrict` is honoured by Search/Refs/Notif/Agents/OLAP (0 processing of a restricted subject, reversible); GA-D7 emits a dated green artifact; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: restrict suppression into the derived stores (Search/Refs/Notif/Agents/OLAP)`; body lists 11.6 wired + the 10.1 restrict fan-out, GA-D7 green (0 processing) with measured numbers. Co-Authored-By trailer.

---

### P-GA-26 — eDiscovery export + the agent-trace holder seam (8.8) + the history-rewrite resumable-activity skeleton

- **BAND.** M2.
- **ROADMAP MILESTONE.** GA-M2 (eDiscovery export 10.7 — content-addressed + Merkle-proof-bearing + legal-hold-frozen; the agent-trace holder seam 8.8 distinct from the audit log; the history-rewrite resumable-activity skeleton) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M2".
- **DEPENDS-ON.** P-GA-20 (eDiscovery rides the audit Merkle substrate), P-GA-22 (the legal-hold-frozen export), the Agent fabric M2 prompts (8.8 the agent-trace seam).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — an export carries a verifiable inclusion proof, not an asserted completeness).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §5.4 (eDiscovery export — content-addressed + Merkle-proof-bearing + legal-hold-frozen), §5.3 (the outbound push-mirror residency gate — the policy ships in P-GA-23; the history-rewrite skeleton is wired here), §3.2 H17 (the agent-trace seam distinct from the audit log).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.7 (eDiscovery export), 8.8 (the agent-trace holder seam, AG-7), 10.6 (the Merkle tree the export proofs ride).
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) **eDiscovery export** (10.7) over the M1/M2 records — content-addressed, inclusion-proof-bearing (against the P-GA-20 Merkle tree), legal-hold-frozen. (2) The **agent-trace holder seam** (8.8, H17) registered as a holder **distinct from the audit log** (trace = reasoning record, erasable; audit = the complete who-did-what); the impl lands with Knowledge in M3 (P-GA-27) — the seam + the distinct-from-audit boundary are wired here. (3) **History-rewrite as a resumable `myelin-flow` activity skeleton** (the op body; the invalidation fan-out surface is M5 P-GA-35 when Git's cache namespaces exist) — named.
  **Floor named:** the agent-trace H17 impl is M3 (P-GA-27); the history-rewrite first-class audited op + invalidation fan-out is M5 (P-GA-35, GA-10). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.7 (eDiscovery export). Consumed/wired: 8.8 (the agent-trace seam), 10.6 (the Merkle tree the eDiscovery proofs ride).
- **GATE / DRILLS.** eDiscovery export carries an inclusion proof against the Merkle tree + is legal-hold-frozen; the agent-trace H17 seam is distinct from the audit log (an architecture test); the history-rewrite skeleton is a resumable activity (the body fires in M5). Telemetry: the export's inclusion proof verifies; the distinct-from-audit boundary holds. CI.
- **TESTS (required).** Unit: eDiscovery export carries an inclusion proof against the Merkle tree + is legal-hold-frozen; the agent-trace H17 seam is distinct from the audit log (an architecture test that trace ≠ audit); the history-rewrite skeleton is a resumable idempotent activity (the invalidation fan-out is deferred). CDC: provider+consumer pair for 10.7 (a legal/auditor consuming the export) + 8.8 (the agent-trace seam). Mutation floor: the export-inclusion-proof + the trace-distinct-from-audit paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** eDiscovery export carries Merkle inclusion proofs + is legal-hold-frozen; the agent-trace H17 seam is wired distinct from the audit log; the history-rewrite resumable-activity skeleton exists; the H17 impl (P-GA-27) + the history-rewrite first-class op (P-GA-35) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M2: eDiscovery export + agent-trace holder seam (8.8) + history-rewrite resumable-activity skeleton`; body lists 10.7 implemented + 8.8 wired, the history-rewrite skeleton, the H17 impl → P-GA-27 and the first-class op → P-GA-35. Co-Authored-By trailer.

---

### P-GA-27 — The producer-subsystem holders (Git H1 / Knowledge H4 / agent-trace H17) register + the DSR fan-out reaches them + the Knowledge instance

- **BAND.** M3.
- **ROADMAP MILESTONE.** GA-M3 (the producer-subsystem holders — H1 Git + H4 Knowledge + H17 agent-trace register into the data map; the DSR fan-out reaches them; the Knowledge instance of the ONE posture by reference; KN-D4/KN-D12 GDPR-anchored) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M3".
- **DEPENDS-ON.** P-GA-09 (the data map the new holders register into), P-GA-12 (the orchestrator fan-out that now reaches them), P-GA-26 (the agent-trace H17 seam), the Git M3 prompts (the H1 holder + the LFS blobs), the Knowledge M3 prompts (the H4 holder + the agent-trace H17 holder impl), the Storage per-subject DEK crypto-shred (11.4).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (erasure-vs-immutability — the producer holders' free-text crypto-shreds via per-subject DEK; the residual is the documented limit).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.2 H1 (Git) / H4 (Knowledge) / H17 (agent trace, distinct from audit), §7.4 (the Knowledge instance: free-text blocks + db-row values crypto-shred via per-subject DEK; the agent-trace holder distinct from the audit log).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the H1/H4/H17 holders), 8.8 (the agent-trace holder), 4.8 (the pseudonym lever), 10.3 (the data map the new holders register into), 11.4 (per-subject DEK).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows KN-D4/KN-D12 (erase → free-text per-subject-DEK shredded incl. backups, embeddings purged, agent traces shredded, attribution → pseudonym; 0 recoverable incl. vectors).
- **DELIVERABLE.** In crate myelin-gdpr-service (the orchestration; the subsystems own their `erase` impls): (1) **register H1 (Git) + H4 (Knowledge) + H17 (agent-trace)** into the data map (the auto-registration hook + the derive-tagged PII fields; the data-map diff in CI surfaces the new fields). The DSR fan-out now reaches them. (2) Wire the orchestrator's fan-out to include H1/H4/H17 in the checklist (the data-map drives it — these appear because they registered). (3) **The Knowledge instance** of the ONE posture (10.9 §7.4): free-text blocks + db-row values crypto-shred via per-subject DEK; the agent-trace holder (H17) is distinct from the audit log (KN-D12).
  **Floor named:** the Git pseudonymous-commit X-7 instance + GIT-D2 is P-GA-28 (the Git data model freeze rides the P-GA-18 prerequisite). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): 10.1 (the fan-out over H1/H4/H17 — the impls are Git/KN/Agent's), 10.9 (the Knowledge by-reference instantiation, confirmed-not-restated). Consumed: 8.8 (the agent-trace holder), 4.8 (the pseudonym lever), 11.4 (per-subject DEK), 10.3 (the data map the new holders register into).
- **GATE / DRILLS.** **KN-D4 / KN-D12** (SCHED, GDPR-anchored) — erase → structured PII purged/pseudonymised, free-text per-subject-DEK crypto-shredded (unrecoverable in op-log/snapshots/backups), embeddings purged, agent traces crypto-shredded, attribution → pseudonym. **0 recoverable incl. vectors; residual per 10.9.** Telemetry: the DSR receipt set + the ledger entry + the key-shred count are the green artifacts; the data-map diff surfaces the new holders (no holder-without-map drift). SCHED.
- **TESTS (required).** Unit: H1/H4/H17 appear in the generated data map after registration (no holder-without-map drift); the fan-out checklist includes H1/H4/H17 because the data map drives it; the agent-trace H17 is distinct from the audit log; the Knowledge instance references the platform posture (no restatement). CDC: provider+consumer pair for 10.1 (the orchestrator fanning to the Git/KN/Agent holders + those holders responding). Drill: the KN-D4/KN-D12 scenarios on the harness (erase → assert 0 recoverable incl. vectors, residual == the ONE posture). Mutation floor: the fan-out-checklist-includes-new-holders path is mandatory-core — state and meet the mutation-score floor (the subsystems' erase-impl floors are owned by Git/KN/Agent).
- **DEFINITION OF DONE.** H1/H4/H17 register into the data map (the diff surfaces them); the DSR fan-out reaches them; the Knowledge instance crypto-shreds free-text + purges embeddings + keeps H17 distinct from audit; KN-D4/KN-D12 emit dated green artifacts (0 recoverable incl. vectors); the Git X-7 instance follow-on (P-GA-28) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M3: producer holders (Git H1/KN H4/agent-trace H17) register + DSR fan-out + Knowledge instance`; body lists 10.1 (H1/H4/H17 fan-out) + 10.9 (KN by reference) implemented, KN-D4/KN-D12 green (0 recoverable incl. vectors) with measured numbers, the Git X-7 instance → P-GA-28. Co-Authored-By trailer.

---

### P-GA-28 — The Git pseudonymous-commit instance of X-7 (10.9 by reference) + GIT-D2

- **BAND.** M3.
- **ROADMAP MILESTONE.** GA-M3 (the Git pseudonymous-commit instance of X-7 — the Git instance of the ONE posture by reference; commits pseudonymous-by-default; GIT-D2's residual == the ONE platform-posture residual; GIT-D2 GDPR-anchored) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M3".
- **DEPENDS-ON.** P-GA-16 (the ONE posture Git references), P-GA-18 (the pseudonymous-commit prerequisite Git consumes), P-GA-27 (H1 registered + the fan-out reaches Git), the Git M3 prompts (pseudonymous commits + the LFS blobs).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (the Git instance: pseudonymous-by-default commits so the immutable hash never bakes erasable PII; the residual is the documented limit).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §7.4 (instantiation per subsystem BY REFERENCE — the Git instance: self-authored content crypto-shreds via per-subject DEK; identity via pseudonym-map shred; commits pseudonymous-by-default; the third-party/immutable residual is the documented limit + `restrict`; GDPR confirms the reference is correct, no restatement; GIT-D2's residual == the ONE platform-posture residual).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.9 (the ONE posture instantiated by reference), 4.8 (the pseudonym lever), 11.4 (per-subject DEK).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GIT-D2 (erase a commit/PR/comment author + LFS → every holder hit; residual == the ONE platform-posture residual; crypto-shred reaches backups).
- **DELIVERABLE.** In crate myelin-gdpr-service (the by-reference instantiation; Git owns the `erase` impl): **The Git instance of the ONE posture** (10.9 §7.4): Git's erasure section **references** the platform posture (P-GA-16) — confirm the reference is correct (no restatement); confirm Git's commits are **pseudonymous-by-default** (the M1 prerequisite from P-GA-18 consumed — the immutable hash holds only `<pseudonym>@<tenant>.noreply`); confirm GIT-D2's residual == the ONE platform-posture residual. The P-GA-18 architecture test now FIRES (Git commits hold only the pseudonym form).
  **Floor named:** the audited history-rewrite erasure path (the rare body-expunge case) is M5 (P-GA-35, GA-10). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): 10.9 (the Git by-reference instantiation, confirmed-not-restated). Consumed: 4.8 (the pseudonym lever), 11.4 (per-subject DEK), 10.1 (the Git H1 fan-out).
- **GATE / DRILLS.** **GIT-D2** (SCHED, GDPR-anchored) — erase a subject who authored commits/PRs/comments + LFS → every holder hit; **residual == the ONE platform-posture residual (10.9)**; crypto-shred reaches backups (the X-7 Git instance proven). Telemetry: the DSR receipt set + the key-shred count are the green artifacts; the P-GA-18 architecture test (commits hold only the pseudonym) now passes. SCHED.
- **TESTS (required).** Unit: the Git erasure section references the platform posture (an architecture test that it does not restate it); Git commits hold only the pseudonym (no real-identity bytes baked in — the P-GA-18 architecture test fires green here); GIT-D2's residual == the ONE platform-posture residual. CDC: provider+consumer pair for 10.9 (the Git instance referencing the posture + the posture as owned — completing the P-GA-16/P-GA-18 stubs). Drill: the GIT-D2 scenario on the harness (erase an author → assert 0 recoverable, residual == the ONE posture, crypto-shred reaches backups). Mutation floor: the pseudonym-form-only-in-commit-bytes check is mandatory-core — state and meet the mutation-score floor (Git's erase-impl floor is owned by Git).
- **DEFINITION OF DONE.** The Git instance of the ONE posture references-not-restates the platform posture; Git commits are pseudonymous-by-default (the P-GA-18 architecture test fires green); GIT-D2's residual == the ONE platform-posture residual; GIT-D2 emits a dated green artifact (0 recoverable, crypto-shred reaches backups); the history-rewrite follow-on (P-GA-35) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M3: the Git pseudonymous-commit instance of X-7 (10.9 by reference)`; body lists 10.9 (Git by reference) implemented, GIT-D2 green (0 recoverable, residual == the ONE posture, crypto-shred reaches backups) with measured numbers, the history-rewrite op → P-GA-35. Co-Authored-By trailer.

---

### P-GA-29 — The CI consumer holder (H2) + the per-subject CI-log DEK crypto-shred reach (CI-D3)

- **BAND.** M4.
- **ROADMAP MILESTONE.** GA-M4 (the CI consumer holder H2 + the per-subject DEK reaching isolable CI log-segment PII — the P5 granularity extension; the CI instance of the ONE posture by reference) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M4".
- **DEPENDS-ON.** P-GA-28 (the by-reference posture pattern), P-GA-24 (the per-derivative erasure fan-out the CI logs ride), the CI M4 prompts (the H2 holder + the T3 log tier + the per-subject CI-log DEK), the Storage prompts that ship 11.4 (the per-subject CI-log DEK extension, C1) + 11.8 (the `(job, step, byte-range)` index).
- **CANON DOCS.**
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.2 H2 [P5] (CI subsystem DB + log segments — run actors (pseudonym), log refs, inline free-text PII in log lines; erasure = pseudonymise + per-subject DEK crypto-shred of isolable log-segment PII (was per-tenant floor) + short-TTL log retention), §7.1 (the structural floor extends to CI log segments — per-subject DEK), §7.4 (the CI instance of the ONE posture, by reference).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the H2 holder), 11.4 (the per-subject CI-log DEK — the C1/P5 extension), 11.8 (the T3 log tier `(job, step, byte-range)` index), 10.9 (the CI instance by reference).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row CI-D3 (erase fans to CI → PII in logs/artifacts/caches/run-state destroyed (per-subject DEK where isolable, per-tenant fallback) incl. backups; structure survives; 0 dangling leak).
- **DELIVERABLE.** In crate myelin-gdpr-service (the orchestration; CI owns the `erase` impl): (1) **register H2 (CI + log segments)** into the data map (CI's PII fields tagged — run actors as pseudonyms, log refs, inline free-text PII in log lines; the data-map diff surfaces them). (2) Wire the orchestrator's fan-out to include H2 with the **per-subject DEK crypto-shred reaching isolable CI log-segment PII** (the P5 extension — was a per-tenant floor): CI's `erase` (CI-D3) destroys PII in logs/artifacts/caches/run-state **per-subject where isolable, per-tenant fallback**, incl. backups; structure survives (the run-graph remains, the PII is shredded). (3) **The CI instance of the ONE posture** (10.9 §7.4) — references the platform posture (no restatement). The `(job, step, byte-range)` index (11.8) lets the erase locate the isolable segments.
  **Floor named:** per-subject is the target where isolable; per-tenant is the fallback for non-isolable inline PII — named (the honest split). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): 10.1 (the fan-out over H2 — the impl is CI's), 10.9 (the CI instance by reference). Consumed: 11.4 (the per-subject CI-log DEK, C1/P5), 11.8 (the log-segment index), 10.3 (the data map).
- **GATE / DRILLS.** **CI-D3** (SCHED, GDPR-anchored) — erase fans to CI → PII in logs/artifacts/caches/run-state destroyed (per-subject DEK where isolable, per-tenant fallback) incl. backups; structure survives. **0 dangling leak.** Telemetry: the DSR receipt + the per-subject-DEK key-shred count are the green artifacts; the data-map diff surfaces H2 (no drift). SCHED.
- **TESTS (required).** Unit: H2 appears in the data map after registration; the fan-out includes H2; CI erase destroys isolable log-segment PII via per-subject DEK (per-tenant fallback for non-isolable); structure (the run graph) survives an erase; the CI instance references the platform posture (no restatement). CDC: provider+consumer pair for 10.1 (the orchestrator fanning to the CI holder + CI responding). Drill: the CI-D3 scenario on the harness (erase → assert 0 dangling leak across logs/artifacts/caches/run-state/backups). Mutation floor: the per-subject-where-isolable / per-tenant-fallback selection path is mandatory-core — state and meet the mutation-score floor (CI's erase-impl floor is owned by CI).
- **DEFINITION OF DONE.** H2 (CI + log segments) registers into the data map; the fan-out reaches it with the per-subject CI-log DEK crypto-shred (per-tenant fallback named); structure survives an erase; the CI instance references-not-restates the platform posture; CI-D3 emits a dated green artifact (0 dangling leak incl. backups); the per-subject/per-tenant-fallback split is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: CI holder (H2) + per-subject CI-log DEK crypto-shred reach`; body lists 10.1 (H2 fan-out) + 10.9 (CI by reference) implemented, CI-D3 green (0 dangling leak) with measured numbers, the per-subject/per-tenant-fallback split named. Co-Authored-By trailer.

---

### P-GA-30 — The Issues (H3) + Chat (H5) consumer holders register + the DSR fan-out reaches them + the instances (ISS-D11 / CHAT-D8)

- **BAND.** M4.
- **ROADMAP MILESTONE.** GA-M4 (the Issues H3 + Chat H5 holders register into the data map; the DSR fan-out reaches them; the Issues/Chat instances of the ONE posture by reference; ISS-D11 + CHAT-D8 GDPR-anchored) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M4".
- **DEPENDS-ON.** P-GA-29 (the consumer-holder pattern + the per-derivative fan-out), P-GA-24 (the per-derivative erasure the Issues/Chat derivatives ride), the Issues M4 prompts (the H3 holder), the Chat M4 prompts (the H5 holder).
- **CANON DOCS.**
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §3.2 H3 (Issues, incl. worklog restricted) / H5 (Chat), §7.4 (the Issues/Chat instances by reference).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the H3/H5 holders), 10.9 (the Issues/Chat instances by reference), 11.6 (OLAP honours restriction).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows ISS-D11 (erase → PII gone from issue row (per-subject DEK), change-log, comments, attachments, OLAP (+restriction), Search incl. embeddings, Refs; post-restore re-erasure; third-party residual is the `[OPEN — LEGAL]` limit), CHAT-D8 (erase → bodies crypto-shred in hot+cold segments+backups; mentions → `[erased user]`; read-state/drafts/unfurl-cache purged; Search/Refs/Notif cascade; 0 recoverable PII).
- **DELIVERABLE.** In crate myelin-gdpr-service (the orchestration; Issues/Chat own their `erase` impls): (1) **register H3 (Issues) + H5 (Chat)** into the data map; the fan-out now reaches them. (2) Wire the orchestrator's fan-out to include H3/H5 with the per-derivative erasure cascades (issue row per-subject DEK + change-log + comments + attachments + OLAP + Search incl. embeddings + Refs for Issues; chat bodies crypto-shred in hot+cold segments+backups + mentions → `[erased user]` + read-state/drafts/unfurl-cache purged + Search/Refs/Notif cascade for Chat). (3) **The Issues/Chat instances of the ONE posture** (10.9 §7.4) — each references it, no restatement.
  **Floor named:** the worklog/productivity Behavioural classification (OQ-H) + the works-council trigger + the SpecialCategory→DPIA route is P-GA-31. After P-GA-31 **all H1–H18 holders exist** — the precondition for GA-D1 (M5, P-GA-32). Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (orchestration): 10.1 (the fan-out over H3/H5 — the impls are Issues/Chat's), 10.9 (the Issues/Chat instances by reference). Consumed: 11.6 (OLAP honours restriction), 10.3 (the data map).
- **GATE / DRILLS.** **ISS-D11** (SCHED, GDPR-anchored) — erase → PII gone from issue row (per-subject DEK), change-log, comments, attachments, OLAP (+restriction), Search (incl. embeddings), Refs; post-restore re-erasure catches a restore; third-party residual is the `[OPEN — LEGAL]` limit. **CHAT-D8** (SCHED, GDPR-anchored) — erase → bodies crypto-shred in hot+cold segments+backups; mentions → `[erased user]`; read-state/drafts/unfurl-cache purged; Search/Refs/Notif cascade. **0 recoverable PII.** Telemetry: the holder receipts + the re-erasure receipt are the green artifacts. SCHED.
- **TESTS (required).** Unit: H3/H5 appear in the data map after registration; the fan-out reaches them; the per-derivative cascades fire (Issues: issue row + change-log + comments + attachments + OLAP + Search + Refs; Chat: hot+cold bodies + mentions + read-state/drafts/unfurl-cache + Search/Refs/Notif); the Issues/Chat instances reference-not-restate the platform posture. CDC: provider+consumer pair for 10.1 (the orchestrator fanning to Issues/Chat holders + responses). Drill: the ISS-D11 + CHAT-D8 scenarios on the harness (erase → assert 0 recoverable PII across all listed holders + the re-erasure leg). Mutation floor: the Issues/Chat per-derivative cascade-completeness path is mandatory-core — state and meet the mutation-score floor (Issues/Chat erase-impl floors are owned by them).
- **DEFINITION OF DONE.** H3 + H5 register into the data map; the fan-out reaches them with the full per-derivative cascades; the Issues/Chat instances reference-not-restate the posture; ISS-D11 + CHAT-D8 emit dated green artifacts (0 recoverable PII); the worklog classification follow-on (P-GA-31) is named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: Issues (H3) + Chat (H5) holders register + DSR fan-out + the instances`; body lists 10.1 (H3/H5 fan-out) + 10.9 (Issues/Chat by reference) implemented, ISS-D11 + CHAT-D8 green (0 recoverable PII) with measured numbers, the worklog classification → P-GA-31. Co-Authored-By trailer.

---

### P-GA-31 — The worklog/productivity/estimate Behavioural classification (OQ-H) + the works-council consultation trigger + the SpecialCategory→DPIA route

- **BAND.** M4.
- **ROADMAP MILESTONE.** GA-M4 (the worklog/productivity/estimate classification OQ-H — `category=Behavioural, role=TenantContent, restricted-by-default`; per-individual rollups OFF by default + the works-council consultation trigger; the SpecialCategory→DPIA route; the 10.2 worklog tag extension) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M4".
- **DEPENDS-ON.** P-GA-30 (the Issues H3 holder the worklog fields hang off), P-GA-07 (the derive the worklog tag extends), P-GA-08 (the SpecialCategory→DPIA marker the worklog route reuses), P-GA-25 (restriction-into-analytics + OLAP the worklog rides), the Issues M4 prompts (the worklog fields).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (name-your-floors — the `[OPEN — LEGAL]` worklog classification ships the structural floor (restricted-by-default tags) and flags the residual to counsel; the platform surfaces the works-council trigger, it does not adjudicate), §8 (the human sign-off is the bottleneck — a works-council/labour-law call is decision-shaped, surfaced not auto-decided).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.4 (worklog/productivity/estimate sensitivity — tagged `category=Behavioural, role=TenantContent, basis=TBD_LEGAL, erasure=CryptoShred(subject_dek), data_role_default=Restricted`; excluded from cross-individual analytics/agent-use for a restricted subject by default; per-individual rollups OFF by default behind explicit tenant-admin enablement that flags the works-council consultation trigger; same per-subject DEK crypto-shred; the SpecialCategory tag routes into the DPIA gate; build-data-as-LLM-training foreclosed by default).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.2 (the worklog/behavioural tags, the OQ-H extension), 11.6 (OLAP honours restriction).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) OQ-H (the worklog classification + the works-council trigger, `[OPEN — LEGAL]`).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row ISS-D11 (the worklog leg — restricted-by-default worklog field excluded from cross-individual analytics; GA-D7 face).
- **DELIVERABLE.** In crate myelin-gdpr (the worklog tag extension) + myelin-gdpr-service: (1) The **worklog/productivity/estimate classification** (OQ-H, the 10.2 extension): tag these fields `category=Behavioural, role=TenantContent, basis=TBD_LEGAL, erasure=CryptoShred(subject_dek), data_role_default=Restricted` (restricted-by-default in cross-individual processing). The worklog carries the same per-subject DEK crypto-shred as other free-text PII. (2) **Per-individual productivity rollups OFF by default**, gated behind an explicit tenant-admin enablement that **surfaces the works-council consultation trigger** (the platform surfaces it; it does not adjudicate). (3) Route a `SpecialCategory`-flagged worklog field into the DPIA gate (P-GA-08). (4) Confirm **build-data-as-LLM-training stays foreclosed-by-default** (no code path feeds tenant content to training — an architecture test).
  **Floor named:** the worklog basis is `TBD_LEGAL` — the structural floor (restricted-by-default tags + per-subject DEK + rollups-off-by-default + the surfaced consultation trigger) ships on the engineering clock; the special-category/works-council ratification is parallel-legal (`[OPEN — LEGAL]`). After this prompt **all H1–H18 holders exist** — the GA-D1 precondition (P-GA-32). Record in writing, dated.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.2 (the worklog/Behavioural tags + the SpecialCategory→DPIA route). Consumed: 11.6 (OLAP honours restriction).
- **GATE / DRILLS.** A restricted-by-default worklog field is excluded from cross-individual analytics (GA-D7 face); a rollup-enablement surfaces the works-council trigger; a SpecialCategory worklog field routes into the DPIA gate. Telemetry: the worklog restriction is observable (0 cross-individual analytics for a restricted subject); the works-council trigger is a surfaced signal, not an auto-decision. CI for the tags + the restriction; SCHED for the worklog leg of ISS-D11.
- **TESTS (required).** Unit: worklog fields carry the Behavioural/restricted-by-default tags + the per-subject-DEK erasure tag; per-individual rollups are OFF by default; enabling a rollup surfaces the works-council consultation trigger (a signal, not an auto-decision); a SpecialCategory worklog field routes into the DPIA gate; build-data-as-LLM-training has no code path feeding tenant content to training (an architecture test). CDC: provider+consumer pair for 10.2 (the worklog-tag consumer). Mutation floor: the restricted-by-default + the rollup-off-by-default + the works-council-trigger-surfacing paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The worklog/productivity classification ships (Behavioural + restricted-by-default + per-subject DEK + rollups-off-by-default + the surfaced works-council trigger + the SpecialCategory→DPIA route); build-data-as-LLM-training stays foreclosed; a restricted worklog field is excluded from cross-individual analytics (GA-D7 face green); all H1–H18 holders now exist (the GA-D1 precondition, named for P-GA-32); the `TBD_LEGAL` worklog basis is named with its parallel-legal follow-on; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M4: worklog Behavioural classification (OQ-H) + works-council trigger + SpecialCategory→DPIA route`; body lists 10.2 (worklog tags) implemented, the restricted-by-default + rollups-off + works-council trigger, all H1–H18 now exist → P-GA-32, the `TBD_LEGAL` worklog residual named, the mutation score. Co-Authored-By trailer.

---

### P-GA-32 — The full H1–H18 DSR fan-out (GA-D1, 0 holders missed) + STOR-D3 at cell scale

- **BAND.** M5.
- **ROADMAP MILESTONE.** GA-M5 (the full DSR/erasure fan-out across all H1–H18 — GA-D1 0 holders missed; the M1/M3/M4 floor drills re-run at cell scale) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M5".
- **DEPENDS-ON.** P-GA-31 (all H1–H18 holders now exist — the GA-D1 precondition), P-GA-14 (the single-cell fan-out this completes), P-GA-15 (the erasure ledger + post-restore re-erasure, now at cell scale), P-GA-20 (the audit certificate seal), the Storage M5 prompts (restore-verify at cell scale).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (prove-it — GA-D1 is a property of the generated data map: 0 holders missed; observability `erasure_fanout_coverage = 100%` is the pass).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 (the data-map-driven fan-out — now complete because every holder exists), §9.1 (the audit Merkle tree is per-tenant; offboarding shreds one tenant's tree).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.1 (the full-fan-out completeness), 10.8 (the erasure ledger at cell scale), 10.3 (the now-complete data map).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D1 (erase a subject seeded into all H1–H18 → fan-out hit every holder; post-erase `locate` = 0 recoverable PII; 0 holders missed), STOR-D3 (restore-resurrects-nothing at cell scale).
- **DELIVERABLE.** In crate myelin-gdpr-service: **the full H1–H18 DSR fan-out** — every holder now exists (P-GA-05 + P-GA-06 + P-GA-27 + P-GA-28 + P-GA-29 + P-GA-30 + P-GA-31), so the data-map-driven fan-out is COMPLETE; the `[OPEN — LEGAL]` posture references are all now resolved. Prove **GA-D1 at cell scale** (the headline GDPR drill): a single `dsr_submit` reaches every holder, erases reliably (per-subject DEK destroyed → unrecoverable in DBs AND backups, embeddings purged-not-hidden, git author pseudonymous), survives a restore (post-restore re-erasure from the ledger), and seals a Merkle-proven certificate. Re-run STOR-D3 at cell scale (restore-resurrects-nothing under world-scale load).
  **Floor named:** multi-cell `member_cells` fan-out is P-GA-33 (GA-D8); the E2E-4 DSAR flagship is P-GA-34. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: the full-fan-out completeness of 10.1. Consumed: 10.8 (the ledger at cell scale), 10.6 (the certificate seal), 10.3 (the now-complete data map).
- **GATE / DRILLS.** **GA-D1** (SCHED) — erase a subject seeded into all H1–H18 → data-map fan-out hit every holder; post-erase `locate` returns 0 recoverable PII. **`erasure_fanout_coverage = 100%`; 0 holders missed.** Plus **STOR-D3 at cell scale** (restore-resurrects-nothing under world-scale load). Telemetry: `erasure_fanout_coverage = 100%`; the Merkle cert is the green artifact. SCHED (whole-cell drill). This is part of the master M5 → M6 GDPR exit.
- **TESTS (required).** Unit: the fan-out checklist enumerates all H1–H18 (driven by the now-complete data map — 0 holders absent). CDC: reuse the 10.1 fan-out CDC pair across the full holder set. Drill: the GA-D1 scenario on the harness against a full cell with mock agents (seed a subject into all H1–H18 across all five subsystems → single `dsr_submit` → assert 0 holders missed, 0 recoverable incl. vectors incl. backups, certificate sealed) + STOR-D3 at cell scale. Mutation floor: the full-fan-out enumeration is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The full H1–H18 DSR fan-out is complete (the data map drives it, 0 holders absent); GA-D1 (0 holders missed, `erasure_fanout_coverage = 100%`) + STOR-D3 at cell scale emit dated green artifacts; the multi-cell follow-on (P-GA-33) + the E2E-4 flagship (P-GA-34) are named in writing; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: full H1–H18 DSR fan-out (GA-D1, 0 holders missed) + STOR-D3 at cell scale`; body lists the full 10.1 fan-out implemented, GA-D1 (0 holders missed) + STOR-D3 cell-scale green with measured numbers, the multi-cell → P-GA-33 and E2E-4 → P-GA-34, the mutation score. Co-Authored-By trailer.

---

### P-GA-33 — Multi-cell DSR fan-out (the member_cells iteration over the cross-cell PII-free bridge, GA-D8)

- **BAND.** M5.
- **ROADMAP MILESTONE.** GA-M5 (the multi-cell floor follow-on — the orchestrator iterates `member_cells ∪ home_cell` over the cross-cell PII-free bridge; per-cell PII-free receipts merged into one certificate; GA-D8 0 cells missed) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M5".
- **DEPENDS-ON.** P-GA-32 (the complete single-cell H1–H18 fan-out this extends to multi-cell), P-GA-13 (the single-cell tenant-operability/offboarding this generalizes), the multi-cell Tenancy M5 prompts (the cross-cell PII-free `CrossCellPointer` bridge live + the control-plane wave-sequencing 12.3/12.6).
- **CANON DOCS.**
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.3 (multi-cell fan-out over the frozen OQ-I bridge — the orchestrator iterates `member_cells ∪ home_cell` (all same-region); the cross-cell carrier is the PII-free `CrossCellPointer{subject, type, correlation_id, home_cell}`; resolution is ALWAYS cell-local — a cell never reads another cell's PII, each erases its own holders + returns a PII-free receipt, merged into one certificate; the cross-cell ordering/atomicity remains the control-plane floor).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the multi-cell `member_cells` iteration), 12.3/12.6 (`member_cells` placement + the cross-cell PII-free bridge).
  - [`../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md`](../../05-refined-shared-systems-architecture/00-reconciliation-decisions.md) OQ-I (the cross-cell bridge frame).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-D8 (multi-cell erasure: fan-out iterates all `member_cells ∪ home_cell`; complete per-cell receipt set; 0 cells missed).
- **DELIVERABLE.** In crate myelin-gdpr-service: **multi-cell fan-out** (the named floor follow-on, OQ-I): the orchestrator **iterates `member_cells ∪ home_cell`** over the now-live cross-cell PII-free pointer bridge (`CrossCellPointer`); resolution is **cell-local** (each cell erases its own holders + returns a **PII-free receipt**, merged into ONE certificate). The cross-cell ordering/atomicity remains the named control-plane floor (the control plane sequences the wave; GDPR runs in each cell) — named, not solved here.
  **Floor named:** the cross-cell ordering/atomicity (a globally-atomic multi-cell erase vs the resumable-per-cell checklist) remains the control-plane floor even at M5 — named, owner = the control plane / multi-cell tenancy. The E2E-4 flagship is P-GA-34. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.4 (the multi-cell `member_cells` iteration, completing the P-GA-14 single-cell floor). Consumed: 12.3/12.6 (the cross-cell PII-free bridge + `member_cells`).
- **GATE / DRILLS.** **GA-D8** (SCHED, FLOOR) — multi-cell erasure: fan-out iterates all `member_cells ∪ home_cell`; merged a complete per-cell receipt set. **0 cells missed.** Telemetry: the per-cell receipt set is the green artifact. SCHED (multi-cell drill).
- **TESTS (required).** Unit: multi-cell fan-out iterates `member_cells ∪ home_cell`; cross-cell resolution is cell-local (a cell never reads another cell's PII — an architecture test); per-cell receipts merge into one certificate; the cross-cell carrier is PII-free (`CrossCellPointer` schema has no PII field). CDC: provider+consumer pair for 10.4 (the multi-cell orchestrator + the cross-cell bridge). Drill: the GA-D8 scenario on the harness (seed a subject across `member_cells ∪ home_cell` → single `dsr_submit` → assert 0 cells missed, per-cell receipts merge). Mutation floor: the multi-cell iteration + the per-cell-receipt merge are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** Multi-cell fan-out iterates `member_cells ∪ home_cell` with cell-local PII-free resolution + per-cell receipt merge; GA-D8 (0 cells missed) emits a dated green artifact; the cross-cell ordering/atomicity control-plane floor is named in writing; the E2E-4 flagship (P-GA-34) is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: multi-cell DSR fan-out (member_cells iteration over the cross-cell PII-free bridge, GA-D8)`; body lists 10.4 (multi-cell) implemented, GA-D8 (0 cells missed) green with measured numbers, the cross-cell ordering/atomicity control-plane floor named, the mutation score. Co-Authored-By trailer.

---

### P-GA-34 — The E2E-4 DSAR fan-out flagship (the whole-system GDPR-by-construction proof)

- **BAND.** M5.
- **ROADMAP MILESTONE.** GA-M5 (GDPR owns the E2E-4 DSAR fan-out flagship — the whole-system chained DSAR: 0 holders missed; 0 recoverable PII incl. vectors incl. backups; residual == the one documented posture; certificate sealed) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M5".
- **DEPENDS-ON.** P-GA-32 (the full H1–H18 fan-out GA-D1), P-GA-33 (the multi-cell fan-out GA-D8), P-GA-15 (post-restore re-erasure), P-GA-20 (the Merkle certificate seal).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §4 (chain mutations end-to-end — E2E-4 is the whole-system chained DSAR, not a single-handler test).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §4.1 (the data-map-driven fan-out), §4.3 (multi-cell), §4.2 (the verifiable certificate).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.4 (the DSAR), 10.1 (the full fan-out), 10.6 (the certificate), 10.8 (the ledger).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) the E2E-4 scenario (§2/§4.4 — 0 holders missed; 0 recoverable PII incl. vectors incl. backups; residual == the one documented posture; certificate sealed).
- **DELIVERABLE.** In crate myelin-gdpr-service: **E2E-4 the DSAR fan-out flagship** (GDPR owns it) — the whole-system GDPR-by-construction proof: a single `dsr_submit` reaches every holder across all five subsystems (single-cell GA-D1 + multi-cell GA-D8), erases reliably (per-subject DEK destroyed → unrecoverable in DBs AND backups, embeddings purged-not-hidden, git author pseudonymous), survives a restore (post-restore re-erasure from the ledger), and seals a Merkle-proven certificate; the residual == the one documented posture (P-GA-16).
  **Floor named:** none new — this is the flagship that consumes the M5 floors (GA-D1, GA-D8, the re-erasure ledger, the certificate). The history-rewrite/outbound-mirror gates are P-GA-35/P-GA-36. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: the E2E-4 flagship proof of 10.4 + 10.1 + 10.6 + 10.8 (no new contract shape). Consumed: all of the above.
- **GATE / DRILLS.** **E2E-4 DSAR fan-out green** (the flagship): H1–H18 coverage receipt set + post-erase `locate` = 0 + the Merkle certificate sealed; residual == the one documented posture. Telemetry: `erasure_fanout_coverage = 100%`; the per-cell receipt set; the Merkle cert are the green artifacts. SCHED (whole-cell + multi-cell drill). This is the master M5 → M6 GDPR exit flagship.
- **TESTS (required).** Drill: the E2E-4 scenario on the harness against a full cell with mock agents (seed a subject into all H1–H18 across all five subsystems + across `member_cells ∪ home_cell` → single `dsr_submit` → assert 0 holders missed, 0 cells missed, 0 recoverable incl. vectors incl. backups, post-restore re-erasure holds, certificate sealed, residual == the one documented posture). CDC: reuse the 10.4/10.1/10.6/10.8 CDC pairs end-to-end. Mutation floor: no new core module (the flagship exercises the M5 floors); reuse P-GA-32/P-GA-33 floors — named.
- **DEFINITION OF DONE.** E2E-4 (the DSAR flagship — 0 holders missed, 0 cells missed, 0 recoverable incl. vectors incl. backups, post-restore re-erasure holds, certificate sealed, residual == the one documented posture) emits a dated green artifact; the drill scenario passes; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: E2E-4 DSAR fan-out flagship (whole-system GDPR-by-construction proof)`; body lists the E2E-4 flagship green (0 holders missed, 0 cells missed, 0 recoverable incl. vectors incl. backups, certificate sealed) with measured numbers. Co-Authored-By trailer.

---

### P-GA-35 — History-rewrite as a first-class audited op (GA-10) + audit tamper-evidence at cell scale (GA-D3 / GA-D6, the E2E-3 leg)

- **BAND.** M5.
- **ROADMAP MILESTONE.** GA-M5 (history-rewrite as a first-class audited op 10.6 §6.6 GA-10 — the `git.history_rewrite` op with the fork/mirror/clone-cache invalidation fan-out; GA-D3/GA-D6 at cell scale + the E2E-3 audit-tamper leg) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M5".
- **DEPENDS-ON.** P-GA-20 (the tamper-evident audit log the history-rewrite op is audited into + GA-D3 at scale), P-GA-26 (the history-rewrite resumable-activity skeleton), P-GA-22 (the legal-hold gate GA-D6 exercises), the Storage M5 prompts (the trust-tier/branch-scoped cache namespaces 11.2 + the within-EU CDN clone/bundle class), the Git M5 prompts (the history-rewrite tool).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (the history-rewrite disruptive path with changed hashes — the rare body-expunge case; the residual third party off-platform clones is named, not pretended-solved).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §6.6 (history-rewrite as a first-class audited op — audited (kind `git.history_rewrite`, actor = the tenant-admin pseudonym, subject = the repo ArtifactRef, outcome recorded); rate-limited (a tenant-initiated op, prevents denial/disruption); the invalidation fan-out to forks/mirrors/clone-cache tied to the trust-tier/branch-scoped cache namespaces (Storage 11.2) — an UntrustedFork-written scope cannot poison the trusted scope, a rewrite purges the stale content-addressed clone/bundle blobs (the within-EU CDN class); crypto-shred reaches the pack tier's shreddables (reflogs, bitmaps, pack backups) via the per-tenant blob DEK but NOT the commit-object bytes — that is what the rewrite is for; a resumable `myelin-flow` activity, idempotent, outbox-emitted).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.6 (the history-rewrite audited op + invalidation fan-out), 11.2 (the trust-scoped cache namespaces + the CDN clone class), 9.2 (the resumable activity).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) rows GA-10 (history-rewrite-invalidation: run a `git.history_rewrite` → the invalidation fan-out reached forks/mirrors/clone-cache, the trust-scoped namespaces purged the stale blobs, the op is audited; 0 stale-PII cache/clone hits; op audited), GA-D3/GA-D6 (audit tamper detected 100% at cell scale; legal-hold defers erasure) + the E2E-3 audit-tamper leg.
- **DELIVERABLE.** In crate myelin-gdpr-service: (1) **History-rewrite as a first-class audited op** (GA-10, 10.6 §6.6): the `git.history_rewrite` op — **audited** (an action in the tamper-evident log, kind `git.history_rewrite`, actor = the tenant-admin pseudonym, subject = the repo ArtifactRef, outcome recorded), **rate-limited** (a tenant-initiated op, prevents denial/disruption), with the **invalidation fan-out** to forks/mirrors/clone-cache tied to Storage's trust-tier/branch-scoped cache namespaces (11.2) — a rewrite purges the stale content-addressed clone/bundle blobs (the within-EU CDN class) so a rewritten history is not served from a cache; crypto-shred reaches the pack tier's shreddables (reflogs, bitmaps, pack backups) via the per-tenant blob DEK (NOT the commit-object bytes — that is what the rewrite is for, the honest split). A resumable `myelin-flow` activity (the skeleton from P-GA-26), idempotent, outbox-emitted. (2) **GA-D3 / GA-D6 at cell scale** + the **E2E-3 audit-tamper leg**: audit tamper detected 100% under world-scale audit volume; legal-hold defers erasure (0 held-scope deletions, resumes on lift); GDPR contributes the tamper-detection proof to E2E-3.
  **Floor named:** the residual (independent off-platform clones a third party holds) is named, not pretended-solved — the invalidation reaches the replicas it can reach. The outbound push-mirror residency gate (GA-11) is P-GA-36. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.6 (the history-rewrite audited op + invalidation fan-out, completing the audit log). Consumed: 11.2 (the trust-scoped cache namespaces + the CDN clone class), 9.2 (the resumable activity).
- **GATE / DRILLS.** **GA-10** (SCHED, NEW) — history-rewrite-invalidation: run a `git.history_rewrite`; the invalidation fan-out reached forks/mirrors/clone-cache, the trust-scoped namespaces purged the stale blobs, the op is audited. **0 stale-PII cache/clone hits; op audited.** **GA-D3 / GA-D6** (SCHED) — audit tamper detected 100% at cell scale; legal-hold defers erasure (0 held-scope deletions, resumes on lift). Telemetry: the invalidation-completeness signal + the audit entry (GA-10); the tamper-detection proof (GA-D3); the hold-defer receipt (GA-D6). SCHED. This rides the master M5 → M6 GDPR exit.
- **TESTS (required).** Unit: a `git.history_rewrite` appends an audit entry (kind/actor/subject/outcome); it is rate-limited; the invalidation fan-out purges the trust-scoped cache namespaces (an UntrustedFork-written scope cannot poison the trusted scope); the crypto-shred reaches reflogs/bitmaps/pack-backups but NOT the commit-object bytes (the honest split); the op is a resumable idempotent activity; a tampered audit entry at cell-scale volume is detected 100% (chain + consistency proof + witness). CDC: provider+consumer pair for 10.6 (the history-rewrite op + the cache-namespace consumer). Drill: the GA-10 + GA-D3/GA-D6 scenarios on the harness. Mutation floor: the invalidation-fan-out completeness path is mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The `git.history_rewrite` op is audited + rate-limited + drives the fork/mirror/clone-cache invalidation fan-out (trust-scoped namespaces purged, crypto-shred reaches the pack shreddables, the commit-object-bytes split named); GA-10 (0 stale-PII cache/clone hits, op audited) + GA-D3 (tamper detected 100% at cell scale) + GA-D6 (0 held-scope deletions) emit dated green artifacts; the GDPR tamper-detection proof feeds E2E-3; the off-platform-clone residual is named; the outbound-mirror gate (P-GA-36) is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: history-rewrite audited op (GA-10) + audit tamper at cell scale (GA-D3/GA-D6, E2E-3 leg)`; body lists 10.6 (history-rewrite) implemented, GA-10 + GA-D3 + GA-D6 green with measured numbers, the off-platform-clone residual named, the outbound-mirror gate → P-GA-36, the mutation score. Co-Authored-By trailer.

---

### P-GA-36 — The outbound push-mirror residency gate (GA-11, deny extra-EU by default / allow within-EU CDN)

- **BAND.** M5.
- **ROADMAP MILESTONE.** GA-M5 (the outbound push-mirror residency gate 10.5 §5.3 GA-11 — a PII-bearing extra-EU push-mirror denied by default; a within-EU CDN clone allowed; 0 default extra-EU PII transfers) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M5".
- **DEPENDS-ON.** P-GA-23 (the `transfer_allowed` policy this gate enforces), the Storage M5 prompts (the within-EU CDN clone/bundle class), the multi-cell Tenancy M5 prompts (the control-plane residency enforcement reading `transfer_allowed`).
- **CANON DOCS.**
  - [`../../external-insights/04-hard-problems.md`](../../external-insights/04-hard-problems.md) §1 (the residual third party off-platform clones is named, not pretended-solved).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §5.3 (the outbound push-mirror residency gate — a mirror config targeting an extra-EU host for PII-bearing content is denied by default at `transfer_allowed`; within-EU CDN clone/bundle distribution is permitted; the gate distinguishes within-EU acceleration (allowed) from extra-EU replication (denied)).
  - [`../../05-refined-shared-systems-architecture/contract-index.md`](../../05-refined-shared-systems-architecture/contract-index.md) rows 10.5 (the outbound-mirror gate), 11.2 (the CDN clone class), 12.x (the control-plane residency enforcement reading `transfer_allowed`).
  - [`../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md`](../../05-refined-shared-systems-architecture/testing-strategy/01-whole-system-e2e-and-drill-catalogue.md) row GA-11 (outbound-residency-gate: a PII-bearing extra-EU push-mirror denied by default; a within-EU CDN clone allowed; 0 default extra-EU PII transfers).
- **DELIVERABLE.** In crate myelin-gdpr-service: **the outbound push-mirror residency gate** (GA-11, 10.5 §5.3): a PII-bearing push-mirror (or any outbound replication config) targeting an extra-EU host is **denied by default** at `transfer_allowed` (GDPR owns the policy; Tenancy/control-plane enforces); a within-EU CDN clone/bundle is **allowed** (the gate distinguishes within-EU acceleration from extra-EU replication). Wire the policy the control-plane gate reads (completing the P-GA-23 `transfer_allowed` policy floor).
  **Floor named:** the residual (independent off-platform clones a third party holds) is named, not pretended-solved. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns: 10.5 (the outbound-mirror gate policy, completing the transfer gate). Consumed: 11.2 (the CDN clone class), 12.x (the control-plane residency enforcement reading `transfer_allowed`).
- **GATE / DRILLS.** **GA-11** (SCHED, NEW) — outbound-residency-gate: a PII-bearing extra-EU push-mirror **denied by default**; a within-EU CDN clone **allowed**. **0 default extra-EU PII transfers.** Telemetry: the transfer-gate decision + 0 egress is the green artifact. SCHED.
- **TESTS (required).** Unit: `transfer_allowed` denies an extra-EU PII-bearing mirror by default + admits a within-EU CDN clone; the gate distinguishes within-EU acceleration (allowed) from extra-EU replication (denied). CDC: provider+consumer pair for 10.5 (the control-plane gate reading `transfer_allowed`). Drill: the GA-11 scenario on the harness (configure an extra-EU PII-bearing mirror → denied; a within-EU CDN clone → allowed; assert 0 default extra-EU transfers). Mutation floor: the deny-by-default + the within-EU-vs-extra-EU distinction paths are mandatory-core — state and meet the mutation-score floor.
- **DEFINITION OF DONE.** The outbound push-mirror residency gate denies extra-EU PII by default + admits within-EU CDN; GA-11 (0 default extra-EU transfers) emits a dated green artifact; the off-platform-clone residual is named; CDC + unit tests + the mutation floor pass; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M5: outbound push-mirror residency gate (GA-11, deny extra-EU by default)`; body lists 10.5 (mirror gate) implemented, GA-11 green (0 default extra-EU transfers) with measured numbers, the off-platform-clone residual named, the mutation score. Co-Authored-By trailer.

---

### P-GA-37 — Dogfood: the GDPR/Audit machinery live on Myelin's own commits + a self-served DSR

- **BAND.** M6.
- **ROADMAP MILESTONE.** GA-M6 (dogfooding — the GDPR/Audit machinery runs on the platform's own commits; the audit log records the team's own human + agent actions; a self-served DSR over the team's own data exercises the fan-out for real; the RoPA + data map live as a Myelin Knowledge space) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M6".
- **DEPENDS-ON.** P-GA-34 (the full DSAR flagship proven — the M6 entry dependency: you do not dogfood real team data onto a substrate whose DSAR fan-out is not green), P-GA-35/P-GA-36 (the M5 GDPR gates green), the M6 dogfood substrate prompts (Myelin hosts its own git/CI/Issues/Knowledge/Chat).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §3 (the cheapest, most honest load generator is the platform's own development).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) §2.2 (the RoPA + the data map live as a Myelin Knowledge space).
  - [`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) §2 M6 (the gate invariant holds end-to-end).
- **DELIVERABLE.** (1) Run the **GDPR/Audit machinery on the platform's own commits** — the audit log records the team's own human + agent actions (the audit consumer is live on the Myelin self-hosting outbox); the every-incident-adds-a-drill loop files a Myelin issue + a reproducing drill. (2) A **self-served DSR over the team's own data** exercises the fan-out for real (a Myelin team member's `dsr_submit` → fan-out → certificate). (3) The **RoPA + the data map live as a Myelin Knowledge space**.
  **Floor named:** the truth-up pass (confirm every PROVEN GDPR row rests on a dated green artifact) is P-GA-38. Record in writing.
- **CONTRACTS TO IMPLEMENT.** Owns (dogfood operation): the live operation of 10.1–10.9 on Myelin's own tenant data. No new contract shape.
- **GATE / DRILLS.** The self-hosting **audit graph is green on the platform's own commits** (the dogfood loop is live: every Myelin commit's actions are audit-logged; a self-served DSR fans out + seals a certificate). Telemetry: `audit_append_lag`/`sth_publish_age`/`erasure_fanout_coverage` read green on Myelin's own tenant; the self-served DSR certificate seals. SCHED.
- **TESTS (required).** Unit/integration: the audit consumer is live on the self-hosting outbox (every Myelin action is logged); a self-served DSR over a Myelin team member's data fans out + seals a certificate. No new mutation floor (the machinery's floors were set in P-GA-05..P-GA-36; here it runs for real).
- **DEFINITION OF DONE.** The GDPR/Audit machinery runs on Myelin's own commits (the audit graph is green on the platform's own actions); a self-served DSR over the team's own data fans out + seals a certificate; the RoPA + data map live as a Myelin Knowledge space; the truth-up pass (P-GA-38) is named in writing; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M6: GDPR/Audit dogfood on Myelin's own commits + a self-served DSR`; body lists the live operation of 10.1–10.9 on Myelin's own tenant, the self-served DSR certificate sealed, the audit graph green, the truth-up pass → P-GA-38. Co-Authored-By trailer.

---

### P-GA-38 — The truth-up pass: confirm every PROVEN GDPR gate rests on a dated green artifact

- **BAND.** M6.
- **ROADMAP MILESTONE.** GA-M6 (the truth-up pass confirms no GDPR gate is red — every PROVEN row rests on a dated green artifact, never a doc claim; the gate invariant holds end-to-end) — [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2 "GA-M6".
- **DEPENDS-ON.** P-GA-37 (the dogfood live — the truth-up reads the real running machinery), all prior GDPR prompts (it enumerates their gates).
- **CANON DOCS.**
  - [`../../external-insights/01-process-and-quality-doctrine.md`](../../external-insights/01-process-and-quality-doctrine.md) §1 (code-wins-over-docs — the truth-up pass confirms every PROVEN GDPR row rests on a dated green artifact, never a doc claim; date every status note).
  - [`../../05-refined-shared-systems-architecture/gdpr-and-audit.md`](../../05-refined-shared-systems-architecture/gdpr-and-audit.md) the §9.2 drill table (every GDPR gate must rest on a dated green artifact).
  - [`../../06-roadmaps/00-master-sequencing.md`](../../06-roadmaps/00-master-sequencing.md) §2 M6 (the gate invariant holds end-to-end: a truth-up pass confirms every PROVEN row rests on a dated green artifact, never a doc claim).
- **DELIVERABLE.** The **truth-up pass**: confirm every PROVEN GDPR row (GA-D1..GA-D8, GA-10, GA-11, GA-D5, STOR-D3/D4-GA-faces, E2E-3/E2E-4) rests on a dated green artifact, never a doc claim; where a row is CLAIMED-not-PROVEN, record it honestly with a date (code-wins-over-docs). Produce the enumerated scorecard (every GDPR gate → its dated green artifact, or a dated CLAIMED-not-PROVEN note).
  **Floor named:** none — this is the closing honesty pass. Record any CLAIMED-not-PROVEN row with a date.
- **CONTRACTS TO IMPLEMENT.** Owns: the truth-up scorecard over 10.1–10.9 (no new contract shape).
- **GATE / DRILLS.** The **truth-up pass confirms no GDPR gate is red** (the gate invariant holds end-to-end). Telemetry: the enumerated scorecard is the green artifact (every PROVEN row dated-green; any CLAIMED-not-PROVEN row recorded with a date). SCHED.
- **TESTS (required).** Integration: the truth-up pass enumerates every GDPR PROVEN row + asserts each rests on a dated green artifact (a row without one is surfaced, not swallowed). No new mutation floor.
- **DEFINITION OF DONE.** The truth-up pass confirms every PROVEN GDPR gate rests on a dated green artifact (no red gate, no doc-claim masquerading as proven); any CLAIMED-not-PROVEN row is recorded honestly with a date; the enumerated scorecard exists; lints green; coverage scanner green. Committed.
- **COMMIT.** Header `P-<NNN> M6: the truth-up pass — every PROVEN GDPR gate rests on a dated green artifact`; body lists the truth-up pass result (every PROVEN row dated-green; any CLAIMED-not-PROVEN row recorded with a date). Co-Authored-By trailer.

---

## Coverage matrix (this file → the GDPR/Audit roadmap milestones)

Every milestone in [`../../06-roadmaps/shared/gdpr-and-audit.md`](../../06-roadmaps/shared/gdpr-and-audit.md) §2
maps to ≥1 prompt; the consolidated index (Phase 7-B) verifies totality against the master gate sequence. The
first-pass coarse prompts P-GA-01..P-GA-16 are split into the finer single-deliverable prompts below — the
"first-pass id" column records the lineage so no coverage is lost.

| Roadmap milestone (band) | First-pass id | Prompt(s) | Drills greened |
|---|---|---|---|
| GA-M0 (M0) — myelin-gdpr glue skeleton: trait signature + core types | P-GA-01 | P-GA-01 | (compile gate) |
| GA-M0 (M0) — `#[personal_data]` derive-attribute + five-tag enum names | P-GA-01 | P-GA-02 | (compile gate) |
| GA-M0 (M0) — the `no-untagged-personal-data` lint (red+green fixtures) | P-GA-01 | P-GA-03 | GA-D5 |
| GA-M1 (M1) — PersonalDataHolder auto-registration hook + holder-registered test | P-GA-01 | P-GA-04 | holder-registered architecture test |
| GA-M1 (M1) — PersonalDataHolder trait bodies + GDPR-owned holders (H18 + H16 carve-out) | P-GA-02 | P-GA-05 | GDPR-owned-holder erasure floor |
| GA-M1 (M1) — upstream-store holder orchestration (H6/H8/H9/H10/H14/H15) + canonical erase order | P-GA-02 | P-GA-06 | M1-holder orchestration floor |
| GA-M1 (M1) — `#[personal_data]` classify-derive macro body + five-tag enum | P-GA-03 | P-GA-07 | GA-D5 (derive face) |
| GA-M1 (M1) — SpecialCategory → DPIA router | P-GA-03 | P-GA-08 | DPIA-marker fires |
| GA-M1 (M1) — data-map / RoPA generator | P-GA-04 | P-GA-09 | 0 fields/holders absent from the map |
| GA-M1 (M1) — CI data-map diff gate + DPIA-route on reclassification | P-GA-04 | P-GA-10 | GA-D5 (diff face) |
| GA-M1 (M1) — DSR orchestrator API + state machine + posture gate | P-GA-05 | P-GA-11 | state-machine + posture gate |
| GA-M1 (M1) — data-map-driven checklist + resumable fan-out + receipts + legal-hold gate | P-GA-05 | P-GA-12 | data-map-driven fan-out; 0 double-erase |
| GA-M1 (M1) — DSR tenant-operability (Art. 28 + offboarding + restrict/rectify/portability) | P-GA-05 | P-GA-13 | tenant-offboarding fan-out |
| GA-M1 (M1) — M1 DSR erasure floor proof + worker-kill resumability (coarse deadline) | P-GA-05 | P-GA-14 | M1 DSR floor; worker-kill resumability |
| GA-M1 (M1) — erasure ledger + post-restore re-erasure | P-GA-06 | P-GA-15 | STOR-D4-GA-face; STOR-D3/ID-D8-GA-face |
| GA-M1 (M1) — the ONE free-text/immutable erasure posture (X-7) written once | P-GA-07 | P-GA-16 | the canonical posture artifact |
| GA-M1 (M1) — structural erasure floor proven on M1 stores (DEK + pseudonym shred + restrict) | P-GA-07 | P-GA-17 | structural floor on M1 stores |
| GA-M1 (M1) — pseudonymous-by-default commit-identity prerequisite for Git M3 | P-GA-07 | P-GA-18 | prerequisite recorded + architecture-test scaffold |
| GA-M1 (M1) — tamper-evident audit log core (outbox-only consumer + hash-chain + Merkle leaves + minimisation) | P-GA-08 | P-GA-19 | audit_append_lag; outbox-only |
| GA-M1 (M1) — audit CT proofs + STH + witness + DSR-receipt seal + H16 carve-out body | P-GA-08 | P-GA-20 | GA-D3 (M1 scale) |
| GA-M2 (M2) — DSR deadline durable timer + nearing-deadline Signal | P-GA-09 | P-GA-21 | GA-D4 |
| GA-M2 (M2) — retention engine (tightest-policy-wins + legal-hold-aware suspend) | P-GA-10 | P-GA-22 | GA-D6 |
| GA-M2 (M2) — consent + sub-processor registries + transfer_allowed gate | P-GA-10 | P-GA-23 | transfer-gate deny-extra-EU-by-default |
| GA-M2 (M2) — per-derivative erasure (Search purge+reindex incl. embeddings + Refs tombstone + reindex-from-source) | P-GA-10 | P-GA-24 | GA-D2; REF-D5/NOTIF-D6 (faces) |
| GA-M2 (M2) — restrict suppression into the derived stores (Search/Refs/Notif/Agents/OLAP) | P-GA-10 | P-GA-25 | GA-D7 |
| GA-M2 (M2) — eDiscovery export + agent-trace seam (8.8) + history-rewrite skeleton | P-GA-10 | P-GA-26 | eDiscovery inclusion proof; trace≠audit |
| GA-M3 (M3) — producer holders (Git H1/KN H4/agent-trace H17) register + fan-out + Knowledge instance | P-GA-11 | P-GA-27 | KN-D4/KN-D12 |
| GA-M3 (M3) — the Git pseudonymous-commit instance of X-7 (10.9 by reference) | P-GA-11 | P-GA-28 | GIT-D2 |
| GA-M4 (M4) — CI holder (H2) + per-subject CI-log DEK | P-GA-12 | P-GA-29 | CI-D3 |
| GA-M4 (M4) — Issues (H3) + Chat (H5) holders register + fan-out + the instances | P-GA-13 | P-GA-30 | ISS-D11; CHAT-D8 |
| GA-M4 (M4) — worklog Behavioural classification (OQ-H) + works-council trigger + SpecialCategory→DPIA route | P-GA-13 | P-GA-31 | GA-D7 (worklog face); DPIA route |
| GA-M5 (M5) — full H1–H18 DSR fan-out (GA-D1) + STOR-D3 at cell scale | P-GA-14 | P-GA-32 | GA-D1; STOR-D3 cell-scale |
| GA-M5 (M5) — multi-cell DSR fan-out (member_cells over the cross-cell bridge) | P-GA-14 | P-GA-33 | GA-D8 |
| GA-M5 (M5) — the E2E-4 DSAR fan-out flagship | P-GA-14 | P-GA-34 | E2E-4 |
| GA-M5 (M5) — history-rewrite audited op (GA-10) + audit tamper at cell scale (GA-D3/GA-D6, E2E-3 leg) | P-GA-15 | P-GA-35 | GA-10; GA-D3 (cell scale); GA-D6 |
| GA-M5 (M5) — outbound push-mirror residency gate (GA-11) | P-GA-15 | P-GA-36 | GA-11 |
| GA-M6 (M6) — dogfood on Myelin's own commits + a self-served DSR | P-GA-16 | P-GA-37 | self-hosting audit graph green |
| GA-M6 (M6) — the truth-up pass | P-GA-16 | P-GA-38 | truth-up (no red gate) |

**Floor → follow-on pairs (each visible, never invisible — EI-04 §4):**
- coarse DSR deadline tracking (P-GA-14) → durable deadline timer + nearing-deadline Signal (P-GA-21, GA-D4).
- single-cell DSR fan-out (P-GA-14) → multi-cell `member_cells` iteration (P-GA-33, GA-D8); cross-cell
  ordering/atomicity remains the named control-plane floor even at M5 (P-GA-33).
- the GDPR-owned holders (P-GA-05) + the M1-shared-layer holder orchestration (P-GA-06) → producer holders
  (P-GA-27/P-GA-28) → consumer holders (P-GA-29/P-GA-30/P-GA-31) → the full H1–H18 fan-out (P-GA-32, GA-D1
  "0 holders missed").
- the M1 DSR erasure floor (P-GA-14) → the backup-recovery / restore-resurrects-nothing proof (P-GA-15,
  STOR-D4/STOR-D3-GA-faces) → the cell-scale re-run (P-GA-32, STOR-D3 cell scale).
- per-tenant CI-log DEK floor → per-subject CI-log DEK (P-GA-29, the P5 extension).
- the receipt construction (P-GA-12) → the receipt-seal into the Merkle tree (P-GA-20).
- the audit-log core (P-GA-19) → the CT proofs/STH/witness + GA-D3 at M1 scale (P-GA-20) → GA-D3 at cell scale
  under world-scale volume (P-GA-35, E2E-3 leg).
- the structural erasure floor on M1 stores (P-GA-17) → the full restriction-into-derived-stores proof
  (P-GA-25, GA-D7).
- the `[OPEN — LEGAL]` residual posture written once (P-GA-16) → the pseudonymous-commit prerequisite (P-GA-18)
  → referenced per subsystem (P-GA-27/P-GA-28/P-GA-29/P-GA-30/P-GA-31) → counsel/DPO ratification of ONE
  lawful-basis statement (parallel-legal, named, never blocking).
- the worklog `TBD_LEGAL` basis (P-GA-31) → counsel ratifies special-category + works-council trigger
  (parallel-legal).
- the history-rewrite resumable-activity skeleton (P-GA-26) → the first-class audited op + invalidation fan-out
  (P-GA-35, GA-10).
- the `transfer_allowed` policy (P-GA-23) → the outbound push-mirror residency gate proven (P-GA-36, GA-11).

**The permanent gates GDPR participates in (re-run forever):** GA-D5 (the lint, every change) and the
restore-verify/re-erasure line (STOR-D3/STOR-D4, every change touching a store — silent data loss meets erasure
outranks every feature, master §4). Every prompt's DEFINITION OF DONE re-confirms all twelve lints green.
